---
phase: 06-backend-api
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/dashboards/__init__.py
  - backend/app/services/dashboards/dashboard_read_service.py
  - backend/app/services/dashboards/health_read_service.py
  - backend/app/services/insights/insight_read_service.py
  - backend/tests/unit/test_dashboard_read_service.py
  - backend/tests/unit/test_insight_read_service.py
  - backend/tests/unit/test_health_read_service.py
autonomous: true
requirements:
  - SALE-01
  - SALE-02
  - SALE-03
  - SALE-04
  - SALE-05
  - SALE-06
  - SALE-07
  - SALES-01
  - SALES-02
  - SALES-03
  - SALES-04
  - MARK-01
  - MARK-02
  - MARK-03
  - MARK-04
  - INSI-01
  - INSI-02
  - INSI-04
  - INSI-05
  - INSI-06
  - UI-06
  - PIPE-04

must_haves:
  truths:
    - "DashboardReadService.get_sales_dashboard(from_date, to_date) aggregates daily_kpi rows for the range and returns all required fields"
    - "DashboardReadService.get_salespeople_dashboard() joins salesperson_daily_kpi with mefi_salespeople to return human-readable names"
    - "DashboardReadService.get_marketing_dashboard() queries raw_mefi_leads for junk_by_source (documented exception)"
    - "DashboardReadService.get_stuck_offers() queries v_mefi_leads_active joined to mefi_lead_history for offers with no activity > 14 days"
    - "InsightReadService.get_today() queries daily_insights WHERE date = yesterday in Europe/Bucharest timezone"
    - "InsightReadService.get_by_date(date) returns None when no row exists for that date"
    - "InsightReadService.get_today() returns generation_failed=True when insight status is 'failed'"
    - "HealthReadService.get_health() marks stale=True when last_sync_at > 26h ago"
    - "All service queries filter by tenant_id"
  artifacts:
    - path: "backend/app/services/dashboards/dashboard_read_service.py"
      provides: "DashboardReadService with get_sales_dashboard, get_salespeople_dashboard, get_marketing_dashboard, get_stuck_offers"
      exports: ["DashboardReadService"]
    - path: "backend/app/services/dashboards/health_read_service.py"
      provides: "HealthReadService with get_health"
      exports: ["HealthReadService"]
    - path: "backend/app/services/insights/insight_read_service.py"
      provides: "InsightReadService with get_today, get_by_date"
      exports: ["InsightReadService"]
  key_links:
    - from: "backend/app/api/v1/dashboards.py (plan 03)"
      to: "backend/app/services/dashboards/dashboard_read_service.py"
      via: "DashboardReadService(session, tenant_id).get_sales_dashboard(from_date, to_date)"
    - from: "backend/app/api/v1/insights.py (plan 03)"
      to: "backend/app/services/insights/insight_read_service.py"
      via: "InsightReadService(session, tenant_id).get_today()"
---

<objective>
Create the three read service classes that query pre-computed metric tables and return structured data dicts for the FastAPI routers.

Purpose: The router layer (Plan 03) depends on these services. All business logic for assembling dashboard responses lives here — the routers are thin wrappers.
Output: DashboardReadService, InsightReadService, HealthReadService, and their unit tests.
</objective>

<execution_context>
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.planning/PROJECT.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.planning/ROADMAP.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.planning/phases/06-backend-api/06-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From backend/app/models/metrics/daily_kpi.py — DailyKpi columns:
  date, tenant_id, leads_total, leads_mail_fb_ig, leads_telefon, leads_whatsapp,
  leads_site, leads_designer, leads_alte, visits_count (nullable), offers_count,
  contracts_count, conversion_l_to_v, conversion_v_to_o, conversion_l_to_o,
  conversion_o_to_c, conversion_l_to_c, revenue (Decimal), avg_deal_size (Decimal),
  leads_total_wow_delta, leads_total_mom_delta, conversion_l_to_v_wow_delta,
  conversion_l_to_v_mom_delta, conversion_v_to_o_wow_delta, conversion_v_to_o_mom_delta,
  conversion_l_to_o_wow_delta, conversion_l_to_o_mom_delta, conversion_o_to_c_wow_delta,
  conversion_o_to_c_mom_delta, conversion_l_to_c_wow_delta, conversion_l_to_c_mom_delta,
  revenue_wow_delta, revenue_mom_delta, avg_deal_size_wow_delta, avg_deal_size_mom_delta

From backend/app/models/metrics/salesperson_kpi.py — SalespersonDailyKpi columns:
  date, tenant_id, salesperson_external_id, leads_assigned, leads_contacted,
  avg_time_to_first_touch_minutes (nullable), visits_conducted, offers_sent,
  deals_won, deals_lost, revenue (Decimal), conversion_l_to_v, conversion_v_to_o,
  conversion_o_to_c, conversion_l_to_c, avg_deal_size (Decimal), data_completeness_pct

From backend/app/models/metrics/source_kpi.py — SourceDailyKpi columns:
  date, tenant_id, source, leads, visits, offers, deals_won, revenue, conversion_rate,
  ad_spend (None), cpl (None), cac (None), roas (None)

From backend/app/models/insights/daily_insight.py — DailyInsight columns:
  date, tenant_id, status, payload_json (JSONB dict), generated_at (datetime | None),
  input_tokens (int | None), output_tokens (int | None), cost_usd (Decimal | None)

From backend/app/models/pipeline.py:
  SyncRun: source, status, completed_at (datetime | None), started_at
  PipelineRun: stage, status, started_at, completed_at (datetime | None)

From backend/app/models/mefi.py:
  MefiSalesperson: external_id (Integer), name (str), tenant_id
  RawMefiLead: __tablename__ = "raw_mefi_leads", external_id, lifecycle, source_id (Integer),
    tenant_id — lifecycle='junk' for junk leads

From backend/app/db/session.py and deps.py:
  AsyncSession from sqlalchemy.ext.asyncio
  get_session() dependency yields AsyncSession

SQLAlchemy 2.x imports needed:
  from sqlalchemy import select, func, and_, text, Integer as SAInteger
  from sqlalchemy.ext.asyncio import AsyncSession
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: DashboardReadService (sales, salespeople, marketing, stuck offers)</name>
  <files>
    backend/app/services/dashboards/__init__.py
    backend/app/services/dashboards/dashboard_read_service.py
    backend/tests/unit/test_dashboard_read_service.py
  </files>
  <read_first>
    backend/app/models/metrics/daily_kpi.py
    backend/app/models/metrics/salesperson_kpi.py
    backend/app/models/metrics/source_kpi.py
    backend/app/models/mefi.py
    backend/app/services/metrics/daily_kpi_service.py
    backend/tests/factories/metrics_factory.py
    backend/app/core/config.py
  </read_first>
  <behavior>
    get_sales_dashboard:
    - Returns dict with keys: period, funnel, conversion_rates, kpi_cards, source_breakdown, revenue_series, stuck_offers
    - funnel.leads = coalesce(SUM(leads_total), 0), funnel.visits = SUM(visits_count) (keep None when all NULL — KI-03)
    - conversion_rates returns last available row values for rates (not averaged) plus last row deltas — use ORDER BY date DESC LIMIT 1 subquery for the last-day rates, or query the last row within range
    - revenue_series is list of {date, revenue} dicts — one dict per date in range (SALE-05: daily granularity, per locked decision)
    - source_breakdown from SourceDailyKpi SUM per source over range
    - stuck_offers: query v_mefi_leads_active joined to mefi_lead_history — see Task 1 action detail

    get_salespeople_dashboard:
    - Returns dict with keys: period, salespeople (list)
    - JOIN SalespersonDailyKpi (SUM aggregated per salesperson) with mefi_salespeople on tenant_id match and CAST(salesperson_external_id AS INTEGER) = mefi_salespeople.external_id (or text comparison — verify by checking mefi_salespeople.external_id type: Integer in model, TEXT in salesperson_daily_kpi)
    - win_rate = deals_won / leads_assigned (compute in Python after aggregation, with None guard)
    - avg_time_to_first_touch_minutes: ROUND(AVG(avg_time_to_first_touch_minutes)) across date range

    get_marketing_dashboard:
    - Returns dict with keys: period, lead_volume_by_source, site_conversion_rate, junk_by_source, ad_spend=None, cpl=None, cac=None, roas=None
    - lead_volume_by_source: per source, list of {date, leads} time-series points (MARK-01)
    - site_conversion_rate: query SourceDailyKpi WHERE source='site', AVG(conversion_rate) over range
    - junk_by_source: MARK-04 documented exception — query raw_mefi_leads directly via text() SQL or ORM. Query: SELECT source_id, COUNT(*) as junk_count FROM raw_mefi_leads WHERE tenant_id=:tid AND lifecycle='junk' AND created_date_local >= :from_date AND created_date_local <= :to_date GROUP BY source_id. Also query total per source: SELECT source_id, COUNT(*) FROM raw_mefi_leads WHERE tenant_id=:tid AND lifecycle IN ('active','lost','junk') AND created_date_local >= :from_date AND created_date_local <= :to_date GROUP BY source_id. Compute junk_pct = junk_count / total. Use source_id integer → source_name mapping from funnel_config or a hardcoded MEFI source_id mapping constant in the service. Add a comment: "MARK-04 documented exception: raw_mefi_leads queried directly; no pre-computed junk-by-source view exists."

    get_stuck_offers:
    - Query v_mefi_leads_active (as text table reference) for leads where reached_offer=true and tenant_id=:tid
    - JOIN mefi_lead_history on lead_external_id = external_id, tenant_id
    - Find leads where MAX(changed_at) < now() - interval 14 days
    - Return list of {external_id, days_stuck, salesperson_name (nullable)} dicts
    - Note: v_mefi_leads_active is a VIEW, not an ORM model — use text() query or sqlalchemy.Table with autoload

    All methods:
    - Accept (self, from_date: date, to_date: date) for dashboard methods
    - __init__(self, session: AsyncSession, tenant_id: UUID)
    - All async
    - Filter all queries by tenant_id
    - Use func.coalesce(func.sum(col), 0) for integer count aggregations (avoids NULL from SUM of all NULLs)
    - Keep NULL (not coalesce to 0) for Decimal rates/revenue to preserve "no data" semantics
  </behavior>
  <action>
    Create backend/app/services/dashboards/__init__.py as empty module (`from __future__ import annotations`).

    Create backend/app/services/dashboards/dashboard_read_service.py with `from __future__ import annotations`.

    Imports: datetime.date, decimal.Decimal, uuid.UUID, datetime.datetime, datetime.timezone, datetime.timedelta, zoneinfo.ZoneInfo, sqlalchemy (select, func, and_, text, cast, Integer as SAInteger), sqlalchemy.ext.asyncio (AsyncSession), structlog, app.models.metrics.daily_kpi (DailyKpi), app.models.metrics.salesperson_kpi (SalespersonDailyKpi), app.models.metrics.source_kpi (SourceDailyKpi), app.models.mefi (MefiSalesperson, RawMefiLead).

    MEFI_SOURCE_ID_TO_NAME mapping constant (dict[int, str]) — derive from the 11 known source categories in docs/SOFABELLE.md and enums.md. At minimum cover: 1→"mail_fb_ig", 2→"mail_fb_ig", 3→"telefon", 4→"whatsapp", 5→"showroom", 6→"site", 7→"designer", 8→"alte", 9→"colaborare", 10→"recomandare". If source_id not in mapping, use str(source_id) as fallback.

    Class DashboardReadService:
      __init__(self, session: AsyncSession, tenant_id: UUID) -> None

      async def get_sales_dashboard(self, from_date: date, to_date: date) -> dict:
        Step 1 — aggregate daily_kpi for range:
          stmt = select(
            func.coalesce(func.sum(DailyKpi.leads_total), 0).label("leads_total"),
            func.sum(DailyKpi.visits_count).label("visits_count"),  # keep NULL semantics
            func.coalesce(func.sum(DailyKpi.offers_count), 0).label("offers_count"),
            func.coalesce(func.sum(DailyKpi.contracts_count), 0).label("contracts_count"),
            func.sum(DailyKpi.revenue).label("revenue"),
            func.sum(DailyKpi.avg_deal_size).label("avg_deal_size"),  # NOTE: use avg not sum for deal size — see note below
          ).where(DailyKpi.tenant_id == self._tenant_id, DailyKpi.date >= from_date, DailyKpi.date <= to_date)
          Note: avg_deal_size for range = total revenue / total contracts (compute in Python from sums above).

        Step 2 — get last-day rates and deltas (last row in range):
          stmt_rates = select(
            DailyKpi.conversion_l_to_v, DailyKpi.conversion_v_to_o, DailyKpi.conversion_l_to_o,
            DailyKpi.conversion_o_to_c, DailyKpi.conversion_l_to_c,
            DailyKpi.leads_total_wow_delta, DailyKpi.leads_total_mom_delta,
            DailyKpi.conversion_l_to_v_wow_delta, DailyKpi.conversion_l_to_v_mom_delta,
            DailyKpi.conversion_v_to_o_wow_delta, DailyKpi.conversion_v_to_o_mom_delta,
            DailyKpi.conversion_l_to_o_wow_delta, DailyKpi.conversion_l_to_o_mom_delta,
            DailyKpi.conversion_o_to_c_wow_delta, DailyKpi.conversion_o_to_c_mom_delta,
            DailyKpi.conversion_l_to_c_wow_delta, DailyKpi.conversion_l_to_c_mom_delta,
            DailyKpi.revenue_wow_delta, DailyKpi.revenue_mom_delta,
            DailyKpi.avg_deal_size_wow_delta, DailyKpi.avg_deal_size_mom_delta,
          ).where(DailyKpi.tenant_id == self._tenant_id, DailyKpi.date >= from_date, DailyKpi.date <= to_date
          ).order_by(DailyKpi.date.desc()).limit(1)

        Step 3 — source breakdown from SourceDailyKpi:
          stmt_src = select(
            SourceDailyKpi.source,
            func.coalesce(func.sum(SourceDailyKpi.leads), 0).label("leads"),
            func.sum(SourceDailyKpi.visits).label("visits"),
            func.coalesce(func.sum(SourceDailyKpi.offers), 0).label("offers"),
            func.coalesce(func.sum(SourceDailyKpi.deals_won), 0).label("deals_won"),
            func.sum(SourceDailyKpi.revenue).label("revenue"),
            func.avg(SourceDailyKpi.conversion_rate).label("conversion_rate"),
          ).where(SourceDailyKpi.tenant_id == self._tenant_id, SourceDailyKpi.date >= from_date, SourceDailyKpi.date <= to_date
          ).group_by(SourceDailyKpi.source)

        Step 4 — revenue time series (per locked decision: always daily granularity):
          stmt_ts = select(DailyKpi.date, DailyKpi.revenue).where(
            DailyKpi.tenant_id == self._tenant_id, DailyKpi.date >= from_date, DailyKpi.date <= to_date
          ).order_by(DailyKpi.date.asc())

        Step 5 — stuck offers (call self.get_stuck_offers(from_date, to_date)):
          Assemble and return dict matching SalesDashboardResponse structure.

      async def get_salespeople_dashboard(self, from_date: date, to_date: date) -> dict:
        Aggregate SalespersonDailyKpi per salesperson for range, JOIN with MefiSalesperson for name.

        ORM join approach:
        stmt = select(
          SalespersonDailyKpi.salesperson_external_id,
          MefiSalesperson.name,
          func.coalesce(func.sum(SalespersonDailyKpi.leads_assigned), 0).label("leads_assigned"),
          func.coalesce(func.sum(SalespersonDailyKpi.visits_conducted), 0).label("visits_conducted"),
          func.coalesce(func.sum(SalespersonDailyKpi.offers_sent), 0).label("offers_sent"),
          func.coalesce(func.sum(SalespersonDailyKpi.deals_won), 0).label("deals_won"),
          func.sum(SalespersonDailyKpi.revenue).label("revenue"),
          func.avg(SalespersonDailyKpi.avg_time_to_first_touch_minutes).label("avg_ttft"),
          func.avg(SalespersonDailyKpi.data_completeness_pct).label("data_completeness_pct"),
          func.avg(SalespersonDailyKpi.conversion_l_to_v).label("conversion_l_to_v"),
          func.avg(SalespersonDailyKpi.conversion_v_to_o).label("conversion_v_to_o"),
          func.avg(SalespersonDailyKpi.conversion_o_to_c).label("conversion_o_to_c"),
          func.avg(SalespersonDailyKpi.conversion_l_to_c).label("conversion_l_to_c"),
        ).outerjoin(
          MefiSalesperson,
          and_(
            MefiSalesperson.tenant_id == SalespersonDailyKpi.tenant_id,
            cast(SalespersonDailyKpi.salesperson_external_id, SAInteger) == MefiSalesperson.external_id,
          )
        ).where(
          SalespersonDailyKpi.tenant_id == self._tenant_id,
          SalespersonDailyKpi.date >= from_date,
          SalespersonDailyKpi.date <= to_date,
        ).group_by(SalespersonDailyKpi.salesperson_external_id, MefiSalesperson.name)

        Compute win_rate in Python: deals_won / leads_assigned if leads_assigned > 0 else None.
        Compute avg_deal_size in Python: revenue / deals_won if deals_won > 0 else None.
        Return dict with salespeople list.

      async def get_marketing_dashboard(self, from_date: date, to_date: date) -> dict:
        Step 1 — lead_volume_by_source time series per source:
          stmt = select(SourceDailyKpi.source, SourceDailyKpi.date, SourceDailyKpi.leads
          ).where(SourceDailyKpi.tenant_id == self._tenant_id, SourceDailyKpi.date >= from_date, SourceDailyKpi.date <= to_date
          ).order_by(SourceDailyKpi.source, SourceDailyKpi.date.asc())
          Group results by source into {source: [{date, leads}]} structure in Python.

        Step 2 — site_conversion_rate:
          stmt_site = select(func.avg(SourceDailyKpi.conversion_rate)).where(
            SourceDailyKpi.tenant_id == self._tenant_id,
            SourceDailyKpi.source == "site",
            SourceDailyKpi.date >= from_date, SourceDailyKpi.date <= to_date,
          )

        Step 3 — junk_by_source (MARK-04 documented exception — raw_mefi_leads query):
          Use text() SQL: SELECT source_id, COUNT(*) as junk_count FROM raw_mefi_leads WHERE tenant_id = :tid AND lifecycle = 'junk' AND created_date_local >= :from_d AND created_date_local <= :to_d GROUP BY source_id
          Also query total: SELECT source_id, COUNT(*) as total FROM raw_mefi_leads WHERE tenant_id = :tid AND created_date_local >= :from_d AND created_date_local <= :to_d GROUP BY source_id
          Bind params as bindparam with UUID tenant_id passed as str(self._tenant_id) via text() bindparams.
          Map source_id → source_name using MEFI_SOURCE_ID_TO_NAME constant.
          Compute junk_pct = Decimal(str(junk_count)) / Decimal(str(total)) if total > 0 else None.

        Return dict with all fields. ad_spend=None, cpl=None, cac=None, roas=None always.

      async def get_stuck_offers(self, from_date: date, to_date: date) -> list[dict]:
        SALE-07: offers stuck > 14 days.
        Use text() SQL to query the view and history table:
          SELECT v.external_id,
                 ms.name as salesperson_name,
                 EXTRACT(EPOCH FROM (now() - MAX(h.changed_at))) / 86400 AS days_stuck
          FROM v_mefi_leads_active v
          LEFT JOIN mefi_lead_history h ON h.lead_external_id = v.external_id AND h.tenant_id = :tid
          LEFT JOIN mefi_salespeople ms ON ms.external_id::text = v.assigned_to_id AND ms.tenant_id = :tid
          WHERE v.tenant_id = :tid
            AND v.reached_offer = true
            AND v.lifecycle NOT IN ('junk')
          GROUP BY v.external_id, ms.name
          HAVING MAX(h.changed_at) < now() - interval '14 days'
             OR MAX(h.changed_at) IS NULL
          ORDER BY days_stuck DESC NULLS LAST
          LIMIT 50
        Pass tenant_id as str(self._tenant_id). Return list of {external_id, days_stuck (int), salesperson_name (str | None)}.

    Create backend/tests/unit/test_dashboard_read_service.py with:
    - test_get_sales_dashboard_aggregates_funnel: mock session.execute returning DailyKpi-like rows; assert funnel.leads = SUM, funnel.visits nullable
    - test_get_sales_dashboard_revenue_series_daily: assert returned revenue_series has one entry per day (SALE-05 locked decision)
    - test_get_sales_dashboard_empty_range: empty DB result → zeros for counts, None for rates
    - test_get_salespeople_dashboard_includes_name: mock join result includes salesperson name from mefi_salespeople
    - test_get_salespeople_dashboard_win_rate_computed: deals_won/leads_assigned computed in Python
    - test_get_marketing_dashboard_ad_spend_null: returned dict.ad_spend is None (MARK-03)
    - test_get_marketing_dashboard_site_conversion_rate: returns Decimal from SourceDailyKpi site rows
    - test_get_stuck_offers_returns_list: mock text query returns stuck offer rows as list of dicts

    Use AsyncMock for session.execute. Use unittest.mock.patch or direct mock injection.
  </action>
  <verify>
    <automated>cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend && python -m pytest tests/unit/test_dashboard_read_service.py -x -v 2>&1 | tail -25</automated>
  </verify>
  <done>test_dashboard_read_service.py passes. DashboardReadService imports cleanly. All 4 methods exist and are async.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: InsightReadService + HealthReadService + tests</name>
  <files>
    backend/app/services/insights/insight_read_service.py
    backend/app/services/dashboards/health_read_service.py
    backend/tests/unit/test_insight_read_service.py
    backend/tests/unit/test_health_read_service.py
  </files>
  <read_first>
    backend/app/models/insights/daily_insight.py
    backend/app/models/pipeline.py
    backend/app/services/insights/insight_service.py
    backend/app/services/insights/insight_repository.py
    backend/tests/factories/insight_factory.py
    backend/app/schemas/insights/daily_insight_schema.py
  </read_first>
  <behavior>
    InsightReadService.get_today():
    - Computes yesterday_bucharest = date.today() in Europe/Bucharest - timedelta(1)
    - Queries DailyInsight WHERE tenant_id=:tid AND date = yesterday_bucharest
    - Returns dict with keys: date, status, generation_failed (bool), generated_at, payload (DailyInsightResponse dict or None)
    - generation_failed=True when status in ('failed', 'fallback') — specifically True when payload_json represents a fallback (INSI-05)
    - Returns None when no row exists for yesterday
    - generation_failed=False when status='success'

    InsightReadService.get_by_date(query_date: date):
    - Queries DailyInsight WHERE tenant_id=:tid AND date = query_date
    - Returns dict same structure as get_today() when row exists
    - Returns None when no row exists (router will 404 on None — INSI-02)

    HealthReadService.get_health():
    - Queries SyncRun WHERE source='mefi' ORDER BY started_at DESC LIMIT 1
    - Queries PipelineRun ORDER BY started_at DESC LIMIT 1
    - Computes stale: bool = last_sync_at is None OR (datetime.now(UTC) - last_sync_at) > timedelta(hours=26) (UI-06)
    - Returns dict: {last_sync_at: datetime | None, last_pipeline_status: str | None, stale: bool}
    - last_sync_at = SyncRun.completed_at of most recent mefi sync

    Tests:
    - test_get_today_returns_yesterday_insight: mock session returns DailyInsight with status='success'; assert returned dict.generation_failed=False
    - test_get_today_returns_none_when_no_row: mock session returns None; assert returned value is None
    - test_get_today_failed_status_sets_generation_failed: DailyInsight.status='failed' → generation_failed=True
    - test_get_today_fallback_status_sets_generation_failed: DailyInsight.status='fallback' → generation_failed=True
    - test_get_by_date_returns_none_for_missing: mock returns None for specific date → service returns None
    - test_get_by_date_returns_insight_dict: mock returns row → dict with correct date
    - test_health_stale_when_last_sync_over_26h: last_sync_at = now() - 27h → stale=True
    - test_health_not_stale_when_recent: last_sync_at = now() - 1h → stale=False
    - test_health_stale_when_no_sync_run: SyncRun query returns None → stale=True
    - test_health_last_pipeline_status: PipelineRun.status='success' → last_pipeline_status='success'
  </behavior>
  <action>
    Create backend/app/services/insights/insight_read_service.py with `from __future__ import annotations`.

    Imports: datetime (date, datetime, timezone, timedelta), zoneinfo (ZoneInfo), uuid (UUID), sqlalchemy (select), sqlalchemy.ext.asyncio (AsyncSession), structlog, app.models.insights.daily_insight (DailyInsight).

    BUCHAREST = ZoneInfo("Europe/Bucharest")

    Class InsightReadService:
      __init__(self, session: AsyncSession, tenant_id: UUID) -> None

      async def get_today(self) -> dict | None:
        now_bucharest = datetime.now(BUCHAREST)
        yesterday = now_bucharest.date() - timedelta(days=1)
        return await self._fetch_for_date(yesterday)

      async def get_by_date(self, query_date: date) -> dict | None:
        return await self._fetch_for_date(query_date)

      async def _fetch_for_date(self, query_date: date) -> dict | None:
        stmt = select(DailyInsight).where(
          DailyInsight.tenant_id == self._tenant_id,
          DailyInsight.date == query_date,
        )
        result = await self._session.execute(stmt)
        row = result.scalar_one_or_none()
        if row is None:
          return None
        generation_failed = row.status in ("failed", "fallback")
        return {
          "date": row.date,
          "status": row.status,
          "generation_failed": generation_failed,
          "generated_at": row.generated_at,
          "payload": row.payload_json,
        }

    Create backend/app/services/dashboards/health_read_service.py with `from __future__ import annotations`.

    Imports: datetime (datetime, timezone, timedelta), uuid (UUID), sqlalchemy (select), sqlalchemy.ext.asyncio (AsyncSession), structlog, app.models.pipeline (SyncRun, PipelineRun).

    Class HealthReadService:
      __init__(self, session: AsyncSession, tenant_id: UUID) -> None

      async def get_health(self) -> dict:
        stmt_sync = select(SyncRun.completed_at, SyncRun.status).where(
          SyncRun.tenant_id == self._tenant_id,
          SyncRun.source == "mefi",
        ).order_by(SyncRun.started_at.desc()).limit(1)

        stmt_pipeline = select(PipelineRun.status).where(
          PipelineRun.tenant_id == self._tenant_id,
        ).order_by(PipelineRun.started_at.desc()).limit(1)

        sync_result = await self._session.execute(stmt_sync)
        sync_row = sync_result.first()
        pipeline_result = await self._session.execute(stmt_pipeline)
        pipeline_row = pipeline_result.first()

        last_sync_at: datetime | None = sync_row.completed_at if sync_row else None
        last_pipeline_status: str | None = pipeline_row.status if pipeline_row else None

        now_utc = datetime.now(timezone.utc)
        if last_sync_at is None:
          stale = True
        else:
          # Ensure timezone-aware comparison
          if last_sync_at.tzinfo is None:
            last_sync_at = last_sync_at.replace(tzinfo=timezone.utc)
          stale = (now_utc - last_sync_at) > timedelta(hours=26)

        return {
          "last_sync_at": last_sync_at,
          "last_pipeline_status": last_pipeline_status,
          "stale": stale,
        }

    Create test files with AsyncMock patterns matching existing test patterns in tests/unit/.
    For InsightReadService tests: mock session.execute to return a MagicMock whose scalar_one_or_none() returns a mock DailyInsight object with the required fields.
    For HealthReadService tests: mock session.execute side_effect to return different results for sync vs pipeline queries.
  </action>
  <verify>
    <automated>cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend && python -m pytest tests/unit/test_insight_read_service.py tests/unit/test_health_read_service.py -x -v 2>&1 | tail -25</automated>
  </verify>
  <done>Both test files pass. InsightReadService and HealthReadService import cleanly. get_today uses yesterday in Europe/Bucharest timezone. stale logic based on 26h threshold.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service→DB | All ORM SELECT queries must include tenant_id WHERE clause — with_loader_criteria fires on ORM SELECT; text() queries require explicit :tid parameter |
| junk_by_source | raw_mefi_leads queried via text() — must bind tenant_id parameter explicitly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-02-01 | Information Disclosure | DashboardReadService tenant isolation | mitigate | All ORM queries include .where(Model.tenant_id == self._tenant_id); text() queries bind :tid parameter |
| T-06-02-02 | Information Disclosure | junk_by_source text() query | mitigate | text() query binds tenant_id via bindparam — Pitfall 5: with_loader_criteria does NOT fire on text() |
| T-06-02-03 | Tampering | Decimal arithmetic win_rate | accept | win_rate computed in Python with None guard — no precision loss from float division |
| T-06-02-04 | Information Disclosure | InsightReadService payload_json | accept | payload_json stores structured anomaly/insight data only; T-05-02-01 ensures no PII in payload |
| T-06-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages — all dependencies already slopcheck-verified |
</threat_model>

<verification>
cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend && python -m pytest tests/unit/test_dashboard_read_service.py tests/unit/test_insight_read_service.py tests/unit/test_health_read_service.py -v 2>&1 | tail -30
</verification>

<success_criteria>
- DashboardReadService exists with async methods: get_sales_dashboard, get_salespeople_dashboard, get_marketing_dashboard, get_stuck_offers
- Revenue series query uses daily granularity per locked decision (not weekly grouping)
- InsightReadService.get_today() queries date = yesterday (Europe/Bucharest)
- InsightReadService.get_by_date(date) returns None for missing dates
- HealthReadService.get_health() returns stale=True when last_sync_at > 26h ago
- All queries filter by tenant_id
- junk_by_source queries raw_mefi_leads with explicit tenant_id bind parameter (comment MARK-04 exception)
- All 3 test files pass
</success_criteria>

<output>
Create `.planning/phases/06-backend-api/06-02-SUMMARY.md` when done
</output>
