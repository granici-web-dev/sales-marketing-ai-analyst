---
phase: 06-backend-api
plan: 03
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - backend/app/api/v1/dashboards.py
  - backend/app/api/v1/insights.py
  - backend/app/api/v1/router.py
  - backend/app/main.py
  - backend/tests/unit/test_insights_router.py
  - backend/tests/integration/test_api_auth.py
autonomous: true
requirements:
  - SALE-01
  - SALE-02
  - SALE-03
  - SALE-04
  - SALE-05
  - SALE-06
  - SALE-07
  - SALES-01
  - SALES-02
  - SALES-03
  - SALES-04
  - MARK-01
  - MARK-02
  - MARK-03
  - MARK-04
  - INSI-01
  - INSI-02
  - INSI-03
  - INSI-04
  - INSI-05
  - INSI-06
  - UI-06
  - PIPE-04

must_haves:
  truths:
    - "GET /api/v1/dashboards/sales?from=2026-05-01&to=2026-05-19 returns 200 with SalesDashboardResponse"
    - "GET /api/v1/dashboards/salespeople?from=...&to=... returns 200 with SalespeopleDashboardResponse"
    - "GET /api/v1/dashboards/marketing?from=...&to=... returns 200 with MarketingDashboardResponse"
    - "GET /api/v1/insights/today returns 200 with insight payload"
    - "GET /api/v1/insights?date=YYYY-MM-DD returns 404 when no insight row exists"
    - "POST /api/v1/insights/refresh enqueues the full daily_pipeline chain (not insights-only)"
    - "POST /api/v1/insights/refresh returns 429 with Retry-After header within the 1-hour rate limit window"
    - "GET /api/v1/health/data returns last_sync_at, last_pipeline_status, stale flag"
    - "All endpoints return 401 for missing/invalid JWT"
    - "from query param uses alias='from' to avoid Python keyword conflict"
  artifacts:
    - path: "backend/app/api/v1/dashboards.py"
      provides: "3 dashboard GET endpoints"
      exports: ["router"]
    - path: "backend/app/api/v1/insights.py"
      provides: "GET /insights/today, GET /insights, POST /insights/refresh"
      exports: ["router"]
    - path: "backend/app/api/v1/router.py"
      provides: "Updated to include dashboards and insights routers"
    - path: "backend/tests/unit/test_insights_router.py"
      provides: "Rate limit unit tests"
    - path: "backend/tests/integration/test_api_auth.py"
      provides: "401 integration tests for all new endpoints"
  key_links:
    - from: "backend/app/api/v1/dashboards.py"
      to: "backend/app/services/dashboards/dashboard_read_service.py"
      via: "DashboardReadService(session, current_user.tenant_id)"
    - from: "backend/app/api/v1/insights.py"
      to: "backend/app/tasks/etl/sync_mefi_leads.py"
      via: "daily_pipeline(str(tenant_id)).delay()"
    - from: "backend/app/api/v1/insights.py"
      to: "redis rate_limit:refresh:{user_id}"
      via: "await r.set(key, '1', nx=True, ex=3600)"
---

<objective>
Wire the FastAPI routers that call the read services from Plan 02 and return the response schemas from Plan 01. Register all routers. Add the /health/data endpoint. Write unit tests for rate limiting and integration tests for auth.

Purpose: This is the HTTP surface that the frontend (Phase 7) will consume. All endpoints go live in this plan.
Output: dashboards.py, insights.py, updated router.py, rate-limit unit tests, auth integration tests.
</objective>

<execution_context>
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.planning/PROJECT.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.planning/ROADMAP.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.planning/phases/06-backend-api/06-RESEARCH.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.planning/phases/06-backend-api/06-01-SUMMARY.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.planning/phases/06-backend-api/06-02-SUMMARY.md

<interfaces>
<!-- Key contracts from Plans 01 and 02 that this plan consumes. -->

From backend/app/core/dependencies.py (Plan 01):
  async def get_current_user(
    credentials: HTTPAuthorizationCredentials = Depends(security),
    session: AsyncSession = Depends(get_session),
  ) -> UserOut
  — UserOut.id: UUID  (used for rate-limit key)
  — raises HTTPException(401) on invalid token

From backend/app/schemas/dashboards/sales.py (Plan 01):
  SalesDashboardResponse(period, funnel, conversion_rates, kpi_cards, source_breakdown, revenue_series, stuck_offers)

From backend/app/schemas/dashboards/salespeople.py (Plan 01):
  SalespeopleDashboardResponse(period, salespeople)

From backend/app/schemas/dashboards/marketing.py (Plan 01):
  MarketingDashboardResponse(period, lead_volume_by_source, site_conversion_rate, junk_by_source, ad_spend, cpl, cac, roas)

From backend/app/schemas/health.py (Plan 01):
  HealthDataResponse(last_sync_at, last_pipeline_status, stale)

From backend/app/services/dashboards/dashboard_read_service.py (Plan 02):
  DashboardReadService(session: AsyncSession, tenant_id: UUID)
    .get_sales_dashboard(from_date, to_date) -> dict
    .get_salespeople_dashboard(from_date, to_date) -> dict
    .get_marketing_dashboard(from_date, to_date) -> dict

From backend/app/services/dashboards/health_read_service.py (Plan 02):
  HealthReadService(session: AsyncSession, tenant_id: UUID)
    .get_health() -> dict

From backend/app/services/insights/insight_read_service.py (Plan 02):
  InsightReadService(session: AsyncSession, tenant_id: UUID)
    .get_today() -> dict | None
    .get_by_date(query_date: date) -> dict | None

From backend/app/tasks/etl/sync_mefi_leads.py (existing):
  def daily_pipeline(tenant_id: str) -> chain object
    — triggers: sync_mefi_leads → calculate_daily_kpis → detect_anomalies → generate_daily_insights
    — call: daily_pipeline(str(tenant_id)).delay()

From backend/app/core/config.py:
  settings.redis_url: str
  settings.sofa_belle_tenant_id: str

From backend/app/api/v1/router.py (existing):
  api_router = APIRouter()
  api_router.include_router(auth.router)
  api_router.include_router(health.router)  # existing health.py with GET /health/live

From backend/app/api/v1/health.py (existing):
  router = APIRouter(prefix="/health", tags=["health"])
  @router.get("/live", response_model=HealthResponse)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: dashboards.py router + health/data endpoint + router registration</name>
  <files>
    backend/app/api/v1/dashboards.py
    backend/app/api/v1/health.py
    backend/app/api/v1/router.py
  </files>
  <read_first>
    backend/app/api/v1/auth.py
    backend/app/api/v1/health.py
    backend/app/api/v1/router.py
    backend/app/core/dependencies.py
    backend/app/schemas/dashboards/sales.py
    backend/app/schemas/dashboards/salespeople.py
    backend/app/schemas/dashboards/marketing.py
    backend/app/schemas/health.py
    backend/app/services/dashboards/dashboard_read_service.py
    backend/app/services/dashboards/health_read_service.py
    backend/app/db/deps.py
  </read_first>
  <behavior>
    GET /api/v1/dashboards/sales:
    - Accepts from_date: date = Query(..., alias="from") and to_date: date = Query(..., alias="to")
    - Requires Depends(get_current_user) — returns 401 if token missing/invalid
    - Calls DashboardReadService(session, UUID(settings.sofa_belle_tenant_id)).get_sales_dashboard(from_date, to_date)
    - Returns SalesDashboardResponse with response_model= annotation
    - Non-ISO-8601 date returns 422 (FastAPI automatic validation)

    GET /api/v1/dashboards/salespeople:
    - Same auth and date param pattern
    - Calls get_salespeople_dashboard(from_date, to_date)
    - Returns SalespeopleDashboardResponse

    GET /api/v1/dashboards/marketing:
    - Same auth and date param pattern
    - Calls get_marketing_dashboard(from_date, to_date)
    - Returns MarketingDashboardResponse

    GET /api/v1/health/data (added to existing health.py):
    - No auth required (health check is public) — do NOT add Depends(get_current_user)
    - Calls HealthReadService(session, UUID(settings.sofa_belle_tenant_id)).get_health()
    - Returns HealthDataResponse

    Router registration:
    - api_router.include_router(dashboards.router) and api_router.include_router(insights.router) added to router.py
    - /health/data added to health.py alongside existing /health/live
  </behavior>
  <action>
    Create backend/app/api/v1/dashboards.py with `from __future__ import annotations`.

    Imports: datetime.date, uuid.UUID, fastapi (APIRouter, Depends, Query), sqlalchemy.ext.asyncio (AsyncSession), structlog, app.db.deps (get_session), app.core.dependencies (get_current_user), app.core.config (settings), app.schemas.auth (UserOut), app.schemas.dashboards.sales (SalesDashboardResponse), app.schemas.dashboards.salespeople (SalespeopleDashboardResponse), app.schemas.dashboards.marketing (MarketingDashboardResponse), app.services.dashboards.dashboard_read_service (DashboardReadService).

    router = APIRouter(prefix="/dashboards", tags=["dashboards"])
    logger = structlog.get_logger(__name__)

    Three endpoints following the auth.py thin-router pattern:
    @router.get("/sales", response_model=SalesDashboardResponse)
    async def get_sales_dashboard(
        from_date: date = Query(..., alias="from"),
        to_date: date = Query(..., alias="to"),
        session: AsyncSession = Depends(get_session),
        current_user: UserOut = Depends(get_current_user),
    ) -> SalesDashboardResponse:
        svc = DashboardReadService(session, UUID(settings.sofa_belle_tenant_id))
        data = await svc.get_sales_dashboard(from_date, to_date)
        return SalesDashboardResponse(**data)

    (Repeat for /salespeople and /marketing with appropriate service calls and response types.)
    Log user_id (not email — PII rule) at DEBUG level on each request.

    Update backend/app/api/v1/health.py — read it first, keep existing /health/live endpoint, ADD:
    @router.get("/data", response_model=HealthDataResponse)
    async def get_health_data(
        session: AsyncSession = Depends(get_session),
    ) -> HealthDataResponse:
        svc = HealthReadService(session, UUID(settings.sofa_belle_tenant_id))
        data = await svc.get_health()
        return HealthDataResponse(**data)

    Imports to add to health.py: uuid.UUID, sqlalchemy.ext.asyncio.AsyncSession, fastapi.Depends, app.db.deps.get_session, app.core.config.settings, app.schemas.health.HealthDataResponse, app.services.dashboards.health_read_service.HealthReadService.

    Update backend/app/api/v1/router.py — read it first, add:
    from app.api.v1 import dashboards, insights
    api_router.include_router(dashboards.router)
    api_router.include_router(insights.router)
    Keep existing include_router(auth.router) and include_router(health.router) lines unchanged.
  </action>
  <verify>
    <automated>cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend && python -c "from app.api.v1.dashboards import router as d; from app.api.v1.router import api_router; print('routers ok'); routes = [r.path for r in api_router.routes]; print(routes)"</automated>
  </verify>
  <done>dashboards.py has 3 GET endpoints. health.py has GET /data alongside existing /live. router.py includes dashboards and insights routers. All import cleanly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: insights.py router (today, by-date, refresh with rate limit) + tests</name>
  <files>
    backend/app/api/v1/insights.py
    backend/tests/unit/test_insights_router.py
    backend/tests/integration/test_api_auth.py
  </files>
  <read_first>
    backend/app/api/v1/auth.py
    backend/app/core/dependencies.py
    backend/app/schemas/insights/daily_insight_schema.py
    backend/app/services/insights/insight_read_service.py
    backend/app/tasks/etl/sync_mefi_leads.py
    backend/app/core/config.py
    backend/tests/integration/test_auth.py
    backend/tests/conftest.py
  </read_first>
  <behavior>
    GET /api/v1/insights/today:
    - Calls InsightReadService.get_today() — returns 200 with insight dict when row exists
    - Returns 404 with {"detail": "No insight available for today"} when get_today() returns None
    - Requires Depends(get_current_user) — 401 if no valid token

    GET /api/v1/insights:
    - Accepts date: date = Query(...) parameter
    - Calls InsightReadService.get_by_date(date)
    - Returns 200 with insight dict when row exists
    - Returns 404 with {"detail": "No insight for this date"} when None (INSI-02)
    - Requires Depends(get_current_user)

    POST /api/v1/insights/refresh:
    - Requires Depends(get_current_user) — 401 if no valid token
    - Rate limit: key = f"rate_limit:refresh:{current_user.id}" in Redis, SET NX EX 3600
    - If key already exists: return 429 with header Retry-After: {ttl} and body {"detail": "Rate limit exceeded. Try again in {ttl} seconds."}
    - If key does NOT exist: import and call daily_pipeline(str(UUID(settings.sofa_belle_tenant_id))).delay() (per locked decision: full pipeline chain, not insights-only)
    - Return 202 with RefreshResponse(pipeline_run_id=str(task.id), enqueued_at=datetime.now(UTC).isoformat())
    - Redis connection: use async with aioredis.from_url(settings.redis_url, decode_responses=True) as r — MUST use async context manager to avoid connection leak (Pitfall 4)
    - Do NOT import or call AsyncAnthropic or any Claude API client — AI-09 enforcement

    Tests (unit — test_insights_router.py):
    - test_refresh_rate_limit_first_call_enqueues: mock Redis SET NX returns True, mock daily_pipeline.delay → returns 202 with pipeline_run_id
    - test_refresh_rate_limit_second_call_returns_429: mock Redis SET NX returns None (key exists), mock ttl returns 3000 → returns 429 with Retry-After: 3000 header
    - test_refresh_does_not_call_claude_directly: assert no calls to anthropic.AsyncAnthropic or client.messages during refresh (AI-09)

    Integration tests (test_api_auth.py — update or create):
    - test_sales_dashboard_requires_auth: GET /api/v1/dashboards/sales without Authorization → 401
    - test_salespeople_dashboard_requires_auth: GET /api/v1/dashboards/salespeople without Authorization → 401
    - test_marketing_dashboard_requires_auth: GET /api/v1/dashboards/marketing without Authorization → 401
    - test_insights_today_requires_auth: GET /api/v1/insights/today without Authorization → 401
    - test_insights_by_date_requires_auth: GET /api/v1/insights?date=2026-05-01 without Authorization → 401
    - test_insights_refresh_requires_auth: POST /api/v1/insights/refresh without Authorization → 401
    - test_health_data_no_auth_required: GET /api/v1/health/data without Authorization → NOT 401 (health is public)

    For integration tests, use async_client from conftest.py. Services should be mocked or the tests marked to not need DB.
  </behavior>
  <action>
    Create backend/app/api/v1/insights.py with `from __future__ import annotations`.

    Imports: datetime (datetime, timezone, date), uuid (UUID), fastapi (APIRouter, Depends, HTTPException, Query, Response), fastapi.responses (JSONResponse), pydantic (BaseModel), redis.asyncio as aioredis, sqlalchemy.ext.asyncio (AsyncSession), structlog, app.db.deps (get_session), app.core.dependencies (get_current_user), app.core.config (settings), app.schemas.auth (UserOut).

    RefreshResponse(BaseModel): pipeline_run_id: str, enqueued_at: str

    RATE_LIMIT_TTL = 3600  # seconds

    router = APIRouter(prefix="/insights", tags=["insights"])
    logger = structlog.get_logger(__name__)

    @router.get("/today"):
    async def get_insight_today(
        session: AsyncSession = Depends(get_session),
        current_user: UserOut = Depends(get_current_user),
    ) -> dict:
        from app.services.insights.insight_read_service import InsightReadService
        svc = InsightReadService(session, UUID(settings.sofa_belle_tenant_id))
        data = await svc.get_today()
        if data is None:
            raise HTTPException(404, "No insight available for today")
        return data

    Note on response_model: GET /today and GET /insights return a dict that wraps DailyInsightResponse payload_json. Use response_model=None and return dict directly, or define a wrapper schema InsightEnvelope in insights.py with fields: date, status, generation_failed, generated_at, payload. Use response_model=InsightEnvelope.
    InsightEnvelope(BaseModel): date: date, status: str, generation_failed: bool, generated_at: datetime | None, payload: dict | None  — no response_model=DailyInsightResponse directly because the payload is a nested JSON.

    @router.get(""):  # GET /insights?date=YYYY-MM-DD
    async def get_insight_by_date(
        query_date: date = Query(..., alias="date"),
        session: AsyncSession = Depends(get_session),
        current_user: UserOut = Depends(get_current_user),
    ) -> dict:
        from app.services.insights.insight_read_service import InsightReadService
        svc = InsightReadService(session, UUID(settings.sofa_belle_tenant_id))
        data = await svc.get_by_date(query_date)
        if data is None:
            raise HTTPException(404, "No insight for this date")
        return data

    @router.post("/refresh", status_code=202, response_model=RefreshResponse)
    async def refresh_insights(
        response: Response,
        session: AsyncSession = Depends(get_session),
        current_user: UserOut = Depends(get_current_user),
    ) -> RefreshResponse:
        rate_key = f"rate_limit:refresh:{current_user.id}"
        async with aioredis.from_url(settings.redis_url, decode_responses=True) as r:
            was_set = await r.set(rate_key, "1", nx=True, ex=RATE_LIMIT_TTL)
            if not was_set:
                ttl = await r.ttl(rate_key)
                retry_after = max(ttl, 0)
                raise HTTPException(
                    status_code=429,
                    detail=f"Rate limit exceeded. Try again in {retry_after} seconds.",
                    headers={"Retry-After": str(retry_after)},
                )

        # Per locked decision: trigger the FULL daily_pipeline chain
        # (sync_mefi_leads → calculate_daily_kpis → detect_anomalies → generate_daily_insights)
        # NOT insights-only. This intentionally re-syncs from MEFI + recalculates all KPIs.
        from app.tasks.etl.sync_mefi_leads import daily_pipeline
        tenant_id_str = str(UUID(settings.sofa_belle_tenant_id))
        task = daily_pipeline(tenant_id_str).delay()

        logger.info("insights.refresh_enqueued", user_id=str(current_user.id), task_id=str(task.id))
        return RefreshResponse(
            pipeline_run_id=str(task.id),
            enqueued_at=datetime.now(timezone.utc).isoformat(),
        )

    Create backend/tests/unit/test_insights_router.py:
    Import: unittest.mock (AsyncMock, MagicMock, patch), pytest, pytest.mark.asyncio.
    Test rate limit logic in isolation — do not need a real Redis or Celery. Mock the aioredis.from_url context manager and daily_pipeline.
    - test_refresh_rate_limit_first_call_enqueues: patch aioredis.from_url context manager; r.set returns True; r.ttl not called; patch daily_pipeline as MagicMock; call refresh_insights endpoint logic; assert daily_pipeline.delay() called once
    - test_refresh_rate_limit_second_call_returns_429: r.set returns None; r.ttl returns 3000; assert HTTPException raised with status_code=429 and headers["Retry-After"] == "3000"
    - test_refresh_ai09_no_anthropic: patch daily_pipeline; assert "anthropic" not in module's namespace at call time

    Update backend/tests/integration/test_api_auth.py (read existing test_auth.py first to follow the same pattern):
    Use async_client fixture from conftest.py.
    Add 7 tests listed in behavior section above.
    All unauthenticated requests (no Authorization header) should return 401.
    For test_health_data_no_auth_required: mock HealthReadService or accept 500 from missing DB — the key assertion is status != 401.
  </action>
  <verify>
    <automated>cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend && python -m pytest tests/unit/test_insights_router.py tests/integration/test_api_auth.py -x -v 2>&1 | tail -30</automated>
  </verify>
  <done>insights.py has 3 endpoints. Rate limit test: first call passes, second returns 429. Auth integration tests: all 6 protected endpoints return 401 without token. health/data returns non-401 without token. All tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→dashboards | Bearer JWT must be verified before any dashboard data is returned |
| client→insights/refresh | JWT required + Redis rate limit prevents DoS via repeated full-pipeline triggers |
| insights/refresh→Celery | daily_pipeline enqueued via task.delay() — never calling Claude API directly from HTTP handler |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03-01 | Spoofing | All dashboard + insight endpoints | mitigate | Depends(get_current_user) on all endpoints; 401 on missing/invalid JWT |
| T-06-03-02 | Denial of Service | POST /insights/refresh | mitigate | Redis SET NX EX 3600 rate limit per user_id; 429 with Retry-After on collision |
| T-06-03-03 | Tampering | Date query params | accept | FastAPI `date` type rejects non-ISO-8601 automatically → 422; no injection path |
| T-06-03-04 | Tampering | AI-09 violation — Claude in HTTP handler | mitigate | refresh endpoint calls daily_pipeline.delay() only; AsyncAnthropic never imported in api/v1/ |
| T-06-03-05 | Information Disclosure | /health/data (public) | accept | Returns only sync timestamps and pipeline status — no lead data, no PII |
| T-06-03-06 | Tampering | Redis connection leak | mitigate | async with aioredis.from_url(...) as r: context manager ensures connection closed after rate-limit check |
| T-06-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages — redis.asyncio already installed and slopcheck-verified |
</threat_model>

<verification>
# AI-09 check — Claude must NOT be called from HTTP handlers
cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend && grep -rn "anthropic\|AsyncAnthropic\|client\.messages" app/api/ && echo "FAIL: Claude API found in api/" || echo "PASS: No Claude API calls in api/"

# Route registration
cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend && python -c "from app.api.v1.router import api_router; paths = [r.path for r in api_router.routes]; print(paths); assert any('/dashboards/sales' in p for p in paths), 'missing /dashboards/sales'; assert any('/insights/today' in p for p in paths), 'missing /insights/today'; assert any('/health/data' in p for p in paths), 'missing /health/data'; print('all routes registered')"

# Unit + integration tests
cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend && python -m pytest tests/unit/test_insights_router.py tests/integration/test_api_auth.py -v 2>&1 | tail -30
</verification>

<success_criteria>
- GET /api/v1/dashboards/sales, /salespeople, /marketing — all registered with auth guard
- GET /api/v1/insights/today — returns 200 or 404, never without auth
- GET /api/v1/insights?date=YYYY-MM-DD — returns 200 or 404
- POST /api/v1/insights/refresh — triggers full daily_pipeline chain (locked decision), not insights-only
- POST /api/v1/insights/refresh — second call within 3600s returns 429 + Retry-After header
- GET /api/v1/health/data — public endpoint, no auth required
- grep app/api/ for anthropic returns zero matches (AI-09)
- test_insights_router.py and test_api_auth.py both pass
</success_criteria>

<output>
Create `.planning/phases/06-backend-api/06-03-SUMMARY.md` when done
</output>
