---
phase: 06-backend-api
plan: 04
type: execute
wave: 3
depends_on:
  - "06-01"
  - "06-02"
  - "06-03"
files_modified:
  - backend/tests/integration/test_phase6_endpoints.py
autonomous: true
requirements:
  - SALE-01
  - SALE-02
  - SALE-03
  - SALE-04
  - SALE-05
  - SALE-07
  - SALES-01
  - SALES-02
  - SALES-03
  - SALES-04
  - MARK-01
  - MARK-02
  - MARK-03
  - MARK-04
  - INSI-01
  - INSI-02
  - INSI-03
  - INSI-05
  - INSI-06
  - UI-06
  - PIPE-04

must_haves:
  truths:
    - "GET /api/v1/dashboards/sales with valid JWT and date range returns 200 with all required fields"
    - "All revenue and monetary fields in API responses are JSON strings, not numbers"
    - "GET /api/v1/insights?date=2020-01-01 returns 404 (no insight for that date)"
    - "POST /api/v1/insights/refresh with valid JWT returns 202 (first call) and 429 (second call within window)"
    - "GET /api/v1/health/data returns stale: bool field"
    - "OpenAPI schema at /docs is accessible and shows all Phase 6 endpoints"
    - "Full test suite passes with no regressions from Phases 1-5"
  artifacts:
    - path: "backend/tests/integration/test_phase6_endpoints.py"
      provides: "Integration tests for all 7 Phase 6 success criteria"
  key_links:
    - from: "backend/tests/integration/test_phase6_endpoints.py"
      to: "backend/app/api/v1/dashboards.py"
      via: "async_client.get('/api/v1/dashboards/sales?from=...&to=...')"
    - from: "backend/tests/integration/test_phase6_endpoints.py"
      to: "backend/app/services/dashboards/dashboard_read_service.py"
      via: "unittest.mock.patch on DashboardReadService"
---

<objective>
Write the integration test suite that exercises all 7 Phase 6 success criteria through the full FastAPI stack (router → service → schema → response). Mocks are used at the service layer, not at the DB layer, so tests run without a real database.

Purpose: Proves the full HTTP stack works end-to-end with proper auth, schema validation, Decimal serialization, 404/429 handling, and response structure.
Output: test_phase6_endpoints.py covering all Phase 6 success criteria.
</objective>

<execution_context>
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.planning/PROJECT.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.planning/ROADMAP.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.planning/phases/06-backend-api/06-RESEARCH.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.planning/phases/06-backend-api/06-01-SUMMARY.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.planning/phases/06-backend-api/06-02-SUMMARY.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.planning/phases/06-backend-api/06-03-SUMMARY.md

<interfaces>
<!-- Key contracts from prior plans that tests will mock and verify. -->

Endpoints to test:
  GET  /api/v1/dashboards/sales?from=YYYY-MM-DD&to=YYYY-MM-DD  → 200 SalesDashboardResponse | 401
  GET  /api/v1/dashboards/salespeople?from=...&to=...           → 200 SalespeopleDashboardResponse | 401
  GET  /api/v1/dashboards/marketing?from=...&to=...             → 200 MarketingDashboardResponse | 401
  GET  /api/v1/insights/today                                   → 200 InsightEnvelope | 404 | 401
  GET  /api/v1/insights?date=YYYY-MM-DD                         → 200 InsightEnvelope | 404 | 401
  POST /api/v1/insights/refresh                                 → 202 RefreshResponse | 429 | 401
  GET  /api/v1/health/data                                      → 200 HealthDataResponse (no auth)
  GET  /docs                                                    → 200 (OpenAPI schema)

Service mock paths for patching:
  "app.api.v1.dashboards.DashboardReadService"
  "app.api.v1.insights.InsightReadService"
  "app.api.v1.insights.daily_pipeline"
  "app.api.v1.insights.aioredis"
  "app.api.v1.health.HealthReadService"  (if health.py imports it)
  "app.core.dependencies.get_current_user"  (override with fixture)

From backend/app/core/security.py:
  create_access_token(data: dict) -> str  — use for minting test JWTs

From backend/tests/factories/dashboard_factory.py (Plan 01):
  DashboardFactory.build_sales_response(from_date, to_date) -> dict
  DashboardFactory.build_salespeople_response(from_date, to_date) -> dict
  DashboardFactory.build_marketing_response(from_date, to_date) -> dict
  DashboardFactory.build_health_response(stale=False) -> dict
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Phase 6 integration test suite (all 7 success criteria)</name>
  <files>
    backend/tests/integration/test_phase6_endpoints.py
  </files>
  <read_first>
    backend/tests/integration/test_auth.py
    backend/tests/integration/test_health.py
    backend/tests/conftest.py
    backend/app/core/security.py
    backend/tests/factories/dashboard_factory.py
    backend/tests/factories/insight_factory.py
    backend/app/api/v1/dashboards.py
    backend/app/api/v1/insights.py
    backend/app/api/v1/health.py
    backend/app/schemas/dashboards/sales.py
  </read_first>
  <behavior>
    SC#1 — GET /dashboards/sales with valid JWT:
    - Returns 200
    - Response JSON contains "funnel" key with leads, visits, offers, contracts
    - Response JSON contains "conversion_rates" key
    - Revenue fields in kpi_cards are JSON strings (not numbers) — isinstance check
    - Response time < 300ms under mock (assert not needed in unit — the 300ms is a production target)

    SC#2 — GET /dashboards/salespeople with valid JWT:
    - Returns 200
    - Response JSON contains "salespeople" list with at least 1 entry
    - SalespersonRow has avg_time_to_first_touch_minutes field (nullable)
    - SalespersonRow has data_completeness_pct field

    SC#3 — GET /dashboards/marketing with valid JWT:
    - Returns 200
    - Response JSON contains "lead_volume_by_source" list
    - Response JSON has "junk_by_source" list
    - Response JSON has "ad_spend": null (MARK-03)
    - Response JSON has "cpl": null, "cac": null, "roas": null

    SC#4 — GET /insights/today and GET /insights?date=...:
    - GET /insights/today with valid JWT returns 200 with "date", "status", "generation_failed", "payload" fields
    - GET /insights?date=2020-01-01 returns 404 (service returns None for that date)
    - GET /insights/today returns 404 when service returns None (no insight for yesterday)
    - generation_failed=True when insight status='failed'

    SC#5 — POST /insights/refresh rate limiting:
    - First call: mock Redis SET NX returns True → 202 with pipeline_run_id (UUID-like string) and enqueued_at
    - Second call within window: mock Redis SET NX returns None, ttl=3000 → 429 response with Retry-After header = "3000"
    - Verify daily_pipeline.delay() called on first call, NOT called on second

    SC#6 — GET /health/data:
    - Returns 200 without Authorization header
    - Response contains "last_sync_at", "last_pipeline_status", "stale" fields
    - When stale=True in mock response, response JSON has "stale": true

    SC#7 — OpenAPI schema:
    - GET /docs returns 200
    - GET /openapi.json returns 200 with "paths" key containing /api/v1/dashboards/sales

    Additional regression:
    - GET /healthz (root health) still returns {"status": "ok"} (Phase 1 regression guard)

    Auth boundary:
    - All dashboard and insight endpoints without Authorization header return 401
    - /health/data without Authorization returns 200 (not 401)
  </behavior>
  <action>
    Create backend/tests/integration/test_phase6_endpoints.py with `from __future__ import annotations`.

    Imports: pytest, pytest_asyncio, uuid (UUID), datetime (date, datetime, timezone, timedelta), decimal (Decimal), json, unittest.mock (AsyncMock, MagicMock, patch), httpx (AsyncClient, ASGITransport), app.main (app), app.core.security (create_access_token), app.core.config (settings), tests.factories.dashboard_factory (DashboardFactory).

    Helper fixture: valid_token (function-scoped) — mint a real JWT:
    token_data = {"sub": str(UUID("00000000-0000-0000-0000-000000000002")), "tenant_id": settings.sofa_belle_tenant_id}
    return create_access_token(token_data)

    Helper fixture: auth_client (function-scoped) — async context manager yielding httpx.AsyncClient with Authorization header:
    async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test") as c:
        c.headers.update({"Authorization": f"Bearer {valid_token}"})
        yield c

    For tests requiring mock services: use @pytest.mark.asyncio and patch the service class in the router's namespace.

    CRITICAL: get_current_user dependency requires a real DB query for the user row. To isolate from DB in integration tests, override the dependency in the app:

    @pytest.fixture
    def override_current_user():
        from app.core.dependencies import get_current_user
        from app.schemas.auth import UserOut
        mock_user = UserOut(id=UUID("00000000-0000-0000-0000-000000000002"), email="test@sofabelle.ro", is_active=True)
        async def _mock_get_current_user():
            return mock_user
        app.dependency_overrides[get_current_user] = _mock_get_current_user
        yield mock_user
        app.dependency_overrides.clear()

    Also override get_session to avoid DB connection:
    @pytest.fixture
    def override_session():
        from app.db.deps import get_session
        mock_session = AsyncMock()
        async def _mock_get_session():
            yield mock_session
        app.dependency_overrides[get_session] = _mock_get_session
        yield mock_session
        # get_current_user also uses get_session, so this applies everywhere

    Write tests:

    @pytest.mark.asyncio
    async def test_sc1_sales_dashboard_200(override_current_user, override_session):
        from_d, to_d = "2026-05-01", "2026-05-19"
        mock_data = DashboardFactory.build_sales_response(date(2026,5,1), date(2026,5,19))
        with patch("app.api.v1.dashboards.DashboardReadService") as MockSvc:
            instance = MockSvc.return_value
            instance.get_sales_dashboard = AsyncMock(return_value=mock_data)
            async with AsyncClient(transport=ASGITransport(app=app), base_url="http://test",
                                   headers={"Authorization": "Bearer dummy"}) as c:
                r = await c.get(f"/api/v1/dashboards/sales?from={from_d}&to={to_d}")
        assert r.status_code == 200
        body = r.json()
        assert "funnel" in body
        assert "conversion_rates" in body
        # Revenue as string check (DATA-04)
        if body["kpi_cards"]["revenue"] is not None:
            assert isinstance(body["kpi_cards"]["revenue"], str), "revenue must be JSON string"

    ... (follow same pattern for SC2–SC7 and auth boundary tests)

    For SC5 (rate limit test):
    Use patch("app.api.v1.insights.aioredis.from_url") as a context manager mock.
    First call: r_mock.set = AsyncMock(return_value=True); mock daily_pipeline → returns task with .id = UUID(...).
    Second call: r_mock.set = AsyncMock(return_value=None); r_mock.ttl = AsyncMock(return_value=3000).
    Assert response.headers["Retry-After"] == "3000" on 429.

    For SC7 (OpenAPI):
    async with AsyncClient(...) as c:
        r = await c.get("/docs")
    assert r.status_code == 200
    async with AsyncClient(...) as c:
        r = await c.get("/openapi.json")
    assert r.status_code == 200
    assert "/api/v1/dashboards/sales" in r.text

    For regression: GET /healthz → 200, body == {"status": "ok"}.

    After writing all tests, also verify full test suite doesn't regress:
    Run: cd backend && pytest --co -q 2>&1 | tail -10 to verify all prior tests still collect cleanly.
  </action>
  <verify>
    <automated>cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend && python -m pytest tests/integration/test_phase6_endpoints.py -v 2>&1 | tail -40</automated>
  </verify>
  <done>test_phase6_endpoints.py passes all SC#1–SC#7 tests and auth boundary tests. Full test suite collects without errors (no regressions from Phases 1–5).</done>
</task>

<task type="auto">
  <name>Task 2: Full suite regression check + OpenAPI validation</name>
  <files></files>
  <read_first>
    backend/app/main.py
    backend/app/api/v1/router.py
  </read_first>
  <action>
    Run the full test suite to catch any regressions:
      cd backend && pytest --cov=app --cov-report=term-missing -q 2>&1

    Verify OpenAPI spec correctness:
      cd backend && python -c "
    import json
    from app.main import app
    openapi = app.openapi()
    paths = list(openapi['paths'].keys())
    required = ['/api/v1/dashboards/sales', '/api/v1/dashboards/salespeople', '/api/v1/dashboards/marketing', '/api/v1/insights/today', '/api/v1/health/data']
    for p in required:
        assert p in paths, f'Missing path: {p}'
    print('OpenAPI paths OK:', required)
    # Verify revenue field is NOT of type number in schema
    sales_schema = openapi['paths'].get('/api/v1/dashboards/sales', {})
    print('Sales endpoint schema present:', bool(sales_schema))
    "

    Verify AI-09 (no Claude API in HTTP handlers):
      cd backend && grep -rn "anthropic\|AsyncAnthropic\|client\.messages" app/api/ && echo "FAIL: AI-09 violation" || echo "PASS: AI-09 compliant"

    Verify revenue time series uses daily granularity (locked decision):
      cd backend && grep -n "revenue_series\|revenue.*series\|daily.*granular" app/services/dashboards/dashboard_read_service.py | head -10

    If any test fails in the full suite: investigate root cause, fix in the failing file (not workarounds), then re-run the suite.
    Document the final test count in the SUMMARY.
  </action>
  <verify>
    <automated>cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend && python -m pytest -q --tb=no 2>&1 | tail -10</automated>
  </verify>
  <done>Full pytest suite passes (or all failures are pre-existing and unrelated to Phase 6). AI-09 grep returns zero matches. OpenAPI spec contains all 5 required Phase 6 paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test→app | Tests use dependency_overrides to isolate from DB and Redis — no credentials or real data exposed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04-01 | Information Disclosure | Test fixtures with mock user | accept | Test user UUID does not correspond to real user; no real credentials in test code |
| T-06-04-02 | Tampering | Full suite regression | mitigate | Task 2 runs full pytest suite; any pre-existing failures must be confirmed pre-existing, not introduced by Phase 6 |
| T-06-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages — tests use existing pytest + httpx already installed |
</threat_model>

<verification>
# All Phase 6 success criteria tests pass
cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend && python -m pytest tests/integration/test_phase6_endpoints.py -v

# Full suite (phases 1-6)
cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend && python -m pytest -q --tb=short 2>&1 | tail -20

# AI-09 enforcement
cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend && grep -rn "anthropic\|AsyncAnthropic" app/api/ && echo "FAIL" || echo "PASS: AI-09 clean"

# OpenAPI schema has all Phase 6 paths
cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend && python -c "from app.main import app; paths=list(app.openapi()['paths']); [print(p) for p in paths if 'dashboard' in p or 'insight' in p or 'health/data' in p]"
</verification>

<success_criteria>
- test_phase6_endpoints.py covers all 7 Phase 6 ROADMAP success criteria
- Revenue fields verified as JSON strings (not numbers) in SC#1 test
- 404 verified for missing insight date (SC#4)
- 429 + Retry-After header verified for second refresh call (SC#5)
- /health/data returns 200 without auth (SC#6)
- /docs returns 200 with OpenAPI schema (SC#7)
- All Phases 1–5 tests still passing (no regressions)
- AI-09 grep returns zero matches in app/api/
</success_criteria>

<output>
Create `.planning/phases/06-backend-api/06-04-SUMMARY.md` when done
</output>
