---
phase: 04-anomaly-detection
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - backend/alembic/versions/007_detected_problems.py
  - backend/app/models/anomaly/__init__.py
  - backend/app/models/anomaly/detected_problem.py
autonomous: true
requirements:
  - ANOM-01
  - ANOM-07

must_haves:
  truths:
    - "migration 007 creates detected_problems table with UNIQUE constraint on (tenant_id, date, rule_id)"
    - "DetectedProblem SQLAlchemy model has all columns from CONTEXT.md schema"
    - "alembic upgrade head runs without error from 006 to 007"
    - "alembic downgrade -1 from 007 drops detected_problems and returns to 006 cleanly"
  artifacts:
    - path: "backend/alembic/versions/007_detected_problems.py"
      provides: "Alembic migration for detected_problems table"
      contains: "op.create_table(\"detected_problems\""
    - path: "backend/app/models/anomaly/detected_problem.py"
      provides: "SQLAlchemy ORM model with 3-col UniqueConstraint"
      contains: "class DetectedProblem"
    - path: "backend/app/models/anomaly/__init__.py"
      provides: "Package init exporting DetectedProblem"
  key_links:
    - from: "backend/alembic/versions/007_detected_problems.py"
      to: "backend/alembic/versions/006_visits_count_nullable.py"
      via: "down_revision = '006'"
    - from: "backend/app/models/anomaly/detected_problem.py"
      to: "app.db.base.TenantScopedMixin"
      via: "class DetectedProblem(Base, TenantScopedMixin)"
---

<objective>
Create Alembic migration 007 adding the detected_problems table and the corresponding SQLAlchemy ORM model.

Purpose: Schema foundation that AnomalyRepository (Wave 3) writes into and that Phase 5 AI Insights reads from.
Output: Migration file + 2 model files. No service logic in this wave.
</objective>

<execution_context>
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.planning/phases/04-anomaly-detection/04-CONTEXT.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.planning/phases/03-metrics-engine/03-PATTERNS.md

<!-- Analog migration and model files -->
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/alembic/versions/006_visits_count_nullable.py
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/app/models/metrics/daily_kpi.py
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/app/db/base.py

<interfaces>
<!-- TenantScopedMixin provides: id (UUID PK), tenant_id (UUID NOT NULL), created_at (TIMESTAMPTZ), updated_at (TIMESTAMPTZ) -->
<!-- Base is SQLAlchemy DeclarativeBase -->
<!-- TIMESTAMPTZ = type alias for TIMESTAMP(timezone=True) -->

From backend/app/db/base.py (key exports):
  class TenantScopedMixin — adds id, tenant_id, created_at, updated_at
  Base — DeclarativeBase
  TIMESTAMPTZ — TIMESTAMP(timezone=True) alias

From backend/app/models/metrics/daily_kpi.py (UniqueConstraint pattern):
  __table_args__ = (UniqueConstraint("tenant_id", "date", name="uq_daily_kpi_tenant_date"),)

detected_problems schema (per 04-CONTEXT.md):
  id UUID PK DEFAULT gen_random_uuid()
  tenant_id UUID NOT NULL REFERENCES tenants(id)
  date DATE NOT NULL
  rule_id TEXT NOT NULL  -- 'slow_first_touch' | 'stuck_offer' | 'showroom_traffic_drop' | 'underperforming_salesperson' | 'junk_lead_quality'
  severity TEXT NOT NULL  -- 'low' | 'medium' | 'high'
  metric TEXT NOT NULL
  current_value NUMERIC(12,4) nullable
  expected_value NUMERIC(12,4) nullable
  estimated_loss_ron NUMERIC(12,2) nullable
  context_json JSONB nullable
  created_at TIMESTAMPTZ NOT NULL DEFAULT now()
  updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
  UNIQUE (tenant_id, date, rule_id)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Alembic migration 007 — detected_problems table</name>
  <files>backend/alembic/versions/007_detected_problems.py</files>
  <read_first>
    - backend/alembic/versions/006_visits_count_nullable.py — file header convention, revision chain pattern, op.create_table structure
    - backend/alembic/versions/004_metrics_schema.py — full op.create_table pattern with ForeignKeyConstraint, op.create_index
    - backend/app/db/base.py — TenantScopedMixin column names to replicate in migration DDL
  </read_first>
  <action>
    Create backend/alembic/versions/007_detected_problems.py

    File header pattern (copy from 006):
      - `from __future__ import annotations`
      - Docstring: "Create detected_problems table for Phase 4 anomaly detection."
      - revision = "007", down_revision = "006", branch_labels = None, depends_on = None
      - Imports: sqlalchemy as sa, from alembic import op, from sqlalchemy.dialects.postgresql import JSONB, UUID

    upgrade() function — single op.create_table("detected_problems", ...) call with columns:
      - sa.Column("id", UUID(as_uuid=True), primary_key=True, server_default=sa.text("gen_random_uuid()"), nullable=False)
      - sa.Column("tenant_id", UUID(as_uuid=True), nullable=False)
      - sa.Column("date", sa.Date, nullable=False)
      - sa.Column("rule_id", sa.Text, nullable=False)
      - sa.Column("severity", sa.Text, nullable=False)
      - sa.Column("metric", sa.Text, nullable=False)
      - sa.Column("current_value", sa.Numeric(12, 4), nullable=True)
      - sa.Column("expected_value", sa.Numeric(12, 4), nullable=True)
      - sa.Column("estimated_loss_ron", sa.Numeric(12, 2), nullable=True)
      - sa.Column("context_json", JSONB, nullable=True)
      - sa.Column("created_at", sa.TIMESTAMP(timezone=True), server_default=sa.text("now()"), nullable=False)
      - sa.Column("updated_at", sa.TIMESTAMP(timezone=True), server_default=sa.text("now()"), nullable=False)
      - sa.ForeignKeyConstraint(["tenant_id"], ["tenants.id"], name="fk_detected_problems_tenant_id")
      - sa.UniqueConstraint("tenant_id", "date", "rule_id", name="uq_detected_problems_tenant_date_rule")

    After op.create_table, add two op.create_index calls:
      - op.create_index("ix_detected_problems_tenant_date", "detected_problems", ["tenant_id", "date"])
      - op.create_index("ix_detected_problems_tenant_rule", "detected_problems", ["tenant_id", "rule_id"])

    downgrade() function:
      - op.drop_index("ix_detected_problems_tenant_rule", table_name="detected_problems")
      - op.drop_index("ix_detected_problems_tenant_date", table_name="detected_problems")
      - op.drop_table("detected_problems")

    CRITICAL — do NOT use ::jsonb PostgreSQL cast syntax in any sa.text() call. Use CAST(:val AS jsonb) with bindparams() if JSONB needed (see 03-PATTERNS.md JSONB pitfall).
  </action>
  <verify>
    <automated>cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend && python -c "import alembic.config; alembic.config.main(argv=['check'])" 2>&1 | head -5; python -c "from alembic.config import Config; from alembic.script import ScriptDirectory; cfg = Config('alembic.ini'); sd = ScriptDirectory.from_config(cfg); rev = sd.get_revision('007'); print('007 exists, down_rev:', rev.down_revision)"</automated>
  </verify>
  <done>
    Migration file 007_detected_problems.py exists. `alembic check` passes (no pending autogenerate changes). The revision "007" is loadable from ScriptDirectory with down_revision="006". downgrade() drops table and indexes in correct order.
  </done>
</task>

<task type="auto">
  <name>Task 2: DetectedProblem SQLAlchemy model + anomaly package init</name>
  <files>
    backend/app/models/anomaly/__init__.py
    backend/app/models/anomaly/detected_problem.py
  </files>
  <read_first>
    - backend/app/models/metrics/daily_kpi.py — full ORM model pattern: TenantScopedMixin, UniqueConstraint with 3 cols, Mapped column declarations, NUMERIC precision
    - backend/app/models/metrics/__init__.py — __init__.py pattern for model packages
    - backend/app/db/base.py — Base, TenantScopedMixin, TIMESTAMPTZ imports
  </read_first>
  <action>
    FILE 1: backend/app/models/anomaly/__init__.py
      Content: `from __future__ import annotations\n\nfrom app.models.anomaly.detected_problem import DetectedProblem\n\n__all__ = ["DetectedProblem"]`

    FILE 2: backend/app/models/anomaly/detected_problem.py

    Header:
      - `from __future__ import annotations`
      - Docstring: "SQLAlchemy ORM model for detected_problems table. One row per rule per day per tenant. UPSERT conflict target: (tenant_id, date, rule_id) — 3-col unique key. Phase 4 Plan 02 — schema foundation."
      - Imports: from __future__ import annotations, from datetime import date, datetime, from decimal import Decimal, from sqlalchemy import Date, Numeric, Text, UniqueConstraint, from sqlalchemy.dialects.postgresql import JSONB, from sqlalchemy.orm import Mapped, mapped_column, from app.db.base import Base, TenantScopedMixin, TIMESTAMPTZ

    Class `DetectedProblem(Base, TenantScopedMixin)`:
      - __tablename__ = "detected_problems"
      - __table_args__ — 3-column UniqueConstraint exactly (Pitfall 5 — 3-col): UniqueConstraint("tenant_id", "date", "rule_id", name="uq_detected_problems_tenant_date_rule")

      Columns (use Mapped[type] annotations, matching migration DDL exactly):
        - date: Mapped[date] = mapped_column(Date, nullable=False)
        - rule_id: Mapped[str] = mapped_column(Text, nullable=False) — one of the 5 rule_id string values
        - severity: Mapped[str] = mapped_column(Text, nullable=False) — 'low' | 'medium' | 'high'
        - metric: Mapped[str] = mapped_column(Text, nullable=False) — e.g. 'time_to_first_touch_minutes'
        - current_value: Mapped[Decimal | None] = mapped_column(Numeric(12, 4), nullable=True)
        - expected_value: Mapped[Decimal | None] = mapped_column(Numeric(12, 4), nullable=True)
        - estimated_loss_ron: Mapped[Decimal | None] = mapped_column(Numeric(12, 2), nullable=True) — NUMERIC(12,2) per D-19
        - context_json: Mapped[dict | None] = mapped_column(JSONB, nullable=True)
        - detected_at: Mapped[datetime] = mapped_column(TIMESTAMPTZ, server_default="now()", nullable=False) — alias for created_at semantics

      Do NOT add a `repr` method — keep model minimal, matching daily_kpi.py style.
  </action>
  <verify>
    <automated>cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend && python -c "from app.models.anomaly import DetectedProblem; from app.models.anomaly.detected_problem import DetectedProblem as DP; print('tablename:', DP.__tablename__); uc = [c for c in DP.__table_args__ if hasattr(c, 'columns')]; print('UniqueConstraint cols:', [col.key for col in uc[0].columns] if uc else 'NOT FOUND')"</automated>
  </verify>
  <done>
    `from app.models.anomaly import DetectedProblem` imports cleanly. DetectedProblem.__tablename__ == "detected_problems". UniqueConstraint covers exactly tenant_id, date, rule_id (3 columns). All 8 custom columns (date, rule_id, severity, metric, current_value, expected_value, estimated_loss_ron, context_json) are present as Mapped declarations. No float types — estimated_loss_ron is Numeric(12,2).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic migration→DB | DDL runs with DB superuser credentials; migration is trusted code path |
| ORM model→DB writes | Core INSERT bypasses with_loader_criteria; AnomalyRepository validates tenant_id per Pitfall 6 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Information Disclosure | detected_problems.context_json | mitigate | context_json contains lead external_ids (not PII) and numeric values only; no customer names, phones, emails |
| T-04-02-02 | Tampering | Migration down_revision chain | mitigate | down_revision="006" hardcoded — alembic check validates chain integrity before any upgrade |
| T-04-02-SC | Tampering | No new package installs in Wave 1 | accept | No new dependencies — all alembic/sqlalchemy types already in pyproject.toml |
</threat_model>

<verification>
1. `cd backend && alembic upgrade 007` — succeeds without error.
2. `cd backend && alembic downgrade 006` — drops detected_problems table and indexes cleanly.
3. `cd backend && python -c "from app.models.anomaly import DetectedProblem; print(DetectedProblem.__tablename__)"` — prints "detected_problems".
4. `cd backend && python -c "from app.models.anomaly.detected_problem import DetectedProblem; cols = {c.key for c in DetectedProblem.__table__.columns}; required = {'id','tenant_id','date','rule_id','severity','metric','current_value','expected_value','estimated_loss_ron','context_json','created_at','updated_at'}; missing = required - cols; print('missing:', missing)"` — prints "missing: set()".
</verification>

<success_criteria>
- Migration 007 exists with revision="007", down_revision="006"
- upgrade() creates detected_problems with UNIQUE(tenant_id, date, rule_id) and both performance indexes
- downgrade() drops indexes then table cleanly
- DetectedProblem model importable from app.models.anomaly
- All 8 domain columns present, monetary column NUMERIC(12,2), metric values NUMERIC(12,4)
- No float types anywhere in the model
</success_criteria>

<output>
Create .planning/phases/04-anomaly-detection/04-02-SUMMARY.md when done
</output>
