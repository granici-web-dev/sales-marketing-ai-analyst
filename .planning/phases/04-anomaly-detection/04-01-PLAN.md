---
phase: 04-anomaly-detection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/unit/test_anomaly_service.py
  - backend/tests/unit/test_anomaly_repository.py
  - backend/tests/integration/test_detect_anomalies_task.py
  - backend/tests/factories/anomaly_factory.py
autonomous: true
requirements:
  - ANOM-01
  - ANOM-02
  - ANOM-03
  - ANOM-04
  - ANOM-05
  - ANOM-06
  - ANOM-07

must_haves:
  truths:
    - "test_anomaly_service.py contains RED-state test functions for all 5 rule methods + run_all_rules"
    - "test_anomaly_repository.py contains RED-state tests for upsert_detected_problem with tenant_id guard"
    - "test_detect_anomalies_task.py contains RED-state integration test for end-to-end task execution"
    - "anomaly_factory.py produces DetectedProblemRowFactory, LeadWithNoTouchFactory, StuckOfferLeadFactory"
    - "All test files import-fail cleanly (ImportError on missing modules, not SyntaxError)"
  artifacts:
    - path: "backend/tests/unit/test_anomaly_service.py"
      provides: "RED tests for 6 rule methods"
    - path: "backend/tests/unit/test_anomaly_repository.py"
      provides: "RED tests for AnomalyRepository UPSERT"
    - path: "backend/tests/integration/test_detect_anomalies_task.py"
      provides: "RED integration test for detect_anomalies Celery task"
    - path: "backend/tests/factories/anomaly_factory.py"
      provides: "factory-boy fixtures for anomaly test data"
  key_links:
    - from: "test_anomaly_service.py"
      to: "app.services.anomaly.anomaly_service.AnomalyService"
      via: "deferred import inside _make_service()"
    - from: "test_anomaly_repository.py"
      to: "app.services.repositories.anomaly_repository.AnomalyRepository"
      via: "deferred import inside _make_repo()"
---

<objective>
Create RED-state test contracts for Phase 4 anomaly detection components before any implementation exists.

Purpose: TDD wave 0 — tests fail with ImportError now, turn GREEN after Waves 1-3 implement the targets.
Output: 4 files — unit tests for service + repository, integration test for Celery task, factory for test data.
</objective>

<execution_context>
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.planning/ROADMAP.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.planning/phases/04-anomaly-detection/04-CONTEXT.md

<!-- Analog test files — replicate their structure exactly -->
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/tests/unit/test_daily_kpi_service.py
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/tests/unit/test_metrics_repository.py
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/tests/integration/test_mefi_etl.py
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/tests/factories/metrics_factory.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create anomaly_factory.py with factory-boy fixtures</name>
  <files>backend/tests/factories/anomaly_factory.py</files>
  <read_first>
    - backend/tests/factories/metrics_factory.py — copy Factory class structure, Meta pattern, builder helpers
    - backend/tests/factories/mefi_factory.py — secondary analog for Sequence/LazyAttribute patterns
    - backend/app/db/base.py — TenantScopedMixin columns (id, tenant_id, created_at, updated_at)
  </read_first>
  <action>
    Create backend/tests/factories/anomaly_factory.py with:

    1. Module header: `from __future__ import annotations`, docstring, all imports (factory, datetime, Decimal, UUID, date).

    2. Constants: `TENANT_ID = UUID("00000000-0000-0000-0000-000000000001")`, `TODAY = date(2026, 5, 28)`.

    3. `DetectedProblemRowFactory(factory.Factory)` — Meta.model = dict. Fields:
       - tenant_id = TENANT_ID
       - date = factory.LazyFunction(lambda: TODAY)
       - rule_id = "slow_first_touch"
       - severity = "high"
       - metric = "time_to_first_touch_minutes"
       - current_value = Decimal("360.00")
       - expected_value = Decimal("300.00")
       - estimated_loss_ron = Decimal("5000.00")
       - context_json = factory.LazyFunction(lambda: {"count": 2, "lead_ids": ["ext-001", "ext-002"], "worst_hours_elapsed": 6.0})

    4. `LeadWithNoTouchFactory(factory.Factory)` — Meta.model = dict. Builds a raw_mefi_lead dict representing a yesterday-created lead with no contact attempt:
       - tenant_id = TENANT_ID
       - external_id = factory.Sequence(lambda n: f"lead-notouched-{n:04d}")
       - created_at_local = factory.LazyFunction(lambda: TODAY)
       - salesperson_external_id = "sp-001"
       - time_to_first_touch_minutes = None  (no contact)
       - lifecycle = "active"
       - funnel_stage = "lead"

    5. `StuckOfferLeadFactory(factory.Factory)` — Meta.model = dict. Builds a lead in 'oferta' stage with last_status_changed > 15 days ago:
       - tenant_id = TENANT_ID
       - external_id = factory.Sequence(lambda n: f"lead-stuck-{n:04d}")
       - funnel_stage = "oferta"
       - lifecycle = "active"
       - last_status_changed_at = factory.LazyFunction(lambda: TODAY - timedelta(days=20))
       - estimated_value = Decimal("25000.00")

    6. Builder helper `make_detected_problem_row(**overrides) -> dict` — calls DetectedProblemRowFactory.build(**overrides).

    7. Builder helper `make_daily_kpi_baseline_rows(n: int = 30, kpi_date=TODAY) -> list[dict]` — returns n dicts representing daily_kpi rows for baseline window, each with:
       - tenant_id = TENANT_ID
       - date = kpi_date - timedelta(days=i) for i in range(1, n+1)
       - conversion_l_to_v = Decimal("0.3200")
       - conversion_o_to_c = Decimal("0.1500")
       - avg_deal_size = Decimal("22000.00")
  </action>
  <verify>
    <automated>cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend && python -c "from tests.factories.anomaly_factory import DetectedProblemRowFactory, LeadWithNoTouchFactory, StuckOfferLeadFactory, make_detected_problem_row, make_daily_kpi_baseline_rows; r = DetectedProblemRowFactory.build(); assert r['rule_id'] == 'slow_first_touch'; assert r['severity'] == 'high'; rows = make_daily_kpi_baseline_rows(5); assert len(rows) == 5; print('anomaly_factory OK')"</automated>
  </verify>
  <done>anomaly_factory.py importable, all 3 factory classes and 2 builder helpers present; DetectedProblemRowFactory.build() returns dict with all 9 required fields; make_daily_kpi_baseline_rows(5) returns 5 dicts.</done>
</task>

<task type="auto">
  <name>Task 2: Create RED-state unit tests for AnomalyService and AnomalyRepository</name>
  <files>
    backend/tests/unit/test_anomaly_service.py
    backend/tests/unit/test_anomaly_repository.py
  </files>
  <read_first>
    - backend/tests/unit/test_daily_kpi_service.py — _make_service() helper, class grouping, AsyncMock pattern, pytest.mark.asyncio usage
    - backend/tests/unit/test_metrics_repository.py — _make_repo() helper, tenant_id ValueError test pattern
    - backend/tests/factories/anomaly_factory.py — factories just created in Task 1
    - backend/.planning/phases/04-anomaly-detection/04-CONTEXT.md — rule thresholds, D-01 to D-20
  </read_first>
  <action>
    Create two test files. Both must fail with ImportError (modules not yet created) — this is the RED state.

    FILE 1: backend/tests/unit/test_anomaly_service.py

    Header: `from __future__ import annotations`, docstring citing ANOM-01 through ANOM-07.
    Imports: pytest, AsyncMock, MagicMock, date, Decimal, UUID, timedelta.
    Constants: TENANT_ID = UUID("00000000-0000-0000-0000-000000000001"), TODAY = date(2026, 5, 28).

    Helper: `_make_service(mock_session=None)` — deferred import of AnomalyService from app.services.anomaly.anomaly_service; returns (service, session).

    Test classes:

    class TestDetectSlowFirstTouch:
      - test_fires_when_lead_has_no_touch_in_5_business_hours: lead created yesterday, time_to_first_touch_minutes = 360 → returns DetectedProblem dict with rule_id="slow_first_touch", severity="high" (ANOM-02, D-06, D-20)
      - test_skips_when_lead_created_outside_business_hours: lead with time_to_first_touch_minutes computed as 0 outside business hours → returns None (ROADMAP SC#2)
      - test_skips_junk_leads: lead in junk_ids set → excluded from count (D-11, ANOM-07)
      - test_loss_formula_uses_drop_factor_025: count × avg_deal_size × 0.25 (D-06)

    class TestDetectStuckOffer:
      - test_fires_when_offer_no_change_15_days: lead in oferta stage, last_changed 20 days ago → rule_id="stuck_offer", severity="medium" (ANOM-03, D-20)
      - test_loss_formula_sum_estimated_value_times_close_rate: estimated_value=25000, close_rate=0.15 → estimated_loss = 3750 (D-07)
      - test_null_estimated_value_contributes_zero: lead with estimated_value=None → adds 0 to sum (D-07)

    class TestDetectShowroomTrafficDrop:
      - test_fires_when_l_to_v_drops_35pct_below_baseline: baseline=0.32, current=0.20 → 37.5% drop → fires (ANOM-04, D-20)
      - test_skips_when_drop_below_35pct_threshold: baseline=0.32, current=0.22 → 31% drop → no row (D-20)
      - test_skips_when_fewer_than_7_baseline_rows: available_days=5 → rule skipped, no row (D-13)

    class TestDetectUnderperformingSalesperson:
      - test_fires_when_win_rate_30pct_below_team_avg: team_avg=0.06, sp_win_rate=0.04 → 33% below → fires (ANOM-05, D-20)
      - test_salesperson_id_in_context_json: context_json contains salesperson_ids list (D-03)
      - test_skips_when_only_one_salesperson: cannot compute team avg with 1 person → no row

    class TestDetectJunkLeadQuality:
      - test_fires_when_junk_rate_gte_25pct: 30 total leads, 8 junk → 26.7% → fires (ANOM-06, D-20 via CONTEXT.md; NOTE: REQUIREMENTS.md baseline says 20% but CONTEXT.md D-20 + ROADMAP SC#6 lock at 25%)
      - test_skips_when_junk_rate_below_25pct: 30 total, 6 junk → 20% → no row
      - test_loss_formula_junk_count_times_deal_size_times_close_rate: 8 × 22000 × 0.15 = 26400 (D-08)

    class TestRunAllRules:
      - test_returns_list_of_detected_problems: mock service returns results from all 5 detect methods → list of dicts
      - test_junk_ids_computed_once: _get_junk_ids called exactly once regardless of rule count (D-11)

    ---

    FILE 2: backend/tests/unit/test_anomaly_repository.py

    Header: `from __future__ import annotations`, docstring citing ANOM-01.
    Imports: pytest, AsyncMock, date, Decimal, UUID.
    Constants: TENANT_ID = UUID("00000000-0000-0000-0000-000000000001"), TODAY = date(2026, 5, 28).

    Helper: `_make_repo(mock_session=None)` — deferred import of AnomalyRepository from app.services.repositories.anomaly_repository; returns (repo, session).

    Test classes:

    class TestUpsertDetectedProblem:
      - test_raises_when_tenant_id_missing: upsert_detected_problem({"date": TODAY, "rule_id": "slow_first_touch"}) → raises ValueError matching "missing tenant_id" (Pitfall 6 pattern from MetricsRepository)
      - test_raises_when_tenant_id_none: row with tenant_id=None → ValueError
      - test_uses_three_column_conflict_target: verifies pg_insert called with on_conflict_do_update index_elements=["tenant_id", "date", "rule_id"] (D-01, Pitfall 5)
      - test_does_not_commit_on_upsert: session.commit NOT called inside upsert_detected_problem (WR-04 pattern — caller commits)
      - test_idempotent_double_upsert: same (tenant_id, date, rule_id) row upserted twice → no duplicate (mocked session returns rowcount=1)
  </action>
  <verify>
    <automated>cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend && python -m pytest tests/unit/test_anomaly_service.py tests/unit/test_anomaly_repository.py --collect-only 2>&1 | grep -E "test session starts|ERROR|collected|no tests" | head -20</automated>
  </verify>
  <done>
    Both files are syntactically valid Python. pytest --collect-only shows all test functions collected (10+ in test_anomaly_service.py, 5 in test_anomaly_repository.py). Running pytest on both files produces ImportError/ModuleNotFoundError for app.services.anomaly.anomaly_service and app.services.repositories.anomaly_repository — confirming RED state. No SyntaxError or IndentationError.
  </done>
</task>

<task type="auto">
  <name>Task 3: Create RED-state integration test for detect_anomalies task</name>
  <files>backend/tests/integration/test_detect_anomalies_task.py</files>
  <read_first>
    - backend/tests/integration/test_mefi_etl.py — integration test structure, DB session fixture, respx mocking patterns
    - backend/tests/unit/test_calculate_daily_kpis.py — task-level test patterns (mock celery, mock services)
    - backend/tests/factories/anomaly_factory.py — Task 1 output
    - backend/app/tasks/etl/calculate_daily_kpis.py — SyncRun write pattern, task signature
  </read_first>
  <action>
    Create backend/tests/integration/test_detect_anomalies_task.py

    Header: `from __future__ import annotations`, docstring citing ANOM-01, ANOM-02, ANOM-06, ANOM-07.
    Imports: pytest, AsyncMock, MagicMock, patch, date, UUID.
    Constants: TENANT_ID = UUID("00000000-0000-0000-0000-000000000001"), TENANT_ID_STR = str(TENANT_ID).

    Test classes:

    class TestDetectAnomaliesTask:

      test_task_writes_syncrun_with_source_anomaly:
        - Mock _detect_async to return {"status": "success", "problems_found": 2, "duration_ms": 100}
        - Patch asyncio.run to call mock _detect_async
        - Call detect_anomalies.apply(args=[TENANT_ID_STR])
        - Assert SyncRun with source="anomaly" was written (ANOM-01, PIPE-04)
        - Import: `from app.tasks.etl.detect_anomalies import detect_anomalies` (deferred, will ImportError RED)

      test_task_validates_tenant_id_as_uuid:
        - detect_anomalies.apply(args=["not-a-uuid"]) → raises ValueError
        - UUID("not-a-uuid") fails at entry point (T-04-xx-01 pattern from calculate_daily_kpis)

      test_task_marks_stale_running_syncrun_as_failed:
        - Pre-seed a SyncRun(source="anomaly", status="running") in DB
        - Run detect_anomalies task
        - Assert stale row updated to status="failed" (WR-06)
        - New running row created, then updated to "success"

      test_slow_first_touch_not_triggered_for_junk_leads (ROADMAP SC#6):
        - Seed: 3 junk leads created yesterday with time_to_first_touch_minutes=None
        - Seed: 30-day daily_kpi baseline rows (>= 7 rows)
        - Run detect_anomalies task against test DB
        - Assert: no detected_problems row with rule_id="slow_first_touch" for those lead IDs (junk excluded)
        - NOTE: This test requires live DB fixture; mark with @pytest.mark.integration

      test_detected_problems_upsert_idempotent:
        - Run detect_anomalies twice for the same date
        - Assert: exactly 1 row in detected_problems per rule_id per date (D-01 UPSERT)
        - NOTE: @pytest.mark.integration
  </action>
  <verify>
    <automated>cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend && python -m pytest tests/integration/test_detect_anomalies_task.py --collect-only 2>&1 | grep -E "test session starts|ERROR|collected|no tests" | head -20</automated>
  </verify>
  <done>
    File is syntactically valid. pytest --collect-only shows 5 test functions collected. Running pytest produces ImportError for app.tasks.etl.detect_anomalies — confirming RED state. No SyntaxError.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test→app imports | Test files import application code — RED state means ImportError, not security boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-SC | Tampering | No new package installs in Wave 0 | accept | All dependencies (factory-boy, pytest-asyncio) already in pyproject.toml from Phase 3 |
</threat_model>

<verification>
1. `cd backend && python -m pytest tests/unit/test_anomaly_service.py --collect-only` — shows 15+ test functions collected, ImportError on run (RED).
2. `cd backend && python -m pytest tests/unit/test_anomaly_repository.py --collect-only` — shows 5 test functions collected, ImportError on run (RED).
3. `cd backend && python -m pytest tests/integration/test_detect_anomalies_task.py --collect-only` — shows 5 test functions collected, ImportError on run (RED).
4. `cd backend && python -c "from tests.factories.anomaly_factory import DetectedProblemRowFactory; print(DetectedProblemRowFactory.build()['rule_id'])"` — prints "slow_first_touch" (GREEN factory).
</verification>

<success_criteria>
- 4 new files created: anomaly_factory.py (GREEN), test_anomaly_service.py (RED), test_anomaly_repository.py (RED), test_detect_anomalies_task.py (RED)
- factory file importable and produces correct dict structures
- All 3 test files collect without SyntaxError; fail with ImportError/ModuleNotFoundError confirming RED state
- Tests cover all 5 rule methods, repository UPSERT guard, end-to-end task SyncRun pattern
- test_slow_first_touch_not_triggered_for_junk_leads directly maps to ROADMAP SC#6 (ANOM-07)
</success_criteria>

<output>
Create .planning/phases/04-anomaly-detection/04-01-SUMMARY.md when done
</output>
