---
phase: 08-ai-chat
plan: 06
type: execute
wave: 4
depends_on: ["08-05"]
files_modified:
  - frontend/src/components/ui/alert-dialog.tsx
  - frontend/src/components/ui/textarea.tsx
  - frontend/src/components/ui/dropdown-menu.tsx
  - frontend/src/lib/chat/parseSSE.ts
  - frontend/src/lib/tool-pill-hints.ts
  - frontend/src/hooks/useChat.ts
  - frontend/src/hooks/useConversations.ts
  - frontend/src/hooks/useSuggestedQuestions.ts
  - frontend/src/components/chat/conversations-list.tsx
  - frontend/src/components/chat/conversation-item.tsx
  - frontend/src/components/chat/chat-main.tsx
  - frontend/src/components/chat/chat-header.tsx
  - frontend/src/components/chat/message-list.tsx
  - frontend/src/components/chat/message-bubble.tsx
  - frontend/src/components/chat/tool-pill.tsx
  - frontend/src/components/chat/tool-pills-row.tsx
  - frontend/src/components/chat/thinking-indicator.tsx
  - frontend/src/components/chat/suggested-questions.tsx
  - frontend/src/components/chat/chat-input.tsx
  - frontend/src/components/chat/markdown-renderer.tsx
  - frontend/src/components/chat/dashboard-link-pill.tsx
  - frontend/src/components/chat/welcome-card.tsx
  - frontend/src/app/(dashboard)/chat/page.tsx
  - frontend/messages/ro.json
  - frontend/messages/en.json
  - frontend/src/components/chat/__tests__/markdown-renderer.test.tsx
  - frontend/src/components/chat/__tests__/thinking-indicator.test.tsx
  - frontend/src/hooks/useChat.test.ts
autonomous: false
requirements: [CHAT-01, CHAT-06, CHAT-07, CHAT-09]
tags: [chat, frontend, sse, react, ui, i18n, markdown]
must_haves:
  truths:
    - "User can navigate to /chat, see existing conversations sidebar, click 'Conversație nouă', type a Romanian question, send, and see the assistant response stream in token-by-token (CHAT-01)"
    - "Tool pills render inline above streaming assistant text with icon + name + humanized hint per UI-SPEC (CHAT-07 + D-10)"
    - "Tool pills collapse to '🔧 N unelte folosite' line after the `done` SSE event (D-10)"
    - "On `regenerate_notice` SSE event, the visible assistant bubble text resets and ThinkingIndicator state switches to 'Verific cifrele...' (D-08)"
    - "MarkdownRenderer renders only allowed elements (whitelist per UI-SPEC); `<a href='/sales'>` becomes a DashboardLinkPill; other hrefs render as plain text (D-19 + D-18a)"
    - "Mobile sheet for conversation list (Phase 7 D-17 NON-NEGOTIABLE carry-forward): hamburger opens at <768px"
    - "Touch targets ≥44px on Send, chips, conversation items, kebab menu, sidebar items"
    - "i18n: all UI strings live under `chat` namespace in ro.json (primary) + en.json (mirror); no hardcoded Romanian in JSX"
    - "useChat hook consumes 7 SSE event types per D-09; chunk-boundary buffer per LM-7 handled correctly"
  artifacts:
    - path: "frontend/src/lib/chat/parseSSE.ts"
      provides: "Pure SSE chunk parser (LM-7 mitigated via buffer-tail pattern)"
      contains: "export function parseSSEChunk"
    - path: "frontend/src/lib/tool-pill-hints.ts"
      provides: "dayCount(dateFrom, dateTo) + periodLabel(input) helpers per UI-SPEC § Hint helpers"
      contains: "export function dayCount"
    - path: "frontend/src/hooks/useChat.ts"
      provides: "SSE consumer hook with fetch + ReadableStream + readAccessToken + 7-event dispatch + regenerate_notice reset + TanStack Query invalidation on done"
      contains: "getReader"
    - path: "frontend/src/hooks/useConversations.ts"
      provides: "useConversationsList(), useConversation(id), useCreateConversation, useArchiveConversation hooks"
    - path: "frontend/src/hooks/useSuggestedQuestions.ts"
      provides: "useSuggestedQuestions(context) — falls back silently on error per D-17"
    - path: "frontend/src/components/chat/markdown-renderer.tsx"
      provides: "react-markdown + remark-gfm with allowedElements whitelist + dashboard link pill mapper per UI-SPEC § Markdown Rendering Rules"
      contains: "remarkGfm"
    - path: "frontend/src/app/(dashboard)/chat/page.tsx"
      provides: "Rebuilt ChatPage replacing the placeholder; split-panel layout (sidebar + ChatMain); Suspense boundary; uses Phase 7 QueryClientProvider"
    - path: "frontend/messages/ro.json"
      provides: "Extended `chat` namespace with all UI-SPEC keys (page, welcome, sidebar, conversation, input, indicators, tools, suggested.static, messages, archive, timestamps)"
    - path: "frontend/messages/en.json"
      provides: "Mirror of ro.json `chat` namespace in English"
  key_links:
    - from: "frontend/src/hooks/useChat.ts"
      to: "frontend/src/lib/chat/parseSSE.ts"
      via: "import { parseSSEChunk }"
    - from: "frontend/src/hooks/useChat.ts"
      to: "/api/v1/chat/conversations/{id}/messages"
      via: "fetch POST + response.body.getReader()"
      pattern: "/api/v1/chat/conversations"
    - from: "frontend/src/components/chat/markdown-renderer.tsx"
      to: "frontend/src/components/chat/dashboard-link-pill.tsx"
      via: "renderLink mapper for /sales|/salespeople|/marketing|/insights|/chat"
    - from: "frontend/src/app/(dashboard)/chat/page.tsx"
      to: "frontend/src/components/chat/chat-main.tsx"
      via: "<ChatMain conversationId={...} />"
---

<objective>
Build the frontend chat UI per UI-SPEC.md. Replaces the `/chat` placeholder with the full split-panel layout (ConversationsList sidebar + ChatMain pane), implements the SSE consumer (`useChat` hook with `fetch` + `ReadableStream` per D-32 — NOT EventSource per LM-2), renders 14 chat-specific components, wires Romanian i18n (`chat` namespace from UI-SPEC § Concrete Copy Block), adds 3 new shadcn primitives (`alert-dialog`, `textarea`, `dropdown-menu`) via the radix-umbrella manual pattern (Phase 7 D-Phase7).

Purpose: Closes CHAT-01 (UI for asking Romanian questions and seeing answers), CHAT-06 (UX latency — optimistic UI + auto-scroll within 100px per D-34), CHAT-07 (thinking indicator + tool pill transparency per D-10), CHAT-09 (no proxy buffering on client side; useChat handles chunk boundaries per LM-7).

Output: 14 chat components + 3 new ui primitives + 3 hooks + 2 lib helpers + rebuilt page.tsx + extended i18n + 3 component tests. After this plan, the Phase 8 stack is complete and ready for HUMAN-UAT against the live Anthropic API.
</objective>

<execution_context>
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-ai-chat/08-CONTEXT.md
@.planning/phases/08-ai-chat/08-RESEARCH.md
@.planning/phases/08-ai-chat/08-PATTERNS.md
@.planning/phases/08-ai-chat/08-UI-SPEC.md
@docs/CHAT.md
@frontend/src/app/(dashboard)/insights/page.tsx
@frontend/src/components/dashboards/insights/problem-card.tsx
@frontend/src/components/sidebar.tsx
@frontend/src/components/ui/sheet.tsx
@frontend/src/components/ui/collapsible.tsx
@frontend/src/hooks/useInsights.ts
@frontend/src/lib/api-client.ts
@frontend/src/lib/formatters.ts
@frontend/messages/ro.json
@frontend/messages/en.json
@frontend/src/app/(dashboard)/chat/page.tsx
@frontend/src/app/(dashboard)/layout.tsx

<interfaces>
<!-- Existing frontend patterns the executor must mirror. -->

From `frontend/src/lib/api-client.ts:11-15` (cookie-based access token read):
```
function readAccessToken(): string | null {
  if (typeof document === "undefined") return null;
  const m = document.cookie.match(/(?:^|;\s*)access_token=([^;]+)/);
  return m ? decodeURIComponent(m[1]) : null;
}
```
Reused verbatim in useChat for the fetch+SSE call (since SSE uses raw fetch, NOT api-client, but reuses the same cookie/header dance).

From `frontend/src/components/ui/sheet.tsx` (Phase 7 radix-umbrella manual pattern):
- Re-exports a shadcn-styled wrapper around `@radix-ui/react-dialog`. New `alert-dialog.tsx` and `dropdown-menu.tsx` follow the same shape (different radix subpath).

From `frontend/src/app/(dashboard)/insights/page.tsx`:
- `"use client"` at top; client component with Suspense wrapper.
- `useTranslations("insights")` for i18n.
- TanStack Query hooks pattern (see useInsights.ts) — reuse for chat hooks.

From `frontend/src/app/(dashboard)/layout.tsx`:
- QueryClientProvider (Phase 7 D-08) already mounted — chat page inherits it; no re-wrapping.

From RESEARCH §7 lines 1048-1150 (useChat sketch — PASTE-VERBATIM starting point).

From UI-SPEC § Concrete Copy Block (ro.json + en.json — paste verbatim).
From UI-SPEC § Markdown Rendering Rules (allowedElements list + custom renderers).
From UI-SPEC § Tool Pill Content Rules (12 tool → lucide icon mapping + hint helpers).
From UI-SPEC § States Matrix (per-component states the executor must implement).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Foundation — 3 shadcn primitives + parseSSE + tool-pill-hints + 3 hooks + i18n namespace</name>
  <files>
    frontend/src/components/ui/alert-dialog.tsx,
    frontend/src/components/ui/textarea.tsx,
    frontend/src/components/ui/dropdown-menu.tsx,
    frontend/src/lib/chat/parseSSE.ts,
    frontend/src/lib/tool-pill-hints.ts,
    frontend/src/hooks/useChat.ts,
    frontend/src/hooks/useConversations.ts,
    frontend/src/hooks/useSuggestedQuestions.ts,
    frontend/messages/ro.json,
    frontend/messages/en.json,
    frontend/src/hooks/useChat.test.ts
  </files>
  <read_first>
    - frontend/src/components/ui/sheet.tsx (Phase 7 radix-umbrella manual pattern — exact template for alert-dialog, dropdown-menu)
    - frontend/src/components/ui/input.tsx (template for textarea wrapper)
    - frontend/src/lib/api-client.ts (readAccessToken function — reuse pattern in useChat)
    - frontend/src/hooks/useInsights.ts (TanStack Query hooks pattern; useMutation for refresh; queryKey conventions)
    - .planning/phases/08-ai-chat/08-RESEARCH.md (§7 useChat sketch lines 1048-1150 paste-verbatim; LM-7 buffer-tail mitigation)
    - .planning/phases/08-ai-chat/08-UI-SPEC.md (§ Concrete Copy Block ro.json + en.json paste-verbatim; § Tool Pill Content Rules hint helpers)
    - .planning/phases/08-ai-chat/08-CONTEXT.md (D-09 7 SSE events; D-17 suggested questions; D-32 fetch+ReadableStream pattern; D-37 i18n)
    - frontend/messages/ro.json (existing structure — extend chat namespace; replace placeholder string)
    - frontend/src/lib/chat/__tests__/parseSSE.test.ts (Wave 0 — test contract that defines parseSSEChunk behavior)
  </read_first>
  <behavior>
    parseSSE tests (from Wave 0 stub — should now PASS):
    - Test PS1: `parseSSEChunk("event: assistant_chunk\ndata: {\"text\":\"Salut\"}\n\n")` returns `[{event: "assistant_chunk", data: {text: "Salut"}}]`.
    - Test PS2: Comment lines starting with `:` are skipped (heartbeat).
    - Test PS3: Multiple events in one chunk separated by `\n\n` are all returned.
    - Test PS4: Malformed JSON in `data:` line is silently skipped (LM-7 tolerance).
    - Test PS5: Empty frames are skipped.

    useChat hook tests (frontend/src/hooks/useChat.test.ts):
    - Test UC1: On `assistant_chunk` event, `tokens` state appends `ev.data.text`.
    - Test UC2: On `tool_use` event, a new pill is added to `toolPills` with state="running".
    - Test UC3: On `tool_result` event, the matching pill (by tool_use_id) is updated to state="done" (or "error" if `ev.data.error`).
    - Test UC4: On `regenerate_notice` event, `tokens` is reset to `""` (D-08 — discard the visible bubble text on guard fail round 1).
    - Test UC5: On `done` event, `isStreaming` becomes false; `queryClient.invalidateQueries({queryKey: ["chat", "conversations"]})` is called.
    - Test UC6: On `error` event, `error` state is set to `ev.data.message_ro`.
    - Test UC7: `cancel()` aborts the active fetch via AbortController.
    - Test UC8 (LM-7 chunk boundary): simulated reader yields fragments — `event: assistant_chunk\ndata: {"text":"Hel` then `lo"}\n\n` then `event: done\n...`; useChat correctly assembles and dispatches 2 events.

    Tool pill hints tests (inline within markdown-renderer tests or own file — at minimum):
    - Test PH1: `dayCount("2026-05-01", "2026-05-31")` returns "30 zile".
    - Test PH2: `dayCount("2026-05-01", "2026-05-01")` returns "1 zi".
    - Test PH3: `periodLabel({date_from: "2026-05-01", date_to: "2026-05-31"})` returns "30 zile" (or "luna asta" if it's the current month — implementation choice documented).
  </behavior>
  <action>
    Build the foundation layer for the chat frontend:

    1. **`frontend/src/components/ui/alert-dialog.tsx`** (~80 lines) — Phase 7 D-Phase7 manual radix-umbrella pattern. Import from `radix-ui` umbrella: `import * as AlertDialogPrimitive from "radix-ui/react-alert-dialog"` (verify the exact import path against existing sheet.tsx — the umbrella may expose subpaths differently). Re-export shadcn new-york-styled wrapper: `AlertDialog`, `AlertDialogTrigger`, `AlertDialogContent`, `AlertDialogHeader`, `AlertDialogFooter`, `AlertDialogTitle`, `AlertDialogDescription`, `AlertDialogAction`, `AlertDialogCancel`. Use the same className conventions as sheet.tsx.

    2. **`frontend/src/components/ui/textarea.tsx`** (~30 lines) — pure native textarea wrapper per UI-SPEC § Component Inventory. Pattern from input.tsx: forwardRef with className merge via `cn(...)`. Default classes: `flex min-h-[60px] w-full rounded-md border border-input bg-background px-3 py-2 text-sm focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring`.

    3. **`frontend/src/components/ui/dropdown-menu.tsx`** (~100 lines) — same radix-umbrella pattern, `radix-ui/react-dropdown-menu`. Exports: `DropdownMenu`, `DropdownMenuTrigger`, `DropdownMenuContent`, `DropdownMenuItem`, `DropdownMenuSeparator`. shadcn-styled with `bg-popover text-popover-foreground border` etc.

    4. **`frontend/src/lib/chat/parseSSE.ts`** (~40 lines) — pure SSE chunk parser. Function `parseSSEChunk(chunk: string): SSEEvent[]`:
       ```
       const events = [];
       for (const frame of chunk.split("\n\n")) {
         if (!frame.trim() || frame.startsWith(":")) continue;
         let eventName = "message", dataLine = "";
         for (const line of frame.split("\n")) {
           if (line.startsWith("event: ")) eventName = line.slice(7).trim();
           else if (line.startsWith("data: ")) dataLine += line.slice(6);
         }
         if (!dataLine) continue;
         try { events.push({ event: eventName, data: JSON.parse(dataLine) }); }
         catch { /* tolerate partial */ }
       }
       return events;
       ```
       Export `SSEEvent` discriminated union type per RESEARCH §7 lines 1052-1059 (7 event types).

    5. **`frontend/src/lib/tool-pill-hints.ts`** (~50 lines) — per UI-SPEC § Hint helpers:
       - `export function dayCount(dateFrom: string, dateTo: string): string` — Romanian-pluralized "N zile" (or "1 zi"). Use date-fns `differenceInCalendarDays`.
       - `export function periodLabel(input: { date_from: string; date_to: string }): string` — returns "30 zile" by default; recognize 7/30/90 day exact ranges and label them.

    6. **`frontend/src/hooks/useChat.ts`** (~120 lines) — PASTE RESEARCH §7 lines 1088-1150 as starting skeleton. Key points:
       - `readAccessToken()` helper (from api-client.ts pattern).
       - `useState`: `isStreaming`, `tokens` (string — accumulated assistant text), `toolPills` (array of `{tool_use_id, name, input, state}`), `error` (string | null).
       - `abortRef = useRef<AbortController | null>(null)`.
       - `sendMessage(content)`:
         - `fetch("/api/v1/chat/conversations/${conversationId}/messages", { method: "POST", headers: { "Content-Type": "application/json", Authorization: \`Bearer ${token}\` (if token) }, credentials: "include", body: JSON.stringify({ content }), signal: ac.signal })`.
         - On non-OK: setError + setIsStreaming(false) + throw.
         - On OK: `const reader = resp.body!.getReader(); const decoder = new TextDecoder(); let buffer = "";`
         - Loop: read chunk, decode, append to buffer, find `lastBoundary = buffer.lastIndexOf("\n\n")`, if -1 continue; else `ready = buffer.slice(0, lastBoundary + 2); buffer = buffer.slice(lastBoundary + 2)`. Call `parseSSEChunk(ready)` and dispatch each event:
           - `assistant_chunk` → `setTokens(t => t + data.text)`.
           - `tool_use` → `setToolPills(p => [...p, {...data, state: "running"}])`.
           - `tool_result` → `setToolPills(p => p.map(x => x.tool_use_id === data.tool_use_id ? {...x, state: data.error ? "error" : "done"} : x))`.
           - `regenerate_notice` → `setTokens("")` (D-08 — discard visible text).
           - `done` → `setIsStreaming(false)`; `qc.invalidateQueries({queryKey: ["chat", "conversations"]})` (for title hot-swap per D-14).
           - `error` → `setIsStreaming(false); setError(data.message_ro)`.
       - `cancel()` → `abortRef.current?.abort()`.
       - Return `{sendMessage, cancel, isStreaming, tokens, toolPills, error}`.

    7. **`frontend/src/hooks/useConversations.ts`** (~80 lines) — TanStack Query hooks per useInsights.ts pattern:
       - `useConversationsList({archived = false}) ` — GET /api/v1/chat/conversations.
       - `useConversation(id: string | null)` — GET /api/v1/chat/conversations/{id}, enabled when id present.
       - `useCreateConversation()` — useMutation POST /api/v1/chat/conversations with optional initial_message; on success invalidates list.
       - `useArchiveConversation()` — useMutation DELETE /api/v1/chat/conversations/{id}; on success invalidates list.
       - staleTime: 60_000 (per Phase 7 conventions).

    8. **`frontend/src/hooks/useSuggestedQuestions.ts`** (~30 lines) — `useSuggestedQuestions(context: "homepage" | ...) ` GET /api/v1/chat/suggested-questions. On error: return empty array silently per D-17 (chips are nice-to-have, never block input).

    9. **`frontend/messages/ro.json` and `en.json`** — PASTE the full `chat` namespace from UI-SPEC § Concrete Copy Block (lines 658-738 for ro, 742-820 for en). Replace the existing placeholder string `"chat": "Chat-ul AI va fi disponibil în curând."` with the new nested namespace. KEEP the existing nav key `"chat": "Chat AI"` (line 22 in ro.json — it's a separate top-level nav.* or root key, not the chat namespace; double-check during execution).

    10. **`frontend/src/hooks/useChat.test.ts`** — Vitest unit tests covering UC1-UC8. Mock `fetch` with a stub that returns a synthetic `ReadableStream` yielding pre-recorded chunks. Use `@testing-library/react` `renderHook`.

    Cite D-08 (regenerate_notice reset), D-09 (7 events), D-14 (TanStack invalidation), D-32 (fetch + ReadableStream), D-37 (i18n), LM-7 (chunk-boundary buffer) in code comments.
  </action>
  <verify>
    <automated>cd frontend && pnpm vitest --run src/lib/chat/__tests__/parseSSE.test.ts src/hooks/useChat.test.ts 2>&1 | tail -20 && pnpm typecheck 2>&1 | tail -10</automated>
  </verify>
  <done>parseSSE tests pass (Wave 0 RED → GREEN); useChat tests pass; 3 primitives + 3 hooks + i18n namespace ready</done>
  <acceptance_criteria>
    - `cd frontend && pnpm vitest --run src/lib/chat/__tests__/parseSSE.test.ts` exits 0 (PS1-PS5 GREEN)
    - `cd frontend && pnpm vitest --run src/hooks/useChat.test.ts` exits 0 (UC1-UC8 GREEN)
    - `cd frontend && pnpm typecheck` exits 0
    - `cd frontend && test -f src/components/ui/alert-dialog.tsx && test -f src/components/ui/textarea.tsx && test -f src/components/ui/dropdown-menu.tsx` exits 0
    - `cd frontend && grep -E "react-alert-dialog|react-dropdown-menu" src/components/ui/alert-dialog.tsx src/components/ui/dropdown-menu.tsx | wc -l` returns at least 2 (radix imports present)
    - `cd frontend && grep -E "getReader|ReadableStream|parseSSEChunk" src/hooks/useChat.ts | wc -l` returns at least 2 (D-32 SSE consumer pattern)
    - `cd frontend && cat messages/ro.json | python -c "import sys, json; data = json.load(sys.stdin); chat = data.get('chat', {}); assert 'welcome' in chat and 'sidebar' in chat and 'tools' in chat and 'suggested' in chat; assert 'salesThisMonth' in chat['suggested']['static']"` exits 0
    - `cd frontend && cat messages/en.json | python -c "import sys, json; data = json.load(sys.stdin); chat = data.get('chat', {}); assert 'welcome' in chat and 'tools' in chat"` exits 0
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 14 chat components + markdown renderer + dashboard-link-pill</name>
  <files>
    frontend/src/components/chat/conversations-list.tsx,
    frontend/src/components/chat/conversation-item.tsx,
    frontend/src/components/chat/chat-main.tsx,
    frontend/src/components/chat/chat-header.tsx,
    frontend/src/components/chat/message-list.tsx,
    frontend/src/components/chat/message-bubble.tsx,
    frontend/src/components/chat/tool-pill.tsx,
    frontend/src/components/chat/tool-pills-row.tsx,
    frontend/src/components/chat/thinking-indicator.tsx,
    frontend/src/components/chat/suggested-questions.tsx,
    frontend/src/components/chat/chat-input.tsx,
    frontend/src/components/chat/markdown-renderer.tsx,
    frontend/src/components/chat/dashboard-link-pill.tsx,
    frontend/src/components/chat/welcome-card.tsx,
    frontend/src/components/chat/__tests__/markdown-renderer.test.tsx,
    frontend/src/components/chat/__tests__/thinking-indicator.test.tsx
  </files>
  <read_first>
    - .planning/phases/08-ai-chat/08-UI-SPEC.md (full file — § Component Inventory, § Tool Pill Content Rules, § Markdown Rendering Rules, § States Matrix, § Accessibility, § Interaction Contracts)
    - .planning/phases/08-ai-chat/08-PATTERNS.md (§ Frontend — Chat Components for each component's analog or NEW PATTERN reference)
    - frontend/src/components/dashboards/insights/problem-card.tsx (Collapsible expand/collapse pattern + 44px touch target + role="article" — analog for ToolPillsRow + MessageBubble + ConversationItem)
    - frontend/src/components/sidebar.tsx (mobile Sheet pattern — analog for ConversationsList mobile sheet)
    - frontend/src/components/ui/sheet.tsx, collapsible.tsx, card.tsx (shadcn primitives to reuse)
    - frontend/src/lib/formatters.ts (Intl.NumberFormat('ro-RO') for KPI numbers in markdown)
  </read_first>
  <behavior>
    MarkdownRenderer tests:
    - Test MR1: `<strong>1.234 RON</strong>` markdown → renders `<strong className="font-semibold text-accent">` per D-19.
    - Test MR2: `[Sales Dashboard](/sales)` → renders `<DashboardLinkPill href="/sales">` (the wrapped Link component).
    - Test MR3: `[Evil](https://evil.com)` → renders plain `<span className="text-muted-foreground italic">` (D-18a client-side defense; server-side guard from plan 08-04 rejects before reaching here).
    - Test MR4: `[Sales](#)` → renders plain `<span>` (bare anchor).
    - Test MR5: Disallowed elements (`<script>`, `<iframe>`, `<img>`) are dropped (XSS hardening).
    - Test MR6: Tables render via shadcn Table primitives wrapped in `overflow-x-auto`.

    ThinkingIndicator tests:
    - Test TI1: State "thinking" renders 3 pulse dots, no label.
    - Test TI2: State "looking-up" renders 3 dots + "Caut datele..." label (from i18n).
    - Test TI3: State "verifying-numbers" renders 3 dots + "Verific cifrele..." label.
    - Test TI4: `role="status" aria-live="polite"` on outer span.
    - Test TI5: `motion-safe:animate-pulse` class on each dot (respects reduced motion).
  </behavior>
  <action>
    Implement all 14 chat components per UI-SPEC § Component Inventory + § States Matrix + § Accessibility. Each component uses `useTranslations("chat")` for strings.

    1. **`thinking-indicator.tsx`** (~30 lines) — UI-SPEC § States Matrix → ThinkingIndicator. 3 dots with `motion-safe:animate-pulse`; optional label from i18n based on `state` prop. `role="status" aria-live="polite"`.

    2. **`dashboard-link-pill.tsx`** (~25 lines) — UI-SPEC § DashboardLinkPill markup (lines 402-410). `<Link>` (next/link) with `bg-accent/10 text-accent rounded-md inline-flex items-center gap-1 px-2 py-0.5 text-sm hover:bg-accent/20`. Append `<ChevronRight size={14} aria-hidden="true" />` per UI-SPEC.

    3. **`markdown-renderer.tsx`** (~120 lines) — UI-SPEC § Markdown Rendering Rules:
       - Import `react-markdown` + `remark-gfm`.
       - `allowedElements` whitelist per UI-SPEC line 337-354.
       - `components` map per UI-SPEC § Custom renderers table: `strong` → `<strong className="font-semibold text-accent">`, `a` → `renderLink` (DashboardLinkPill or plain span), `table` → shadcn Table wrapped in `overflow-x-auto`, `thead`/`tbody`/`tr`/`th`/`td` → shadcn TableHeader/TableBody/TableRow/TableHead/TableCell, `code` → `<code className="font-mono text-xs bg-secondary px-1 py-0.5 rounded">`, `p`/`ul`/`ol`/`li` per UI-SPEC.
       - `renderLink({href, children})`:
         - `if (href === "#") return <span>{children}</span>` (UI-SPEC line 388).
         - `const DASHBOARD_PATHS = new Set(["/sales", "/salespeople", "/marketing", "/insights", "/chat"])` (UI-SPEC line 377-383).
         - `if (DASHBOARD_PATHS.has(href)) return <DashboardLinkPill href={href}>{children}</DashboardLinkPill>`.
         - `else return <span className="text-muted-foreground italic">{children}</span>` (D-18a client-side defense).
       - `unwrapDisallowed: false` so XSS attempts are dropped entirely.

    4. **`tool-pill.tsx`** (~70 lines) — UI-SPEC § Tool Pill Content Rules. Per-tool lucide-react icon map (table at lines 299-312): get_kpi → Calculator, get_funnel_data → Filter, get_salesperson_performance → Users, get_leads → List, compare_periods → GitCompare, get_loss_reasons → TrendingDown, get_lead_categories_breakdown → PieChart, get_showroom_performance → Store, get_recent_insight → Lightbulb, explain_metric → BookOpen, get_stuck_leads → Hourglass, get_trend → LineChart. Props: `{ name, input, state }`. Compute hint via `tool-pill-hints.ts` helpers. Visual states (UI-SPEC § States Matrix): running (animate-pulse on icon), done (static, text-muted-foreground), error (bg-destructive/10). `role="status"`, `aria-label` per UI-SPEC.

    5. **`tool-pills-row.tsx`** (~50 lines) — UI-SPEC § ToolPillsRow + analog problem-card.tsx Collapsible. Container for ToolPills. When `state="streaming"`: expanded, wrap on narrow viewports. When `state="complete"` (after `done`): collapsed to single line `🔧 {n} unelte folosite` using ICU plural via `t("chat.tools.collapsedSummary", {count: n})`. Use shadcn `Collapsible` from Phase 7.

    6. **`message-bubble.tsx`** (~80 lines) — UI-SPEC § MessageBubble. Props: `{ message, variant: "user" | "assistant", state }`. Card-based per problem-card.tsx analog. Variants:
       - user: right-aligned, `bg-secondary`, `md:max-w-[75%] max-w-[90%]`. Pending state: opacity-70.
       - assistant: left-aligned, `bg-background border border-border`. Renders `<ToolPillsRow>` above `<MarkdownRenderer>`. While streaming-empty: shows `<ThinkingIndicator state="thinking">`. While regenerating: text content cleared, ThinkingIndicator state="verifying-numbers". Fallback state: left border `text-destructive` + the Romanian fallback message.
       - `role="article" aria-label={t("messages.userAriaLabel" | "messages.assistantAriaLabel")}` per UI-SPEC § Accessibility.

    7. **`message-list.tsx`** (~80 lines) — UI-SPEC § MessageList. Scrollable container with `role="log" aria-live="polite" aria-atomic="false"`. Tracks `scrollTop`; computes `distanceFromBottom = scrollHeight - scrollTop - clientHeight`. If < 100px: auto-scroll on each new chunk (D-34). Else: show floating "↓ Mesaje noi" button bottom-right (`bg-accent text-accent-foreground rounded-full shadow-md`, accent #8 from UI-SPEC) that smooth-scrolls to bottom on click.

    8. **`chat-input.tsx`** (~90 lines) — UI-SPEC § ChatInput + Keyboard shortcuts. Auto-growing textarea via `useRef<HTMLTextAreaElement>` + `useEffect` on value: `el.style.height = "auto"; el.style.height = el.scrollHeight + "px"`. Send button on the right. Keyboard: Enter sends, Shift+Enter newline, Cmd/Ctrl+Enter sends. Send button: `bg-accent text-accent-foreground min-h-[44px] min-w-[44px]` (touch target NON-NEGOTIABLE). Disabled while `isStreaming`. Tooltip when disabled: `t("chat.input.sendDisabledTooltip")` = "Așteaptă răspunsul...". Accept `initialContent` prop (for chip click → insert behavior per D-17).

    9. **`suggested-questions.tsx`** (~50 lines) — UI-SPEC § SuggestedQuestions. Fetch via `useSuggestedQuestions(context)`. Render rounded chips (Button variant=outline, `rounded-full min-h-[44px]`). Hover state: border + text turn accent (UI-SPEC accent #6). On click: call prop `onSelect(question)` → inserts into input (NOT auto-send per D-17). Loading: 5 Skeleton chips. Error: returns null (silent fail). Hidden when active conversation has ≥1 user message. `role="group" aria-label={t("chat.suggested.sectionAriaLabel")}`.

    10. **`welcome-card.tsx`** (~50 lines) — UI-SPEC § WelcomeCard. Centered Card with H1 ("Bună! Întreabă-mă despre datele tale."), body paragraph, and 5-7 suggested chips. Visible only when zero conversations exist. Mobile: smaller H1 (`text-xl`), padding adjusted.

    11. **`chat-header.tsx`** (~50 lines) — UI-SPEC § ChatHeader. Renders conversation title + kebab menu (DropdownMenu from Task 1) with "Arhivează conversația" trigger. Opens AlertDialog confirmation (D-15 + UI-SPEC § "Archive conversation"). On confirm: call `useArchiveConversation()` mutation; on success navigate to `/chat` (no conversation). Title hot-swaps when AI title arrives via TanStack Query refetch on `done`.

    12. **`conversation-item.tsx`** (~50 lines) — UI-SPEC § ConversationItem. `<Link href={`/chat?conversation_id=${id}`}>` (next/link). States: idle (`text-sm text-foreground`), hover (`bg-secondary/70`), active (`bg-accent/10 text-accent border-l-[3px] border-accent` — UI-SPEC accent #3), archiving (opacity-50). `min-h-[44px]` touch target. Kebab on the right.

    13. **`conversations-list.tsx`** (~100 lines) — UI-SPEC § ConversationsList. Fetches via `useConversationsList()`. Desktop: `260px fixed sidebar bg-secondary` (UI-SPEC layout anatomy). Mobile: rendered inside Sheet via `<Sheet>` from sheet.tsx + hamburger trigger in app topbar. Section heading "Conversații recente". "Conversație nouă" button at top (`bg-accent`). States: loading (5 Skeleton items), empty ("Nu există conversații încă."), populated, error (inline retry button). `<nav aria-label="Conversații">`.

    14. **`chat-main.tsx`** (~150 lines) — UI-SPEC § ChatMain. Owns the `useChat(conversationId)` hook. Renders: ChatHeader, MessageList (with messages from `useConversation` + the in-progress assistant bubble fed from useChat's `tokens` + `toolPills`), SuggestedQuestions (conditional — only if input empty), ChatInput. Wires `chatInput.onSend(content)` → `useChat.sendMessage(content)`. Optimistic UI per D-33: append user message instantly + an empty assistant bubble with ThinkingIndicator inside before SSE round-trip. On `conversation_meta` event: swap optimistic IDs → real DB IDs. State branches per UI-SPEC § ChatMain (no-conversation, empty-conversation, active-conversation, streaming).

    15. **`__tests__/markdown-renderer.test.tsx`** — covers MR1-MR6 via `@testing-library/react` `render`.

    16. **`__tests__/thinking-indicator.test.tsx`** — covers TI1-TI5.

    Every component cites the relevant D-NN (D-10 tool pills, D-18a link whitelist, D-19 markdown, D-32 fetch SSE, D-33 optimistic UI, D-34 autoscroll, D-36 mobile sheet) in a top-of-file comment.
  </action>
  <verify>
    <automated>cd frontend && pnpm vitest --run src/components/chat/__tests__/ 2>&1 | tail -20 && pnpm typecheck 2>&1 | tail -10</automated>
  </verify>
  <done>MR1-MR6 + TI1-TI5 pass; all 14 components typecheck; no hardcoded Romanian in JSX</done>
  <acceptance_criteria>
    - `cd frontend && pnpm vitest --run src/components/chat/__tests__/markdown-renderer.test.tsx src/components/chat/__tests__/thinking-indicator.test.tsx` exits 0
    - `cd frontend && pnpm typecheck` exits 0
    - `cd frontend && ls src/components/chat/ | grep -E '\.tsx$' | wc -l` returns at least 14
    - `cd frontend && grep -E "useTranslations\(\"chat\"\)" src/components/chat/*.tsx | wc -l` returns at least 8 (most components use chat namespace; some leaf components like dashboard-link-pill may not need i18n)
    - `cd frontend && grep -E "min-h-\[44px\]|min-w-\[44px\]" src/components/chat/*.tsx | wc -l` returns at least 3 (Send, ChatInput, ConversationItem — NON-NEGOTIABLE touch targets)
    - `cd frontend && grep -E "Bună!|Conversație nouă|Întreabă orice|Se gândește|Verific cifrele|Caut datele" src/components/chat/*.tsx` returns NOTHING (no hardcoded Romanian — all via i18n)
    - `cd frontend && grep -E "react-markdown|remark-gfm" src/components/chat/markdown-renderer.tsx | wc -l` returns at least 2
    - `cd frontend && grep -E "allowedElements" src/components/chat/markdown-renderer.tsx` returns at least 1 match
    - `cd frontend && grep -E "DASHBOARD_PATHS|/sales.*\/salespeople" src/components/chat/markdown-renderer.tsx` returns at least 1 match (D-18a client-side whitelist)
    - `cd frontend && grep -cE "bg-accent" src/components/chat/*.tsx` returns at most 10 — accent usage stays within the 8 reserved-for slots from UI-SPEC § Color (lint manually if over: send button, new-conv button, active-item, KPI bold, dashboard pill, chip hover, focus ring, new-messages float). Document any deliberate additions.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Rebuild /chat page.tsx + wire everything together</name>
  <files>
    frontend/src/app/(dashboard)/chat/page.tsx
  </files>
  <read_first>
    - frontend/src/app/(dashboard)/chat/page.tsx (current placeholder — DELETE and rebuild from scratch per UI-SPEC)
    - frontend/src/app/(dashboard)/insights/page.tsx (full file — Suspense + useSearchParams + useTranslations pattern)
    - frontend/src/app/(dashboard)/layout.tsx (Phase 7 — QueryClientProvider inherited; do NOT re-wrap)
    - .planning/phases/08-ai-chat/08-UI-SPEC.md (full file — § Layout / Structure anatomy desktop + mobile, § States Matrix → ChatPage states)
    - All chat components from Task 2
  </read_first>
  <action>
    Rebuild `frontend/src/app/(dashboard)/chat/page.tsx` from scratch (DELETE the placeholder content). Structure per UI-SPEC § Layout anatomy:

    1. `"use client";` at top.
    2. Imports: React, Suspense, useSearchParams from next/navigation, useTranslations from next-intl, the 14 chat components, Skeleton from shadcn ui.
    3. Component `ChatPageContent()`:
       - `const searchParams = useSearchParams()`.
       - `const conversationId = searchParams.get("conversation_id")`.
       - Render the split-panel layout:
         - Desktop (≥ 768px): `<div className="flex h-[calc(100vh-56px)]">` (subtract app topbar) with `<ConversationsList />` (260px fixed) on the left and `<ChatMain conversationId={conversationId} />` flex-1 on the right.
         - Mobile (< 768px): `<div className="flex flex-col h-[calc(100vh-56px)]">` with hamburger in the app topbar (which mounts a Sheet wrapping ConversationsList) + `<ChatMain conversationId={conversationId} />` full-width. ConversationsList passes `mode="mobile-sheet"` prop and renders inside the Sheet.
       - State branch per UI-SPEC § ChatPage States: no-conversation (sidebar + ChatMain default state), conversation-loading (skeleton inside ChatMain), conversation-loaded (active rendering), conversation-not-found (handled inside ChatMain with the t("chat.conversation.notFound") message + "Start new" button).
    4. Default export `ChatPage()`:
       - `<Suspense fallback={<Skeleton className="h-96 w-full" />}>` wrapping `<ChatPageContent />`.
    5. Cite D-13 (sidebar), D-32 (SSE consumer is in useChat — page doesn't touch SSE directly), D-36 (mobile NON-NEGOTIABLE) in top-of-file comment.

    Verify against UI-SPEC § States Matrix → ChatPage table that all 4 states render correctly.
  </action>
  <verify>
    <automated>cd frontend && pnpm typecheck 2>&1 | tail -10 && pnpm lint --fix src/app/\(dashboard\)/chat/page.tsx 2>&1 | tail -5</automated>
  </verify>
  <done>page.tsx rebuilt; typecheck + lint pass; placeholder text removed</done>
  <acceptance_criteria>
    - `cd frontend && pnpm typecheck` exits 0
    - `cd frontend && pnpm lint 2>&1 | grep -c "Chat AI va fi disponibil"` returns 0 (placeholder text removed)
    - `cd frontend && grep -E "ConversationsList|ChatMain|Suspense" src/app/\(dashboard\)/chat/page.tsx | wc -l` returns at least 3
    - `cd frontend && grep -E "useSearchParams" src/app/\(dashboard\)/chat/page.tsx` returns at least 1
    - `cd frontend && grep -E "QueryClientProvider" src/app/\(dashboard\)/chat/page.tsx` returns 0 (inherited from layout — must NOT re-wrap per Phase 7 D-08)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: BLOCKING mobile + Romanian-language UAT checkpoint (D-17 mobile-first + CHAT-09 latency + CHAT-04 Romanian quality)</name>
  <files>N/A — manual verification</files>
  <read_first>
    - .planning/phases/08-ai-chat/08-UI-SPEC.md (§ Breakpoints, § Accessibility — touch target 44px, § States Matrix)
    - .planning/phases/08-ai-chat/08-VALIDATION.md (§ Manual-Only Verifications — streaming responsiveness, Romanian title quality, tool pill clarity, honest "Nu am acces" behavior)
    - .planning/phases/08-ai-chat/08-CONTEXT.md (D-17 mobile-first NON-NEGOTIABLE; D-36 carry-forward from Phase 7 D-17)
  </read_first>
  <action>
    **BLOCKING HUMAN VERIFICATION** per Phase 7 D-17 NON-NEGOTIABLE carry-forward (D-36) + RESEARCH § Validation Architecture manual-only items.

    User-facing checklist for the executor:

    Visual + interaction (mobile 360px, tablet 768px, desktop 1280px in DevTools device emulation):
    1. **Mobile 360px (Chrome DevTools — Pixel 8 emulation):**
       - Navigate to `/chat`. Verify the hamburger ☰ icon appears in the app topbar.
       - Tap hamburger → Sheet slides from left (Phase 7 D-17 carry-forward pattern), `w-[85vw] max-w-[320px]`.
       - "Conversație nouă" button is visible at top, minimum 44px tall. Tap → creates conversation, sheet closes.
       - Type a Romanian question in the textarea ("Cum stăm cu vânzările luna asta?"). Send button is min-h-[44px] min-w-[44px] and uses bg-accent.
       - Press Enter → assistant bubble appears with ThinkingIndicator. First token must appear within 2s (CHAT-09).
       - Tool pills render inline with icon + name + Romanian hint. After `done` event, they collapse to "🔧 N unelte folosite".
       - Markdown tables overflow-x-auto (don't break layout).
    2. **Tablet 768px:** ConversationsList shows as fixed 260px panel. ChatMain flex-1 to the right. Bubbles max-w-[75%].
    3. **Desktop 1280px:** Same as 768px but MessageList uses `max-w-3xl mx-auto` for readability.

    Romanian language quality (3 fresh conversations against the live Anthropic API — set ANTHROPIC_API_KEY in .env):
    1. Ask: "Cum stăm comparativ cu săptămâna trecută?" → Romanian-language response, references `compare_periods` tool, includes actual WoW delta values (CHAT-01 SC#1).
    2. Ask: "Care e CAC-ul Meta?" → Honest refusal containing "nu am acces" or equivalent (CHAT-05 SC#3 — NO fabricated numbers).
    3. Ask: "Care e cel mai bun vânzător?" → Mentions actual salesperson name from the whitelist (Roibu Valeria, Raileanu Leon, Godja Adina Maria, Dragoi Mihaela, Zagrian Emilia, Moaca Andreea) — no fictitious "Maria Popescu" type names (CHAT-10).
    4. After turn 1, conversation title in sidebar updates to 4-6 Romanian words via D-14 title-gen (within ~5s).

    Inline dashboard links:
    1. Ask a question where Claude is likely to mention a dashboard (e.g., "Unde văd lead-urile blocate?") → expect inline `[Sales Dashboard](/sales)` rendered as a pill with bg-accent/10 + ChevronRight icon.

    Touch targets (DevTools mobile emulation):
    1. Inspect Send button: computed height ≥ 44px.
    2. Inspect ConversationItem: computed height ≥ 44px.
    3. Inspect chip: computed height ≥ 44px.

    Accessibility:
    1. Keyboard-only flow: Tab → New conversation button → first ConversationItem → ChatHeader kebab → MessageList → SuggestedQuestions chips → Textarea → Send. All reachable, focus ring visible (`ring-2 ring-ring`).
    2. Screen reader: open MessageList, send a question. SR announces "Răspuns asistent AI" + completed text. Tool pill states are NOT individually announced (per UI-SPEC § Live regions).

    Resume signal:
    - Type `approved` to proceed.
    - OR type `reject: <issue>` with specifics (e.g., "Send button is 40px on iPhone SE", "regenerate_notice doesn't clear bubble text", "Romanian title is in English"). Executor then opens a deviation note and either applies a same-task fix or schedules a Plan 08-07 gap-closure.
  </action>
  <verify>
    <automated>echo "Manual UAT checkpoint — no automated verify"</automated>
  </verify>
  <done>Human has confirmed all 4 sections (visual/interaction, Romanian language quality, dashboard links, touch targets, accessibility) PASS at 360px / 768px / 1280px</done>
  <acceptance_criteria>
    - User types "approved" after exercising all 12 checks above
    - OR a "reject" with specific issue → triggers a same-task fix or 08-07 gap closure
    - At minimum: mobile 360px renders without horizontal scroll; first token < 2s on live Anthropic; Romanian responses do not contain fictitious salesperson names or Meta/Google CAC numbers
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user keystroke → textarea state | client-only; no XSS surface (React text rendering) |
| useChat fetch → /api/v1/chat/.../messages | reuses readAccessToken from api-client.ts; cookie + Authorization Bearer; same-origin via Next.js rewrite |
| Anthropic markdown → react-markdown DOM | allowedElements whitelist prevents script/iframe/img injection; D-18a link whitelist prevents phishing |
| SSE event JSON → React state | data.text appended; data.tool_use_id matched against pill array — bounded |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure (cross-tenant) | useConversation fetch | mitigate | Backend enforces tenant filter (plan 08-05); frontend trusts the 404 response without further verification |
| T-08-02 | Tampering (XSS via markdown) | markdown-renderer.tsx | mitigate | `allowedElements` whitelist drops script/iframe/img/svg/h1-h6/style; no `unwrapDisallowed`; no `dangerouslySetInnerHTML`; D-18a link whitelist prevents `[label](javascript:...)` (would be dropped as malformed URL by react-markdown) |
| T-08-03 | Information Disclosure (error event leak) | useChat error handler | mitigate | Surfaces only `data.message_ro` (sanitized by backend per plan 08-05). No `console.log(error.stack)` leaked to UI |
| T-08-06 | Denial of Service (uncancellable stream) | useChat cancel | mitigate | `AbortController` aborts fetch on unmount or explicit cancel; backend's disconnect_probe (D-12) drops orchestrator early |
| T-08-PI | Tampering (prompt injection visible to user) | message-bubble.tsx | mitigate | Even if Claude attempts to render adversarial markdown, the allowedElements whitelist sanitizes; backend guard already rejected unsupported numbers/entities before reaching this layer |
| T-08-MOBILE | (carry-forward) UX-blocker | mobile sheet pattern | mitigate | BLOCKING mobile checkpoint (Task 4) enforces D-17 NON-NEGOTIABLE; cannot ship without explicit human approval at 360/768/1280px |
</threat_model>

<verification>
- `cd frontend && pnpm vitest --run` exits 0 (full chat test suite green)
- `cd frontend && pnpm typecheck` exits 0
- `cd frontend && pnpm lint` exits 0
- `cd frontend && grep -E "Bună!|Conversație nouă|Caut datele|Verific cifrele|Întreabă orice" src/components/chat/*.tsx src/app/\(dashboard\)/chat/page.tsx` returns NOTHING (all i18n)
- `cd frontend && grep -cE "min-h-\[44px\]" src/components/chat/*.tsx` returns at least 3 (NON-NEGOTIABLE touch targets)
- `cd frontend && cat messages/ro.json | python -c "import sys, json; d = json.load(sys.stdin); assert 'chat' in d and 'welcome' in d['chat'] and 'suggested' in d['chat'] and 'static' in d['chat']['suggested']; assert len(d['chat']['suggested']['static']) >= 5"` exits 0
- Mobile checkpoint Task 4 returns "approved"
</verification>

<success_criteria>
- CHAT-01 closed: User types Romanian question in /chat textarea, presses Enter, sees Claude's streamed response token-by-token with grounded data
- CHAT-06 closed: SC#6 — first token < 2s on live Anthropic API; auto-scroll within 100px (D-34) keeps user pinned to latest content
- CHAT-07 closed: Tool pills + ThinkingIndicator render per D-10 + UI-SPEC § States Matrix; collapsed summary after `done`
- CHAT-09 closed: useChat hook handles chunk boundaries (LM-7); no `EventSource` (LM-2); cancellable via AbortController; rate-limit + 409 messages from backend surfaced to user in Romanian
- 14 components + 3 ui primitives + 3 hooks + 2 lib helpers + rebuilt page wire together
- i18n: all UI strings live in `chat` namespace; ro primary, en mirror; no hardcoded Romanian
- Mobile NON-NEGOTIABLE: BLOCKING checkpoint Task 4 confirms 360/768/1280px rendering
- Accent reserved-for list (UI-SPEC § Color): manually verified no rogue `bg-accent` usage
</success_criteria>

<output>
Create `.planning/phases/08-ai-chat/08-06-SUMMARY.md` when done. Include: line count per component, test pass/fail counts (parseSSE PS1-PS5, useChat UC1-UC8, MarkdownRenderer MR1-MR6, ThinkingIndicator TI1-TI5), Task 4 mobile checkpoint approval signal, any deviations from UI-SPEC (with rationale), HUMAN-UAT note: Phase 8 ready for /gsd:verify-work.
</output>
