---
phase: 08-ai-chat
plan: 04
type: execute
wave: 3
depends_on: ["08-02", "08-03"]
files_modified:
  - backend/app/services/chat/prompt_builder.py
  - backend/app/services/chat/hallucination_guard.py
  - backend/app/services/chat/title_generator.py
  - backend/app/services/chat/orchestrator.py
  - backend/app/services/chat/repositories/__init__.py
  - backend/app/services/chat/repositories/conversation_repository.py
  - backend/app/services/chat/repositories/message_repository.py
  - backend/app/services/chat/repositories/tool_call_repository.py
  - backend/app/schemas/chat/__init__.py
  - backend/app/schemas/chat/conversation.py
  - backend/app/schemas/chat/message.py
  - backend/app/schemas/chat/sse_events.py
  - backend/tests/unit/chat/test_chat_prompt_builder.py
  - backend/tests/unit/chat/test_hallucination_guard.py
  - backend/tests/unit/chat/test_title_generator.py
  - backend/tests/unit/chat/test_chat_orchestrator.py
autonomous: true
requirements: [CHAT-02, CHAT-03, CHAT-04, CHAT-05, CHAT-09, CHAT-10]
tags: [chat, orchestrator, guard, prompt, anthropic, streaming, services]
must_haves:
  truths:
    - "ChatOrchestrator.run_turn() yields (event_name, payload) async generator matching D-09 SSE event schema (7 event types)"
    - "Hallucination guard rejects assistant text containing numeric tokens not in allowed-numbers set (±1% tolerance per D-06); regenerate budget = 1 (D-07); on 2nd failure stream Romanian fallback copy from D-07 and persist hallucination_flag=true"
    - "Number guard reuses Phase 5 NUMBER_PATTERN + extract_numbers_from_text verbatim (no fork)"
    - "Entity whitelist enforced for capitalized 2-3-word names (salespeople from mefi_salespeople, showrooms from {Brașov, București, Cluj-Napoca}, 11 source categories from MEFI_SOURCE_ID_TO_NAME)"
    - "Link whitelist: only {/sales, /salespeople, /marketing, /insights, /chat, #} per D-18a; other hrefs trigger regeneration"
    - "System prompt cached via cache_control: {type: 'ephemeral'} on last system block (D-27); LM-6 mitigated"
    - "Tool loop: stream Claude tokens → on stop_reason=tool_use, dispatch tools via TOOLS_REGISTRY in parallel (asyncio.gather) → max 5 rounds (D-30) → emit tool_use + tool_result SSE events per round"
    - "Title generator (D-14): asyncio.create_task fire-and-forget with module-level _pending set (LM-8); claude-haiku-4-5 → fallback to claude-sonnet-4-5; 3s timeout → fallback to truncated first user message"
    - "3 repositories (Conversation, Message, ToolCall) validate tenant_id in row before write (Phase 5 WR-04 pattern)"
    - "SSE event Pydantic schemas exist for all 7 event types per D-09"
  artifacts:
    - path: "backend/app/services/chat/prompt_builder.py"
      provides: "build_system_prompt(tenant_facts) returning list[dict] with cache_control on last block + Sofa Belle facts + MVP1 data limits + persona-detection prompt"
      contains: "cache_control"
      min_lines: 80
    - path: "backend/app/services/chat/hallucination_guard.py"
      provides: "check_response(text, tool_results, entity_whitelist) -> list[str] (empty = PASS); recursive number walker; derived ops; link + entity whitelists"
      contains: "def check_response"
      min_lines: 90
    - path: "backend/app/services/chat/title_generator.py"
      provides: "schedule_title_generation() with fire-and-forget asyncio.create_task + _pending registry + Haiku→Sonnet fallback"
      contains: "_pending"
    - path: "backend/app/services/chat/orchestrator.py"
      provides: "ChatOrchestrator.run_turn() async generator with streaming + tool loop + guard + persistence + title-gen schedule"
      contains: "MAX_TOOL_ROUNDS = 5"
      min_lines: 200
    - path: "backend/app/services/chat/repositories/conversation_repository.py"
      provides: "ConversationRepository: insert + list + get_by_id + soft_archive with tenant_id validation"
    - path: "backend/app/services/chat/repositories/message_repository.py"
      provides: "MessageRepository: insert_user_message + insert_assistant_message + load_history(limit) with tenant_id validation"
    - path: "backend/app/services/chat/repositories/tool_call_repository.py"
      provides: "ToolCallRepository: insert_tool_call (audit row per D-21)"
    - path: "backend/app/schemas/chat/sse_events.py"
      provides: "7 Pydantic v2 SSE event schemas per D-09: ConversationMetaEvent, ToolUseEvent, ToolResultEvent, AssistantChunkEvent, RegenerateNoticeEvent, DoneEvent, ErrorEvent"
  key_links:
    - from: "backend/app/services/chat/orchestrator.py"
      to: "backend/app/services/chat/tools/__init__.py"
      via: "from app.services.chat.tools import TOOLS_REGISTRY, get_all_tools"
      pattern: "TOOLS_REGISTRY\\["
    - from: "backend/app/services/chat/orchestrator.py"
      to: "backend/app/services/chat/hallucination_guard.py"
      via: "check_response(text, tool_results, entity_whitelist)"
      pattern: "check_response"
    - from: "backend/app/services/chat/hallucination_guard.py"
      to: "backend/app/services/insights/number_validator.py"
      via: "from app.services.insights.number_validator import NUMBER_PATTERN, extract_numbers_from_text"
    - from: "backend/app/services/chat/orchestrator.py"
      to: "anthropic.AsyncAnthropic"
      via: "client.messages.stream(...)"
      pattern: "AsyncAnthropic"
---

<objective>
Deliver the service layer that powers AI Chat: the Claude conversation orchestrator (streaming + bounded tool loop), the strict hallucination guard (numbers + entities + links), the fire-and-forget title generator (Haiku→Sonnet fallback), the Romanian-first system prompt builder (with `cache_control` ephemeral on the last block), and three repositories for persistence. Plus the 7 SSE-event Pydantic schemas per D-09.

Purpose: This plan is the heart of Phase 8. It implements:
- CHAT-02 (multi-tool turn via D-30 bounded loop with parallel `asyncio.gather` dispatch + MAX_TOOL_ROUNDS=5)
- CHAT-03 (history persists — repositories write user/assistant messages + tool_call audit per D-21)
- CHAT-04 (Sofa Belle system prompt with salesperson roster + showrooms + MVP1 data limits per D-26)
- CHAT-05 (honest refusal — system prompt explicitly forbids fabricating Meta/Google/TikTok data; guard rejects unsupported numbers/entities)
- CHAT-09 (TTFT < 2s — system prompt cached via D-27; parallel tool dispatch keeps round-trip < 500ms)
- CHAT-10 (zero hallucinated entities/numbers — guard with ±1% tolerance per D-06 + whitelist per D-06/D-18a)

Output: 13 new files (5 services + 3 repos + 3 schemas + package inits) + 4 unit test files. Plans 08-05 (router) and 08-06 (frontend) consume this layer.
</objective>

<execution_context>
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-ai-chat/08-CONTEXT.md
@.planning/phases/08-ai-chat/08-RESEARCH.md
@.planning/phases/08-ai-chat/08-PATTERNS.md
@docs/CHAT.md
@docs/SOFABELLE.md
@docs/api-references/mefi/enums.md
@backend/app/services/insights/insight_service.py
@backend/app/services/insights/prompt_builder.py
@backend/app/services/insights/number_validator.py
@backend/app/services/repositories/insight_repository.py
@backend/app/services/dashboards/dashboard_read_service.py
@backend/app/core/config.py
@backend/app/models/chat/__init__.py
@CLAUDE.md

<interfaces>
<!-- Reusable Phase 5 patterns. PASTE-VERBATIM citations. -->

From `backend/app/services/insights/insight_service.py:26-34, 92, 186-202` (canonical AsyncAnthropic pattern):
- Module-level: `from anthropic import AsyncAnthropic` (for test patchability — LM-1 mitigation).
- Module-level constants: `MODEL = "claude-sonnet-4-5"`, `MAX_TOKENS = 4096`, `TEMPERATURE = 0.2`.
- Instantiated inside method body: `client = AsyncAnthropic(api_key=settings.anthropic_api_key)`.
- Cost formula: `Decimal(str((input/1_000_000) * 3.0 + (output/1_000_000) * 15.0))` for Sonnet 4.5.

From `backend/app/services/insights/prompt_builder.py:64-86` (cache_control system prompt):
- `build_system_prompt() -> list[dict]` returns `[{"type":"text","text":...}, {"type":"text","text":"...","cache_control":{"type":"ephemeral"}}]`.

From `backend/app/services/insights/number_validator.py:28-30, 33-84, 197-211` (number extraction + tolerance):
- `NUMBER_PATTERN = re.compile(r"\b(\d[\d.,]*\d|\d)\b")` — Romanian thousands handler.
- `extract_numbers_from_text(text: str) -> list[float]` — normalization for `.` (thousands) vs `,` (decimal).
- Tolerance check: `abs(num - ref) / max(abs(ref), 1e-9) <= 0.01` (±1% per D-06).
- Skip rules: `num <= 10` (small counts), `1900 <= num <= 2100` (calendar years).

From `backend/app/services/repositories/insight_repository.py:32-78` (repo pattern):
- Constructor: `__init__(session: AsyncSession, tenant_id: UUID)`.
- Cross-tenant guard: `if row["tenant_id"] != self._tenant_id: raise ValueError("cross-tenant write blocked")`.
- No-commit policy: caller commits atomically.

From RESEARCH §2 lines 552-716 (orchestrator skeleton — PASTE-VERBATIM into orchestrator.py).
From RESEARCH §3 lines 721-815 (hallucination_guard — PASTE-VERBATIM).
From RESEARCH §6 lines 994-1043 (title_generator — PASTE-VERBATIM).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SSE event schemas + 3 repositories (conversation, message, tool_call)</name>
  <files>
    backend/app/schemas/chat/__init__.py,
    backend/app/schemas/chat/conversation.py,
    backend/app/schemas/chat/message.py,
    backend/app/schemas/chat/sse_events.py,
    backend/app/services/chat/repositories/__init__.py,
    backend/app/services/chat/repositories/conversation_repository.py,
    backend/app/services/chat/repositories/message_repository.py,
    backend/app/services/chat/repositories/tool_call_repository.py,
    backend/tests/unit/chat/test_chat_repositories.py
  </files>
  <read_first>
    - backend/app/services/repositories/insight_repository.py (full file — repo template, cross-tenant guard, no-commit policy)
    - backend/app/schemas/insights/daily_insight_schema.py (full file — Pydantic v2 template)
    - .planning/phases/08-ai-chat/08-CONTEXT.md (D-09 — SSE event schema list; D-15 — soft archive shape; D-16 — history window 20 msgs anchor first user; D-21 — per-tool audit row; D-22 — tenant isolation seam)
    - .planning/phases/08-ai-chat/08-PATTERNS.md (§ "schemas/chat/sse_events.py" NEW PATTERN — paste-verbatim)
    - backend/app/models/chat/ (read all 3 model files — column lists must match repo INSERT/SELECT shape)
  </read_first>
  <behavior>
    - Test 1: 7 SSE event schemas exist with the documented fields per D-09: ConversationMetaEvent, ToolUseEvent, ToolResultEvent, AssistantChunkEvent, RegenerateNoticeEvent, DoneEvent, ErrorEvent.
    - Test 2: ConversationOut schema has `id, title, created_at, last_message_at, archived`; `MessageOut.role` is `Literal["user","assistant"]` (NOT tool_use/tool_result — those are internal).
    - Test 3: `ConversationRepository.insert(row)` raises `ValueError("cross-tenant write blocked")` when `row["tenant_id"]` differs from repo's `tenant_id`.
    - Test 4: `ConversationRepository.list_conversations(archived=False)` SQL filters on `tenant_id == self._tenant_id AND archived == False` ordered by `last_message_at DESC`.
    - Test 5: `MessageRepository.load_history(conversation_id, limit=20)` returns last 20 messages with D-16 anchor: keep the FIRST user message + most recent 19 (NOT just oldest 20 or newest 20).
    - Test 6: `MessageRepository.insert_user_message(conversation_id, content)` and `.insert_assistant_message(conversation_id, content, tokens_used, duration_ms, hallucination_flag, regenerate_count)` both return the new message UUID and update the parent conversation's `last_message_at`.
    - Test 7: `ToolCallRepository.insert_tool_call(message_id, tool_name, input_args, output_data, duration_ms, error)` per D-21 (audit row).
    - Test 8: `ConversationRepository.soft_archive(conversation_id)` sets `archived=true` (D-15 — no hard delete in MVP1).
  </behavior>
  <action>
    1. **`backend/app/schemas/chat/__init__.py`** — export all schemas.

    2. **`backend/app/schemas/chat/conversation.py`** (~40 lines) — `ConversationOut`, `ConversationListOut(BaseModel): conversations: list[ConversationOut]`, `CreateConversationRequest(BaseModel): initial_message: str | None = None`. Per PATTERNS.md analog (`daily_insight_schema.py`).

    3. **`backend/app/schemas/chat/message.py`** (~30 lines) — `MessageOut(role: Literal["user","assistant"], content: str, created_at: datetime, tokens_used: int | None, hallucination_flag: bool, regenerate_count: int)`, `SendMessageRequest(content: str = Field(..., min_length=1, max_length=10000))`.

    4. **`backend/app/schemas/chat/sse_events.py`** (~70 lines) — PASTE the 7 schemas from PATTERNS.md § sse_events.py (lines 311-349) verbatim. Each schema is a Pydantic v2 BaseModel. Module docstring cites D-09.

    5. **`backend/app/services/chat/repositories/__init__.py`** — empty package marker + re-exports.

    6. **`backend/app/services/chat/repositories/conversation_repository.py`** (~90 lines):
       - Constructor `__init__(session, tenant_id)`.
       - `async def insert_conversation(self, row: dict) -> UUID` — validates `row["tenant_id"] == self._tenant_id`; INSERTs via SQLAlchemy ORM (`ChatConversation(**row)` + `session.add` + `await session.flush()`); returns the new id. Per WR-04: NO commit (caller commits).
       - `async def list_conversations(self, *, archived: bool = False, limit: int = 50) -> list[ChatConversation]` — SELECT with `tenant_id == self._tenant_id`, `archived == archived`, ORDER BY `last_message_at DESC`, LIMIT `limit`.
       - `async def get_by_id(self, conversation_id) -> ChatConversation | None` — SELECT with tenant filter.
       - `async def update_title(self, conversation_id, title) -> None` — UPDATE with tenant filter; used by title_generator.
       - `async def update_last_message_at(self, conversation_id) -> None` — UPDATE to `now()`.
       - `async def soft_archive(self, conversation_id) -> None` — UPDATE `archived=true` per D-15.

    7. **`backend/app/services/chat/repositories/message_repository.py`** (~80 lines):
       - Constructor.
       - `async def insert_user_message(self, conversation_id, content) -> UUID` — INSERT role='user', returns id, calls `conversation_repo.update_last_message_at` (or composes — see below).
       - `async def insert_assistant_message(self, conversation_id, content, *, tokens_used, duration_ms, hallucination_flag, regenerate_count, message_id: UUID | None = None) -> UUID` — INSERT role='assistant' (passing `id=message_id` to honor the assistant_msg_id pre-allocated by orchestrator for SSE `conversation_meta` event).
       - `async def load_history(self, conversation_id, limit=20) -> list[dict]` — D-16 ANCHOR + RECENT: load all messages for the conversation ordered by created_at ASC; if more than `limit`, keep the first user message + the most recent `limit-1` messages. Returns list of dicts shaped for Anthropic API (`[{"role": "user|assistant", "content": "..."}]`). Internal `tool_use`/`tool_result` messages excluded from history rendering (they're stored for audit but not replayed to Claude).

    8. **`backend/app/services/chat/repositories/tool_call_repository.py`** (~40 lines):
       - Constructor.
       - `async def insert_tool_call(self, *, message_id, tool_name, input_args, output_data, duration_ms, error) -> UUID` per D-21 audit row. Validates `tenant_id`.

    9. **`backend/tests/unit/chat/test_chat_repositories.py`** — covers Tests 1-8 above using AsyncMock(spec=AsyncSession) and `mock_session.execute / mock_session.scalar / mock_session.add` assertions. No DB required.

    Cite D-09 (sse_events), D-15 (soft_archive), D-16 (load_history anchor), D-21 (tool_call audit), D-22 (tenant isolation via TenantScopedMixin + explicit tenant_id check), WR-04 (no-commit policy) in module docstrings.
  </action>
  <verify>
    <automated>cd backend && .venv/bin/python -m pytest tests/unit/chat/test_chat_repositories.py -x -v 2>&1 | tail -30 && .venv/bin/python -c "from app.schemas.chat.sse_events import ConversationMetaEvent, ToolUseEvent, ToolResultEvent, AssistantChunkEvent, RegenerateNoticeEvent, DoneEvent, ErrorEvent; print('OK')"</automated>
  </verify>
  <done>All 8 repository behaviors pass; 7 SSE event schemas import; cross-tenant write blocked enforced</done>
  <acceptance_criteria>
    - `cd backend && .venv/bin/python -m pytest tests/unit/chat/test_chat_repositories.py -x` exits 0
    - `cd backend && .venv/bin/python -c "from app.schemas.chat.sse_events import ConversationMetaEvent, ToolUseEvent, ToolResultEvent, AssistantChunkEvent, RegenerateNoticeEvent, DoneEvent, ErrorEvent"` exits 0
    - `cd backend && grep -E 'cross-tenant write blocked' backend/app/services/chat/repositories/*.py | wc -l` returns at least 3
    - `cd backend && grep 'archived=True\|archived = True\|archived=true' backend/app/services/chat/repositories/conversation_repository.py` returns at least 1 match (D-15)
    - `cd backend && grep -E 'D-16|first user message|anchor' backend/app/services/chat/repositories/message_repository.py` returns at least 1 match
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prompt builder + hallucination guard + title generator (3 services with full unit tests)</name>
  <files>
    backend/app/services/chat/prompt_builder.py,
    backend/app/services/chat/hallucination_guard.py,
    backend/app/services/chat/title_generator.py,
    backend/tests/unit/chat/test_chat_prompt_builder.py,
    backend/tests/unit/chat/test_hallucination_guard.py,
    backend/tests/unit/chat/test_title_generator.py
  </files>
  <read_first>
    - backend/app/services/insights/prompt_builder.py (full file — D-26 base + cache_control pattern lines 64-86)
    - backend/app/services/insights/number_validator.py (full file — NUMBER_PATTERN + extract_numbers_from_text + tolerance pattern to REUSE verbatim, do not fork)
    - .planning/phases/08-ai-chat/08-RESEARCH.md (§3 hallucination_guard sketch lines 721-815 paste-verbatim; §6 title_generator sketch lines 994-1043 paste-verbatim)
    - .planning/phases/08-ai-chat/08-CONTEXT.md (D-06 strict-mode mechanics; D-07 retry budget + fallback copy; D-14 title-gen Haiku→Sonnet; D-18a link whitelist; D-26..D-28 system prompt + persona)
    - .planning/phases/08-ai-chat/08-PATTERNS.md (§ prompt_builder pattern — Sofa Belle facts to inject; § hallucination_guard pattern — recursive walker + derived numbers; § title_generator pattern — LM-8 _pending set)
    - docs/CHAT.md (§5 full base system prompt to paste — Romanian, business tone, persona inference)
    - docs/SOFABELLE.md (showrooms, salesperson roster, avg cycle, business hours)
    - docs/api-references/mefi/enums.md (Active salesperson full names: Roibu Valeria, Raileanu Leon, Godja Adina Maria, Dragoi Mihaela, Zagrian Emilia, Moaca Andreea)
  </read_first>
  <behavior>
    Prompt builder:
    - Test PB1: `build_system_prompt(tenant_facts)` returns `list[dict]` of len >= 2.
    - Test PB2: Last item has `"cache_control": {"type": "ephemeral"}` per D-27.
    - Test PB3: Concatenated text contains all 6 salesperson names from D-09: Roibu Valeria, Raileanu Leon, Godja Adina Maria, Dragoi Mihaela, Zagrian Emilia, Moaca Andreea.
    - Test PB4: Text contains MVP1 data limit block: phrases "estimated_value", "Nu am acces", "Meta", "Google", "TikTok", "GA4", "GSC".
    - Test PB5: Text contains showroom names: Brașov, București, Cluj-Napoca.
    - Test PB6: Text instructs Claude to use markdown links `[Label](/sales)` (D-18 — no date params in MVP1).

    Hallucination guard:
    - Test HG1: `check_response("Vânzările au crescut cu 23.400 RON", tool_results=[{"revenue_delta": "23400"}], entity_whitelist={})` returns `[]` (PASS — number matches tool result within ±1%).
    - Test HG2: `check_response("Vânzările au crescut cu 99.999 RON", tool_results=[{"revenue_delta": "23400"}], entity_whitelist={})` returns a list containing `"number:99999"` (FAIL — outside ±1%).
    - Test HG3: Derived percentage works: tool result `{a: 100, b: 50}` allows `200%` (a/b*100) in response text.
    - Test HG4: Numbers ≤ 10 are skipped (LM-11 list-marker guard).
    - Test HG5: Calendar years 1900-2100 are skipped.
    - Test HG6: Link `[Sales](https://evil.com)` returns `link:https://evil.com` (D-18a — only `/sales,/salespeople,/marketing,/insights,/chat,#` allowed).
    - Test HG7: Link `[Sales Dashboard](/sales)` passes.
    - Test HG8: Capitalized entity not in whitelist (e.g., "Maria Popescu") returns `entity:Maria Popescu`.
    - Test HG9: Salesperson in whitelist (e.g., "Raileanu Leon") passes.
    - Test HG10: Empty tool_results allow only common-knowledge numbers (no business numbers slip through).

    Title generator:
    - Test TG1: `schedule_title_generation(...)` adds the task to module-level `_pending` set (LM-8 GC mitigation).
    - Test TG2: Primary model is `claude-haiku-4-5`; on 400 (mocked exception), retries with `claude-sonnet-4-5`.
    - Test TG3: On 3s timeout (mocked `asyncio.wait_for` → TimeoutError) on BOTH attempts, fallback is truncated first user message (first 60 chars + `…`).
    - Test TG4: On success, calls the provided `update_callback(conversation_id, title)`.
    - Test TG5: Title is stripped of final punctuation (`.,!?`) and quotes.
  </behavior>
  <action>
    1. **`backend/app/services/chat/prompt_builder.py`** (~120 lines) per D-26 + D-27 + D-28:
       - Module-level `SYSTEM_PROMPT_TEXT` — Romanian base prompt from docs/CHAT.md §5 (paste verbatim from the spec).
       - Module-level `TENANT_FACTS_BLOCK_TEMPLATE` — template with `{tenant_name}`, `{showrooms}`, `{salesperson_roster}`, `{avg_cycle_days}`, `{business_hours}` placeholders.
       - Module-level `MVP1_DATA_LIMITS_BLOCK` — explicit Romanian block:
         > "**Limite de date MVP1:** Nu ai acces la datele de reclamă (Meta, Google, TikTok, GA4, Search Console) sau la transcripte de apeluri. `estimated_value` este NULL pentru toate lead-urile Sofa Belle în această iterație — nu inventa cifre de revenue per-lead. Dacă utilizatorul întreabă despre aceste date, răspunde onest: 'Nu am acces la datele de reclamă/web în această versiune'."
       - Module-level `OUTPUT_FORMAT_BLOCK` — instructs markdown links `[Sales Dashboard](/sales)`, bold KPIs with `**...**`, no external URLs.
       - `def build_system_prompt(tenant_facts: dict) -> list[dict]`:
         - Returns `[{"type": "text", "text": SYSTEM_PROMPT_TEXT + interpolated_tenant_facts + MVP1_DATA_LIMITS_BLOCK}, {"type": "text", "text": OUTPUT_FORMAT_BLOCK, "cache_control": {"type": "ephemeral"}}]` per D-27.
       - Helper `def _build_tenant_facts(tenant_name: str = "Sofa Belle") -> dict`: returns `{tenant_name, industry: "mobilă premium", showrooms: ["Brașov", "București", "Cluj-Napoca"], salesperson_roster: ["Roibu Valeria", "Raileanu Leon", "Godja Adina Maria", "Dragoi Mihaela", "Zagrian Emilia", "Moaca Andreea"], avg_cycle_days: 30, business_hours: "Mon–Sun 09:00–19:00 Europe/Bucharest"}`.

    2. **`backend/app/services/chat/hallucination_guard.py`** (~120 lines) — PASTE-VERBATIM from RESEARCH §3 lines 721-815, with these tweaks:
       - `from app.services.insights.number_validator import NUMBER_PATTERN, extract_numbers_from_text` (REUSE verbatim — do not fork).
       - `TOLERANCE = Decimal("0.01")` (±1% per D-06).
       - `COMMON_DAYS = {Decimal(n) for n in range(1, 32)}`.
       - `COMMON_YEARS = {Decimal(y) for y in range(1900, 2101)}`.
       - `COMMON_ROUND_PERCENTS = {Decimal("0"), Decimal("50"), Decimal("100")}`.
       - `_extract_numbers_recursive(value) -> set[Decimal]` — walks dict/list/str/int/float (RESEARCH §3 lines 735-749).
       - `_compute_derived(base) -> set[Decimal]` — pairwise %, sum, diff, quantized to 1 decimal (RESEARCH §3 lines 752-765).
       - `check_response(response_text, tool_results, entity_whitelist=None) -> list[str]`:
         - Build allowed = recursive(tool_results) ∪ derived ∪ commons.
         - For each number in response: skip if ≤10 or in [1900,2100]; else check ±1% against any allowed; if no match, append `f"number:{num}"`.
         - Check links per D-18a: `ALLOWED_HREFS = {"/sales", "/salespeople", "/marketing", "/insights", "/chat", "#"}`; reject anything else as `f"link:{href}"`.
         - Check entities (if `entity_whitelist` provided): regex `r"\b([A-ZĂÂÎȘȚ][a-zăâîșț]+ [A-ZĂÂÎȘȚ][a-zăâîșț]+(?: [A-ZĂÂÎȘȚ][a-zăâîșț]+)?)\b"` (RESEARCH §3 lines 809-813) against the union of `whitelist.salespeople ∪ whitelist.showrooms ∪ whitelist.categories`; non-matches → `f"entity:{name}"`.
         - Return `unsupported` list (empty = PASS).
       - Helper `async def build_entity_whitelist(session, tenant_id) -> dict[str, set[str]]`: queries `mefi_salespeople` for full names, returns `{"salespeople": set[str], "showrooms": {"Brașov","București","Cluj","Cluj-Napoca"}, "categories": set(MEFI_SOURCE_ID_TO_NAME.values())}`.

    3. **`backend/app/services/chat/title_generator.py`** (~80 lines) — PASTE-VERBATIM RESEARCH §6 lines 994-1043 with:
       - Module-level `_pending: set[asyncio.Task] = set()` per LM-8.
       - `TITLE_MODEL_PRIMARY = "claude-haiku-4-5"`, `TITLE_MODEL_FALLBACK = "claude-sonnet-4-5"`, `TITLE_TIMEOUT_S = 3.0`.
       - `schedule_title_generation(conversation_id, first_user, first_assistant, update_callback)` adds task to `_pending` + `task.add_done_callback(_pending.discard)`.
       - `async def _generate(...)` — try primary then fallback model; on both failures, use `first_user[:60].rstrip() + ("…" if len > 60 else "")`.
       - Prompt: `"Generează un titlu de 4-6 cuvinte în română pentru această conversație, descriind tema principală. Răspunde DOAR cu titlul, fără punctuație finală sau ghilimele."`
       - Strip final `.,!?` and quotes from result.

    4. **`backend/tests/unit/chat/test_chat_prompt_builder.py`** — covers PB1-PB6.

    5. **`backend/tests/unit/chat/test_hallucination_guard.py`** — covers HG1-HG10 (extend the Phase 5 test_number_validator.py shape).

    6. **`backend/tests/unit/chat/test_title_generator.py`** — covers TG1-TG5 with `patch("app.services.chat.title_generator.AsyncAnthropic")`.
  </action>
  <verify>
    <automated>cd backend && .venv/bin/python -m pytest tests/unit/chat/test_chat_prompt_builder.py tests/unit/chat/test_hallucination_guard.py tests/unit/chat/test_title_generator.py -x -v 2>&1 | tail -40</automated>
  </verify>
  <done>All PB1-PB6 + HG1-HG10 + TG1-TG5 tests pass</done>
  <acceptance_criteria>
    - `cd backend && .venv/bin/python -m pytest tests/unit/chat/test_chat_prompt_builder.py tests/unit/chat/test_hallucination_guard.py tests/unit/chat/test_title_generator.py -x` exits 0
    - `cd backend && .venv/bin/python -c "from app.services.chat.prompt_builder import build_system_prompt, _build_tenant_facts; blocks = build_system_prompt(_build_tenant_facts()); assert blocks[-1].get('cache_control', {}).get('type') == 'ephemeral'"` exits 0
    - `cd backend && grep -E 'Roibu|Raileanu|Godja|Dragoi|Zagrian|Moaca' backend/app/services/chat/prompt_builder.py | wc -l` returns at least 6
    - `cd backend && grep -E 'Meta|Google|TikTok|GA4' backend/app/services/chat/prompt_builder.py` returns at least 4 matches (MVP1 data limits block)
    - `cd backend && grep -E 'NUMBER_PATTERN|extract_numbers_from_text' backend/app/services/chat/hallucination_guard.py` returns at least 1 import line (reuse verbatim — no fork)
    - `cd backend && grep '_pending' backend/app/services/chat/title_generator.py | head -3` returns at least 2 matches (LM-8 detach pattern)
    - `cd backend && grep -E 'claude-haiku-4-5|claude-sonnet-4-5' backend/app/services/chat/title_generator.py | wc -l` returns 2
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ChatOrchestrator.run_turn() — streaming + tool loop + guard + persistence + title-gen schedule</name>
  <files>
    backend/app/services/chat/orchestrator.py,
    backend/tests/unit/chat/test_chat_orchestrator.py
  </files>
  <read_first>
    - .planning/phases/08-ai-chat/08-RESEARCH.md (§2 orchestrator skeleton lines 552-716 — PASTE-VERBATIM as starting point; § Pitfalls 2-3 — regenerate prompt + title-gen race; § "Architectural Responsibility Map")
    - .planning/phases/08-ai-chat/08-CONTEXT.md (D-08 server-side post-stream guard; D-09 7 SSE events; D-12 disconnect probe; D-29 per-request AsyncAnthropic; D-30 5 rounds + asyncio.gather; D-31 cost tracking)
    - .planning/phases/08-ai-chat/08-PATTERNS.md (§ orchestrator.py NEW PATTERN — RESEARCH §2 reference)
    - backend/app/services/insights/insight_service.py (full file — AsyncAnthropic instantiation pattern, structlog binding, cost formula)
    - backend/app/services/chat/prompt_builder.py (Task 2 output — build_system_prompt + _build_tenant_facts)
    - backend/app/services/chat/hallucination_guard.py (Task 2 output — check_response + build_entity_whitelist)
    - backend/app/services/chat/title_generator.py (Task 2 output — schedule_title_generation)
    - backend/app/services/chat/tools/__init__.py (Plan 08-03 output — TOOLS_REGISTRY + get_all_tools)
    - backend/app/services/chat/repositories/* (Task 1 output — 3 repos)
  </read_first>
  <behavior>
    - Test O1: `ChatOrchestrator(session, tenant_id, user_id)` constructs and binds structlog with `tenant_id` + `service="chat_orchestrator"`.
    - Test O2: `run_turn(conversation_id, user_text, disconnect_probe)` is an async generator yielding `(event_name, payload)` tuples.
    - Test O3 (single-turn no-tool): mocked client.messages.stream returns text_delta events with stop_reason="end_turn". Yields: `conversation_meta`, multiple `assistant_chunk`, `done` (no tool_use, no regenerate_notice).
    - Test O4 (single tool round): mocked stream first yields tool_use block + intermediate text, stop_reason="tool_use"; orchestrator dispatches via TOOLS_REGISTRY (mocked), emits `tool_use` + `tool_result`; second iteration yields final text + stop_reason="end_turn". Yields all 7 event types in order.
    - Test O5 (3 distinct tools — CHAT-02 SC#2): multi_tool fixture from Wave 0; orchestrator dispatches 3 tools in parallel via asyncio.gather; emits 3 tool_use + 3 tool_result events.
    - Test O6 (MAX_TOOL_ROUNDS=5 cap): if Claude keeps returning stop_reason="tool_use", orchestrator breaks after 5 rounds, finishes streaming whatever final text was produced, persists message. Asserts loop iteration count.
    - Test O7 (hallucination guard PASS): mocked guard returns []; flow completes; `done` event fires with `hallucination_flag=False`.
    - Test O8 (hallucination guard FAIL round 1 + PASS round 2): guard returns ["number:99999"] first, then []; orchestrator emits `regenerate_notice` between rounds; final `done` has `hallucination_flag=False` and `regenerate_count=1`.
    - Test O9 (hallucination guard FAIL both rounds — D-07): guard returns failures twice; orchestrator emits regenerate_notice once, then streams the Romanian fallback `"Nu pot da un răspuns precis pe baza datelor disponibile. Te rog reformulează întrebarea."`, persists with `hallucination_flag=True`, `regenerate_count=1`, emits `done` with `hallucination_flag=True`.
    - Test O10 (disconnect mid-stream — D-12): `disconnect_probe()` returns True mid-loop; orchestrator returns early without persisting assistant message. User message is already persisted (was inserted before stream started).
    - Test O11 (title-gen scheduled on turn 1 only): on first turn (history empty), `schedule_title_generation` called once post-`done`; on turn 2+ (history non-empty), NOT called.
    - Test O12 (cost tracking — D-31): `total_usage` accumulates `input_tokens + output_tokens + cache_read_input_tokens` across all rounds; structlog log line emits cost in USD via the Phase 5 cost formula.
    - Test O13 (tool handler exception — D-21): when a tool handler raises, orchestrator catches, emits `tool_result` with `error=true`, passes `is_error=true` to Claude in the tool_result message, persists `chat_tool_calls.error` populated.
  </behavior>
  <action>
    Implement `backend/app/services/chat/orchestrator.py` (~250 lines) using RESEARCH §2 lines 552-716 as the starting skeleton. Apply these additional concerns:

    Module-level:
    ```
    from anthropic import AsyncAnthropic    # LM-1 mitigation — module-level for test-patchability ONLY
    MODEL = "claude-sonnet-4-5"             # D-26 — locked
    MAX_TOKENS = 4096
    TEMPERATURE = 0.3                       # D-30 — slightly higher than Phase 5's 0.2 (chat is conversational)
    MAX_TOOL_ROUNDS = 5                     # D-30 hard cap (cost guard)
    HISTORY_LIMIT = 20                      # D-16
    ```

    Class `ChatOrchestrator`:
    - `__init__(self, session, tenant_id, user_id)`: bind structlog with `tenant_id`, `service="chat_orchestrator"`, `user_id` (no PII per CLAUDE.md #6).
    - `async def run_turn(self, conversation_id, user_text, disconnect_probe) -> AsyncIterator[tuple[str, dict]]`:
      1. Persist user message via `MessageRepository.insert_user_message`. Commit BEFORE the stream begins so the user's question is preserved even if the stream aborts (D-12).
      2. Pre-allocate `assistant_msg_id = uuid4()`. Emit `("conversation_meta", {conversation_id, message_id_user, message_id_assistant})`.
      3. `client = AsyncAnthropic(api_key=settings.anthropic_api_key)` (LM-1: instantiation INSIDE method, never module-level).
      4. Build `system = build_system_prompt(_build_tenant_facts())`, `tools = get_all_tools()`, load history via `MessageRepository.load_history(conversation_id, limit=HISTORY_LIMIT)`, append user message.
      5. Build entity whitelist once via `await build_entity_whitelist(session, tenant_id)`.
      6. Outer loop `for guard_attempt in (0, 1)` (D-07 budget = 1 regenerate):
         - Reset `accumulated_text = ""`, `accumulated_tool_results = []`.
         - Inner loop `for round_idx in range(MAX_TOOL_ROUNDS)`:
           - `async with client.messages.stream(model=MODEL, max_tokens=MAX_TOKENS, temperature=TEMPERATURE, system=system, tools=tools, messages=messages) as stream:`
             - `async for event in stream:`
               - `if await disconnect_probe(): return` (D-12).
               - If event is `content_block_start` with `tool_use`: emit `("tool_use", {tool_use_id, name, input: {}})` (input still streaming — placeholder).
               - If event is `content_block_delta` with `text_delta`: `accumulated_text += event.delta.text`; emit `("assistant_chunk", {"text": event.delta.text})`.
             - `final_msg = await stream.get_final_message()`.
           - Accumulate `total_usage` per D-31 (input_tokens + output_tokens + cache_read_input_tokens).
           - `if final_msg.stop_reason != "tool_use": break`.
           - Extract `tool_use_blocks = [b for b in final_msg.content if b.type == "tool_use"]`.
           - Parallel dispatch via `asyncio.gather` (D-30): for each block, look up in TOOLS_REGISTRY, validate input via `tool.input_schema.model_validate(block.input)`, call `tool.handler(self._tenant_id, self._session, validated)` with `try/except`. Persist via `ToolCallRepository.insert_tool_call` (D-21 audit). Emit `("tool_result", {tool_use_id, output_preview: json.dumps(result, default=str)[:200], duration_ms, error: bool})`.
           - Append `{"role": "assistant", "content": final_msg.content}` and `{"role": "user", "content": tool_results_for_claude}` to `messages` (Pitfall 2 — keep tool_results in history; don't re-run tools on regenerate).
         - After inner loop ends (final assistant text in hand): run `unsupported = check_response(accumulated_text, accumulated_tool_results, entity_whitelist)`.
         - If `unsupported is empty`: break outer loop (PASS).
         - If `guard_attempt == 0`: emit `("regenerate_notice", {"reason": "hallucination_guard"})`; append corrective user message per RESEARCH lines 683-685; continue outer loop.
         - If `guard_attempt == 1` (D-07 second failure): set `accumulated_text` to the Romanian fallback; persist assistant message with `hallucination_flag=True`, `regenerate_count=1`; emit `done`; return.
      7. After outer loop succeeds: persist assistant message with `hallucination_flag=False`, `regenerate_count=guard_attempt`. Emit `("done", {message_id, total_input_tokens, total_output_tokens, duration_ms, hallucination_flag: False})`.
      8. Pitfall 3: schedule title-gen AFTER persisting the assistant message + emitting `done`. Only on turn 1 (`if len(history) == 0`).
      9. Log cost via structlog: `self._log.info("chat.turn.complete", input_tokens=..., output_tokens=..., cache_read=..., cost_usd=str(cost))` (D-31 — store in logs only, no DB column in MVP1).

    Error handling: wrap the whole generator in a try/except. On unhandled exception, emit `("error", {"code": "internal", "message_ro": "A apărut o problemă. Te rog încearcă din nou."})` (sanitized — never leak Anthropic API errors per T-08-03).

    Helpers used inside the class:
    - `_persist_user_message`, `_persist_assistant_message`, `_persist_tool_call` — thin wrappers around the 3 repositories.
    - `_tenant_facts()` — calls `_build_tenant_facts()` from prompt_builder.

    `backend/tests/unit/chat/test_chat_orchestrator.py` — covers O1-O13 using mocked AsyncAnthropic per PATTERNS.md test pattern and the Wave 0 cassettes (`tests/fixtures/anthropic_responses/basic_turn.py` for O3, `multi_tool.py` for O5). Use `sse_collector` fixture from Wave 0 conftest to drain the async generator into a list and assert event sequence.
  </action>
  <verify>
    <automated>cd backend && .venv/bin/python -m pytest tests/unit/chat/test_chat_orchestrator.py -x -v 2>&1 | tail -50</automated>
  </verify>
  <done>O1-O13 all pass; orchestrator emits all 7 SSE event types correctly; guard regenerate flow works; disconnect mid-stream drops partial work; title-gen scheduled on turn 1</done>
  <acceptance_criteria>
    - `cd backend && .venv/bin/python -m pytest tests/unit/chat/test_chat_orchestrator.py -x` exits 0
    - `cd backend && grep -E 'MAX_TOOL_ROUNDS = 5|MAX_TOOL_ROUNDS=5' backend/app/services/chat/orchestrator.py` returns 1 match (D-30)
    - `cd backend && grep -E 'cache_control|build_system_prompt' backend/app/services/chat/orchestrator.py | wc -l` returns at least 1
    - `cd backend && grep -E 'asyncio\.gather' backend/app/services/chat/orchestrator.py` returns at least 1 match (D-30 parallel dispatch)
    - `cd backend && grep -E 'disconnect_probe' backend/app/services/chat/orchestrator.py | wc -l` returns at least 1 (D-12)
    - `cd backend && grep -E 'regenerate_notice|hallucination_flag' backend/app/services/chat/orchestrator.py | wc -l` returns at least 2 (D-07 + D-08)
    - `cd backend && grep -E 'schedule_title_generation' backend/app/services/chat/orchestrator.py` returns at least 1 match (D-14)
    - `cd backend && grep -E 'AsyncAnthropic\(api_key' backend/app/services/chat/orchestrator.py` returns at least 1 match (D-29 per-request instantiation)
    - `cd backend && grep -E '^from anthropic import AsyncAnthropic' backend/app/services/chat/orchestrator.py` returns exactly 1 match (LM-1 module-level import only)
    - `cd backend && .venv/bin/python -m pytest tests/unit/chat/ -x -v 2>&1 | tail -3 | grep -E 'passed|failed'` shows full chat unit suite passing
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input (textarea content) → system prompt | concatenated into messages list; prompt-injection attempts mitigated by system instructions + guard |
| tool results (JSON) → Claude context | tool outputs become part of `messages` for next round; may contain entity names — guard whitelist checks against them in FINAL output |
| Claude output → user (SSE stream) | streamed token-by-token; guard runs POST-stream-complete (D-08) before persisting; first stream is visually discarded on guard fail |
| AsyncAnthropic exceptions → SSE error event | sanitized — `{code, message_ro}` only; raw exception details never reach client (T-08-03) |
| title generator → conversation row UPDATE | fire-and-forget; no impact on user-facing turn |
| structlog cost line → logs | logged as `cost_usd` string; no PII; safe for production observability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure (cross-tenant) | tool dispatch in orchestrator | mitigate | Orchestrator passes `self._tenant_id` to every `tool.handler(tenant_id, session, inp)` call (LM-3 contract enforced by plan 08-03 registry test). Repositories validate `row["tenant_id"] == self._tenant_id` (T1 acceptance criterion) |
| T-08-02 | Tampering (prompt injection) | system prompt + guard | mitigate | (a) System prompt explicitly instructs Claude to respond in Romanian with grounded numbers and to defer when unsure (D-26). (b) Guard whitelist for entities/numbers/links runs server-side post-stream — Claude cannot bypass by clever prose. (c) D-07 retry budget caps regenerate cost. (d) Fallback message ensures bad responses still produce sane UX. (e) Anthropic's tool input schema validates against Pydantic before any handler runs (LM-10 mitigation) |
| T-08-03 | Information Disclosure (API key / trace leak) | SSE error event | mitigate | Orchestrator's outermost try/except returns `{"code": "internal", "message_ro": "A apărut o problemă..."}` — never `str(exc)`. structlog logs the full exception via `log.exception(...)` for ops, not the client. AnthropicAPIError, RateLimitError, AuthenticationError all collapsed to generic Romanian message |
| T-08-04 | Denial of Service (cost runaway) | MAX_TOOL_ROUNDS + history limit + guard budget | mitigate | (a) `MAX_TOOL_ROUNDS=5` caps tool dispatches per turn (D-30). (b) `HISTORY_LIMIT=20` caps input tokens via D-16 anchor-and-recent. (c) `MAX_TOKENS=4096` caps output. (d) Guard regenerate budget = 1 (D-07). (e) Title-gen `TITLE_TIMEOUT_S=3.0` prevents long Haiku calls. Per-tenant rate limit enforced at router layer in plan 08-05 |
| T-08-05 | Information Disclosure (PII in logs) | structlog binding | mitigate | structlog.bind(tenant_id=..., service=..., user_id=...). NEVER bind user_text, accumulated_text, or salesperson personal details. structlog.info events only log token counts + cost + durations + tool names (CLAUDE.md #6) |
| T-08-06 | Denial of Service (slowloris-style SSE hold) | streaming endpoint | mitigate (partial in this plan, completed in 08-05) | This plan's contribution: disconnect_probe (D-12) drops the orchestrator early when client disconnects. Plan 08-05 adds router-level Redis stream lock (D-11) + 3-min TTL + Caddy idle-timeout heartbeat (RESEARCH § Pitfall 1 — emit `:\n\n` every 15s) |
| T-08-INJ | Tampering | adversarial prompt patterns | mitigate | Adversarial test fixture from Wave 0 (`tests/adversarial/adversarial_chat_questions.yaml` — 15 trap questions) exercises injection patterns nightly; D-07 + D-08 ensure unsupported output never reaches user |
</threat_model>

<verification>
- `cd backend && .venv/bin/python -m pytest tests/unit/chat/ -x -v` exits 0 (full chat unit suite green: models + repositories + prompt_builder + hallucination_guard + title_generator + orchestrator + tools + registry)
- `cd backend && .venv/bin/python -c "from app.services.chat.orchestrator import ChatOrchestrator, MODEL, MAX_TOOL_ROUNDS, TEMPERATURE; assert MODEL == 'claude-sonnet-4-5'; assert MAX_TOOL_ROUNDS == 5"` exits 0
- `cd backend && grep -c 'AsyncAnthropic' backend/app/services/chat/orchestrator.py` returns at least 2 (one for import, one for instantiation)
- `cd backend && grep -c 'AsyncAnthropic' backend/app/services/chat/title_generator.py` returns at least 2 (same pattern)
- `cd backend && .venv/bin/python -m pytest tests/unit/chat/test_anthropic_scope.py -x` STILL PASSES (CHAT-08 gate — chat.py not yet present; gate is vacuous)
</verification>

<success_criteria>
- CHAT-02 supported: orchestrator dispatches ≥3 tools in parallel via asyncio.gather; MAX_TOOL_ROUNDS=5 cap honored
- CHAT-03 supported: 3 repositories handle conversation/message/tool_call persistence with tenant isolation
- CHAT-04 supported: prompt_builder injects salesperson roster + showrooms + MVP1 data limits
- CHAT-05 supported: system prompt forbids fabricating Meta/Google/TikTok numbers; guard's entity whitelist rejects fictitious names
- CHAT-09 supported: cache_control reduces TTFT; parallel tool dispatch keeps round-trip < 500ms
- CHAT-10 supported: guard's ±1% numeric tolerance + entity whitelist + link whitelist
- All D-NN decisions cited in module docstrings (D-06..D-31 covered in this plan)
- LM-1, LM-3, LM-6, LM-8, LM-9, LM-10, LM-11 mitigated
</success_criteria>

<output>
Create `.planning/phases/08-ai-chat/08-04-SUMMARY.md` when done. Include: line counts for each new file, key test results (orchestrator multi-tool turn passed, guard regenerate flow passed, title-gen Haiku→Sonnet fallback passed), Anthropic SDK version verified to support messages.stream(tools=, cache_control=) together, any deviations from RESEARCH sketches.
</output>
