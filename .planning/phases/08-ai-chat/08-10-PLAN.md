---
phase: 08-ai-chat
plan: 10
type: execute
wave: 3
depends_on: ["08-07", "08-08", "08-09"]
files_modified:
  - .planning/phases/08-ai-chat/08-HUMAN-UAT.md
autonomous: false
gap_closure: true
requirements: [CHAT-01, CHAT-03, CHAT-05, CHAT-06, CHAT-07, CHAT-09, CHAT-10]
threat_model_ref: "T-08-03 (Information Disclosure — UX); T-08-06 (DoS — latency under real network); D-14 / D-17 / D-36 contract verification"
tags: [chat, human-uat, checkpoint, gap-closure, BLOCKING]
must_haves:
  truths:
    - "A structured `08-HUMAN-UAT.md` file exists at `.planning/phases/08-ai-chat/08-HUMAN-UAT.md` modeled after `.planning/phases/05-ai-insights/05-HUMAN-UAT.md`, capturing all 11 items from 08-VERIFICATION.md `human_verification:` frontmatter as an ordered checklist with `expected` / `result` slots the operator fills in"
    - "Each of the 11 items references the underlying CR-* / D-XX / SC# it gates so the operator knows WHY each test exists"
    - "The 11 items include explicit pre-conditions: CR-01 fix shipped (plan 08-07 SUMMARY.md complete), CR-02/CR-03/CR-05/WR-10 fix shipped (plan 08-08 SUMMARY.md complete), CR-04/CR-06 fix shipped (plan 08-09 SUMMARY.md complete) — operator MUST verify those summaries exist before starting UAT"
    - "A `checkpoint:human-verify` BLOCKING gate task exists as the final task of this plan, requiring the operator to either return 'approved' (Phase 8 done, eligible for SUMMARY) or file follow-up gaps which become a new gap-closure cycle"
    - "The UAT file's frontmatter includes `status: pending`, `phase: 08-ai-chat`, `source: [08-VERIFICATION.md, 08-REVIEW.md]`, `started: <fill on execution>`, `updated: <fill on execution>`, mirroring the 05-HUMAN-UAT.md frontmatter shape"
  artifacts:
    - path: ".planning/phases/08-ai-chat/08-HUMAN-UAT.md"
      provides: "Structured 11-item human verification checklist for Phase 8, modeled after 05-HUMAN-UAT.md; carries the deferred 08-06 Task 4 BLOCKING UAT into the gap-closure cycle"
      contains: "human_verification"
  key_links:
    - from: ".planning/phases/08-ai-chat/08-HUMAN-UAT.md"
      to: ".planning/phases/08-ai-chat/08-VERIFICATION.md"
      via: "All 11 items derived verbatim from 08-VERIFICATION.md frontmatter human_verification: array (lines 77-110)"
      pattern: "08-VERIFICATION"
    - from: ".planning/phases/08-ai-chat/08-HUMAN-UAT.md"
      to: ".planning/phases/05-ai-insights/05-HUMAN-UAT.md"
      via: "Format modeled after this file (yaml frontmatter + ordered numbered tests + Summary block)"
---

<objective>
Capture the 11 deferred human-verification items from 08-VERIFICATION.md `human_verification:` array (originally scoped to 08-06 Task 4 BLOCKING UAT, deferred and never executed) into a structured `.planning/phases/08-ai-chat/08-HUMAN-UAT.md` file the operator can run against the FIXED code (post CR-01..CR-06). Then BLOCK phase advancement on the operator's "approved" verdict.

Per 08-VERIFICATION.md:
- 11 items listed under `human_verification:` (frontmatter lines 77-110), covering: mobile 360/768/1280px responsiveness; live Anthropic Romanian quality (CHAT-01/CHAT-05/CHAT-10); D-14 title persistence (CHAT-04 — blocked by CR-03); CHAT-06 inline dashboard links; touch targets ≥ 44px (D-36); keyboard + screen reader (a11y); conversation persistence across browser sessions (CHAT-03 / SC#5 — blocked by CR-01).
- All 11 items depend on the CR fixes in plans 08-07 / 08-08 / 08-09 having SHIPPED. The operator MUST confirm those SUMMARYs exist before running UAT.

Per the gap-closure scope (planning context):
- A `checkpoint:human-verify` BLOCKING gate task is the LAST task. Phase 8 cannot be declared complete until the operator returns "approved" (or files new gaps that trigger another gap-closure cycle).

Purpose: Closes the deferred 08-06 Task 4 carry-forward — the only remaining HUMAN-NEEDED items from 08-VERIFICATION.md SC scoring (SC#3 and SC#6 marked HUMAN-NEEDED; SC#1, SC#4, SC#5 marked PARTIAL/FAILED with CR fixes lifting them once 07/08/09 ship). After the operator approves, all 7 Phase 8 success criteria are either VERIFIED (existing) or operator-confirmed (the 11 items here).

Output: 1 new file (`.planning/phases/08-ai-chat/08-HUMAN-UAT.md`) + 1 BLOCKING checkpoint gating phase advancement.
</objective>

<execution_context>
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-ai-chat/08-CONTEXT.md
@.planning/phases/08-ai-chat/08-VERIFICATION.md
@.planning/phases/08-ai-chat/08-REVIEW.md
@.planning/phases/08-ai-chat/08-UI-SPEC.md
@.planning/phases/08-ai-chat/08-07-PLAN.md
@.planning/phases/08-ai-chat/08-08-PLAN.md
@.planning/phases/08-ai-chat/08-09-PLAN.md
@.planning/phases/05-ai-insights/05-HUMAN-UAT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write 08-HUMAN-UAT.md checklist file</name>
  <files>.planning/phases/08-ai-chat/08-HUMAN-UAT.md</files>
  <read_first>
    - .planning/phases/05-ai-insights/05-HUMAN-UAT.md (reference structure — frontmatter shape, Tests section ordering, Summary block with total/passed/issues/pending counts)
    - .planning/phases/08-ai-chat/08-VERIFICATION.md (frontmatter `human_verification:` array — all 11 items verbatim, lines 77-110)
    - .planning/phases/08-ai-chat/08-UI-SPEC.md (referenced for accessibility and mobile breakpoint expectations the operator will check against)
    - .planning/phases/08-ai-chat/08-CONTEXT.md (D-14, D-17, D-36 — the locked decisions the UAT confirms in real running code)
    - .planning/phases/08-ai-chat/08-07-PLAN.md, .planning/phases/08-ai-chat/08-08-PLAN.md, .planning/phases/08-ai-chat/08-09-PLAN.md (pre-condition references — operator confirms their SUMMARY.md files exist before starting)
  </read_first>
  <action>
    Create .planning/phases/08-ai-chat/08-HUMAN-UAT.md with the following structure (modeled after .planning/phases/05-ai-insights/05-HUMAN-UAT.md):

    Frontmatter (between `---` lines) MUST include: `status: pending`, `phase: 08-ai-chat`, `source: [08-VERIFICATION.md, 08-REVIEW.md]`, `started: <fill on execution>`, `updated: <fill on execution>`.

    Body sections in order:

    `## Pre-conditions` — listing the three plans whose SUMMARY.md files MUST exist before UAT starts:
    - Plan 08-07 SUMMARY.md exists (CR-01 cross-user authorization fix shipped — required for items 4, 5, 11 below)
    - Plan 08-08 SUMMARY.md exists (CR-02 + CR-03 + CR-05 + WR-10 shipped — required for items 1, 7 below)
    - Plan 08-09 SUMMARY.md exists (CR-04 + CR-06 shipped — required for items 4, 5, 6 below)
    Operator MUST verify all three exist before proceeding. If any is missing, the UAT is blocked.

    `## Setup` section listing operational requirements:
    - Backend running locally with all migrations applied (`alembic current` shows 009)
    - Frontend running locally on http://localhost:3000
    - Valid ANTHROPIC_API_KEY set in backend .env (needed for items 4-7, 11)
    - Seeded Sofa Belle test data (1219 leads, 71 contracts — confirm with a SELECT COUNT against v_mefi_leads_active)
    - Two distinct authenticated user sessions available (for item 4 cross-user re-test — confirms CR-01 fix end-to-end). If multi-user authn UI is not available locally, use direct DB seed of a second user_id and curl with manually-crafted JWT cookies.

    `## Tests` section listing all 11 items from 08-VERIFICATION.md `human_verification:` as numbered subsections. For each item use this template:

    ```
    ### N. <Short title>

    **Source:** 08-VERIFICATION.md human_verification[N-1]
    **Gates:** <which SC# / CR-* / D-XX / CHAT-XX this gate>
    **Test:** <verbatim from VERIFICATION.md `test:` field>
    **Expected:** <verbatim from VERIFICATION.md `expected:` field>
    **Result:** [to be filled by operator: PASS / FAIL / partial — include observed behavior]
    ```

    The 11 items (mapped from 08-VERIFICATION.md lines 77-110):
    1. Mobile 360px DevTools (Pixel 8): hamburger Sheet, touch targets ≥ 44px, first token < 2s (Gates: D-36 / CHAT-09 / SC#6)
    2. Tablet 768px: ConversationsList 260px panel; ChatMain flex-1; bubbles max-w-[75%] (Gates: D-36)
    3. Desktop 1280px: MessageList max-w-3xl mx-auto (Gates: UI-SPEC readability contract)
    4. Live "Cum stăm comparativ cu săptămâna trecută?" — Romanian + compare_periods + non-zero WoW delta + chat_tool_calls row + NOT fallback string (Gates: CHAT-01 / SC#1 — depends on 08-09 CR-04 fix)
    5. Live "Care e CAC-ul Meta?" — honest "Nu am acces" refusal, NO fake CAC number (Gates: CHAT-05 / SC#3)
    6. Live "Care e cel mai bun vânzător?" — real Sofa Belle salesperson name (one of Roibu Valeria, Raileanu Leon, Godja Adina Maria, Dragoi Mihaela, Zagrian Emilia, Moaca Andreea), NOT "Maria Popescu" or invented; chat_tool_calls log shows get_salesperson_performance was invoked (Gates: CHAT-10 / SC#4)
    7. After first turn, sidebar title updates from null/truncated to 4-6 Romanian words within ~5s; chat_conversations.title in DB updated (Gates: D-14 — depends on 08-08 CR-03 fix)
    8. Inline dashboard link: "Unde văd lead-urile blocate?" → response includes [Sales Dashboard](/sales) rendered as DashboardLinkPill with bg-accent/10 + ChevronRight; click navigates (Gates: CHAT-06)
    9. Touch targets ≥ 44px (Send button, ConversationItem, suggested-question chip) verified via DevTools mobile computed-style inspector (Gates: D-36 NON-NEGOTIABLE)
    10. Keyboard-only navigation + screen-reader announces "Răspuns asistent AI" per UI-SPEC § Accessibility (Gates: UI-SPEC a11y contract)
    11. Conversation persistence across browser sessions: send message, close browser, reopen, navigate to /chat, click conversation → prior messages and follow-up context visible AS THE SAME USER; switch to a different user and confirm prior user's conversation is NOT visible (Gates: CHAT-03 / SC#5 — depends on 08-07 CR-01 fix)

    `## Summary` section (operator fills after UAT) — keys: total: 11, passed, failed, partial, blocked.

    `## Gaps` section: "[to be filled by operator with any FAIL items — these become a new gap-closure cycle via /gsd:plan-phase 08 --gaps]"

    `## Operator Sign-off`: Operator name, Date (YYYY-MM-DD), Verdict (`approved` or `gaps-filed`). If verdict is `approved` → Phase 8 advances to SUMMARY-ready. If verdict is `gaps-filed` → a new gap-closure cycle is initiated against the FAIL items listed in §Gaps.

    Use 11 numbered headings rather than a single table for readability and easy fill-in. The format should be identical in feel to 05-HUMAN-UAT.md — frontmatter at top, narrative Tests, Summary at the bottom.

    Do NOT inline verbatim Romanian text or extensive code excerpts — reference the underlying VERIFICATION.md lines so the source-of-truth remains canonical and the UAT file stays compact.
  </action>
  <verify>
    File exists at the absolute path; frontmatter contains `status: pending`; exactly 11 numbered `### N.` test subsections; each subsection has `**Source:**`, `**Gates:**`, `**Test:**`, `**Expected:**`, `**Result:**` lines; Summary section present with `total: 11` and operator-fill slots; Operator Sign-off section present.

    Shell checks:
    - `test -f .planning/phases/08-ai-chat/08-HUMAN-UAT.md` exits 0
    - `grep -cE "^### [0-9]+\." .planning/phases/08-ai-chat/08-HUMAN-UAT.md` returns 11
    - `grep -cE "^\*\*Gates:\*\*" .planning/phases/08-ai-chat/08-HUMAN-UAT.md` returns 11
    - `grep -cE "^\*\*Expected:\*\*" .planning/phases/08-ai-chat/08-HUMAN-UAT.md` returns 11
    - `grep -cE "^\*\*Result:\*\*" .planning/phases/08-ai-chat/08-HUMAN-UAT.md` returns 11
    - `grep -E "^status: pending" .planning/phases/08-ai-chat/08-HUMAN-UAT.md` returns 1 match
  </verify>
  <done>
    08-HUMAN-UAT.md file created at the canonical path; 11 numbered tests with the five required fields each; Pre-conditions, Setup, Summary, Gaps, Operator Sign-off sections present; structure mirrors 05-HUMAN-UAT.md.
  </done>
  <acceptance_criteria>
    - Source: `.planning/phases/08-ai-chat/08-HUMAN-UAT.md` exists
    - Source: frontmatter contains `status: pending`, `phase: 08-ai-chat`, `source: [08-VERIFICATION.md, 08-REVIEW.md]`
    - Source: exactly 11 `### N.` subsections under `## Tests`
    - Source: each test subsection has `**Source:**`, `**Gates:**`, `**Test:**`, `**Expected:**`, `**Result:**` fields
    - Source: `## Pre-conditions`, `## Setup`, `## Summary`, `## Gaps`, `## Operator Sign-off` sections all present
    - CLI: `grep -cE "^### [0-9]+\." .planning/phases/08-ai-chat/08-HUMAN-UAT.md` returns 11
    - CLI: `grep -cE "08-07-SUMMARY\.md|08-08-SUMMARY\.md|08-09-SUMMARY\.md" .planning/phases/08-ai-chat/08-HUMAN-UAT.md` returns ≥ 3 (each summary referenced in Pre-conditions)
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: BLOCKING human-verify — operator runs 08-HUMAN-UAT.md against fixed code</name>
  <what-built>
    All Phase 8 gap-closure fixes are now shipped:
    - 08-07: CR-01 — ConversationRepository takes user_id; every chat endpoint filters by (tenant_id, user_id); send_message verifies ownership BEFORE rate-limit/stream-lock; cross-user integration tests pass
    - 08-08: CR-02 (stream-lock acquired BEFORE rate-limit INCR; 429 releases lock), CR-03 (detached title task owns its own AsyncSessionLocal session — no captured orchestrator session), CR-05 (three explicit commit boundaries in run_turn), WR-10 (structured warning replaces bare except)
    - 08-09: CR-04 (Romanian sentence-starter stopwords skip false-positive entity matches), CR-06 (static dict replaces f-string column interpolation in get_loss_reasons)
    - 08-10 Task 1: 08-HUMAN-UAT.md file written with 11 numbered tests

    Phase 8 cannot advance to SUMMARY until the operator runs all 11 items in 08-HUMAN-UAT.md and signs off.
  </what-built>
  <how-to-verify>
    Run the full 08-HUMAN-UAT.md sequence:

    1. **Verify pre-conditions:** open `.planning/phases/08-ai-chat/08-07-SUMMARY.md`, `.planning/phases/08-ai-chat/08-08-SUMMARY.md`, and `.planning/phases/08-ai-chat/08-09-SUMMARY.md` — all three must exist and be marked complete. If any is missing, this checkpoint is blocked and you must complete the upstream plan first.

    2. **Run Setup:** boot backend (`docker compose up -d` + `cd backend && uvicorn app.main:app --reload --port 8000`), boot frontend (`cd frontend && pnpm dev`), confirm ANTHROPIC_API_KEY env present, confirm seeded Sofa Belle data via `docker compose exec postgres psql -U postgres -d sofabelle -c "SELECT COUNT(*) FROM v_mefi_leads_active"` returns ≥ 1000.

    3. **Execute all 11 tests** in the order listed in 08-HUMAN-UAT.md. For each test:
       - Read the **Test:** and **Expected:** fields
       - Perform the action (DevTools mobile emulation, live Anthropic conversation, etc.)
       - Fill the **Result:** field with PASS / FAIL / partial + observed behavior
       - For tests 4-7 and 11, capture the chat_tool_calls SQL state via `psql -c "SELECT tool_name, output_data->'tool_use_id' FROM chat_tool_calls ORDER BY created_at DESC LIMIT 10"` to confirm tool dispatch

    4. **Fill Summary block** (total: 11, passed, failed, partial, blocked).

    5. **Fill Gaps section** — if any test FAILED or was partial, list the specific gap; if all PASS, write "None".

    6. **Sign off:** fill Operator name, Date, Verdict:
       - `approved` if all 11 PASS (or if any partial/blocked items are explicitly waived with a written rationale)
       - `gaps-filed` if one or more tests failed AND the failures are not waived — this triggers a new gap-closure cycle via `/gsd:plan-phase 08 --gaps`

    7. **Commit** the filled 08-HUMAN-UAT.md to git with message: `docs(08-10): complete Phase 8 HUMAN-UAT — <approved|gaps-filed>`

    Return to the executor either:
    - "approved" — Phase 8 advances to SUMMARY-ready
    - A description of the gaps you filed and the trigger command (`/gsd:plan-phase 08 --gaps`)
  </how-to-verify>
  <resume-signal>Type "approved" if all 11 PASS (or partial items explicitly waived in writing); otherwise describe the failures and run `/gsd:plan-phase 08 --gaps` to initiate a follow-up cycle.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automated test suite → human operator's UAT verdict | Code fixes in 08-07/08-08/08-09 are tested by pytest, but the user-visible behavior (mobile layout, Romanian quality, title appearing in sidebar, a11y) requires human verification. This plan's boundary is between "tests green" and "real users would accept this". |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Information Disclosure (UX-level — fallback string shown when CR-04 false-positive fires) | live Romanian conversations (UAT items 4, 6) | mitigate | Items 4 and 6 directly verify the CR-04 fix in real-network real-Anthropic conditions. PASS = guard correctly accepts grounded responses; FAIL = a new gap is filed against the residual false-positive pattern. |
| T-08-06 | Denial of Service (latency under real network) | UAT item 1 (first token < 2s on mobile 360px) | mitigate | Item 1 measures first-token TTL in DevTools mobile-emulation network throttle. PASS = CHAT-09 SC#6 confirmed end-to-end. |
| T-08-01 | Information Disclosure (cross-user persistence) | UAT item 11 (browser-session persistence + multi-user) | mitigate | Item 11 verifies CR-01 fix end-to-end with two distinct user sessions. Confirms CHAT-03 SC#5 promise of "history stored per user". |
| T-08-D14 | Tampering (D-14 title contract — wrong title persisted or no title at all) | UAT item 7 (title appears within ~5s) | mitigate | Item 7 verifies the CR-03 fix: title is written by the detached task's own session AFTER the request closes. PASS = D-14 contract delivered end-to-end. |
| T-08-A11Y | Information Disclosure / Usability (a11y) | UAT items 9, 10 | accept (low risk single-user pilot) | Items 9 (touch targets) and 10 (keyboard + SR) are NON-NEGOTIABLE per Phase 7 D-17 carry-forward (D-36). Operator may waive item 10 with written rationale if assistive-tech testing is not available at the time of UAT (Sofa Belle pilot is non-blind owner) — item 9 cannot be waived. |
</threat_model>

<verification>
**Sanity checks (run after Task 1 completes; Task 2 is the operator-driven gate):**

```bash
# File exists with expected structure
test -f .planning/phases/08-ai-chat/08-HUMAN-UAT.md
grep -cE "^### [0-9]+\." .planning/phases/08-ai-chat/08-HUMAN-UAT.md        # Expect 11
grep -cE "^\*\*Gates:\*\*" .planning/phases/08-ai-chat/08-HUMAN-UAT.md      # Expect 11
grep -cE "^\*\*Expected:\*\*" .planning/phases/08-ai-chat/08-HUMAN-UAT.md   # Expect 11
grep -cE "^\*\*Result:\*\*" .planning/phases/08-ai-chat/08-HUMAN-UAT.md     # Expect 11
grep -E "^status: pending" .planning/phases/08-ai-chat/08-HUMAN-UAT.md       # Expect 1 match
grep -cE "08-07-SUMMARY\.md|08-08-SUMMARY\.md|08-09-SUMMARY\.md" .planning/phases/08-ai-chat/08-HUMAN-UAT.md   # Expect ≥ 3
grep -cE "^## (Pre-conditions|Setup|Tests|Summary|Gaps|Operator Sign-off)" .planning/phases/08-ai-chat/08-HUMAN-UAT.md   # Expect ≥ 6
```
</verification>

<success_criteria>
1. 08-HUMAN-UAT.md created with the 11-item checklist + Pre-conditions + Setup + Summary + Gaps + Operator Sign-off sections; mirrors 05-HUMAN-UAT.md shape.
2. Each test references the underlying CR-* / D-XX / SC# / CHAT-XX it gates.
3. BLOCKING checkpoint task 2 gates phase advancement on the operator's verdict.
4. Pre-conditions explicitly cite plans 08-07, 08-08, 08-09 SUMMARYs as prerequisites.
5. After operator returns `approved`, Phase 8 is eligible for final SUMMARY.md write and STATE.md update (status: complete).
6. After operator returns `gaps-filed`, the next `/gsd:plan-phase 08 --gaps` cycle begins against the listed failures.
7. Conventional Commits:
   - `docs(08-10): add 08-HUMAN-UAT.md checklist for deferred 08-06 Task 4`
   - `docs(08-10): operator sign-off on 08-HUMAN-UAT — <approved|gaps-filed>` (post-checkpoint)
</success_criteria>

<output>
Create `.planning/phases/08-ai-chat/08-10-SUMMARY.md` after the operator signs off. The summary MUST:
- Record the operator's verdict (`approved` or `gaps-filed`)
- Cite the filled `## Summary` block from 08-HUMAN-UAT.md (counts of passed / failed / partial / blocked)
- If `approved`: declare Phase 8 SUMMARY-ready and instruct STATE.md update to mark Phase 8 status complete
- If `gaps-filed`: list the failed items as new gaps and instruct invocation of `/gsd:plan-phase 08 --gaps` for a follow-up cycle
- Confirm all 6 BLOCKER (CR-01..CR-06) gaps from 08-VERIFICATION.md are now closed in production behavior (not just unit tests)
</output>
