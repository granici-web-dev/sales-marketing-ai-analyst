---
phase: 01-foundation
plan: 07
type: execute
wave: 2
depends_on:
  - 01-02
files_modified:
  - frontend/package.json
  - frontend/next.config.ts
  - frontend/components.json
  - frontend/tsconfig.json
  - frontend/proxy.ts
  - frontend/messages/ro.json
  - frontend/messages/en.json
  - frontend/src/i18n/routing.ts
  - frontend/src/i18n/request.ts
  - frontend/src/app/globals.css
  - frontend/src/app/layout.tsx
  - frontend/src/app/(auth)/login/page.tsx
  - frontend/src/app/(dashboard)/layout.tsx
  - frontend/src/app/(dashboard)/page.tsx
  - frontend/src/app/(dashboard)/marketing/page.tsx
  - frontend/src/app/(dashboard)/sales/page.tsx
  - frontend/src/app/(dashboard)/salespeople/page.tsx
  - frontend/src/app/(dashboard)/insights/page.tsx
  - frontend/src/app/(dashboard)/chat/page.tsx
  - frontend/src/app/(dashboard)/integrations/page.tsx
  - frontend/src/app/(dashboard)/settings/page.tsx
  - frontend/src/lib/api-client.ts
  - frontend/src/components/sidebar.tsx
  - frontend/src/components/topbar.tsx
autonomous: true
requirements:
  - UI-01
  - AUTH-03

must_haves:
  truths:
    - "proxy.ts (NOT middleware.ts) exports function named 'proxy' — Next.js 16 convention"
    - "Unauthenticated GET to /dashboard redirects to /login via proxy.ts jose.jwtVerify"
    - "Sidebar shows all 8 nav items in canonical SPEC §13.1 order with lucide-react icons"
    - "Login page renders with Romanian strings from messages/ro.json"
    - "next-intl v4 NextIntlClientProvider wraps root layout"
    - "getRequestConfig returns {locale, messages} — locale is mandatory in next-intl v4"
    - "localePrefix: 'never' — URLs are /sales not /ro/sales"
    - "api-client.ts has 401 interceptor that calls POST /api/v1/auth/refresh before retrying"
    - "All 8 dashboard placeholder routes render 'În curând' heading"
  artifacts:
    - path: "frontend/proxy.ts"
      provides: "Auth + i18n routing (Next.js 16 convention)"
      exports: ["proxy", "config"]
    - path: "frontend/src/i18n/routing.ts"
      provides: "next-intl routing config"
      exports: ["routing"]
    - path: "frontend/src/i18n/request.ts"
      provides: "getRequestConfig returning {locale, messages}"
    - path: "frontend/src/app/layout.tsx"
      provides: "Root layout with NextIntlClientProvider"
    - path: "frontend/src/app/(dashboard)/layout.tsx"
      provides: "Dashboard shell with sidebar + topbar"
    - path: "frontend/src/app/(auth)/login/page.tsx"
      provides: "Login form with zod + react-hook-form"
    - path: "frontend/src/components/sidebar.tsx"
      provides: "Fixed 240px sidebar with 8 nav items"
    - path: "frontend/messages/ro.json"
      provides: "Romanian translation strings (38 keys from UI-SPEC)"
    - path: "frontend/src/lib/api-client.ts"
      provides: "Fetch wrapper with 401 interceptor for token refresh"
  key_links:
    - from: "frontend/proxy.ts"
      to: "frontend/src/i18n/routing.ts"
      via: "import { routing } from './src/i18n/routing'"
      pattern: "from.*i18n/routing"
    - from: "frontend/src/app/layout.tsx"
      to: "next-intl"
      via: "NextIntlClientProvider"
      pattern: "NextIntlClientProvider"
    - from: "frontend/src/i18n/request.ts"
      to: "frontend/messages/ro.json"
      via: "import(`../../messages/${locale}.json`)"
      pattern: "messages.*locale.*json"
    - from: "frontend/src/app/(dashboard)/layout.tsx"
      to: "frontend/src/components/sidebar.tsx"
      via: "import Sidebar"
      pattern: "from.*components/sidebar"
---

<objective>
Scaffold the complete Next.js 16 frontend: project creation, shadcn/ui initialization, next-intl v4 with Romanian default locale, proxy.ts authentication guard, sidebar shell with all 8 nav items, login page with Romanian copy, and 8 placeholder dashboard routes. This satisfies UI-01 (Romanian i18n from day 1), AUTH-03 (unauthenticated redirect to login), and D-11/D-12/D-13.

Purpose: After this plan, a developer can open http://localhost:3000, be redirected to /login (if no token), log in, and see the full sidebar shell with "În curând" placeholder pages for all 8 nav items.

Output: Complete frontend directory (~24 files). The frontend does NOT fetch real data in Phase 1 — all pages show static "În curând" placeholders.
</objective>

<execution_context>
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@CLAUDE.md

<interfaces>
<!-- Key patterns from RESEARCH.md that must be reproduced exactly -->

proxy.ts (Next.js 16 — from RESEARCH.md Pattern 6):
  - Filename: proxy.ts (NOT middleware.ts)
  - Export: export async function proxy(request: NextRequest)
  - Runtime: nodejs (NOT edge)
  - JWT verify: jose.jwtVerify(token, new TextEncoder().encode(process.env.JWT_SECRET_KEY))
  - Reads: request.cookies.get("access_token")?.value
  - Public paths: ["/login", "/api/v1/auth"]
  - Falls through to handleI18nRouting(request) on auth success

next-intl v4 (from RESEARCH.md Pattern 6):
  routing.ts: defineRouting({ locales: ["ro", "en"], defaultLocale: "ro", localePrefix: "never" })
  request.ts: getRequestConfig returns { locale, messages } — locale MANDATORY (next-intl v4 breaking change)
  layout.tsx: NextIntlClientProvider required (next-intl v4 breaking change)

Translation keys (from 01-UI-SPEC.md Translation File Contract):
  All 38 keys defined in UI-SPEC must be in messages/ro.json and messages/en.json

UI-SPEC layout (from 01-UI-SPEC.md):
  Sidebar: 240px fixed, background #F4F4F6, 8 nav items, py-[12px] px-3, 44px touch target
  Active nav: hsl(221 83% 95%) background, #2563EB text/icon, 3px solid left indicator
  Topbar: 56px fixed, background #F4F4F6
  Main content: margin-left 240px, margin-top 56px, padding 32px

Login card (from 01-UI-SPEC.md):
  400px width, 32px padding, card on #F4F4F6 background
  Elements: wordmark → subtitle → email field → password field → submit button

Nav items canonical order (D-12, SPEC §13.1):
  1. LayoutDashboard → / → "Prezentare generală"
  2. TrendingUp → /marketing → "Marketing"
  3. Briefcase → /sales → "Vânzări"
  4. Users → /salespeople → "Agenți de vânzări"
  5. Lightbulb → /insights → "Analize AI"
  6. MessageSquare → /chat → "Chat AI"
  7. Plug → /integrations → "Integrări"
  8. Settings → /settings → "Setări"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Next.js 16 project, install dependencies, shadcn init, next-intl config, proxy.ts, and translation files</name>
  <files>
    frontend/package.json
    frontend/next.config.ts
    frontend/components.json
    frontend/tsconfig.json
    frontend/proxy.ts
    frontend/messages/ro.json
    frontend/messages/en.json
    frontend/src/i18n/routing.ts
    frontend/src/i18n/request.ts
    frontend/src/app/globals.css
    frontend/src/app/layout.tsx
    frontend/src/lib/api-client.ts
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md — Pattern 6: proxy.ts FULL code (exact: import createMiddleware, jwtVerify, routing; PUBLIC_PATHS; export async function proxy; export const config with matcher)
    - .planning/phases/01-foundation/01-RESEARCH.md — Pattern 6: next-intl three required pieces (routing.ts, request.ts, layout.tsx with NextIntlClientProvider)
    - .planning/phases/01-foundation/01-RESEARCH.md — Common Pitfalls #1 (proxy.ts rename), #2 (locale mandatory in getRequestConfig return)
    - .planning/phases/01-foundation/01-RESEARCH.md — State of the Art table (Next.js 16 breaking: params/searchParams are Promises now; next lint removed; serverRuntimeConfig removed)
    - .planning/phases/01-foundation/01-UI-SPEC.md — Translation File Contract (exact JSON structure for ro.json and en.json with all 38 keys)
    - .planning/phases/01-foundation/01-CONTEXT.md — D-13 (next-intl Romanian default; login page + sidebar labels have strings from day 1)
  </read_first>
  <action>
    The frontend directory does not exist yet. Execute these steps in order:

    Step 1 — Create Next.js 16 project:
    Run `pnpm dlx create-next-app@16.2.6 frontend --typescript --tailwind --app --no-src-dir=false --import-alias "@/*"` from the project root. This creates `frontend/` with Next.js 16.2.6 baseline. If this creates files at a different path, adjust accordingly.

    Step 2 — Install additional dependencies:
    ```
    cd frontend
    pnpm add next-intl@4.12.0 jose@6.2.3 @tanstack/react-query@5.100.11 zustand@5.0.13 zod@4.4.3 react-hook-form@7.76.0
    ```

    Step 3 — Initialize shadcn:
    ```
    cd frontend
    pnpm dlx shadcn@latest init
    ```
    When prompted: select New York style, CSS variables enabled, Tailwind v4. After init, `components.json` exists. Then install required Phase 1 components:
    ```
    pnpm dlx shadcn@latest add button input label separator
    ```

    Step 4 — Create/update `frontend/next.config.ts`:
    - `import type { NextConfig } from "next"`
    - `import createNextIntlPlugin from "next-intl/plugin"`
    - `const withNextIntl = createNextIntlPlugin("./src/i18n/request.ts")`
    - `const config: NextConfig = { turbopack: {} }` (top-level, not experimental.turbopack — Next.js 16 change)
    - `export default withNextIntl(config)`

    Step 5 — Create `frontend/src/i18n/routing.ts`:
    - `import { defineRouting } from "next-intl/routing"`
    - `export const routing = defineRouting({ locales: ["ro", "en"] as const, defaultLocale: "ro", localePrefix: "never" })`

    Step 6 — Create `frontend/src/i18n/request.ts`:
    - `import { getRequestConfig } from "next-intl/server"`
    - `import { routing } from "./routing"`
    - Export default `getRequestConfig(async ({ requestLocale }) => { let locale = await requestLocale; if (!locale || !routing.locales.includes(locale as any)) { locale = routing.defaultLocale; } return { locale, messages: (await import(\`../../messages/\${locale}.json\`)).default } })`
    - NOTE: `locale` MUST be returned in the object — this is a next-intl v4 breaking change (Common Pitfall #2)

    Step 7 — Create `frontend/proxy.ts` (NOT middleware.ts — this is the Next.js 16 rename):
    Implement exactly from RESEARCH.md Pattern 6:
    - `import createMiddleware from "next-intl/middleware"`
    - `import { type NextRequest, NextResponse } from "next/server"`
    - `import { jwtVerify } from "jose"`
    - `import { routing } from "./src/i18n/routing"`
    - `const handleI18nRouting = createMiddleware(routing)`
    - `const PUBLIC_PATHS = ["/login", "/api/v1/auth", "/api"]`
    - `export async function proxy(request: NextRequest)` — reads `access_token` cookie, jwtVerify with `new TextEncoder().encode(process.env.JWT_SECRET_KEY!)`, redirects to /login if invalid/missing, passes through via `handleI18nRouting(request)` if valid
    - `export const config = { matcher: ["/((?!_next/static|_next/image|favicon.ico).*)"] }`
    - Runtime: `export const runtime = "nodejs"` — DO NOT set to "edge"

    Step 8 — Create `frontend/messages/ro.json` and `frontend/messages/en.json`:
    Use the EXACT JSON from .planning/phases/01-foundation/01-UI-SPEC.md Translation File Contract section (38 keys across auth, nav, placeholder, locale namespaces). Copy verbatim.

    Step 9 — Update `frontend/src/app/globals.css`:
    Add shadcn CSS variables for the color palette from UI-SPEC: `--background: 0 0% 100%`, `--foreground: 240 10% 4%`, `--secondary: 240 5% 96%`, `--accent: 221 83% 53%`, `--border: 240 6% 90%`, `--muted-foreground: 240 4% 46%`. These are the tokens referenced in sidebar.tsx and login page.

    Step 10 — Create `frontend/src/app/layout.tsx` (root layout — NextIntlClientProvider required):
    - `import { NextIntlClientProvider } from "next-intl"` — REQUIRED in next-intl v4
    - `import { Inter } from "next/font/google"`
    - `const inter = Inter({ subsets: ["latin"] })`
    - Root layout wraps children with `<NextIntlClientProvider>` and `<html lang="ro"><body className={inter.className}>`

    Step 11 — Create `frontend/src/lib/api-client.ts`:
    - Fetch wrapper function `apiFetch(url: string, options?: RequestInit)` — calls native `fetch`, if response.status === 401, calls `POST /api/v1/auth/refresh` (refresh_token cookie sent automatically by browser), retries the original request once. On second 401, clears access_token cookie and calls `window.location.href = "/login"`.
    - `export const apiClient = { get: (url: string) => apiFetch(url), post: (url: string, body: unknown) => apiFetch(url, {method: "POST", headers: {"Content-Type": "application/json"}, body: JSON.stringify(body)}) }`
    - This implements D-02 (interceptor-based refresh — transparent to UI)
  </action>
  <verify>
    <automated>cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/frontend && grep -c "export async function proxy" proxy.ts && grep -c "localePrefix.*never" src/i18n/routing.ts && grep -c "NextIntlClientProvider" src/app/layout.tsx && grep -c "Prezentare" messages/ro.json</automated>
  </verify>
  <acceptance_criteria>
    - frontend/proxy.ts exists (NOT middleware.ts) and contains `export async function proxy`
    - frontend/proxy.ts contains `jwtVerify` (jose import)
    - frontend/proxy.ts does NOT contain `export const runtime = "edge"` (must be nodejs)
    - frontend/proxy.ts contains `access_token` cookie read
    - frontend/src/i18n/routing.ts contains `localePrefix: "never"`
    - frontend/src/i18n/request.ts contains `locale` in the return object (not just `messages`)
    - frontend/src/app/layout.tsx contains `NextIntlClientProvider`
    - frontend/messages/ro.json contains `"Prezentare generală"` (nav.overview key)
    - frontend/messages/ro.json contains `"Intră în cont"` (auth.loginButton key)
    - frontend/messages/ro.json contains `"În curând"` (placeholder.comingSoon key)
    - frontend/messages/en.json exists and is valid JSON
    - frontend/src/lib/api-client.ts contains `401` and `/api/v1/auth/refresh`
    - `grep -c "middleware.ts" frontend/` returns 0 (old filename must NOT exist)
  </acceptance_criteria>
  <done>Next.js 16 project created, shadcn initialized (New York style), next-intl v4 configured (ro default, localePrefix:never), proxy.ts with jwtVerify auth guard, ro.json/en.json with all 38 translation keys, api-client.ts with 401 interceptor</done>
</task>

<task type="auto">
  <name>Task 2: Sidebar, topbar, login page, dashboard shell layout, and 8 placeholder pages</name>
  <files>
    frontend/src/components/sidebar.tsx
    frontend/src/components/topbar.tsx
    frontend/src/app/(auth)/login/page.tsx
    frontend/src/app/(dashboard)/layout.tsx
    frontend/src/app/(dashboard)/page.tsx
    frontend/src/app/(dashboard)/marketing/page.tsx
    frontend/src/app/(dashboard)/sales/page.tsx
    frontend/src/app/(dashboard)/salespeople/page.tsx
    frontend/src/app/(dashboard)/insights/page.tsx
    frontend/src/app/(dashboard)/chat/page.tsx
    frontend/src/app/(dashboard)/integrations/page.tsx
    frontend/src/app/(dashboard)/settings/page.tsx
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-UI-SPEC.md — Layout Contract (exact sidebar: 240px width, py-[12px] px-3, 44px height, active state hsl(221 83% 95%), 3px solid left border indicator)
    - .planning/phases/01-foundation/01-UI-SPEC.md — Component Contracts: Login Page (400px card, 32px padding, elements order, zod validation, error flows)
    - .planning/phases/01-foundation/01-UI-SPEC.md — Nav Item Order (canonical 8 items with lucide icons and routes from D-12)
    - .planning/phases/01-foundation/01-UI-SPEC.md — Copywriting Contract (placeholder page subtitles — exact Romanian strings per page)
    - .planning/phases/01-foundation/01-UI-SPEC.md — Interaction Contracts (auth flow: store access_token in non-HttpOnly cookie after JSON response; navigate to / on success)
    - .planning/phases/01-foundation/01-CONTEXT.md — D-11 (full shell layout in Phase 1), D-12 (all 8 nav items), D-13 (Romanian strings)
  </read_first>
  <action>
    Create `frontend/src/components/sidebar.tsx`:
    - `"use client"`
    - `import { usePathname } from "next/navigation"`
    - `import { useTranslations } from "next-intl"`
    - `import Link from "next/link"`
    - Import these exact lucide-react icons: `LayoutDashboard, TrendingUp, Briefcase, Users, Lightbulb, MessageSquare, Plug, Settings`
    - Nav items array with 8 entries — exact order per D-12 and UI-SPEC Nav Item Order:
      `[{href: "/", icon: LayoutDashboard, labelKey: "overview"}, {href: "/marketing", icon: TrendingUp, labelKey: "marketing"}, ...]`
    - Component renders fixed 240px sidebar: `<aside className="fixed left-0 top-0 h-screen w-[240px] bg-[hsl(240_5%_96%)] border-r border-[hsl(240_6%_90%)] flex flex-col">`
    - Logo area: `<div className="h-14 flex items-center px-4 border-b border-[hsl(240_6%_90%)]"><span className="text-base font-semibold">Sofa Belle</span></div>`
    - Nav: `<nav className="flex-1 p-2 space-y-0.5">` — maps over items
    - Each nav item: `<Link href={item.href} className={...}>` — applies active state when `usePathname() === item.href` (or `pathname.startsWith(item.href)` for non-root)
    - Active item styles per UI-SPEC: `bg-[hsl(221_83%_95%)] text-[#2563EB]` + `border-l-[3px] border-[#2563EB]`; inactive: `text-[#71717A] hover:bg-[hsl(240_5%_92%)] hover:text-[#09090B]`
    - Each item: `<Icon size={16} /> <span className="text-sm">{t(item.labelKey)}</span>` with `gap-2`

    Create `frontend/src/components/topbar.tsx`:
    - `"use client"`
    - `import { useTranslations, useRouter } from "next-intl"`
    - `import { usePathname } from "next/navigation"`
    - `import { Button } from "@/components/ui/button"`
    - Map pathname to page title key from `nav` translations (e.g., "/" → "overview", "/marketing" → "marketing")
    - Fixed topbar: `<header className="fixed top-0 left-[240px] right-0 h-14 bg-[hsl(240_5%_96%)] border-b border-[hsl(240_6%_90%)] flex items-center justify-between px-6">`
    - Left: page title `<span className="text-xl font-semibold">{pageTitle}</span>`
    - Right: locale toggle `<Button variant="ghost" size="sm" onClick={...}>` showing "RO" or "EN"
    - Locale toggle calls `useRouter().push("/", {locale: currentLocale === "ro" ? "en" : "ro"})` via next-intl router

    Create `frontend/src/app/(auth)/login/page.tsx`:
    - `"use client"`
    - `import { useForm } from "react-hook-form"`
    - `import { zodResolver } from "@hookform/resolvers/zod"` (install: `pnpm add @hookform/resolvers`)
    - `import { z } from "zod"`
    - `import { useTranslations } from "next-intl"`
    - `import { Button } from "@/components/ui/button"`, `{ Input }`, `{ Label }`
    - `import { Loader2 } from "lucide-react"`
    - Zod schema: `z.object({ email: z.string().email(t("auth.emailInvalid")), password: z.string().min(1, t("auth.fieldRequired")) })`
    - onSubmit: calls `POST /api/v1/auth/login` via `fetch("/api/v1/auth/login", {method: "POST", body: JSON.stringify(data), headers: {"Content-Type": "application/json"}})`. On success: `document.cookie = \`access_token=\${result.access_token}; path=/; SameSite=Lax\`` (non-HttpOnly — proxy.ts reads this). Navigates to "/". On 401: sets `authError` state to `t("auth.invalidCredentials")`.
    - Layout: `<div className="min-h-screen flex items-center justify-center bg-white"><div className="w-[400px] p-8 bg-[hsl(240_5%_96%)] rounded-xl border border-[hsl(240_6%_90%)]">`
    - Elements per UI-SPEC: wordmark (28px semibold) → subtitle (14px muted) → email field → password field → submit button (full width, Accent bg)
    - Error banner below form: `<div className="border-l-4 border-[#DC2626] bg-[#FEF2F2] px-4 py-3 text-[#DC2626] text-xs">{authError}</div>`
    - Submit button loading state: show `<Loader2 size={16} className="animate-spin mr-2" />` when `isSubmitting`

    Create `frontend/src/app/(dashboard)/layout.tsx`:
    - Server component (no "use client")
    - Imports `Sidebar` from `@/components/sidebar` and `Topbar` from `@/components/topbar`
    - Returns: `<div className="min-h-screen"><Sidebar /><Topbar /><main className="ml-[240px] mt-14 p-8 bg-white min-h-[calc(100vh-56px)]">{children}</main></div>`

    Create 8 placeholder pages — each at their respective path. All identical structure, differing only in icon and subtitle:

    `frontend/src/app/(dashboard)/page.tsx` (Overview):
    - `"use client"` (to use useTranslations)
    - `import { useTranslations } from "next-intl"`
    - `import { LayoutDashboard } from "lucide-react"`
    - Renders: `<div className="flex flex-col items-center justify-center min-h-[calc(100vh-56px-64px)]"><LayoutDashboard size={48} className="text-[#71717A] mb-4" /><h1 className="text-xl font-semibold mb-2">{t("placeholder.comingSoon")}</h1><p className="text-sm text-[#71717A] max-w-sm text-center">{t("placeholder.overview")}</p></div>`

    Remaining 7 pages follow the same pattern with their respective icon and subtitle key:
    - marketing/page.tsx → TrendingUp icon, `placeholder.marketing`
    - sales/page.tsx → Briefcase icon, `placeholder.sales`
    - salespeople/page.tsx → Users icon, `placeholder.salespeople`
    - insights/page.tsx → Lightbulb icon, `placeholder.insights`
    - chat/page.tsx → MessageSquare icon, `placeholder.chat`
    - integrations/page.tsx → Plug icon, `placeholder.integrations`
    - settings/page.tsx → Settings icon, `placeholder.settings`
  </action>
  <verify>
    <automated>cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/frontend && grep -c "LayoutDashboard" src/components/sidebar.tsx && grep -c "În curând\|comingSoon" src/app/\(dashboard\)/page.tsx && grep -c "w-\[240px\]" src/components/sidebar.tsx && ls src/app/\(dashboard\)/</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/sidebar.tsx contains `w-[240px]`
    - frontend/src/components/sidebar.tsx contains all 8 nav routes (/, /marketing, /sales, /salespeople, /insights, /chat, /integrations, /settings)
    - frontend/src/components/sidebar.tsx imports `LayoutDashboard, TrendingUp, Briefcase, Users, Lightbulb, MessageSquare, Plug, Settings` from lucide-react
    - frontend/src/components/sidebar.tsx contains `py-[12px]` (touch target height per UI-SPEC)
    - frontend/src/app/(auth)/login/page.tsx contains `Loader2` spinner
    - frontend/src/app/(auth)/login/page.tsx contains `access_token` cookie set after successful login
    - frontend/src/app/(dashboard)/layout.tsx imports Sidebar and Topbar
    - frontend/src/app/(dashboard)/layout.tsx contains `ml-[240px]` and `mt-14`
    - All 8 placeholder pages exist: page.tsx, marketing/page.tsx, sales/page.tsx, salespeople/page.tsx, insights/page.tsx, chat/page.tsx, integrations/page.tsx, settings/page.tsx
    - Each placeholder page imports its respective lucide-react icon
    - `grep -c "@tremor" frontend/src/` returns 0 (no Tremor imports — UI-07 constraint)
  </acceptance_criteria>
  <done>Sidebar (240px fixed, 8 nav items, active state), topbar (page title + locale toggle), login page (zod validation, auth flow, loading state), dashboard shell layout (ml-240px + mt-56px), and 8 placeholder pages (each with icon + "În curând" + page-specific Romanian subtitle) all created</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → proxy.ts | All incoming requests pass through proxy.ts for auth check |
| access_token cookie → jose.jwtVerify | Token verified server-side before rendering any protected page |
| login form → POST /api/v1/auth/login | User credentials cross trust boundary; backend validates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Elevation of Privilege | CVE-2025-29927 Next.js middleware bypass | mitigate | Using Next.js 16.2.6 (patched version); proxy.ts function verifies JWT before passing through; backend independently validates on every API call |
| T-07-02 | Spoofing | access_token stored in non-HttpOnly cookie (JS readable) | accept | This is an intentional design choice (per RESEARCH.md Assumption A1) — access token is short-lived (15 min); refresh token (long-lived) is HttpOnly and cannot be read by JS; acceptable security tradeoff for proxy.ts server-side read |
| T-07-03 | Spoofing | proxy.ts using "edge" runtime | mitigate | proxy.ts does NOT use edge runtime (Pitfall #1); jose works in Node.js; runtime="nodejs" set explicitly |
| T-07-04 | Information Disclosure | PII in frontend logs | mitigate | No `console.log(password)` or similar in any frontend file; login error shows generic message "Email sau parolă incorectă" (not specific field which one was wrong) |
| T-07-05 | Spoofing | next-intl locale in URL manipulated | accept | `localePrefix: "never"` means no locale in URL to manipulate; locale from cookie/header only |
</threat_model>

<verification>
After both tasks complete:
1. `grep -c "export async function proxy" frontend/proxy.ts` returns 1
2. `grep -c "middleware.ts" frontend/` returns 0
3. `grep -c "NextIntlClientProvider" frontend/src/app/layout.tsx` returns 1
4. `grep -c "locale" frontend/src/i18n/request.ts` returns at least 2 (in parameter and in return object)
5. `ls frontend/src/app/(dashboard)/` lists: layout.tsx, page.tsx, marketing, sales, salespeople, insights, chat, integrations, settings
6. `grep -rn "@tremor" frontend/src/` returns empty (no Tremor imports)
7. `grep -c "w-\[240px\]" frontend/src/components/sidebar.tsx` returns 1
8. `grep -c "Prezentare generală" frontend/messages/ro.json` returns 1
</verification>

<success_criteria>
- proxy.ts (not middleware.ts) protects all routes except /login and /api/v1/auth
- Unauthenticated request to /dashboard redirects to /login (AUTH-03, SC#3)
- Login page renders with Romanian strings from ro.json (UI-01)
- Sidebar shows 8 nav items in canonical order with lucide icons (D-11, D-12)
- All 8 dashboard routes show "În curând" placeholder with page-specific Romanian subtitle (D-12)
- next-intl v4 wired correctly: NextIntlClientProvider in root layout, locale returned from getRequestConfig (D-13)
- api-client.ts intercepts 401 and refreshes token transparently (D-02)
- No @tremor/react imports anywhere in frontend
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-07-SUMMARY.md` when done
</output>
