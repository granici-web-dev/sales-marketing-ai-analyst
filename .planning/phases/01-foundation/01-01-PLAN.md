---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/__init__.py
  - backend/tests/unit/__init__.py
  - backend/tests/integration/__init__.py
  - backend/tests/conftest.py
  - backend/tests/unit/test_tenant_isolation.py
  - backend/tests/unit/test_models.py
  - backend/tests/unit/test_logging.py
  - backend/tests/unit/test_worker_init.py
  - backend/tests/integration/test_auth.py
  - backend/tests/integration/test_migrations.py
  - backend/tests/integration/test_celery.py
autonomous: true
requirements:
  - INFRA-02
  - INFRA-03
  - INFRA-04
  - INFRA-05
  - INFRA-06
  - AUTH-01
  - AUTH-02
  - PIPE-04

must_haves:
  truths:
    - "test_tenant_isolation.py exists and contains test_query_without_tenant_context_raises before any enforcement code"
    - "pytest tests/unit -x --collect-only lists all 4 unit test files with at least 1 test each"
    - "pytest tests/integration -x --collect-only lists all 3 integration test files with at least 1 test each"
    - "tests/conftest.py defines async_client, db_session, and test_tenant fixtures"
  artifacts:
    - path: "backend/tests/unit/test_tenant_isolation.py"
      provides: "D-06 failing test — proves enforcement seam is required"
      contains: "test_query_without_tenant_context_raises"
    - path: "backend/tests/conftest.py"
      provides: "Shared fixtures for all test suites"
      exports: ["async_client", "db_session", "test_tenant"]
    - path: "backend/tests/unit/test_models.py"
      provides: "INFRA-02 schema inspection"
      contains: "test_tenant_id_not_null"
    - path: "backend/tests/unit/test_logging.py"
      provides: "INFRA-06 structlog PII guard"
      contains: "test_no_pii_in_log_output"
    - path: "backend/tests/unit/test_worker_init.py"
      provides: "INFRA-05 fork-safety test"
      contains: "test_worker_process_init_disposes_engine"
    - path: "backend/tests/integration/test_auth.py"
      provides: "AUTH-01, AUTH-02 tests"
      contains: "test_login_success"
    - path: "backend/tests/integration/test_migrations.py"
      provides: "PIPE-04, SC#2 tests"
      contains: "test_tables_exist"
    - path: "backend/tests/integration/test_celery.py"
      provides: "INFRA-04, SC#4 tests"
      contains: "test_redbeat_alive"
  key_links:
    - from: "backend/tests/conftest.py"
      to: "backend/app/db/session.py"
      via: "AsyncSessionLocal import"
      pattern: "from app\\.db\\.session import"
    - from: "backend/tests/unit/test_tenant_isolation.py"
      to: "backend/app/core/exceptions.py"
      via: "TenantIsolationError import"
      pattern: "TenantIsolationError"
---

<objective>
Write all test stubs BEFORE any implementation code exists. This is the Wave 0 gate: tests define the contracts that implementation plans must satisfy. The D-06 failing test (test_query_without_tenant_context_raises) is a REQUIRED deliverable per CONTEXT.md and SC#6 — it must exist before the with_loader_criteria enforcement code is written.

Purpose: Nyquist compliance — no implementation task runs without a test that can catch regressions. Tests are written against expected interfaces, not existing code.

Output: 11 test files + conftest.py + __init__.py stubs. All files are skeletons with concrete test function signatures and import stubs. Tests will fail (ImportError or assertion failures) until implementation plans complete — this is expected.
</objective>

<execution_context>
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Test infrastructure — __init__.py files, conftest.py, and pyproject.toml test config</name>
  <files>
    backend/tests/__init__.py
    backend/tests/unit/__init__.py
    backend/tests/integration/__init__.py
    backend/tests/conftest.py
    backend/pyproject.toml
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md — Section "Validation Architecture" (pyproject.toml asyncio_mode=auto config, pytest-asyncio 1.2.0)
    - .planning/phases/01-foundation/01-RESEARCH.md — Pattern 1 (SQLAlchemy async session pattern, AsyncSessionLocal)
    - .planning/phases/01-foundation/01-RESEARCH.md — Pattern 5 (FastAPI auth endpoint signatures, TestClient usage)
  </read_first>
  <action>
    Create three empty `__init__.py` files at `backend/tests/__init__.py`, `backend/tests/unit/__init__.py`, and `backend/tests/integration/__init__.py`.

    Create `backend/pyproject.toml` with `[tool.pytest.ini_options]` section: `asyncio_mode = "auto"`, `testpaths = ["tests"]`, `python_files = ["test_*.py"]`. Also include `[project]` with `name = "sales-analyst-backend"`, `requires-python = ">=3.11"`. Include a `[project.optional-dependencies]` dev section listing: `pytest`, `pytest-asyncio==1.2.0`, `pytest-cov`, `httpx`, `factory-boy`, `freezegun`.

    Create `backend/tests/conftest.py` with:
    - `from __future__ import annotations` at top
    - Import stubs (wrapped in try/except ImportError to survive before implementation): `from app.main import app`, `from app.db.session import AsyncSessionLocal`, `from app.core.tenancy import set_tenant_id`
    - `TEST_TENANT_ID = UUID("00000000-0000-0000-0000-000000000001")` constant
    - `@pytest.fixture async def async_client()` — yields `AsyncClient(app=app, base_url="http://test")` from `httpx.AsyncClient` (wrap import in try/except; if app not available yet, yield None and skip)
    - `@pytest.fixture async def db_session()` — calls `set_tenant_id(TEST_TENANT_ID)`, yields `AsyncSessionLocal()` session, rollback on teardown
    - `@pytest.fixture def test_tenant()` — returns a dict `{"id": TEST_TENANT_ID, "name": "Sofa Belle"}` (no DB required — used by unit tests)
    - All fixtures use `from uuid import UUID`, `import pytest`
    - Use `from __future__ import annotations` in all files
  </action>
  <verify>
    <automated>cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend && python -c "import ast; ast.parse(open('tests/conftest.py').read()); print('conftest.py valid Python')"</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/__init__.py exists (empty file)
    - backend/tests/unit/__init__.py exists (empty file)
    - backend/tests/integration/__init__.py exists (empty file)
    - backend/tests/conftest.py contains `TEST_TENANT_ID = UUID("00000000-0000-0000-0000-000000000001")`
    - backend/tests/conftest.py contains `async def async_client`
    - backend/tests/conftest.py contains `async def db_session`
    - backend/tests/conftest.py contains `def test_tenant`
    - backend/pyproject.toml contains `asyncio_mode = "auto"`
    - backend/pyproject.toml contains `testpaths = ["tests"]`
    - `python -c "import ast; ast.parse(open('backend/tests/conftest.py').read())"` exits 0
  </acceptance_criteria>
  <done>3 __init__.py files exist, conftest.py parses as valid Python, pyproject.toml has asyncio_mode=auto</done>
</task>

<task type="auto">
  <name>Task 2: Unit test stubs — tenant isolation (D-06), models (INFRA-02), logging (INFRA-06), worker init (INFRA-05)</name>
  <files>
    backend/tests/unit/test_tenant_isolation.py
    backend/tests/unit/test_models.py
    backend/tests/unit/test_logging.py
    backend/tests/unit/test_worker_init.py
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md — Pattern 1 (exact D-06 failing test code: _tenant_id_var.set(None), TenantIsolationError, select(User))
    - .planning/phases/01-foundation/01-RESEARCH.md — Code Examples section (Base model: TenantScopedMixin, TimestampMixin, PG_UUID, TIMESTAMPTZ)
    - .planning/phases/01-foundation/01-RESEARCH.md — Pattern 7 (structlog configure_logging, merge_contextvars, bind_contextvars)
    - .planning/phases/01-foundation/01-RESEARCH.md — Pattern 4 (worker_process_init: engine.dispose() + recreate pattern)
    - .planning/phases/01-foundation/01-VALIDATION.md — Per-Task Verification Map (exact test function names per requirement)
    - .planning/phases/01-foundation/01-CONTEXT.md — D-06 (MANDATORY: failing test written before enforcement code)
  </read_first>
  <action>
    Create `backend/tests/unit/test_tenant_isolation.py` — this is the D-06 required failing test per CONTEXT.md and SC#6:
    - `from __future__ import annotations`
    - Import stubs: `from app.db.session import AsyncSessionLocal`, `from app.models.user import User`, `from app.core.exceptions import TenantIsolationError`, `from app.core.tenancy import _tenant_id_var`
    - `from sqlalchemy import select`
    - `import pytest`
    - `async def test_query_without_tenant_context_raises()` — sets `_tenant_id_var.set(None)`, opens `AsyncSessionLocal()`, executes `select(User)`, asserts `TenantIsolationError` is raised. Docstring: "SC#6: DB layer rejects queries when no tenant context is set (D-06 required deliverable)."
    - `async def test_query_with_tenant_context_succeeds()` — sets `_tenant_id_var.set(UUID("00000000-0000-0000-0000-000000000001"))`, opens session, executes select — should NOT raise. Mark with `pytest.mark.skip(reason="Requires live DB — run in integration suite")` since unit tests have no DB.

    Create `backend/tests/unit/test_models.py` — INFRA-02 schema inspection:
    - `from __future__ import annotations`
    - Import stubs: `from app.db.base import Base, TenantScopedMixin`, `from app.models.user import User`, `from app.models.pipeline import SyncRun, PipelineRun`, `from app.models.tenant import Tenant`
    - `from sqlalchemy.dialects.postgresql import TIMESTAMPTZ`
    - `def test_tenant_id_not_null()` — inspects `User.__table__.columns["tenant_id"]`, asserts `nullable=False`
    - `def test_created_at_is_timestamptz()` — inspects `User.__table__.columns["created_at"]`, asserts column type is `TIMESTAMPTZ`
    - `def test_pipeline_run_has_tenant_id()` — inspects `PipelineRun.__table__.columns`, asserts "tenant_id" in columns
    - `def test_sync_run_has_tenant_id()` — inspects `SyncRun.__table__.columns`, asserts "tenant_id" in columns
    - `def test_tenant_model_has_no_tenant_id()` — asserts `"tenant_id" not in Tenant.__table__.columns` (tenants table is the exception per CLAUDE.md)

    Create `backend/tests/unit/test_logging.py` — INFRA-06 PII guard:
    - `from __future__ import annotations`
    - Import stubs: `from app.core.logging import configure_logging`
    - `import structlog`, `import logging`, `import json`
    - `def test_json_output_contains_tenant_id()` — calls `configure_logging()`, captures log output via `logging.handlers.MemoryHandler` or `io.StringIO`, calls `structlog.get_logger().bind(tenant_id="test-tenant").info("test event")`, parses JSON, asserts `"tenant_id"` key present
    - `def test_no_pii_in_log_output()` — logs a record with fields `customer_name="Ion Popescu"`, `customer_phone="0721000000"`, `customer_email="ion@test.ro"`, asserts those literal strings do NOT appear in the serialized JSON log line (test that structlog does not auto-include kwargs as fields when not explicitly bound)

    Create `backend/tests/unit/test_worker_init.py` — INFRA-05 fork-safety:
    - `from __future__ import annotations`
    - Import stubs: `from app.db import session as db_session`
    - `from unittest.mock import patch, MagicMock`
    - `import asyncio`
    - `def test_worker_process_init_disposes_engine()` — patches `db_session.engine` with a MagicMock, patches `db_session._create_engine` to return a new MagicMock, calls `db_session.init_worker_process()` (the function wired to `worker_process_init` signal), asserts original engine's `dispose` was called, asserts `db_session.engine` is now the new mock (not the old one)
    - `def test_worker_process_init_recreates_session_factory()` — same setup, after `init_worker_process()` asserts `db_session.AsyncSessionLocal` is not None

    All 4 files use `from __future__ import annotations`. All imports are inside try/except blocks at module level with a module-level `pytestmark = pytest.mark.skip(reason="Stubs — implementation pending")` guard — EXCEPT for `test_tenant_isolation.py` where `test_query_without_tenant_context_raises` must NOT be skipped (it's D-06 required).
  </action>
  <verify>
    <automated>cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend && python -c "import ast; [ast.parse(open(f).read()) for f in ['tests/unit/test_tenant_isolation.py','tests/unit/test_models.py','tests/unit/test_logging.py','tests/unit/test_worker_init.py']]; print('All unit test files valid Python')"</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/unit/test_tenant_isolation.py contains `def test_query_without_tenant_context_raises`
    - backend/tests/unit/test_tenant_isolation.py contains `TenantIsolationError`
    - backend/tests/unit/test_tenant_isolation.py contains `_tenant_id_var.set(None)`
    - backend/tests/unit/test_models.py contains `def test_tenant_id_not_null`
    - backend/tests/unit/test_models.py contains `def test_tenant_model_has_no_tenant_id`
    - backend/tests/unit/test_logging.py contains `def test_no_pii_in_log_output`
    - backend/tests/unit/test_worker_init.py contains `def test_worker_process_init_disposes_engine`
    - All 4 files parse without SyntaxError: `python -c "import ast; ast.parse(open('backend/tests/unit/test_tenant_isolation.py').read())"` exits 0
  </acceptance_criteria>
  <done>All 4 unit test stubs created, parse as valid Python, D-06 test is not skipped and references TenantIsolationError and _tenant_id_var.set(None)</done>
</task>

<task type="auto">
  <name>Task 3: Integration test stubs — auth (AUTH-01/02), migrations (PIPE-04), celery (INFRA-04)</name>
  <files>
    backend/tests/integration/test_auth.py
    backend/tests/integration/test_migrations.py
    backend/tests/integration/test_celery.py
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md — Pattern 5 (FastAPI auth endpoint exact signatures: POST /api/v1/auth/login, POST /api/v1/auth/refresh, response_model=TokenResponse, Set-Cookie header)
    - .planning/phases/01-foundation/01-RESEARCH.md — Pattern 3 (celery-redbeat: KEYS redbeat::* Redis check, celery inspect ping → pong)
    - .planning/phases/01-foundation/01-RESEARCH.md — Pattern 2 (Alembic async env.py; tables: tenants, users, sync_runs, pipeline_runs)
    - .planning/phases/01-foundation/01-VALIDATION.md — Per-Task Verification Map (exact test function names: test_login_success, test_refresh_rotates, test_redbeat_alive, test_tables_exist)
  </read_first>
  <action>
    Create `backend/tests/integration/test_auth.py` — AUTH-01 and AUTH-02:
    - `from __future__ import annotations`
    - Import stubs: `from httpx import AsyncClient`, `import pytest`
    - `pytestmark = pytest.mark.integration`
    - `async def test_login_success(async_client: AsyncClient)` — skips if async_client is None. POSTs `{"email": "admin@sofabelle.ro", "password": "..."}` to `/api/v1/auth/login`. Asserts: status 200, JSON body has `"access_token"` key, `"token_type": "bearer"`, response `Set-Cookie` header contains `"refresh_token"` and `"HttpOnly"`. Docstring: "AUTH-01: Login with valid credentials returns 200 + access_token JSON + HttpOnly refresh cookie."
    - `async def test_login_invalid_credentials(async_client: AsyncClient)` — POSTs wrong password, asserts status 401
    - `async def test_refresh_rotates(async_client: AsyncClient)` — skips if async_client is None. First POSTs to login to get refresh cookie. Then POSTs to `/api/v1/auth/refresh` with the cookie. Asserts: status 200, new `access_token` in body, `Set-Cookie` with new `refresh_token`. Docstring: "AUTH-02: Refresh token endpoint rotates cookie and returns new access token."
    - `async def test_protected_route_without_token(async_client: AsyncClient)` — GETs `/` without auth cookie, asserts 401 or 307 redirect to `/login`

    Create `backend/tests/integration/test_migrations.py` — PIPE-04 and SC#2:
    - `from __future__ import annotations`
    - Import stubs: `import asyncpg`, `import os`, `import pytest`
    - `pytestmark = pytest.mark.integration`
    - `async def test_tables_exist()` — connects to `DATABASE_URL` env var (skips with `pytest.skip` if not set). Queries `information_schema.tables WHERE table_schema='public'`. Asserts all four table names present: `"tenants"`, `"users"`, `"sync_runs"`, `"pipeline_runs"`. Docstring: "PIPE-04, SC#2: alembic upgrade head must create all base tables."
    - `async def test_tenant_id_not_null_constraint()` — queries `information_schema.columns WHERE table_name='users' AND column_name='tenant_id'`. Asserts `is_nullable = 'NO'`.
    - `async def test_timestamps_are_timestamptz()` — queries `information_schema.columns WHERE table_name='users' AND column_name='created_at'`. Asserts `data_type = 'timestamp with time zone'`.

    Create `backend/tests/integration/test_celery.py` — INFRA-04 and SC#4:
    - `from __future__ import annotations`
    - Import stubs: `import redis`, `import pytest`, `from app.tasks.celery_app import celery_app`
    - `pytestmark = pytest.mark.integration`
    - `def test_redbeat_alive()` — connects to Redis at `REDIS_URL` env var (skips if not set). Runs `KEYS redbeat::*` (or `r.keys("redbeat::*")`). Asserts result is a non-empty list. Docstring: "INFRA-04, SC#4: celery-redbeat stores schedule keys in Redis after beat starts."
    - `def test_celery_worker_pings()` — calls `celery_app.control.inspect().ping()` with timeout=5. Asserts response dict is not None and at least one worker replied with `{"ok": "pong"}` or similar. Marks with `pytest.mark.skip(reason="Requires running worker")` — this is a manual gate test.

    All 3 files: `from __future__ import annotations` at top. All fixtures from conftest.py used by name (not re-defined). Imports inside try/except at module level where app code is referenced.
  </action>
  <verify>
    <automated>cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend && python -c "import ast; [ast.parse(open(f).read()) for f in ['tests/integration/test_auth.py','tests/integration/test_migrations.py','tests/integration/test_celery.py']]; print('All integration test stubs valid Python')"</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/integration/test_auth.py contains `def test_login_success`
    - backend/tests/integration/test_auth.py contains `def test_refresh_rotates`
    - backend/tests/integration/test_auth.py contains `Set-Cookie` string in body (response cookie assertion)
    - backend/tests/integration/test_migrations.py contains `def test_tables_exist`
    - backend/tests/integration/test_migrations.py contains `"pipeline_runs"` string (table name assertion)
    - backend/tests/integration/test_celery.py contains `def test_redbeat_alive`
    - All 3 files parse as valid Python: `python -c "import ast; ast.parse(open('backend/tests/integration/test_auth.py').read())"` exits 0
  </acceptance_criteria>
  <done>3 integration test stubs created, all parse as valid Python, test_login_success and test_tables_exist and test_redbeat_alive all present</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test code → app imports | Test stubs import from app modules not yet built; must survive ImportError gracefully |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | test stubs for D-06 | mitigate | test_query_without_tenant_context_raises must NOT be skipped — it is the proof that enforcement exists; any skip marker on that function is a defect |
| T-01-ID | Information Disclosure | test_logging.py | mitigate | test_no_pii_in_log_output explicitly asserts customer_name / customer_phone / customer_email do not appear in JSON log output |
</threat_model>

<verification>
After all 3 tasks complete:

1. `python -c "import ast; [ast.parse(open(f).read()) for f in ['backend/tests/__init__.py','backend/tests/unit/__init__.py','backend/tests/integration/__init__.py','backend/tests/conftest.py','backend/tests/unit/test_tenant_isolation.py','backend/tests/unit/test_models.py','backend/tests/unit/test_logging.py','backend/tests/unit/test_worker_init.py','backend/tests/integration/test_auth.py','backend/tests/integration/test_migrations.py','backend/tests/integration/test_celery.py']]; print('All test files valid Python')"` exits 0

2. `grep -c "test_query_without_tenant_context_raises" backend/tests/unit/test_tenant_isolation.py` returns 1

3. `grep -c "TenantIsolationError" backend/tests/unit/test_tenant_isolation.py` returns at least 1

4. `grep -c "asyncio_mode" backend/pyproject.toml` returns 1
</verification>

<success_criteria>
- 11 test files + 3 __init__.py files created in backend/tests/
- pyproject.toml has asyncio_mode=auto and testpaths=["tests"]
- D-06 test (test_query_without_tenant_context_raises) exists in test_tenant_isolation.py and is NOT marked as skip
- All files parse as valid Python (no SyntaxError)
- conftest.py defines async_client, db_session, test_tenant fixtures
- All 11 requirement IDs targeted by these tests are present in the test file docstrings or assertions
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>
