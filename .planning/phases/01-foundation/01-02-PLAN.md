---
phase: 01-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - docker-compose.prod.yml
  - backend/Dockerfile
  - frontend/Dockerfile
  - backend/.env.example
  - .env.example
autonomous: true
requirements:
  - INFRA-01
  - INFRA-04

must_haves:
  truths:
    - "docker compose up -d starts all 7 containers (postgres, redis, backend, worker, beat, flower, frontend) without error"
    - "curl http://localhost:8000/healthz returns 200 within 30 seconds of docker compose up -d"
    - "backend, worker, and beat containers have depends_on with condition: service_healthy for postgres and redis"
    - "beat container starts with celery-redbeat scheduler command"
    - "Flower is accessible on http://localhost:5555"
  artifacts:
    - path: "docker-compose.yml"
      provides: "7-service dev stack with healthchecks and depends_on"
      contains: "condition: service_healthy"
    - path: "backend/Dockerfile"
      provides: "Python 3.11 backend image with uv"
      contains: "FROM python:3.11-slim"
    - path: "frontend/Dockerfile"
      provides: "Node.js 24 frontend image with pnpm"
      contains: "FROM node:"
    - path: "backend/.env.example"
      provides: "Backend env var template"
      contains: "DATABASE_URL"
  key_links:
    - from: "docker-compose.yml backend service"
      to: "backend/Dockerfile"
      via: "build: ./backend"
      pattern: "build:.*backend"
    - from: "docker-compose.yml worker service"
      to: "backend/Dockerfile"
      via: "shared image build context"
      pattern: "command:.*celery.*worker"
    - from: "docker-compose.yml beat service"
      to: "celery-redbeat"
      via: "command: celery beat -S redbeat.RedBeatScheduler"
      pattern: "RedBeatScheduler"
---

<objective>
Create Docker Compose dev stack with all 7 services (postgres, redis, backend, worker, beat, flower, frontend) and their Dockerfiles. Establish healthchecks and depends_on chains per D-08, D-09, D-10. Services must start in correct order with `docker compose up -d`.

Purpose: Fulfills INFRA-01 (SC#1) — working dev environment with single command. Also satisfies INFRA-04 partial (Celery + beat containers with correct startup commands).

Output: docker-compose.yml, docker-compose.prod.yml skeleton, backend/Dockerfile, frontend/Dockerfile, .env.example files.
</objective>

<execution_context>
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Key config values from RESEARCH.md that must be reproduced exactly -->

Docker Healthchecks (from RESEARCH.md Pattern 8):
- postgres: test: ["CMD-SHELL", "pg_isready -d analyst -U analyst"], interval: 5s, timeout: 5s, retries: 10, start_period: 10s
- redis: test: ["CMD", "redis-cli", "ping"], interval: 5s, timeout: 3s, retries: 10
- backend: test: ["CMD-SHELL", "curl -f http://localhost:8000/healthz || exit 1"], interval: 10s, timeout: 5s, retries: 5, start_period: 15s

Celery commands (from RESEARCH.md Pattern 3 and 8):
- worker: celery -A app.tasks.celery_app worker --loglevel=info
- beat: celery -A app.tasks.celery_app beat -S redbeat.RedBeatScheduler --loglevel=info
- flower: celery -A app.tasks.celery_app flower --port=5555

Env vars referenced in backend services:
- DATABASE_URL=postgresql+asyncpg://analyst:${POSTGRES_PASSWORD}@postgres:5432/analyst
- REDIS_URL=redis://redis:6379/0
- JWT_SECRET_KEY=${JWT_SECRET_KEY}
- SOFA_BELLE_TENANT_ID=00000000-0000-0000-0000-000000000001
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend and frontend Dockerfiles + .env.example files</name>
  <files>
    backend/Dockerfile
    frontend/Dockerfile
    backend/.env.example
    .env.example
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md — Standard Stack section (Python 3.11, uv, uvicorn 0.39.0, Node.js v24)
    - .planning/phases/01-foundation/01-RESEARCH.md — Environment Availability table (Python 3.11 required, pnpm and uv must be installed)
    - CLAUDE.md — Commands section (backend startup: uvicorn app.main:app --reload --port 8000; frontend: pnpm dev)
    - CLAUDE.md — Core Principles (No secrets in code — all via env vars; Pydantic Settings)
  </read_first>
  <action>
    Create `backend/Dockerfile`:
    - Base: `FROM python:3.11-slim`
    - Install: `RUN pip install uv`
    - `WORKDIR /app`
    - Copy pyproject.toml first (cache layer): `COPY pyproject.toml .`
    - Install dependencies: `RUN uv pip install --system -e ".[dev]"` (without dev extras for prod layer)
    - `COPY . .`
    - Default CMD: `CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]`
    - Expose port 8000
    - Do NOT embed secrets; all config via ENV

    Create `frontend/Dockerfile`:
    - Base: `FROM node:24-alpine`
    - Install pnpm: `RUN npm install -g pnpm`
    - `WORKDIR /app`
    - Copy package.json and pnpm-lock.yaml first (cache layer)
    - `RUN pnpm install`
    - `COPY . .`
    - Default CMD: `CMD ["pnpm", "dev"]` (dev server; prod uses `pnpm build && pnpm start`)
    - Expose port 3000

    Create `backend/.env.example` with these keys (no real values):
    ```
    DATABASE_URL=postgresql+asyncpg://analyst:changeme@localhost:5432/analyst
    REDIS_URL=redis://localhost:6379/0
    JWT_SECRET_KEY=change-this-to-a-secure-random-string-min-32-chars
    JWT_ALGORITHM=HS256
    ACCESS_TOKEN_EXPIRE_MINUTES=15
    REFRESH_TOKEN_EXPIRE_DAYS=30
    SOFA_BELLE_TENANT_ID=00000000-0000-0000-0000-000000000001
    LOG_LEVEL=INFO
    ```

    Create `.env.example` (root, for Docker Compose):
    ```
    POSTGRES_PASSWORD=changeme
    POSTGRES_DB=analyst
    POSTGRES_USER=analyst
    JWT_SECRET_KEY=change-this-to-a-secure-random-string-min-32-chars
    ```

    Note: The actual `.env` file must be created by the developer by copying `.env.example` and setting real values. Add `.env` to `.gitignore` if not already present.
  </action>
  <verify>
    <automated>cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst && grep -c "FROM python:3.11-slim" backend/Dockerfile && grep -c "DATABASE_URL" backend/.env.example && grep -c "POSTGRES_PASSWORD" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - backend/Dockerfile contains `FROM python:3.11-slim`
    - backend/Dockerfile contains `pip install uv`
    - backend/Dockerfile contains `uvicorn app.main:app`
    - backend/Dockerfile exposes port 8000 (EXPOSE 8000)
    - frontend/Dockerfile contains `FROM node:24-alpine`
    - frontend/Dockerfile contains `pnpm install`
    - backend/.env.example contains DATABASE_URL, REDIS_URL, JWT_SECRET_KEY, SOFA_BELLE_TENANT_ID
    - .env.example contains POSTGRES_PASSWORD, JWT_SECRET_KEY
    - No real secrets or passwords in any Dockerfile (no hardcoded credentials)
  </acceptance_criteria>
  <done>4 files created: backend/Dockerfile (python:3.11-slim + uv), frontend/Dockerfile (node:24-alpine + pnpm), backend/.env.example (8 keys), .env.example (4 keys)</done>
</task>

<task type="auto">
  <name>Task 2: docker-compose.yml (7 services with healthchecks) and docker-compose.prod.yml skeleton</name>
  <files>
    docker-compose.yml
    docker-compose.prod.yml
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md — Pattern 8 (exact Docker Compose healthcheck config for postgres and redis, depends_on service_healthy)
    - .planning/phases/01-foundation/01-CONTEXT.md — D-08 (6+1=7 containers: postgres, redis, backend, worker, beat, frontend, PLUS flower from D-09)
    - .planning/phases/01-foundation/01-CONTEXT.md — D-09 (Flower on port 5555)
    - .planning/phases/01-foundation/01-CONTEXT.md — D-10 (depends_on condition: service_healthy for postgres and redis before backend/worker/beat)
    - .planning/phases/01-foundation/01-RESEARCH.md — Pattern 3 (beat command: celery beat -S redbeat.RedBeatScheduler; worker and beat MUST be separate containers — anti-pattern from Common Pitfalls)
  </read_first>
  <action>
    Create `docker-compose.yml` with these services:

    postgres:
    - image: postgres:16-alpine
    - environment: POSTGRES_DB, POSTGRES_USER, POSTGRES_PASSWORD (all from .env)
    - volumes: postgres_data:/var/lib/postgresql/data
    - healthcheck: CMD-SHELL "pg_isready -d analyst -U analyst", interval: 5s, timeout: 5s, retries: 10, start_period: 10s
    - ports: "5432:5432" (dev only)

    redis:
    - image: redis:7-alpine
    - volumes: redis_data:/data
    - healthcheck: CMD "redis-cli" "ping", interval: 5s, timeout: 3s, retries: 10
    - ports: "6379:6379" (dev only)

    backend:
    - build: ./backend
    - context: ./backend
    - env_file: backend/.env (created from .env.example)
    - environment: DATABASE_URL (uses postgres container hostname), REDIS_URL
    - depends_on: postgres (condition: service_healthy), redis (condition: service_healthy)
    - ports: "8000:8000"
    - volumes: ./backend:/app (live reload in dev)
    - command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
    - healthcheck: CMD-SHELL "curl -f http://localhost:8000/healthz || exit 1", interval: 10s, timeout: 5s, retries: 5, start_period: 15s

    worker:
    - build: ./backend (same image as backend)
    - env_file: backend/.env
    - depends_on: postgres (condition: service_healthy), redis (condition: service_healthy)
    - volumes: ./backend:/app
    - command: celery -A app.tasks.celery_app worker --loglevel=info

    beat:
    - build: ./backend (same image as backend)
    - env_file: backend/.env
    - depends_on: redis (condition: service_healthy)
    - volumes: ./backend:/app
    - command: celery -A app.tasks.celery_app beat -S redbeat.RedBeatScheduler --loglevel=info
    Note: beat depends only on redis (not postgres) — beat only writes schedule to Redis

    flower:
    - build: ./backend (same image as backend — celery is already installed)
    - env_file: backend/.env
    - depends_on: redis (condition: service_healthy)
    - ports: "5555:5555"
    - command: celery -A app.tasks.celery_app flower --port=5555
    - Note: Flower from D-09 for debugging Celery tasks

    frontend:
    - build: ./frontend
    - volumes: ./frontend:/app, /app/node_modules (anonymous volume to preserve node_modules)
    - ports: "3000:3000"
    - environment: NEXT_PUBLIC_API_URL=http://backend:8000
    - depends_on: backend (no healthcheck condition — frontend can start while backend warms up)

    volumes section: postgres_data, redis_data

    Create `docker-compose.prod.yml` as a minimal skeleton that overrides dev settings:
    - Inherits base docker-compose.yml via `extends` or lists only changed service properties
    - Backend: removes --reload from uvicorn command
    - Frontend: changes command to pnpm build && pnpm start (or Next.js production server)
    - No volume mounts for source code (no ./backend:/app)
    - Add comment: "# Production: run with: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d"
    - This is a skeleton — Phase 9 fills in Caddy, Sentry, backup config
  </action>
  <verify>
    <automated>cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst && docker compose config --quiet 2>&1 | head -5; echo "Exit: $?"</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml contains exactly 7 services: postgres, redis, backend, worker, beat, flower, frontend
    - docker-compose.yml contains `condition: service_healthy` for postgres and redis in backend depends_on
    - docker-compose.yml contains `condition: service_healthy` for postgres and redis in worker depends_on
    - docker-compose.yml contains `redbeat.RedBeatScheduler` in beat service command
    - docker-compose.yml contains flower service with port 5555
    - docker-compose.yml contains `pg_isready` in postgres healthcheck
    - docker-compose.yml contains `redis-cli ping` in redis healthcheck
    - docker compose config (validation only — no containers started) exits 0 after .env file is created from .env.example
    - worker and beat are SEPARATE services (no combined `celery worker beat` command)
  </acceptance_criteria>
  <done>docker-compose.yml has 7 services with healthchecks; beat uses RedBeatScheduler; docker compose config validates successfully</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env → docker-compose.yml | Secrets injected via env_file; must never be committed |
| container network → host | Backend/frontend ports exposed on localhost only in dev |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | .env files | mitigate | .env.example committed; .env added to .gitignore; gitleaks pre-commit hook blocks actual secrets |
| T-02-02 | Tampering | Shared DB connections across fork | mitigate | worker_process_init signal in Plan 05 disposes + recreates engine; Docker worker container gets its own process space |
| T-02-03 | Spoofing | Flower (port 5555) — no auth in dev | accept | Flower is dev-only tool; not exposed in docker-compose.prod.yml; acceptable risk in local dev |
| T-02-SC | Tampering | npm/pip installs in Dockerfiles | mitigate | All packages verified in Package Legitimacy Audit in RESEARCH.md; no [ASSUMED] or [SUS] packages |
</threat_model>

<verification>
After both tasks complete:
1. `docker compose config --quiet` exits 0 (requires .env file created from .env.example)
2. `grep -c "condition: service_healthy" docker-compose.yml` returns at least 4 (postgres+redis for backend, postgres+redis for worker)
3. `grep -c "RedBeatScheduler" docker-compose.yml` returns 1
4. `grep -c "5555" docker-compose.yml` returns at least 1 (Flower port)
5. `grep -c "FROM python:3.11-slim" backend/Dockerfile` returns 1
</verification>

<success_criteria>
- docker compose up -d (after .env created) starts all 7 services without error
- curl http://localhost:8000/healthz returns 200 within 30 seconds (once backend Plan 06 is complete)
- postgres and redis healthchecks pass before backend/worker/beat start
- beat uses redbeat.RedBeatScheduler command
- Flower accessible on port 5555
- No hardcoded secrets in any committed file
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-02-SUMMARY.md` when done
</output>
