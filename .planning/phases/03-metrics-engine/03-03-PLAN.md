---
phase: 03-metrics-engine
plan: "03"
type: execute
wave: 2
depends_on: ["03-01", "03-02"]
files_modified:
  - backend/app/services/metrics/__init__.py
  - backend/app/services/metrics/business_hours.py
  - backend/app/services/metrics/daily_kpi_service.py
  - backend/app/services/metrics/salesperson_kpi_service.py
  - backend/app/services/metrics/source_kpi_service.py
  - backend/app/services/repositories/metrics_repository.py
autonomous: true
requirements: [METR-02, METR-03, METR-04, METR-05, METR-06]

must_haves:
  truths:
    - "business_minutes_between() returns business minutes between two UTC datetimes, DST-correct via zoneinfo, business-hours-clamped per D-07"
    - "DailyKpiService computes 5 conversion rates with zero-division guards returning NULL not error (METR-02)"
    - "DailyKpiService computes WoW (D-7) and MoM (D-30) deltas; NULL when prior row absent or prior=0 (METR-05, D-11)"
    - "SalespersonKpiService emits one row per active salesperson (is_active=true only — D-17), including avg_time_to_first_touch_minutes and data_completeness_pct"
    - "SourceKpiService emits exactly 7 rows per day per tenant (D-04 — mail_fb_ig|telefon|whatsapp|site|designer|alte|google), even when some have 0 leads"
    - "Designer category detection joins mefi_lead_history WHERE to_status_id=24 (D-02), overrides source_id mapping"
    - "MetricsRepository validates tenant_id in every row dict before INSERT (Pitfall 6) and uses correct 2-col vs 3-col conflict targets per table (Pitfall 5)"
    - "All services query v_mefi_leads_active only — never raw_mefi_* tables directly (D-14)"
    - "All money/rate arithmetic uses Decimal — never float (D-16)"
  artifacts:
    - path: "backend/app/services/metrics/business_hours.py"
      provides: "business_minutes_between() pure-Python utility using zoneinfo"
      exports: ["business_minutes_between"]
    - path: "backend/app/services/metrics/daily_kpi_service.py"
      provides: "DailyKpiService.compute_daily_kpis() with conversion rates and WoW/MoM deltas"
      exports: ["DailyKpiService"]
    - path: "backend/app/services/metrics/salesperson_kpi_service.py"
      provides: "SalespersonKpiService.compute_for_date() with TTFT + data_completeness_pct"
      exports: ["SalespersonKpiService"]
    - path: "backend/app/services/metrics/source_kpi_service.py"
      provides: "SourceKpiService.compute_for_date() emitting 7 rows with designer detection"
      exports: ["SourceKpiService"]
    - path: "backend/app/services/repositories/metrics_repository.py"
      provides: "MetricsRepository.upsert_daily_kpi / upsert_salesperson_kpi / upsert_source_kpi"
      exports: ["MetricsRepository"]
    - path: "backend/app/services/metrics/__init__.py"
      provides: "Re-exports the three services + business_hours util"
  key_links:
    - from: "backend/app/services/metrics/daily_kpi_service.py"
      to: "v_mefi_leads_active"
      via: "SQLAlchemy text() or core select against the view"
      pattern: "v_mefi_leads_active"
    - from: "backend/app/services/metrics/source_kpi_service.py"
      to: "mefi_lead_history.to_status_id == 24"
      via: "designer detection subquery"
      pattern: "to_status_id.*24"
    - from: "backend/app/services/metrics/salesperson_kpi_service.py"
      to: "mefi_salespeople.is_active"
      via: "JOIN MefiSalesperson with is_active == True filter"
      pattern: "is_active"
    - from: "backend/app/services/repositories/metrics_repository.py"
      to: "DailyKpi / SalespersonDailyKpi / SourceDailyKpi models"
      via: "pg_insert(...).on_conflict_do_update(...)"
      pattern: "on_conflict_do_update"
---

<objective>
Build the metric service layer that turns the conformed Phase 2 views into the three metric table rows per day, plus the repository that atomically UPSERTs those rows. This is the business-logic core of Phase 3 — every conversion rate, every WoW/MoM delta, every business-hours-adjusted first-touch minute, and every source categorization lives here.

Purpose: Make the Wave 0 service and repository tests turn GREEN. The Celery task in Plan 04 is a thin orchestration layer over these services.

Output: 4 service/util files + 1 repository file + 1 metrics package __init__.
</objective>

<execution_context>
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/CLAUDE.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/docs/CONVENTIONS.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/docs/ARCHITECTURE.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.planning/phases/03-metrics-engine/03-CONTEXT.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.planning/phases/03-metrics-engine/03-RESEARCH.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.planning/phases/03-metrics-engine/03-PATTERNS.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/app/services/repositories/mefi_repository.py
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/app/models/mefi.py
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/app/models/metrics/daily_kpi.py
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/app/models/metrics/salesperson_kpi.py
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/app/models/metrics/source_kpi.py
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/tests/unit/test_business_hours.py
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/tests/unit/test_daily_kpi_service.py
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/tests/unit/test_salesperson_kpi_service.py
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/tests/unit/test_source_kpi_service.py
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/tests/unit/test_metrics_repository.py

<interfaces>
<!-- Existing infrastructure these services build upon. -->

From backend/app/services/repositories/mefi_repository.py (THE pattern to copy):
```python
class MefiRepository:
    def __init__(self, session: AsyncSession, tenant_id: UUID) -> None: ...
    async def bulk_upsert_leads(self, rows: list[dict]) -> int:
        # 1. validate tenant_id in every row → raise ValueError if missing
        # 2. deferred model import inside function body (fork-safe)
        # 3. pg_insert(Model).values(rows).on_conflict_do_update(index_elements=[...], set_={...})
        # 4. await session.execute + commit
```

From backend/app/models/metrics/* (built in Plan 02):
- DailyKpi.__table_args__ unique key = (tenant_id, date)
- SalespersonDailyKpi.__table_args__ unique key = (tenant_id, salesperson_external_id, date)
- SourceDailyKpi.__table_args__ unique key = (tenant_id, source, date)  ← 3-col, Pitfall 5

From backend/app/models/mefi.py:
- MefiLeadHistory.lead_external_id (Text NOT NULL — joins to RawMefiLead.external_id)
- MefiLeadHistory.to_status_id (Int NOT NULL — 24 = DESIGNER)
- MefiLeadHistory.changed_at (TIMESTAMPTZ NOT NULL — "first touch" = earliest changed_at per lead)
- MefiSalesperson.is_active (Bool NULLABLE — D-17 filter == True only, NULL excluded)
- MefiSalesperson.external_id (Integer NOT NULL — MEFI user ID)

Conformed view v_mefi_leads_active (updated in Plan 02 with history-joined reached_* booleans) selected columns include: tenant_id, external_id, status_id, source_id, lifecycle, assigned_to_id, estimated_value, created_at_source, status_changed_at, reached_visit, reached_offer, reached_contract, created_at_local, status_changed_at_local.

Tenant.funnel_config JSONB keys (after migration 004):
- source_categories: {"mail_fb_ig":[2,11],"telefon":[10],"whatsapp":[9],"site":[6],"designer":[],"alte":[3,4,5,7,12,13]}
- business_hours: {"days":[0,1,2,3,4,5,6],"open":"09:00","close":"19:00","tz":"Europe/Bucharest"}
- (designer category from source_categories is intentionally empty []; D-02 says designer is derived from status_id=24 in history, not source_id)
- (google category is NOT in funnel_config.source_categories — it is hardcoded to source_id=1 per D-01 because Sofa Belle's "Google" source is a single MEFI source ID)

Public method signatures (the planner locks these — Plan 04 task wires them):
```python
def business_minutes_between(start_utc: datetime, end_utc: datetime, open_time: time, close_time: time, work_days: list[int], tz: ZoneInfo) -> int

class DailyKpiService:
    def __init__(self, session: AsyncSession, tenant_id: UUID) -> None
    async def compute_for_date(self, kpi_date: date) -> dict  # returns daily_kpi row dict ready for upsert

class SalespersonKpiService:
    def __init__(self, session: AsyncSession, tenant_id: UUID) -> None
    async def compute_for_date(self, kpi_date: date) -> list[dict]  # one dict per active salesperson

class SourceKpiService:
    def __init__(self, session: AsyncSession, tenant_id: UUID) -> None
    async def compute_for_date(self, kpi_date: date) -> list[dict]  # exactly 7 dicts (D-04)

class MetricsRepository:
    def __init__(self, session: AsyncSession, tenant_id: UUID) -> None
    async def upsert_daily_kpi(self, row: dict) -> None
    async def upsert_salesperson_kpis(self, rows: list[dict]) -> int
    async def upsert_source_kpis(self, rows: list[dict]) -> int
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build business_hours util + MetricsRepository + metrics package __init__</name>
  <files>backend/app/services/metrics/__init__.py, backend/app/services/metrics/business_hours.py, backend/app/services/repositories/metrics_repository.py</files>
  <read_first>
    - backend/app/services/repositories/mefi_repository.py (FULL FILE — copy class structure, tenant_id validation loop, deferred model imports, pg_insert ON CONFLICT pattern, session.execute + commit)
    - .planning/phases/03-metrics-engine/03-RESEARCH.md (Pattern 2 — business_minutes_between with zoneinfo + edge cases for outside-hours and DST; Pitfall 5 — 3-col conflict target for source_daily_kpi; Pitfall 6 — tenant_id validation gate)
    - .planning/phases/03-metrics-engine/03-PATTERNS.md (section 'metrics_repository.py' — verbatim tenant_id validation guard + per-table conflict target list)
    - .planning/phases/03-metrics-engine/03-CONTEXT.md (D-06 business-hours adjustment, D-07 Mon-Sun 09:00-19:00 Bucharest schedule)
    - backend/tests/unit/test_business_hours.py (Wave 0 contract — edge cases business_minutes_between must satisfy: lead_created_outside_hours, lead_created_at_close, end_before_start_returns_zero, dst_spring_forward_bucharest)
    - backend/tests/unit/test_metrics_repository.py (Wave 0 contract — UPSERT 3-col conflict for SourceDailyKpi, ValueError on missing tenant_id, idempotent double-upsert)
  </read_first>
  <behavior>
    - business_hours.py exports `business_minutes_between(start_utc: datetime, end_utc: datetime, open_time: time, close_time: time, work_days: list[int], tz: ZoneInfo) -> int` — pure function, no async, no DB; handles: end<=start returns 0; lead created outside hours skips to next open window before counting; lead crossing day boundary sums minutes per day; DST transition (EET↔EEST) correct via zoneinfo
    - MetricsRepository class with three async methods: `upsert_daily_kpi(row: dict) -> None` uses pg_insert(DailyKpi).on_conflict_do_update(index_elements=["tenant_id","date"], ...); `upsert_salesperson_kpis(rows: list[dict]) -> int` uses index_elements=["tenant_id","salesperson_external_id","date"]; `upsert_source_kpis(rows: list[dict]) -> int` uses index_elements=["tenant_id","source","date"] (3-col, Pitfall 5)
    - Every public method validates tenant_id presence in row(s) and raises ValueError with informative message including the conflict key (date, salesperson_external_id, or source) when missing
    - Empty input lists return 0 / None without calling session.execute (Pitfall 6 short-circuit)
    - Deferred model imports inside each method body (fork-safety per INFRA-05)
    - metrics/__init__.py re-exports DailyKpiService, SalespersonKpiService, SourceKpiService, and business_minutes_between (placeholders for the services that arrive in Task 2 — the __init__ should import them inside try/except ImportError if they don't yet exist at the moment Task 1 runs, OR simply not import services in __init__.py and let Plan 04 import them directly)
  </behavior>
  <action>
    Create `backend/app/services/metrics/` directory and `__init__.py` (empty is acceptable, OR re-export only what already exists — do NOT import unbuilt services from this file). For `business_hours.py`: implement `business_minutes_between` exactly matching the algorithm in 03-RESEARCH.md Pattern 2 — convert both datetimes to local tz via `start_utc.astimezone(tz)`, iterate forward via a `_next_open(current, open_time, work_days, tz)` helper that advances to the next valid open boundary, count minutes per business window via `int((min(close_dt, end_local) - current).total_seconds() / 60)`. Handle: weekday check via `current.weekday() in work_days`; outside-window jump to next day's open via `_next_open(current + timedelta(days=1), ...)`; current<open_dt advances to open_dt without counting; current>=close_dt advances to next valid open. Loop terminates when `current >= end_local`. Return integer minutes. For `metrics_repository.py`: copy structural skeleton of `mefi_repository.py` — same `__init__(self, session, tenant_id)`, same `for row in rows: if "tenant_id" not in row or row["tenant_id"] is None: raise ValueError(...)` validation, same `from app.models.metrics.X import X  # deferred — fork-safe` pattern. Use `from sqlalchemy.dialects.postgresql import insert as pg_insert`. For `upsert_daily_kpi(row)` take a SINGLE dict (Plan 04 calls once per day) — wrap in `pg_insert(DailyKpi).values([row])`. For salesperson and source methods take a list. `update_cols` for each table = all dict keys EXCEPT `{"id"}` and the unique-key columns (so `tenant_id`, `date`, etc. stay immutable on conflict). Return rowcount from `result.rowcount`. After each execute, call `await self._session.commit()`. The `__init__.py` for metrics package can be just `from __future__ import annotations` + an empty body or a docstring — keep it minimal so importing `app.services.metrics.business_hours` works without side effects.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; pytest tests/unit/test_business_hours.py tests/unit/test_metrics_repository.py -x</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/app/services/metrics/__init__.py` exists
    - File `backend/app/services/metrics/business_hours.py` exists and contains text `def business_minutes_between` AND `ZoneInfo` AND `astimezone` AND `weekday()`
    - File `backend/app/services/repositories/metrics_repository.py` exists and contains text `class MetricsRepository` AND `pg_insert` AND `on_conflict_do_update` AND `index_elements=["tenant_id", "date"]` AND `index_elements=["tenant_id", "salesperson_external_id", "date"]` AND `index_elements=["tenant_id", "source", "date"]` AND `raise ValueError`
    - Every new file begins with `from __future__ import annotations`
    - `cd backend &amp;&amp; pytest tests/unit/test_business_hours.py -x` exits 0 (all 5+ edge case tests GREEN)
    - `cd backend &amp;&amp; pytest tests/unit/test_metrics_repository.py -x` exits 0 (UPSERT, tenant validation, 3-col conflict tests GREEN)
    - `python -c "from app.services.metrics.business_hours import business_minutes_between; from datetime import datetime, time; from zoneinfo import ZoneInfo; tz=ZoneInfo('Europe/Bucharest'); s=datetime(2026,5,25,7,0,tzinfo=tz); e=datetime(2026,5,25,10,30,tzinfo=tz); print(business_minutes_between(s, e, time(9,0), time(19,0), [0,1,2,3,4,5,6], tz))"` prints `90` (Mon 09:00→10:30 = 90 minutes, start 07:00 is pre-open and skipped to 09:00)
  </acceptance_criteria>
  <done>business_hours util passes all DST/edge case tests, MetricsRepository validates tenant_id and uses correct conflict targets per table, both Wave 0 test files GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build DailyKpiService + SalespersonKpiService + SourceKpiService</name>
  <files>backend/app/services/metrics/daily_kpi_service.py, backend/app/services/metrics/salesperson_kpi_service.py, backend/app/services/metrics/source_kpi_service.py</files>
  <read_first>
    - backend/app/services/repositories/mefi_repository.py (deferred-import pattern, structlog binding, session.execute pattern)
    - backend/app/models/mefi.py (RawMefiLead, MefiLeadHistory, MefiSalesperson — column names the services query)
    - backend/app/models/tenant.py (Tenant.funnel_config — services read source_categories and business_hours from here)
    - .planning/phases/03-metrics-engine/03-RESEARCH.md (Pattern 1 SQLAlchemy UPSERT, Pattern 3 Designer detection JOIN, Pattern 4 WoW/MoM delta helper, Pitfall 1 DST, Pitfall 4 integer division, Pitfall 6 with_loader_criteria gap on Core inserts)
    - .planning/phases/03-metrics-engine/03-PATTERNS.md (sections 'daily_kpi_service.py' through 'source_kpi_service.py' — conversion rate SQL pattern, designer subquery, data_completeness_pct SQL, business_hours config read helper)
    - .planning/phases/03-metrics-engine/03-CONTEXT.md (D-01 google as standalone source row, D-02 designer detection, D-04 seven categories, D-05 first-touch = first history row, D-10 yesterday default, D-11 NULL on missing prior, D-13 ever-reached funnel, D-14 view-only queries, D-15 AT TIME ZONE, D-17 active salespeople only)
    - backend/tests/unit/test_daily_kpi_service.py (Wave 0 contract)
    - backend/tests/unit/test_salesperson_kpi_service.py (Wave 0 contract)
    - backend/tests/unit/test_source_kpi_service.py (Wave 0 contract)
  </read_first>
  <behavior>
    - DailyKpiService.compute_for_date(kpi_date) returns ONE row dict matching DailyKpi columns: tenant_id, date=kpi_date; leads_total/leads_mail_fb_ig/leads_telefon/leads_whatsapp/leads_site/leads_designer/leads_alte (Ints); visits_count/offers_count/contracts_count (Ints from v_mefi_leads_active reached_* booleans); 5 conversion rates as Decimal | None with NULLIF zero-division guard (METR-02); revenue as Decimal | None (SUM estimated_value of leads with reached_contract=true); avg_deal_size = revenue / contracts_count or None; WoW/MoM deltas for leads_total, revenue, avg_deal_size, all 5 conversion rates (16 delta values) computed via _compute_delta(current, prior) helper returning None when prior is None or 0 (D-11); ad-spend/CPL/CAC/ROAS/calls/web/sentiment cols left out of the row dict (NULL by default in schema — populated by future iterations per D-08)
    - SalespersonKpiService.compute_for_date(kpi_date) returns LIST of row dicts, one per active salesperson — JOIN MefiSalesperson with `is_active == True` filter (D-17, excludes NULL); per-rep query against v_mefi_leads_active WHERE assigned_to_id = sp.external_id grouped by date in Bucharest tz: leads_assigned (count), leads_contacted (count distinct lead_external_id in mefi_lead_history WHERE the lead is assigned to this rep), avg_time_to_first_touch_minutes (call business_minutes_between for each lead's created_at_source → first history row changed_at, average — NULL for leads with no history per D-05), visits_conducted/offers_sent/deals_won/deals_lost (counts), revenue (SUM estimated_value of deals_won), 4 conversion rates with NULLIF guard, avg_deal_size, data_completeness_pct = (COUNT WHERE estimated_value IS NOT NULL) / COUNT(*) * 100 as Decimal NUMERIC(5,2) (METR-06)
    - SourceKpiService.compute_for_date(kpi_date) returns EXACTLY 7 row dicts in order: mail_fb_ig, telefon, whatsapp, site, designer, alte, google (D-04) — even when count is 0; reads tenants.funnel_config.source_categories at task start; designer detection via subquery `SELECT lead_external_id FROM mefi_lead_history WHERE tenant_id = :tid AND to_status_id = 24` with explicit tenant_id filter (NOT covered by with_loader_criteria for Core queries — Pitfall 6); google = source_id == 1 (D-01); each row dict: tenant_id, source=category_name, date=kpi_date, leads (count), visits/offers/deals_won (counts), revenue (Decimal SUM), conversion_rate (deals_won/leads with NULLIF guard); ad_spend/cpl/cac/roas left NULL (D-08, populated in Iteration 2)
    - All services use `AT TIME ZONE 'Europe/Bucharest'` for date grouping (D-15) — call `func.timezone('Europe/Bucharest', v.c.created_at_source)` in SQLAlchemy core, OR use a raw text() snippet `(r.created_at_source AT TIME ZONE 'Europe/Bucharest')::date` if working with the view directly
    - All services query `v_mefi_leads_active` only (D-14) — wire via SQLAlchemy `text("v_mefi_leads_active")` table reflection or a sa.table() literal with explicit columns; never SELECT from raw_mefi_leads
    - All services use Decimal arithmetic throughout (D-16); cast Integer counts to Numeric(12,4) before division to avoid PostgreSQL integer-division truncation (Pitfall 4)
    - All services log via `logger.bind(tenant_id=..., service="xxx").info("event_name", kpi_date=str(kpi_date), records=N)` — never log PII
  </behavior>
  <action>
    Create three service files following the structural pattern of `mefi_repository.py`: `from __future__ import annotations`, module docstring, imports section (sqlalchemy.func/case/select/text + AsyncSession + structlog + Decimal + date + UUID), `logger = structlog.get_logger(__name__)`, then class definition with `__init__(self, session, tenant_id)`. Inside each `compute_for_date(kpi_date)` body, defer model imports (`from app.models.mefi import RawMefiLead, MefiLeadHistory, MefiSalesperson` and `from app.models.metrics.X import X`). For DailyKpiService: build one aggregate query against the view returning leads_total, visits/offers/contracts via `func.count(case((v.c.reached_visit == True, v.c.id)))` pattern; compute conversion_l_to_v as `func.cast(visits_count, Numeric(12,4)) / func.nullif(leads_total, 0)`; revenue as `func.sum(case((v.c.reached_contract == True, v.c.estimated_value)))`. Then add a helper `async def _fetch_prior_row(kpi_date, days_back)` that selects `DailyKpi` row WHERE tenant_id == self._tenant_id AND date == kpi_date - timedelta(days=days_back); and `def _compute_delta(current, prior)` returning `(current - prior) / prior` only when both non-None and prior != 0, else None. For per-source leads counts in daily_kpi (leads_mail_fb_ig etc.), build a single query that GROUPS BY source category via a `CASE` expression and pivots into 6 columns plus a 7th 'google' column — keep designer detection consistent with SourceKpiService (history JOIN on to_status_id=24). For SalespersonKpiService: SELECT MefiSalesperson WHERE is_active == True (use `MefiSalesperson.is_active.is_(True)` to be explicit about IS TRUE vs NULL), then for each rep build a per-rep query against v_mefi_leads_active filtered by assigned_to_id == rep.external_id grouped by date in Bucharest tz. For avg_time_to_first_touch_minutes: SELECT lead external_id + created_at_source from v_mefi_leads_active WHERE assigned_to_id = rep AND date_local = kpi_date, LEFT JOIN to first mefi_lead_history row per lead (use a window function or subquery: `SELECT lead_external_id, MIN(changed_at) AS first_touch_at FROM mefi_lead_history WHERE tenant_id = :tid GROUP BY lead_external_id`), then in Python iterate the rows and call business_minutes_between(created_at_source, first_touch_at, ...) for each, average the non-NULL results into an int — NULL when zero leads have history rows. Read business_hours config via `await self._get_business_hours()` which selects `Tenant.funnel_config["business_hours"]` for the current tenant_id and falls back to `{"days":[0,1,2,3,4,5,6],"open":"09:00","close":"19:00","tz":"Europe/Bucharest"}` if missing. For SourceKpiService: read `source_categories` mapping from `tenants.funnel_config` at start, build the CASE expression for category assignment with designer-detection subquery taking precedence (a lead with to_status_id=24 in history is `designer` regardless of source_id — D-02), google forced to source_id==1 (D-01). Aggregate by category, then in Python build a 7-element list with one dict per category in the canonical order `["mail_fb_ig","telefon","whatsapp","site","designer","alte","google"]` — fill missing categories with zero/null defaults so the list always has exactly 7 entries. NEVER include `with_loader_criteria` reliance for Core queries — explicitly include `WHERE tenant_id = self._tenant_id` in every SELECT (Pitfall 6 / T-03-02-03 from Plan 02 inherited).
  </action>
  <verify>
    <automated>cd backend &amp;&amp; pytest tests/unit/test_daily_kpi_service.py tests/unit/test_salesperson_kpi_service.py tests/unit/test_source_kpi_service.py -x</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/app/services/metrics/daily_kpi_service.py` exists and contains text `class DailyKpiService` AND `compute_for_date` AND `func.nullif` AND `_compute_delta` AND `_fetch_prior_row` AND `timedelta(days=7)` AND `timedelta(days=30)` AND `v_mefi_leads_active`
    - File `backend/app/services/metrics/salesperson_kpi_service.py` exists and contains text `class SalespersonKpiService` AND `MefiSalesperson` AND `is_active` AND `business_minutes_between` AND `data_completeness_pct` AND `business_hours`
    - File `backend/app/services/metrics/source_kpi_service.py` exists and contains text `class SourceKpiService` AND `to_status_id` AND `24` AND `"mail_fb_ig"` AND `"telefon"` AND `"whatsapp"` AND `"site"` AND `"designer"` AND `"alte"` AND `"google"` (all 7 categories) AND `source_categories`
    - Every new file begins with `from __future__ import annotations` AND imports `from decimal import Decimal`
    - `grep -v '^#' backend/app/services/metrics/*.py | grep -c "raw_mefi_leads"` returns 0 (D-14 — services NEVER query raw_mefi_* directly)
    - `grep -v '^#' backend/app/services/metrics/*.py | grep -c "float("` returns 0 (D-16 — no float casts)
    - `cd backend &amp;&amp; pytest tests/unit/test_daily_kpi_service.py tests/unit/test_salesperson_kpi_service.py tests/unit/test_source_kpi_service.py -x` exits 0 (all Wave 0 service tests GREEN)
    - `python -c "from app.services.metrics.daily_kpi_service import DailyKpiService; from app.services.metrics.salesperson_kpi_service import SalespersonKpiService; from app.services.metrics.source_kpi_service import SourceKpiService; print('imports-ok')"` prints `imports-ok`
  </acceptance_criteria>
  <done>All three service files exist, query v_mefi_leads_active only, use Decimal throughout, properly filter by tenant_id, and all corresponding Wave 0 unit tests turn GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| service → DB Core query | Core SELECT/INSERT bypasses with_loader_criteria — services MUST add tenant_id filter explicitly |
| funnel_config read | Tenant-controlled JSONB shapes query behavior — malformed config could crash service |
| MetricsRepository INSERT | Core insert bypasses with_loader_criteria — tenant_id MUST be in row dict |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03-01 | Information Disclosure | mefi_lead_history designer subquery in SourceKpiService | mitigate | Explicit `MefiLeadHistory.tenant_id == self._tenant_id` filter in every SELECT — Core queries bypass with_loader_criteria (Pitfall 6, T-02-02 lineage) |
| T-03-03-02 | Information Disclosure | MetricsRepository INSERT | mitigate | tenant_id validation loop raises ValueError if missing from row dict (Pitfall 6) |
| T-03-03-03 | Tampering | Decimal precision in revenue / conversion rates | mitigate | All arithmetic in Decimal; integer counts cast to Numeric before division to avoid Pitfall 4 integer-division truncation; no float() calls in services (D-16) |
| T-03-03-04 | Tampering | business_hours config injection | mitigate | service falls back to hardcoded D-07 default if funnel_config.business_hours missing or malformed; Python time parsing rejects non-HH:MM strings |
| T-03-03-05 | Information Disclosure | structlog PII | accept | service binds tenant_id (UUID), kpi_date (date), service name only — no lead names/emails/phones/external_ids in log lines |
| T-03-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages — sqlalchemy, structlog, zoneinfo (stdlib) all verified per RESEARCH.md Package Legitimacy Audit |
</threat_model>

<verification>
- `from app.services.metrics.business_hours import business_minutes_between` resolves
- `from app.services.metrics.daily_kpi_service import DailyKpiService` resolves
- `from app.services.metrics.salesperson_kpi_service import SalespersonKpiService` resolves
- `from app.services.metrics.source_kpi_service import SourceKpiService` resolves
- `from app.services.repositories.metrics_repository import MetricsRepository` resolves
- `grep -v '^#' backend/app/services/metrics/*.py | grep -c "raw_mefi_leads"` returns 0 (D-14)
- `grep -v '^#' backend/app/services/metrics/*.py | grep -c "float("` returns 0 (D-16)
- `grep -v '^#' backend/app/services/metrics/source_kpi_service.py | grep -c "tenant_id"` returns ≥ 2 (explicit tenant filter on history subquery + view query)
- All Wave 0 service/util/repository tests GREEN: `pytest tests/unit/test_business_hours.py tests/unit/test_metrics_repository.py tests/unit/test_daily_kpi_service.py tests/unit/test_salesperson_kpi_service.py tests/unit/test_source_kpi_service.py -x` exits 0
</verification>

<success_criteria>
- 4 service/util files + 1 repository file + 1 package __init__ = 6 files created
- All 5 Wave 0 unit test files for services/util/repository turn GREEN
- ROADMAP SC#2 satisfied (5 conversion rates with zero-divisor guards)
- ROADMAP SC#3 satisfied (per-salesperson metrics including time_to_first_touch + data_completeness_pct)
- ROADMAP SC#5 satisfied (WoW/MoM deltas with NULL on missing prior)
- METR-02, METR-03, METR-04, METR-05, METR-06 all have service-level implementation backing
- D-01 through D-17 each have at least one code path implementing them; references to decision IDs documented in module docstrings
</success_criteria>

<output>
Create `.planning/phases/03-metrics-engine/03-03-SUMMARY.md` when done following the standard summary template.
</output>
