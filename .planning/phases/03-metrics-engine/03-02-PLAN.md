---
phase: 03-metrics-engine
plan: "02"
type: execute
wave: 1
depends_on: ["03-01"]
files_modified:
  - backend/alembic/versions/004_metrics_schema.py
  - backend/app/models/metrics/__init__.py
  - backend/app/models/metrics/daily_kpi.py
  - backend/app/models/metrics/salesperson_kpi.py
  - backend/app/models/metrics/source_kpi.py
  - backend/app/models/__init__.py
autonomous: true
requirements: [METR-01, METR-02, METR-03, METR-04, METR-05, METR-06]

must_haves:
  truths:
    - "Running `alembic upgrade head` creates daily_kpi, salesperson_daily_kpi, source_daily_kpi tables with full SPEC.md §7 schema"
    - "Migration ADDS WoW/MoM delta columns to daily_kpi (NOT in SPEC.md §7 — Schema Gap 1)"
    - "Migration ADDS data_completeness_pct to salesperson_daily_kpi (NOT in SPEC.md §7 — Schema Gap 2)"
    - "Migration UPDATES tenants.funnel_config with business_hours key (D-07, Schema Gap 4)"
    - "Migration REPLACES v_mefi_leads_active with history-based reached_visit/offer/contract booleans (D-13, Schema Gap 5)"
    - "SQLAlchemy models are discoverable by Alembic autogenerate via app/models/__init__.py export"
    - "source_daily_kpi.source is TEXT (no enum) holding 7 funnel_config category names per D-04/D-09"
    - "All metric tables have NUMERIC types for money/rates — never float (D-16)"
  artifacts:
    - path: "backend/alembic/versions/004_metrics_schema.py"
      provides: "Migration creating 3 tables + view update + funnel_config seed"
      contains: "revision: str = \"004\""
    - path: "backend/app/models/metrics/daily_kpi.py"
      provides: "DailyKpi SQLAlchemy ORM model"
      exports: ["DailyKpi"]
    - path: "backend/app/models/metrics/salesperson_kpi.py"
      provides: "SalespersonDailyKpi ORM model with data_completeness_pct"
      exports: ["SalespersonDailyKpi"]
    - path: "backend/app/models/metrics/source_kpi.py"
      provides: "SourceDailyKpi ORM model with 3-col unique (tenant_id, source, date)"
      exports: ["SourceDailyKpi"]
    - path: "backend/app/models/metrics/__init__.py"
      provides: "Re-exports all three metric model classes"
    - path: "backend/app/models/__init__.py"
      provides: "Updated to import metric models for Alembic autogenerate discovery"
      contains: "from app.models.metrics"
  key_links:
    - from: "backend/app/models/__init__.py"
      to: "backend/app/models/metrics/__init__.py"
      via: "import"
      pattern: "from app.models.metrics"
    - from: "backend/alembic/versions/004_metrics_schema.py"
      to: "tenants.funnel_config JSONB column"
      via: "UPDATE ... SET funnel_config = funnel_config || CAST(:patch AS jsonb)"
      pattern: "business_hours"
    - from: "backend/alembic/versions/004_metrics_schema.py"
      to: "mefi_lead_history table"
      via: "CREATE OR REPLACE VIEW v_mefi_leads_active ... LEFT JOIN mefi_lead_history"
      pattern: "mefi_lead_history"
---

<objective>
Build the Phase 3 schema foundation: Alembic migration 004 that creates the three metric tables (full SPEC.md §7 schema + the four documented gaps), refreshes `v_mefi_leads_active` to use history-based "ever reached" logic, seeds `business_hours` into `tenants.funnel_config`, and the matching SQLAlchemy ORM models so the metric services in Plan 03 can query them.

Purpose: Lock the persistent data contract for Phase 3 before any service or task code is written. Schema must turn the Wave 0 model and migration tests GREEN.

Output: 1 Alembic migration file + 3 model files + 1 metrics package __init__ + updated models/__init__.py.
</objective>

<execution_context>
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/CLAUDE.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/docs/CONVENTIONS.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/docs/ARCHITECTURE.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.planning/phases/03-metrics-engine/03-CONTEXT.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.planning/phases/03-metrics-engine/03-RESEARCH.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.planning/phases/03-metrics-engine/03-PATTERNS.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.planning/phases/02-mefi-etl/02-01-SUMMARY.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/alembic/versions/003_mefi_schema.py
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/app/models/mefi.py
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/app/models/__init__.py
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/app/db/base.py
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/tests/unit/test_migration_004.py
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/tests/unit/test_metrics_models.py

<interfaces>
<!-- Existing infrastructure these new files build upon. -->

From backend/app/db/base.py:
```python
class Base(DeclarativeBase): ...
class TimestampMixin:
    created_at: Mapped[datetime] = mapped_column(TIMESTAMPTZ, server_default=func.now(), nullable=False)
    updated_at: Mapped[datetime] = mapped_column(TIMESTAMPTZ, server_default=func.now(), onupdate=func.now(), nullable=False)
class TenantScopedMixin(TimestampMixin):
    id: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), primary_key=True, default=uuid4)
    tenant_id: Mapped[UUID] = mapped_column(PG_UUID(as_uuid=True), nullable=False, index=True)
TIMESTAMPTZ = DateTime(timezone=True)
```

From backend/app/models/mefi.py (existing schema being JOINed by the updated view):
```python
class RawMefiLead(Base, TenantScopedMixin):
    __tablename__ = "raw_mefi_leads"
    external_id: Mapped[str]    # TEXT NOT NULL
    status_id: Mapped[int | None]
    offer_sent_flag: Mapped[bool | None]
    # ...

class MefiLeadHistory(Base, TenantScopedMixin):
    __tablename__ = "mefi_lead_history"
    lead_external_id: Mapped[str]  # TEXT NOT NULL — joins to RawMefiLead.external_id
    to_status_id: Mapped[int]      # INT NOT NULL
    from_status_id: Mapped[int | None]
    changed_at: Mapped[datetime]   # TIMESTAMPTZ NOT NULL
```

Existing tenants.funnel_config JSONB (from migration 003 line 77-95) ALREADY contains keys: funnel_stages, offer_sent_flag_field, source_categories, lifecycle_filter, showroom_field, junk_statuses. This plan ADDS the `business_hours` key idempotently — does NOT replace existing keys.

Existing v_mefi_leads_active view (003_mefi_schema.py V_MEFI_LEADS_ACTIVE constant) uses CURRENT-STATUS-ONLY logic for reached_visit/offer/contract. Migration 004 REPLACES this view with a version that LEFT JOINs mefi_lead_history so "ever reached" booleans are correct (D-13, ROADMAP SC#1).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create SQLAlchemy metric models + register with models/__init__.py</name>
  <files>backend/app/models/metrics/__init__.py, backend/app/models/metrics/daily_kpi.py, backend/app/models/metrics/salesperson_kpi.py, backend/app/models/metrics/source_kpi.py, backend/app/models/__init__.py</files>
  <read_first>
    - backend/app/models/mefi.py (model pattern source — `class XxxModel(Base, TenantScopedMixin)`, `__table_args__ = (UniqueConstraint(...),)`, deferred imports, Mapped[T] + mapped_column, NUMERIC(12,2) for revenue)
    - backend/app/db/base.py (TenantScopedMixin already provides id+tenant_id+created_at+updated_at — do NOT redeclare those columns)
    - backend/app/models/__init__.py (current export pattern — must add metric imports in 'Phase 3 models' section)
    - .planning/phases/03-metrics-engine/03-PATTERNS.md (sections 'backend/app/models/metrics/daily_kpi.py' through 'source_kpi.py' — column lists, Mapped typing)
    - .planning/phases/03-metrics-engine/03-RESEARCH.md (Schema Gap Analysis sections 1-3, 'SPEC.md §7 Schema — Verbatim Column Reference' — authoritative list for every column)
    - SPEC.md (Section 7 — Слой 3: Aggregated metrics — DDL for all three tables; offset roughly lines 744-858)
    - backend/tests/unit/test_metrics_models.py (Wave 0 contract — UniqueConstraint names: uq_daily_kpi_tenant_date, uq_salesperson_daily_kpi_tenant_sp_date, uq_source_daily_kpi_tenant_source_date)
  </read_first>
  <behavior>
    - DailyKpi (`__tablename__ = "daily_kpi"`) contains: date (Date NOT NULL); ad-spend cols spend_meta/spend_google/spend_tiktok/spend_digital_total NUMERIC(10,2) NULL; web_sessions Int NULL, web_conversion_rate Numeric(5,4) NULL; leads_mail_fb_ig, leads_telefon, leads_whatsapp, leads_site, leads_designer, leads_alte, leads_total Int NULL; cpl_overall Numeric(8,2) NULL, cpl_by_channel JSONB NULL; visits_count, offers_count, contracts_count Int NOT NULL default 0; conversion_l_to_v, conversion_v_to_o, conversion_l_to_o, conversion_o_to_c, conversion_l_to_c Numeric(5,4) NULL; revenue Numeric(12,2) NULL, avg_deal_size Numeric(10,2) NULL, avg_deal_size_per_day Numeric(10,2) NULL, cost_acquisition_contract Numeric(12,2) NULL; cac Numeric(10,2) NULL, roas Numeric(8,2) NULL; calls_total, calls_answered, calls_missed Int NULL, avg_call_duration_seconds Int NULL, avg_sentiment_score Numeric(3,2) NULL; calculated_at TIMESTAMPTZ NOT NULL server_default=now(); WoW/MoM delta pairs Numeric(8,4) NULL for: leads_total, conversion_l_to_v, conversion_v_to_o, conversion_l_to_o, conversion_o_to_c, conversion_l_to_c, revenue, avg_deal_size (8 metrics × 2 deltas = 16 delta columns); __table_args__ contains UniqueConstraint("tenant_id","date", name="uq_daily_kpi_tenant_date")
    - SalespersonDailyKpi (`__tablename__ = "salesperson_daily_kpi"`) contains: salesperson_external_id Text NOT NULL; date Date NOT NULL; leads_assigned, leads_contacted, avg_time_to_first_touch_minutes Int NULL; visits_conducted, offers_sent, deals_won, deals_lost Int NULL; revenue Numeric(12,2) NULL; conversion_l_to_v, conversion_v_to_o, conversion_o_to_c, conversion_l_to_c Numeric(5,4) NULL; avg_deal_size Numeric(10,2) NULL; calls_made, calls_answered, avg_call_duration_seconds Int NULL; avg_sentiment_score Numeric(3,2) NULL; data_completeness_pct Numeric(5,2) NULL (Schema Gap 2 — METR-06, NOT in SPEC.md §7); calculated_at TIMESTAMPTZ NOT NULL server_default=now(); __table_args__ contains UniqueConstraint("tenant_id","salesperson_external_id","date", name="uq_salesperson_daily_kpi_tenant_sp_date")
    - SourceDailyKpi (`__tablename__ = "source_daily_kpi"`) contains: source Text NOT NULL (TEXT not enum per D-09); date Date NOT NULL; leads Int NULL; ad_spend Numeric(10,2) NULL, cpl Numeric(8,2) NULL, cac Numeric(10,2) NULL, roas Numeric(8,2) NULL; visits, offers, deals_won Int NULL; revenue Numeric(12,2) NULL; conversion_rate Numeric(5,4) NULL; calculated_at TIMESTAMPTZ NOT NULL server_default=now(); __table_args__ contains UniqueConstraint("tenant_id","source","date", name="uq_source_daily_kpi_tenant_source_date") — 3-column key per Pitfall 5
    - models/metrics/__init__.py re-exports all three classes
    - models/__init__.py adds "Phase 3 models" section importing DailyKpi, SalespersonDailyKpi, SourceDailyKpi and includes them in __all__
  </behavior>
  <action>
    Create the metrics package directory `backend/app/models/metrics/` with __init__.py re-exporting `DailyKpi`, `SalespersonDailyKpi`, `SourceDailyKpi`. Each model file follows the structural pattern of `backend/app/models/mefi.py`: `from __future__ import annotations` first line, then module docstring referencing the relevant Phase 3 plan and migration 004, then imports (`from datetime import date, datetime`, `from decimal import Decimal`, `from sqlalchemy import Date, Integer, Numeric, Text, UniqueConstraint`, `from sqlalchemy.dialects.postgresql import JSONB`, `from sqlalchemy.orm import Mapped, mapped_column`, `from app.db.base import Base, TenantScopedMixin, TIMESTAMPTZ`), then class definition. Use `Mapped[T | None]` for nullable cols, `Mapped[T]` for NOT NULL. Use `mapped_column(Numeric(12, 2), nullable=True)` for money, `mapped_column(Numeric(5, 4), nullable=True)` for conversion rates, `mapped_column(Numeric(8, 4), nullable=True)` for WoW/MoM deltas (range [-1, +infinity] for fractions), `mapped_column(Numeric(5, 2), nullable=True)` for data_completeness_pct (range 0.00-100.00 per Assumption A3). For visits_count/offers_count/contracts_count in DailyKpi use `mapped_column(Integer, nullable=False, server_default="0")` (NOT NULL with default per Phase 3 always computing these). For calculated_at use `mapped_column(TIMESTAMPTZ, server_default="now()", nullable=False)`. After all three model files exist, modify backend/app/models/__init__.py: add `# Phase 3 models — metric tables` comment section and `from app.models.metrics import DailyKpi, SalespersonDailyKpi, SourceDailyKpi  # noqa: F401`, then extend __all__ list. The `metrics/__init__.py` MUST re-export the three classes with `from app.models.metrics.daily_kpi import DailyKpi` etc. so the `from app.models.metrics import ...` aggregate import in `app/models/__init__.py` resolves cleanly.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python -c "from app.models.metrics import DailyKpi, SalespersonDailyKpi, SourceDailyKpi; print(DailyKpi.__tablename__, SalespersonDailyKpi.__tablename__, SourceDailyKpi.__tablename__)" 2>&amp;1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/app/models/metrics/__init__.py` exists
    - File `backend/app/models/metrics/daily_kpi.py` exists and contains text `class DailyKpi` AND `__tablename__ = "daily_kpi"` AND `uq_daily_kpi_tenant_date` AND `leads_total_wow_delta` AND `leads_total_mom_delta` AND `conversion_l_to_v_wow_delta` AND `conversion_l_to_c_mom_delta` AND `revenue_wow_delta` AND `avg_deal_size_mom_delta` AND `cpl_by_channel` AND `JSONB`
    - File `backend/app/models/metrics/salesperson_kpi.py` exists and contains text `class SalespersonDailyKpi` AND `__tablename__ = "salesperson_daily_kpi"` AND `uq_salesperson_daily_kpi_tenant_sp_date` AND `salesperson_external_id` AND `avg_time_to_first_touch_minutes` AND `data_completeness_pct` AND `Numeric(5, 2)`
    - File `backend/app/models/metrics/source_kpi.py` exists and contains text `class SourceDailyKpi` AND `__tablename__ = "source_daily_kpi"` AND `uq_source_daily_kpi_tenant_source_date` AND `"tenant_id"` AND `"source"` AND `"date"` within the same UniqueConstraint call
    - `backend/app/models/__init__.py` contains text `from app.models.metrics import DailyKpi, SalespersonDailyKpi, SourceDailyKpi` AND `"DailyKpi"` in __all__
    - Every new file begins with `from __future__ import annotations`
    - `cd backend &amp;&amp; pytest tests/unit/test_metrics_models.py -x` exits 0 (Wave 0 model tests now GREEN)
    - `grep -v '^#' backend/app/models/metrics/daily_kpi.py | grep -c "Numeric(8, 4)"` returns >= 16 (8 WoW + 8 MoM delta columns)
  </acceptance_criteria>
  <done>All three metric models exist, are importable as `from app.models.metrics import ...`, registered in app/models/__init__.py for Alembic autogenerate, and test_metrics_models.py passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Alembic migration 004 (3 metric tables + view update + business_hours seed)</name>
  <files>backend/alembic/versions/004_metrics_schema.py</files>
  <read_first>
    - backend/alembic/versions/003_mefi_schema.py (FULL FILE — copy structural pattern: header docstring, FUNNEL_CONFIG-style module-level dict for BUSINESS_HOURS_PATCH, V_MEFI_LEADS_ACTIVE constant, upgrade()/downgrade() functions, CAST(:patch AS jsonb) workaround for the bindparam parser pitfall)
    - .planning/phases/03-metrics-engine/03-PATTERNS.md (section '004_metrics_schema.py' — file header template, BUSINESS_HOURS_PATCH constant shape, idempotent JSONB seed via `funnel_config || CAST(:patch AS jsonb)` with `(funnel_config->>'business_hours') IS NULL` guard)
    - .planning/phases/03-metrics-engine/03-RESEARCH.md (Schema Gap Analysis Gap 5 — updated v_mefi_leads_active DDL must LEFT JOIN mefi_lead_history; D-13 "ever reached" semantics; ad-spend cols nullable per D-08)
    - backend/app/models/metrics/daily_kpi.py (column source of truth from Task 1 — migration DDL must produce identical column types, names, nullability, defaults)
    - backend/app/models/metrics/salesperson_kpi.py
    - backend/app/models/metrics/source_kpi.py
    - backend/tests/unit/test_migration_004.py (Wave 0 contract — asserts revision="004", down_revision="003", BUSINESS_HOURS_PATCH dict shape, presence of mefi_lead_history in updated view DDL, presence of delta cols, data_completeness_pct, three UNIQUE constraints)
  </read_first>
  <behavior>
    - File begins with `from __future__ import annotations` + module docstring listing all 6 upgrade steps + revision metadata block (revision="004", down_revision="003", branch_labels=None, depends_on=None)
    - Module-level `BUSINESS_HOURS_PATCH` dict equals `{"business_hours": {"days": [0,1,2,3,4,5,6], "open": "09:00", "close": "19:00", "tz": "Europe/Bucharest"}}` (D-07)
    - Module-level `V_MEFI_LEADS_ACTIVE_V004` constant: CREATE OR REPLACE VIEW v_mefi_leads_active with explicit column list (no SELECT *) + LEFT JOIN to a derived subquery `(SELECT lead_external_id, BOOL_OR(to_status_id IN (17,3,1)) AS hist_reached_visit, BOOL_OR(to_status_id IN (3,1)) AS hist_reached_offer FROM mefi_lead_history WHERE tenant_id = r.tenant_id GROUP BY lead_external_id) h ON h.lead_external_id = r.external_id` — equivalent SQL is acceptable as long as reached_visit/offer/contract are computed as (current_status_matches OR ever_in_history). reached_contract remains `r.status_id = 1` (only WON counts). Keep created_at_local, status_changed_at_local AT TIME ZONE 'Europe/Bucharest' computed columns. Keep `WHERE r.lifecycle IN ('active','lost')`. Comment block: "D-13: ever-reached funnel logic via history JOIN — supersedes 003 current-status-only approximation"
    - Module-level `V_MEFI_LEADS_ACTIVE_V003` constant: copy the EXACT V_MEFI_LEADS_ACTIVE DDL from 003_mefi_schema.py verbatim so downgrade() can restore it
    - upgrade() steps in order: (1) op.create_table("daily_kpi", ...) with all columns from DailyKpi model + ForeignKeyConstraint(tenant_id->tenants.id) + replicate TenantScopedMixin columns (id UUID PK, tenant_id UUID NOT NULL, created_at TIMESTAMPTZ server_default now() NOT NULL, updated_at TIMESTAMPTZ server_default now() NOT NULL) — migration is standalone DDL per 003 pattern; (2) op.create_table("salesperson_daily_kpi", ...) including data_completeness_pct Numeric(5,2) NULL; (3) op.create_table("source_daily_kpi", ...) with source Text NOT NULL; (4) op.create_index("ix_daily_kpi_tenant_date", "daily_kpi", ["tenant_id","date"]), op.create_index("ix_salesperson_daily_kpi_tenant_date", ...), op.create_index("ix_source_daily_kpi_tenant_date", ...); (5) op.create_unique_constraint("uq_daily_kpi_tenant_date","daily_kpi",["tenant_id","date"]), op.create_unique_constraint("uq_salesperson_daily_kpi_tenant_sp_date","salesperson_daily_kpi",["tenant_id","salesperson_external_id","date"]), op.create_unique_constraint("uq_source_daily_kpi_tenant_source_date","source_daily_kpi",["tenant_id","source","date"]); (6) op.execute("DROP VIEW IF EXISTS v_mefi_leads_active") then op.execute(V_MEFI_LEADS_ACTIVE_V004) — replace history-less view from 003 (Gap 5); (7) Idempotent JSONB seed: `UPDATE tenants SET funnel_config = funnel_config || CAST(:patch AS jsonb) WHERE slug = 'sofa-belle' AND (funnel_config->>'business_hours') IS NULL` with `.bindparams(patch=json.dumps(BUSINESS_HOURS_PATCH))`
    - downgrade() steps in reverse: remove business_hours key (`SET funnel_config = funnel_config - 'business_hours'`), DROP VIEW IF EXISTS v_mefi_leads_active + recreate V_MEFI_LEADS_ACTIVE_V003, drop unique constraints, drop indexes, drop tables in order source_daily_kpi → salesperson_daily_kpi → daily_kpi
  </behavior>
  <action>
    Copy the structural skeleton of `backend/alembic/versions/003_mefi_schema.py` verbatim (header docstring, revision/down_revision/branch_labels/depends_on block, module-level constants section, upgrade/downgrade functions). Replace revision="003" with revision="004" and down_revision="002" with down_revision="003". Use the JSONB seed pattern documented in 003 line 425-438 — `CAST(:patch AS jsonb)` NOT `:patch::jsonb` (named-bindparam parser pitfall is documented in the 003 file comment block and was a real bug fixed in commit 3df06f26). For the updated view, build V_MEFI_LEADS_ACTIVE_V004 as a multiline triple-quoted string. The history JOIN should use a correlated subquery or a LEFT JOIN to `mefi_lead_history h ON h.tenant_id = r.tenant_id AND h.lead_external_id = r.external_id` aggregated via GROUP BY — produce one row per raw_mefi_leads row even when no history rows exist. `reached_visit := COALESCE(BOOL_OR(h.to_status_id = 17 OR h.to_status_id = 3 OR h.to_status_id = 1), false) OR r.status_id IN (17,3,1)` — the OR-with-current-status ensures a lead currently AT status 17 with no history row still resolves to reached_visit=true. Same logic for reached_offer (statuses 3,1) and reached_contract (status 1 only). Capture V_MEFI_LEADS_ACTIVE_V003 by copying the exact string from 003_mefi_schema.py (lines 104-163) into a module-level constant in 004 so downgrade restores the old view. Use ForeignKeyConstraint(["tenant_id"], ["tenants.id"], name="fk_daily_kpi_tenant_id") and equivalent for the other two tables. Replicate the TenantScopedMixin columns explicitly in each op.create_table call (id UUID PK default uuid_generate or just `nullable=False` without server_default — Phase 1 migration 001 sets `default=uuid4` at the SQLAlchemy ORM layer; the migration uses `nullable=False` and the application provides UUIDs on INSERT — match 003 pattern exactly). The seed UPDATE WHERE clause uses `(funnel_config->>'business_hours') IS NULL` so re-running `alembic upgrade head` does not overwrite manually edited configs.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python -c "import ast; t=ast.parse(open('alembic/versions/004_metrics_schema.py').read()); print('parsed', len([n for n in ast.walk(t) if isinstance(n, ast.FunctionDef)]))" &amp;&amp; pytest tests/unit/test_migration_004.py -x</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/alembic/versions/004_metrics_schema.py` exists
    - File contains text `revision: str = "004"` AND `down_revision: Union[str, None] = "003"`
    - File contains text `BUSINESS_HOURS_PATCH = {` AND `"days": [0, 1, 2, 3, 4, 5, 6]` AND `"open": "09:00"` AND `"close": "19:00"` AND `"tz": "Europe/Bucharest"`
    - File contains text `op.create_table(` AND `"daily_kpi"` AND `"salesperson_daily_kpi"` AND `"source_daily_kpi"`
    - File contains text `leads_total_wow_delta` AND `leads_total_mom_delta` AND `conversion_l_to_v_wow_delta` AND `conversion_l_to_c_mom_delta` AND `revenue_wow_delta` AND `avg_deal_size_mom_delta`
    - File contains text `data_completeness_pct` AND `Numeric(5, 2)`
    - File contains text `uq_daily_kpi_tenant_date` AND `uq_salesperson_daily_kpi_tenant_sp_date` AND `uq_source_daily_kpi_tenant_source_date`
    - File contains text `CREATE OR REPLACE VIEW v_mefi_leads_active` AND `mefi_lead_history` (Gap 5 view replacement)
    - File contains text `CAST(:patch AS jsonb)` AND `(funnel_config->>'business_hours') IS NULL` (idempotent seed guard)
    - File contains text `V_MEFI_LEADS_ACTIVE_V003` (downgrade restore constant)
    - `cd backend &amp;&amp; pytest tests/unit/test_migration_004.py -x` exits 0
    - `cd backend &amp;&amp; python -c "import ast; ast.parse(open('alembic/versions/004_metrics_schema.py').read())"` exits 0
    - `grep -v '^#' backend/alembic/versions/004_metrics_schema.py | grep -c "op.create_table"` returns exactly 3
  </acceptance_criteria>
  <done>Migration 004 file exists, parses, passes all Wave 0 migration AST tests, and (when run via `alembic upgrade head` inside Docker) produces the three metric tables + the history-joined view + the business_hours seed in funnel_config.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| alembic→postgres | Migration runs DDL against the live DB as a privileged role |
| tenants.funnel_config UPDATE | JSONB merge may overwrite tenant-owned config if guard absent |
| view → query layer | v_mefi_leads_active is consumed by metric services; SQL injection here propagates to all consumers |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Tampering | tenants.funnel_config seed | mitigate | Idempotent guard `AND (funnel_config->>'business_hours') IS NULL` prevents overwriting manual edits across repeat migrations (Pattern from 003 T-02-01) |
| T-03-02-02 | Information Disclosure | v_mefi_leads_active view | mitigate | View explicitly SELECTs r.tenant_id — every downstream query MUST filter `WHERE tenant_id = :tid` per CLAUDE.md Principle #3 (T-02-02 inherited from 003) |
| T-03-02-03 | Information Disclosure | mefi_lead_history JOIN in updated view | mitigate | JOIN condition includes `h.tenant_id = r.tenant_id` — prevents cross-tenant history bleed via the view |
| T-03-02-04 | Tampering | NUMERIC precision for money/rates | mitigate | All money cols NUMERIC(12,2), rates NUMERIC(5,4), deltas NUMERIC(8,4), completeness NUMERIC(5,2) — no FLOAT/REAL/DOUBLE PRECISION anywhere in migration (D-16, T-02-03 inherited) |
| T-03-02-05 | Denial of Service | metric table growth | accept | One row per tenant per day per dimension — bounded growth (Sofa Belle ≤ 365 daily_kpi/year + 365*6 salesperson_kpi + 365*7 source_kpi); index on (tenant_id, date) covers query plans |
| T-03-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan — alembic, sqlalchemy already verified per RESEARCH.md Package Legitimacy Audit (9/9 OK) |
</threat_model>

<verification>
- All three metric models importable via `from app.models.metrics import DailyKpi, SalespersonDailyKpi, SourceDailyKpi`
- `backend/app/models/__init__.py` exports them so Alembic autogenerate sees them (verified by 003 inheriting same pattern)
- Migration 004 parses as valid Python AST
- Wave 0 tests `test_metrics_models.py` and `test_migration_004.py` both turn GREEN
- View update preserves backward compatibility: existing Phase 2 sync code reads `v_mefi_leads_active` and gets the same columns plus corrected reached_* booleans
- `grep -v '^#' backend/alembic/versions/004_metrics_schema.py | grep -c "FLOAT\|REAL\|DOUBLE PRECISION"` returns 0 (Decimal-only per D-16)
</verification>

<success_criteria>
- 3 model files + 1 metrics __init__ + updated models/__init__.py + 1 migration file (total 5 new files, 1 modified)
- Wave 0 tests `pytest backend/tests/unit/test_metrics_models.py backend/tests/unit/test_migration_004.py -x` exit 0
- `alembic upgrade head` (run in Docker, manual verification) creates 3 tables + replaces view + seeds business_hours WITHOUT errors and is idempotent on re-run
- ROADMAP SC#1 column scaffolding ready: source_daily_kpi has 3-col unique key supporting "7 rows per day per tenant"
- ROADMAP SC#5 column scaffolding ready: daily_kpi has wow/mom delta columns for all required metrics
- ROADMAP SC#3 column ready: salesperson_daily_kpi has data_completeness_pct
- ROADMAP SC#6 enabled: updated view applies AT TIME ZONE 'Europe/Bucharest' in computed columns
</success_criteria>

<output>
Create `.planning/phases/03-metrics-engine/03-02-SUMMARY.md` when done following the standard summary template.
</output>
