---
phase: 03-metrics-engine
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/tests/unit/test_daily_kpi_service.py
  - backend/tests/unit/test_salesperson_kpi_service.py
  - backend/tests/unit/test_source_kpi_service.py
  - backend/tests/unit/test_business_hours.py
  - backend/tests/unit/test_metrics_repository.py
  - backend/tests/unit/test_calculate_daily_kpis.py
  - backend/tests/unit/test_migration_004.py
  - backend/tests/unit/test_metrics_models.py
  - backend/tests/factories/__init__.py
  - backend/tests/factories/metrics_factory.py
autonomous: true
requirements: [METR-01, METR-02, METR-03, METR-04, METR-05, METR-06]

must_haves:
  truths:
    - "All 8 Phase 3 test files exist as discoverable pytest modules"
    - "Test files import the to-be-built service/repo/model symbols and fail at import or assertion (RED stage of TDD)"
    - "metrics_factory.py provides DailyKpiRowFactory, SalespersonKpiRowFactory, SourceKpiRowFactory and helper builders"
    - "Every locked decision D-01..D-12 has at least one assertion that will lock the contract"
  artifacts:
    - path: "backend/tests/unit/test_daily_kpi_service.py"
      provides: "Conversion-rate, zero-division, WoW/MoM delta test scaffolds"
      min_lines: 60
    - path: "backend/tests/unit/test_salesperson_kpi_service.py"
      provides: "Per-salesperson, time_to_first_touch, data_completeness_pct test scaffolds"
      min_lines: 60
    - path: "backend/tests/unit/test_source_kpi_service.py"
      provides: "Source categorization (7 cats), designer detection test scaffolds"
      min_lines: 50
    - path: "backend/tests/unit/test_business_hours.py"
      provides: "Business-hours arithmetic edge case scaffolds (DST, out-of-hours, end<=start)"
      min_lines: 50
    - path: "backend/tests/unit/test_metrics_repository.py"
      provides: "UPSERT idempotency, tenant_id validation, 3-col conflict target scaffolds"
      min_lines: 60
    - path: "backend/tests/unit/test_calculate_daily_kpis.py"
      provides: "Task entry-point, retry, date defaulting (D-10/D-12) scaffolds"
      min_lines: 40
    - path: "backend/tests/unit/test_migration_004.py"
      provides: "AST-based migration validation (revision chain, table list, columns)"
      min_lines: 60
    - path: "backend/tests/unit/test_metrics_models.py"
      provides: "Model __tablename__ + UniqueConstraint scaffolds"
      min_lines: 30
    - path: "backend/tests/factories/metrics_factory.py"
      provides: "Factory-boy builders for metric row dicts"
      min_lines: 60
  key_links:
    - from: "backend/tests/unit/test_*.py"
      to: "backend/tests/conftest.py"
      via: "TEST_TENANT_ID UUID('00000000-0000-0000-0000-000000000001')"
      pattern: "TEST_TENANT_ID|TENANT_ID = UUID"
    - from: "backend/tests/factories/metrics_factory.py"
      to: "backend/tests/factories/mefi_factory.py"
      via: "factory.Factory pattern import"
      pattern: "import factory"
---

<objective>
Create all eight RED-state pytest test files and the metrics factory module that Phase 3 service, repository, task, and migration implementations must satisfy. This is the Wave 0 / Nyquist gate: every downstream task in Plan 02/03/04 ships only when these tests turn GREEN.

Purpose: Lock the contract for Phase 3 surface area before any implementation file exists, so executor agents have concrete assertions to drive RED→GREEN cycles and no service is built without a test file already failing for the right reason.

Output: 8 test files under `backend/tests/unit/` (importable but failing) plus `backend/tests/factories/metrics_factory.py` + `backend/tests/factories/__init__.py`.
</objective>

<execution_context>
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/CLAUDE.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/docs/CONVENTIONS.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.planning/phases/03-metrics-engine/03-CONTEXT.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.planning/phases/03-metrics-engine/03-VALIDATION.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.planning/phases/03-metrics-engine/03-PATTERNS.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/tests/conftest.py
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/tests/unit/test_mefi_repository.py
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/tests/unit/test_migration_003.py
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/tests/factories/mefi_factory.py

<interfaces>
<!-- Symbols the test files will import. These do NOT exist yet — that is the point (RED). -->

Future modules to be built in Plans 02/03/04:

```
# Plan 02 (models + migration):
from app.models.metrics.daily_kpi import DailyKpi
from app.models.metrics.salesperson_kpi import SalespersonDailyKpi
from app.models.metrics.source_kpi import SourceDailyKpi

# Plan 03 (services + repository + business_hours util):
from app.services.metrics.business_hours import business_minutes_between
from app.services.metrics.daily_kpi_service import DailyKpiService
from app.services.metrics.salesperson_kpi_service import SalespersonKpiService
from app.services.metrics.source_kpi_service import SourceKpiService
from app.services.repositories.metrics_repository import MetricsRepository

# Plan 04 (Celery task + chain wiring):
from app.tasks.etl.calculate_daily_kpis import calculate_daily_kpis, _calc_async
from app.tasks.etl.sync_mefi_leads import daily_pipeline
```

Existing infrastructure tests will reuse:
- `TEST_TENANT_ID = UUID("00000000-0000-0000-0000-000000000001")` from backend/tests/conftest.py
- AsyncMock / MagicMock pattern from test_mefi_repository.py
- AST-inspection pattern (_load_xxx_via_ast, _get_revision_value) from test_migration_003.py
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create service-layer test stubs + business-hours unit tests + metrics factory</name>
  <files>backend/tests/unit/test_daily_kpi_service.py, backend/tests/unit/test_salesperson_kpi_service.py, backend/tests/unit/test_source_kpi_service.py, backend/tests/unit/test_business_hours.py, backend/tests/factories/__init__.py, backend/tests/factories/metrics_factory.py</files>
  <read_first>
    - backend/tests/unit/test_mefi_repository.py (pattern source — _make_repo helper, TestXxx class grouping, AsyncMock pattern, MagicMock for SELECT results)
    - backend/tests/factories/mefi_factory.py (pattern source — factory.Factory subclass with Meta, LazyFunction, Sequence; make_xxx helper functions at module level)
    - backend/tests/conftest.py (TEST_TENANT_ID constant — replicate exact UUID '00000000-0000-0000-0000-000000000001' in test files)
    - .planning/phases/03-metrics-engine/03-VALIDATION.md (Critical Edge Case Tests table — test names map 1:1 to required tests)
    - .planning/phases/03-metrics-engine/03-PATTERNS.md (section 'backend/tests/factories/metrics_factory.py' — DailyKpiRowFactory + make_seven_source_rows examples)
    - .planning/phases/03-metrics-engine/03-CONTEXT.md (D-04 seven categories: mail_fb_ig | telefon | whatsapp | site | designer | alte | google; D-07 business hours Mon-Sun 09:00-19:00)
  </read_first>
  <behavior>
    - test_daily_kpi_service.py contains: TestConversionRates::test_conversion_rate_zero_denominator (METR-02 SC#2), TestWoWMoMDeltas::test_wow_delta_null_when_no_prior_row (METR-05, D-11), TestWoWMoMDeltas::test_mom_delta_null_when_prior_is_zero (D-11), TestWoWMoMDeltas::test_delta_precision_numeric (SC#4)
    - test_salesperson_kpi_service.py contains: TestPerSalespersonMetrics::test_inactive_salesperson_excluded (METR-04, D-17), TestTimeToFirstTouch::test_no_history_returns_null (METR-04, D-05), TestDataCompletenessPct::test_data_completeness_pct (METR-06)
    - test_source_kpi_service.py contains: TestSourceCategorization::test_seven_source_rows_per_day (METR-03, D-04 — 7 categories asserted), TestDesignerDetection::test_designer_category_overrides_source_id (METR-03, D-02), TestSourceCategorization::test_tiktok_folded_into_mail_fb_ig (D-03)
    - test_business_hours.py contains: test_lead_created_inside_hours_first_touch_same_window, test_lead_created_outside_hours_first_touch_next_window (D-07 Mon-Sun), test_lead_created_at_close (1-minute case), test_end_before_start_returns_zero, test_dst_spring_forward_bucharest
    - metrics_factory.py exports: DailyKpiRowFactory, SalespersonKpiRowFactory, SourceKpiRowFactory (all `Meta.model = dict`) plus make_designer_source_row(kpi_date, leads) and make_seven_source_rows(kpi_date) returning list of 7 dicts in the exact order ['mail_fb_ig','telefon','whatsapp','site','designer','alte','google']
    - All test files use `TENANT_ID = UUID("00000000-0000-0000-0000-000000000001")` at module level
    - All test files import the future service/util modules — imports will fail at collection (RED) until Plan 03 lands
  </behavior>
  <action>
    Create all six files following CONVENTIONS.md (from __future__ import annotations, pytest.mark.asyncio for async tests, ruff format line length 100). Mirror the structural pattern from test_mefi_repository.py: module docstring + Requirements line + module-level TENANT_ID + _make_service helper + TestXxx classes grouping tests by behavior. For tests of methods that do not yet exist, wrap the import inside a `def _make_service(mock_session=None)` helper so the file parses but tests fail at AsyncMock invocation (RED). Each test method MUST contain a real assertion calling the (not-yet-built) service method — no `pass`-only bodies. Use AsyncMock for AsyncSession, MagicMock with __iter__ for select() result mocking (pattern from test_mefi_repository.py lines 134-140). For test_business_hours.py the import `from app.services.metrics.business_hours import business_minutes_between` is real-imported at module top (not deferred) — this makes the file RED at collection, which is desired. For metrics_factory.py use `factory.Factory` with `class Meta: model = dict`, populate with realistic Sofa Belle defaults (leads_total ~10-30/day, revenue Decimal("15000.00"), date=date(2026,5,24)). Reference all D-XX decisions explicitly in module docstrings (e.g. "Tests D-04 — 7 source categories per day"). Create backend/tests/factories/__init__.py as empty file if it does not exist. Do NOT create __init__.py for backend/tests/unit/ — it already exists.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python -c "import ast; [ast.parse(open(p).read()) for p in ['tests/unit/test_daily_kpi_service.py','tests/unit/test_salesperson_kpi_service.py','tests/unit/test_source_kpi_service.py','tests/unit/test_business_hours.py','tests/factories/metrics_factory.py']]; print('ast-parse-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/tests/unit/test_daily_kpi_service.py` exists and contains text `TestConversionRates` AND `TestWoWMoMDeltas` AND `test_conversion_rate_zero_denominator` AND `test_wow_delta_null_when_no_prior_row`
    - File `backend/tests/unit/test_salesperson_kpi_service.py` exists and contains text `test_inactive_salesperson_excluded` AND `test_data_completeness_pct`
    - File `backend/tests/unit/test_source_kpi_service.py` exists and contains text `test_seven_source_rows_per_day` AND `test_designer_category_overrides_source_id`
    - File `backend/tests/unit/test_business_hours.py` exists and contains text `test_dst_spring_forward_bucharest` AND `test_end_before_start_returns_zero` AND `from app.services.metrics.business_hours import business_minutes_between`
    - File `backend/tests/factories/metrics_factory.py` exists and contains text `class DailyKpiRowFactory` AND `class SalespersonKpiRowFactory` AND `class SourceKpiRowFactory` AND `def make_seven_source_rows`
    - File `backend/tests/factories/__init__.py` exists (empty file acceptable)
    - Every file begins with `from __future__ import annotations`
    - `python -c "import ast; ast.parse(open('backend/tests/unit/test_daily_kpi_service.py').read())"` exits 0 (and same for the other 4 files)
    - Running `cd backend &amp;&amp; pytest tests/unit/test_daily_kpi_service.py tests/unit/test_source_kpi_service.py tests/unit/test_business_hours.py --collect-only 2>&amp;1 | tail -5` shows ImportError or collected-but-failing tests (RED — services not built yet)
  </acceptance_criteria>
  <done>All six files exist, parse as Python, are discoverable by pytest, and FAIL at collection or assertion because target service/util modules do not yet exist (RED).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create repository, task, migration, and model test stubs</name>
  <files>backend/tests/unit/test_metrics_repository.py, backend/tests/unit/test_calculate_daily_kpis.py, backend/tests/unit/test_migration_004.py, backend/tests/unit/test_metrics_models.py</files>
  <read_first>
    - backend/tests/unit/test_mefi_repository.py (pattern for repository tests — _make_repo, TestBulkUpsertLeads class)
    - backend/tests/unit/test_migration_003.py (pattern for migration tests — _load_xxx_via_ast helpers, MIGRATION_PATH, AST inspection avoids alembic import in system Python)
    - backend/tests/unit/test_sync_mefi_leads.py (pattern for Celery task tests — name assertion, retry config assertion, mocked asyncio.run)
    - backend/tests/unit/test_mefi_models.py (pattern for model tests — __tablename__, UniqueConstraint name assertion)
    - .planning/phases/03-metrics-engine/03-RESEARCH.md (Schema Gap Analysis — Phase 3 migration MUST ADD wow/mom delta cols + data_completeness_pct + business_hours seed + view update with history JOIN)
    - .planning/phases/03-metrics-engine/03-PATTERNS.md (sections 'metrics_repository.py' and 'calculate_daily_kpis.py' — UPSERT 3-col conflict target for SourceDailyKpi (Pitfall 5), daily_pipeline() chain extension)
  </read_first>
  <behavior>
    - test_metrics_repository.py contains: TestUpsertDailyKpi::test_raises_when_tenant_id_missing (Pitfall 6), TestUpsertSalespersonKpi::test_uses_three_column_conflict_target (tenant_id+salesperson_external_id+date), TestUpsertSourceKpi::test_source_uses_three_column_conflict_target (Pitfall 5 — tenant_id+source+date), test_upsert_idempotent_same_date (METR-01 SC#1 — same row count after double-upsert)
    - test_calculate_daily_kpis.py contains: test_task_name_is_tasks_etl_calculate_daily_kpis (task.name == "tasks.etl.calculate_daily_kpis"), test_signature_accepts_optional_calculation_date (D-12), test_default_calculation_date_is_yesterday_bucharest (D-10), test_retry_config (autoretry_for=(Exception,), max_retries=3)
    - test_migration_004.py contains AST-based checks: test_revision_is_004, test_down_revision_is_003, test_creates_three_metric_tables (daily_kpi, salesperson_daily_kpi, source_daily_kpi present in upgrade source), test_creates_wow_mom_delta_columns (leads_total_wow_delta, leads_total_mom_delta, conversion_l_to_v_wow_delta etc. all present), test_creates_data_completeness_pct (data_completeness_pct present in salesperson_daily_kpi block), test_business_hours_patch_dict (BUSINESS_HOURS_PATCH dict literal asserts days=[0..6], open=09:00, close=19:00, tz=Europe/Bucharest), test_replaces_v_mefi_leads_active_with_history_join (Gap 5 — DDL string contains 'mefi_lead_history' join)
    - test_metrics_models.py contains: test_daily_kpi_tablename, test_daily_kpi_unique_constraint_two_cols (tenant_id+date), test_salesperson_kpi_unique_constraint_three_cols (tenant_id+salesperson_external_id+date), test_source_kpi_unique_constraint_three_cols (tenant_id+source+date)
  </behavior>
  <action>
    Mirror test_mefi_repository.py + test_migration_003.py + test_sync_mefi_leads.py + test_mefi_models.py exactly. For test_migration_004.py copy the _load_funnel_config_via_ast / _get_revision_value pattern from test_migration_003.py verbatim — set MIGRATION_PATH = Path(__file__).parent.parent.parent / "alembic" / "versions" / "004_metrics_schema.py" and parse the file via ast.parse(MIGRATION_PATH.read_text()) so tests run without alembic installed (same environmental constraint documented in 02-01-SUMMARY.md). For migration tests, use `source_text = MIGRATION_PATH.read_text()` and assertions like `assert "leads_total_wow_delta" in source_text` to check column presence at the source-text level (AST table-introspection is too brittle for op.create_table multi-arg calls). For test_calculate_daily_kpis.py mock asyncio.run via @patch decorator and use `from app.tasks.etl.calculate_daily_kpis import calculate_daily_kpis` inside test bodies wrapped in try/except ImportError so the test file parses but tests fail RED. The retry-config test reads task.autoretry_for and task.max_retries attributes from the Celery @task-decorated function. For test_metrics_models.py copy structural pattern from test_mefi_models.py — import models inside try/except for RED tolerance, assert __tablename__ string, then iterate __table_args__ tuple looking for UniqueConstraint by name (`uq_daily_kpi_tenant_date`, `uq_salesperson_daily_kpi_tenant_sp_date`, `uq_source_daily_kpi_tenant_source_date`). Use TENANT_ID = UUID("00000000-0000-0000-0000-000000000001") at module level in repository and task test files.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; python -c "import ast; [ast.parse(open(p).read()) for p in ['tests/unit/test_metrics_repository.py','tests/unit/test_calculate_daily_kpis.py','tests/unit/test_migration_004.py','tests/unit/test_metrics_models.py']]; print('ast-parse-ok')"</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/tests/unit/test_metrics_repository.py` exists and contains text `test_raises_when_tenant_id_missing` AND `test_source_uses_three_column_conflict_target` AND `test_upsert_idempotent_same_date`
    - File `backend/tests/unit/test_calculate_daily_kpis.py` exists and contains text `tasks.etl.calculate_daily_kpis` AND `test_default_calculation_date_is_yesterday_bucharest` AND `test_signature_accepts_optional_calculation_date`
    - File `backend/tests/unit/test_migration_004.py` exists and contains text `MIGRATION_PATH` AND `004_metrics_schema.py` AND `leads_total_wow_delta` AND `data_completeness_pct` AND `BUSINESS_HOURS_PATCH` AND `mefi_lead_history` (for Gap 5 view-join assertion)
    - File `backend/tests/unit/test_metrics_models.py` exists and contains text `uq_daily_kpi_tenant_date` AND `uq_salesperson_daily_kpi_tenant_sp_date` AND `uq_source_daily_kpi_tenant_source_date`
    - Every file begins with `from __future__ import annotations`
    - `python -c "import ast; ast.parse(open('backend/tests/unit/test_migration_004.py').read())"` exits 0
    - Running `cd backend &amp;&amp; pytest tests/unit/test_migration_004.py --collect-only 2>&amp;1 | grep -E 'collected|error'` shows pytest collected the tests (even though the migration file does not exist yet — AST helper returns None and tests fail with informative pytest.fail)
  </acceptance_criteria>
  <done>All four files exist, parse as Python, register with pytest collection, and FAIL because target migration/models/repository/task do not yet exist (RED).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem→pytest | Test files are read by pytest; malicious code injected here runs in the dev environment |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | test files | accept | All test files committed via Conventional Commits and reviewed via git diff — no remote test loading |
| T-03-01-SC | Tampering | npm/pip/cargo installs | accept | No new packages installed in this plan — factory-boy 3.3.3 already installed per RESEARCH.md Package Legitimacy Audit (9/9 OK) |
</threat_model>

<verification>
- All 8 test files exist under backend/tests/unit/ and backend/tests/factories/
- Every file is valid Python (ast.parse exits 0)
- pytest discovers the tests (`pytest --collect-only`)
- Tests fail RED (because Plan 02/03/04 implementations are not yet built)
- Every D-XX decision from CONTEXT.md has at least one assertion locking the contract
</verification>

<success_criteria>
- 8 unit test files + 1 factory file + tests/factories/__init__.py created
- `cd backend && python -c "import ast; [ast.parse(open(p).read()) for p in ['tests/unit/test_daily_kpi_service.py','tests/unit/test_salesperson_kpi_service.py','tests/unit/test_source_kpi_service.py','tests/unit/test_business_hours.py','tests/unit/test_metrics_repository.py','tests/unit/test_calculate_daily_kpis.py','tests/unit/test_migration_004.py','tests/unit/test_metrics_models.py','tests/factories/metrics_factory.py']]; print('ALL OK')"` prints `ALL OK`
- All test files declare `TENANT_ID = UUID("00000000-0000-0000-0000-000000000001")` (matches conftest.py TEST_TENANT_ID)
- Critical edge case tests from 03-VALIDATION.md table are all present as test method names
</success_criteria>

<output>
Create `.planning/phases/03-metrics-engine/03-01-SUMMARY.md` when done following the standard summary template.
</output>
