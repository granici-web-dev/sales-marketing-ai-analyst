---
phase: 03-metrics-engine
plan: "04"
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03"]
files_modified:
  - backend/app/tasks/etl/calculate_daily_kpis.py
  - backend/app/tasks/etl/sync_mefi_leads.py
  - backend/app/tasks/celery_app.py
autonomous: true
requirements: [METR-01]

must_haves:
  truths:
    - "`celery -A app.tasks.celery_app inspect registered | grep calculate_daily_kpis` shows the task is loaded by the worker"
    - "Calling calculate_daily_kpis(tenant_id) with no calculation_date defaults to yesterday in Europe/Bucharest (D-10)"
    - "Calling calculate_daily_kpis(tenant_id, '2026-05-20') computes KPIs for that explicit date (D-12 backfill)"
    - "Task writes daily_kpi (1 row), salesperson_daily_kpi (N rows, N = active rep count), source_daily_kpi (7 rows per D-04) — total > 0 rows per successful run"
    - "Task writes one SyncRun row with source='metrics', status='success', records_synced=total rows written, duration_ms set"
    - "Re-running the task for the same date produces NO duplicate rows (UPSERT idempotency per METR-01 SC#1)"
    - "daily_pipeline() in sync_mefi_leads.py chains sync_mefi_leads.si() → calculate_daily_kpis.si() (D-18 PIPE-01)"
    - "ETL failure halts the chain — calculate_daily_kpis does not run if sync_mefi_leads fails (PIPE-02)"
    - "Task uses NullPool engine per invocation + deferred imports per Pitfall 2 / INFRA-05"
    - "calculation_date parameter parsed as Python `date` object before query construction (T-03-04-02 — SQL injection mitigation)"
  artifacts:
    - path: "backend/app/tasks/etl/calculate_daily_kpis.py"
      provides: "calculate_daily_kpis Celery task entry point"
      contains: "tasks.etl.calculate_daily_kpis"
      exports: ["calculate_daily_kpis", "_calc_async"]
    - path: "backend/app/tasks/sync_mefi_leads.py (modified)"
      provides: "daily_pipeline() chain extension"
      contains: "calculate_daily_kpis.si"
    - path: "backend/app/tasks/celery_app.py (modified)"
      provides: "include list extended with calculate_daily_kpis task module"
      contains: "app.tasks.etl.calculate_daily_kpis"
  key_links:
    - from: "backend/app/tasks/etl/calculate_daily_kpis.py"
      to: "DailyKpiService / SalespersonKpiService / SourceKpiService"
      via: "deferred import + asyncio.run(_calc_async(...))"
      pattern: "from app.services.metrics"
    - from: "backend/app/tasks/etl/calculate_daily_kpis.py"
      to: "MetricsRepository"
      via: "upsert_daily_kpi / upsert_salesperson_kpis / upsert_source_kpis"
      pattern: "MetricsRepository"
    - from: "backend/app/tasks/etl/sync_mefi_leads.py daily_pipeline()"
      to: "calculate_daily_kpis Celery task"
      via: "chain(sync_mefi_leads.si(tenant_id), calculate_daily_kpis.si(tenant_id))"
      pattern: "calculate_daily_kpis.si"
---

<objective>
Wire the Phase 3 metric services + repository into a Celery task that runs as the second link in the daily ETL chain. The task computes daily_kpi, salesperson_daily_kpi, and source_daily_kpi rows for a target date (default = yesterday in Bucharest tz) and UPSERTs them atomically. Final wave of Phase 3.

Purpose: Make METR-01 SC#1 true end-to-end — Celery beat triggers sync at 04:00, chain executes ETL then immediately calls calculate_daily_kpis, all three metric tables populated for the previous day, downstream Phase 4 anomaly detection can read these tables tomorrow morning. Re-runs are idempotent.

Output: 1 new Celery task file + 2 modified files (sync_mefi_leads chain extension + celery_app include list).
</objective>

<execution_context>
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/CLAUDE.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/docs/CONVENTIONS.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/docs/ARCHITECTURE.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.planning/phases/03-metrics-engine/03-CONTEXT.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.planning/phases/03-metrics-engine/03-RESEARCH.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.planning/phases/03-metrics-engine/03-PATTERNS.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.planning/phases/02-mefi-etl/02-03-SUMMARY.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/app/tasks/celery_app.py
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/app/tasks/etl/sync_mefi_leads.py
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/app/services/metrics/daily_kpi_service.py
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/app/services/metrics/salesperson_kpi_service.py
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/app/services/metrics/source_kpi_service.py
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/app/services/repositories/metrics_repository.py
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/tests/unit/test_calculate_daily_kpis.py
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend/tests/unit/test_sync_mefi_leads.py

<interfaces>
<!-- Existing infrastructure this task wires together. -->

From backend/app/tasks/celery_app.py (current state — must be MODIFIED):
```python
celery_app = Celery(
    "sales_analyst",
    broker=settings.redis_url,
    backend=settings.redis_url,
    include=[
        "app.tasks",
        "app.tasks.etl.sync_mefi_leads",
        "app.tasks.etl.backfill_mefi_leads",
        # ↑ Plan 04 ADDS: "app.tasks.etl.calculate_daily_kpis"
    ],
)
```

From backend/app/tasks/etl/sync_mefi_leads.py daily_pipeline() (lines 311-321 — must be MODIFIED):
```python
def daily_pipeline(tenant_id: str) -> object:
    """Phase 2: chain only contains sync_mefi_leads.
    Phase 3 will extend this chain with calculate_metrics.si() etc."""
    from celery import chain
    return chain(sync_mefi_leads.si(tenant_id))
    # ↑ Plan 04 EXTENDS to: chain(sync_mefi_leads.si(tenant_id), calculate_daily_kpis.si(tenant_id))
```

Task signature contract from CONTEXT.md D-12:
```python
@celery_app.task(bind=True, autoretry_for=(Exception,), max_retries=3, default_retry_delay=60,
                  name="tasks.etl.calculate_daily_kpis")
def calculate_daily_kpis(self, tenant_id: str, calculation_date: str | None = None) -> dict:
    # - tenant_id: UUID string (validated as UUID at entry)
    # - calculation_date: ISO date string YYYY-MM-DD, OR None → yesterday in Bucharest
    # - returns {"status": "success"|"failed", "records_written": N, "duration_ms": M, "kpi_date": "YYYY-MM-DD"}
```

Async core signature:
```python
async def _calc_async(tenant_id: UUID, calculation_date: str | None) -> dict:
    # 1. set_tenant_id(tenant_id)
    # 2. resolve kpi_date = parse(calculation_date) OR yesterday in Bucharest (D-10)
    # 3. create NullPool engine + TaskSession (Pitfall 2)
    # 4. SyncRun(source="metrics", status="running") committed
    # 5. instantiate DailyKpiService, SalespersonKpiService, SourceKpiService
    # 6. compute_for_date(kpi_date) → row dict / lists
    # 7. MetricsRepository.upsert_daily_kpi(daily_row)
    # 8. MetricsRepository.upsert_salesperson_kpis(salesperson_rows)
    # 9. MetricsRepository.upsert_source_kpis(source_rows) — always 7 rows
    # 10. SyncRun.status="success", records_synced=1+len(sp_rows)+7, duration_ms set
    # 11. return summary dict; on exception update SyncRun.status="failed" with error_msg
    # 12. finally: await task_engine.dispose()
```

SyncRun model (existing, backend/app/models/pipeline.py):
- source: Text NOT NULL  ← Plan 04 uses "metrics" (distinguishes from "mefi" / "backfill")
- status: Text NOT NULL  ← "running" → "success" | "failed"
- records_synced: Int NULL
- duration_ms: Int NULL
- error_msg: Text NULL
- started_at: TIMESTAMPTZ NOT NULL
- completed_at: TIMESTAMPTZ NULL
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create calculate_daily_kpis Celery task + register with celery_app</name>
  <files>backend/app/tasks/etl/calculate_daily_kpis.py, backend/app/tasks/celery_app.py</files>
  <read_first>
    - backend/app/tasks/etl/sync_mefi_leads.py (FULL FILE — copy structural pattern: sync entry-point with try/except + asyncio.run, _sync_async deferred-import body, NullPool engine creation, SyncRun lifecycle, structlog binding, finally:dispose)
    - backend/app/tasks/celery_app.py (FULL FILE — include list must add app.tasks.etl.calculate_daily_kpis; preserve all existing config keys and beat schedule)
    - .planning/phases/03-metrics-engine/03-RESEARCH.md (Pattern 5 Celery chain extension, Pitfall 2 NullPool requirement, Task Skeleton example with deferred imports)
    - .planning/phases/03-metrics-engine/03-PATTERNS.md (section 'calculate_daily_kpis.py' — full skeleton including SyncRun source='metrics', date defaulting to yesterday in Bucharest, _calc_async body with three service calls)
    - .planning/phases/03-metrics-engine/03-CONTEXT.md (D-10 yesterday default, D-12 task signature, D-18 chain position)
    - backend/app/services/metrics/daily_kpi_service.py (compute_for_date signature returns ONE dict)
    - backend/app/services/metrics/salesperson_kpi_service.py (compute_for_date signature returns LIST of dicts)
    - backend/app/services/metrics/source_kpi_service.py (compute_for_date signature returns LIST of 7 dicts)
    - backend/app/services/repositories/metrics_repository.py (upsert_daily_kpi / upsert_salesperson_kpis / upsert_source_kpis signatures)
    - backend/tests/unit/test_calculate_daily_kpis.py (Wave 0 contract — task name "tasks.etl.calculate_daily_kpis", signature accepts optional calculation_date, default is yesterday Bucharest, retry config)
  </read_first>
  <behavior>
    - File `backend/app/tasks/etl/calculate_daily_kpis.py` begins with `from __future__ import annotations`, module docstring describing the daily KPI calculation task as second link in PIPE-01 chain (D-18) with date defaulting to yesterday in Europe/Bucharest (D-10)
    - Imports: asyncio, date, datetime, timedelta, UTC from datetime, UUID from uuid, structlog, celery_app
    - Module-level `logger = structlog.get_logger(__name__)`
    - Sync entry point `@celery_app.task(bind=True, autoretry_for=(Exception,), max_retries=3, default_retry_delay=60, name="tasks.etl.calculate_daily_kpis")` `def calculate_daily_kpis(self, tenant_id: str, calculation_date: str | None = None) -> dict`: wraps `asyncio.run(_calc_async(UUID(tenant_id), calculation_date))` in try/except, re-raises via `self.retry(exc=exc) from exc`
    - `async def _calc_async(tenant_id: UUID, calculation_date: str | None) -> dict`: ALL DB/model/service imports deferred inside this function body (Pitfall 2 / INFRA-05); calls `set_tenant_id(tenant_id)`; creates NullPool engine `create_async_engine(settings.database_url, poolclass=NullPool)`; creates TaskSession; resolves kpi_date via `date.fromisoformat(calculation_date)` if provided else `datetime.now(ZoneInfo("Europe/Bucharest")).date() - timedelta(days=1)`; creates SyncRun(source="metrics", status="running") and commits; instantiates DailyKpiService/SalespersonKpiService/SourceKpiService and MetricsRepository; calls `daily_row = await daily_svc.compute_for_date(kpi_date)` + `sp_rows = await sp_svc.compute_for_date(kpi_date)` + `src_rows = await src_svc.compute_for_date(kpi_date)`; calls `await repo.upsert_daily_kpi(daily_row)` + `await repo.upsert_salesperson_kpis(sp_rows)` + `await repo.upsert_source_kpis(src_rows)`; total_records = 1 + len(sp_rows) + len(src_rows); updates SyncRun to status="success" with records_synced=total_records, duration_ms=elapsed; commits; returns `{"status":"success","kpi_date":kpi_date.isoformat(),"records_written":total_records,"duration_ms":elapsed_ms}`; on exception: in `except` block open a fresh TaskSession and update the running SyncRun row to status="failed" + error_msg=str(exc)[:500] + completed_at=now, then re-raise; `finally`: `await task_engine.dispose()`
    - Input validation: `UUID(tenant_id)` at sync entry rejects malformed tenant_id strings (T-03-04-01); `date.fromisoformat(calculation_date)` rejects non-ISO date strings (T-03-04-02 — SQL injection mitigation)
    - celery_app.py `include` list updated: add string `"app.tasks.etl.calculate_daily_kpis"` to the existing list — preserve all other entries and config keys
  </behavior>
  <action>
    Copy structural skeleton from `backend/app/tasks/etl/sync_mefi_leads.py` lines 1-110 + lines 281-308 (error handler block). Substitute: task name "tasks.etl.calculate_daily_kpis", SyncRun source "metrics", remove Redis lock + MEFI client + pagination (not needed — calculation is short and idempotent via UPSERT). Inside `_calc_async`, defer ALL imports — even `from zoneinfo import ZoneInfo` — to the function body. Use `kpi_date_str = calculation_date if calculation_date else None` and parse via `date.fromisoformat(calculation_date)` (Python rejects non-ISO strings with ValueError, propagating to the autoretry exception handler which is correct — invalid date should not silently default to yesterday). Structure of computation: open ONE TaskSession context manager, instantiate all 4 (3 services + 1 repository) sharing the session, run computes sequentially (not concurrently — services may share read locks). After all three UPSERTs complete, fetch the SyncRun row via `await session.get(SyncRun, sync_run.id)` to update its status (or keep the original ORM instance in scope — preferred since SQLAlchemy tracks identity). Use `structlog.get_logger(__name__).bind(tenant_id=str(tenant_id), task="calculate_daily_kpis")` and emit events: `"kpi.compute_start"`, `"kpi.daily_done"`, `"kpi.salespeople_done"`, `"kpi.sources_done"`, `"kpi.complete"` with `kpi_date=str(kpi_date), records_written=N, duration_ms=M`. NEVER log lead names, salesperson names, or any PII (INFRA-06). For celery_app.py: read the current `include=[...]` list and add `"app.tasks.etl.calculate_daily_kpis"` as the last entry; do NOT touch `task_routes`, beat schedule, conf.update kwargs, or worker_process_init handler. Verify final list order: `["app.tasks", "app.tasks.etl.sync_mefi_leads", "app.tasks.etl.backfill_mefi_leads", "app.tasks.etl.calculate_daily_kpis"]`. The error path opens a NEW TaskSession because the original session may have rolled back; copy the `try: from sqlalchemy import select as _select; ...` pattern verbatim from sync_mefi_leads.py lines 281-308 substituting `_SR.source == "metrics"` for the filter clause.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; pytest tests/unit/test_calculate_daily_kpis.py -x &amp;&amp; python -c "from app.tasks.celery_app import celery_app; print('registered' if 'tasks.etl.calculate_daily_kpis' in celery_app.tasks else 'MISSING')"</automated>
  </verify>
  <acceptance_criteria>
    - File `backend/app/tasks/etl/calculate_daily_kpis.py` exists and contains text `from __future__ import annotations` AND `name="tasks.etl.calculate_daily_kpis"` AND `def calculate_daily_kpis` AND `async def _calc_async` AND `NullPool` AND `set_tenant_id` AND `MetricsRepository` AND `DailyKpiService` AND `SalespersonKpiService` AND `SourceKpiService` AND `ZoneInfo("Europe/Bucharest")` AND `timedelta(days=1)` AND `date.fromisoformat`
    - File contains text `source="metrics"` (SyncRun discriminator)
    - File contains text `autoretry_for=(Exception,)` AND `max_retries=3` AND `default_retry_delay=60`
    - All DB/model/service imports inside `_calc_async` function body — `grep -E "^from app\\." backend/app/tasks/etl/calculate_daily_kpis.py | grep -v "celery_app"` returns 0 lines (no module-level app.* imports except celery_app)
    - `backend/app/tasks/celery_app.py` `include` list contains text `"app.tasks.etl.calculate_daily_kpis"`
    - `cd backend &amp;&amp; pytest tests/unit/test_calculate_daily_kpis.py -x` exits 0
    - `cd backend &amp;&amp; python -c "from app.tasks.celery_app import celery_app; assert 'tasks.etl.calculate_daily_kpis' in celery_app.tasks; print('OK')"` prints `OK`
  </acceptance_criteria>
  <done>calculate_daily_kpis task file exists, is registered with the Celery app, follows the fork-safe NullPool pattern, and the Wave 0 task test file turns GREEN.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend daily_pipeline() chain in sync_mefi_leads.py + add chain integration test</name>
  <files>backend/app/tasks/etl/sync_mefi_leads.py, backend/tests/unit/test_sync_mefi_leads.py</files>
  <read_first>
    - backend/app/tasks/etl/sync_mefi_leads.py (lines 311-321 — daily_pipeline() function being modified; preserve docstring intent, comment block above the function, and module structure)
    - backend/tests/unit/test_sync_mefi_leads.py (existing chain test will be extended — read to understand existing test class structure before adding)
    - .planning/phases/03-metrics-engine/03-CONTEXT.md (D-18 chain semantics; calculate_daily_kpis uses .si() immutable signature)
    - .planning/phases/03-metrics-engine/03-RESEARCH.md (Pattern 5 — `.si()` not `.s()` because calculate_daily_kpis does not consume the ETL result)
  </read_first>
  <behavior>
    - `daily_pipeline(tenant_id: str)` in sync_mefi_leads.py returns `chain(sync_mefi_leads.si(tenant_id), calculate_daily_kpis.si(tenant_id))` — TWO links, both using immutable signatures (D-18)
    - The deferred `from app.tasks.etl.calculate_daily_kpis import calculate_daily_kpis` import lives inside the daily_pipeline function body (not at module top — keeps sync_mefi_leads module load fast and avoids circular import risk via the celery_app include list)
    - Docstring updated: "Phase 3 chain: sync_mefi_leads → calculate_daily_kpis. Phase 4 will append detect_anomalies.si(tenant_id)."
    - test_sync_mefi_leads.py adds a new test method `test_daily_pipeline_chains_calculate_daily_kpis` in the existing TestDailyPipeline class (or creates the class if it does not exist) — asserts the returned chain object has exactly 2 tasks and the second task's name == "tasks.etl.calculate_daily_kpis"
  </behavior>
  <action>
    Locate the `def daily_pipeline(tenant_id: str) -> object:` function at the bottom of `backend/app/tasks/etl/sync_mefi_leads.py` (currently lines 311-321 per file read). Replace its body to: `from celery import chain` + `from app.tasks.etl.calculate_daily_kpis import calculate_daily_kpis  # noqa: PLC0415` + `return chain(sync_mefi_leads.si(tenant_id), calculate_daily_kpis.si(tenant_id))`. Update the docstring to reflect Phase 3 chain (remove "Phase 2: chain only contains sync_mefi_leads" placeholder; add "Phase 3: sync_mefi_leads → calculate_daily_kpis (D-18, PIPE-01)"). For test_sync_mefi_leads.py: read the existing file, locate or create class TestDailyPipeline (if absent — follow existing test class structure in the file), add a synchronous test method `test_daily_pipeline_chains_calculate_daily_kpis(self) -> None` that imports `from app.tasks.etl.sync_mefi_leads import daily_pipeline`, calls `chain_obj = daily_pipeline("00000000-0000-0000-0000-000000000001")`, and asserts: `assert len(chain_obj.tasks) == 2`, `assert chain_obj.tasks[0].name == "tasks.etl.sync_mefi_leads"`, `assert chain_obj.tasks[1].name == "tasks.etl.calculate_daily_kpis"`. If the chain object uses `.tasks` vs `.body` vs another attribute (Celery 5.x exposes `.tasks` on a chain group), inspect the type and adapt the assertion accordingly using introspection if needed. Do NOT add a test that actually invokes the chain (requires running Celery+Redis); restrict to chain composition assertion. Use the existing `TENANT_ID = UUID("00000000-0000-0000-0000-000000000001")` constant or copy it from conftest.
  </action>
  <verify>
    <automated>cd backend &amp;&amp; pytest tests/unit/test_sync_mefi_leads.py -x &amp;&amp; python -c "from app.tasks.etl.sync_mefi_leads import daily_pipeline; c = daily_pipeline('00000000-0000-0000-0000-000000000001'); names=[t.name for t in c.tasks]; print(names); assert names == ['tasks.etl.sync_mefi_leads', 'tasks.etl.calculate_daily_kpis'], 'chain mismatch'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/tasks/etl/sync_mefi_leads.py` daily_pipeline function body contains text `calculate_daily_kpis.si(tenant_id)` AND `chain(sync_mefi_leads.si(tenant_id), calculate_daily_kpis.si(tenant_id))`
    - The deferred import line `from app.tasks.etl.calculate_daily_kpis import calculate_daily_kpis` is inside the function body, not at module top (`grep -n "from app.tasks.etl.calculate_daily_kpis" backend/app/tasks/etl/sync_mefi_leads.py` reports a line number AFTER `def daily_pipeline`)
    - `backend/tests/unit/test_sync_mefi_leads.py` contains text `test_daily_pipeline_chains_calculate_daily_kpis` AND asserts on `tasks.etl.calculate_daily_kpis`
    - `cd backend &amp;&amp; pytest tests/unit/test_sync_mefi_leads.py -x` exits 0 (all existing tests + new chain test GREEN)
    - `cd backend &amp;&amp; python -c "from app.tasks.etl.sync_mefi_leads import daily_pipeline; c = daily_pipeline('00000000-0000-0000-0000-000000000001'); print([t.name for t in c.tasks])"` prints `['tasks.etl.sync_mefi_leads', 'tasks.etl.calculate_daily_kpis']`
    - All Phase 3 unit tests GREEN: `cd backend &amp;&amp; pytest tests/unit/test_business_hours.py tests/unit/test_daily_kpi_service.py tests/unit/test_salesperson_kpi_service.py tests/unit/test_source_kpi_service.py tests/unit/test_metrics_repository.py tests/unit/test_calculate_daily_kpis.py tests/unit/test_sync_mefi_leads.py tests/unit/test_migration_004.py tests/unit/test_metrics_models.py -x` exits 0
  </acceptance_criteria>
  <done>daily_pipeline() chains sync_mefi_leads → calculate_daily_kpis, beat schedule (already at 04:00 Bucharest from Plan 02-03) will trigger both tasks sequentially, and the entire Phase 3 unit test suite is GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Celery worker → Postgres | Task runs as the worker DB role with full read on conformed views + write on metric tables |
| beat schedule → worker | Celery beat fires daily_pipeline at 04:00 Bucharest — no authentication step, scheduling is trusted-by-construction |
| task arg → SQL | `calculation_date` arrives as string from beat or manual CLI call — must be parsed before any query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04-01 | Spoofing | tenant_id parameter | mitigate | `UUID(tenant_id)` at sync entry point — malformed strings raise ValueError before any DB access (pattern inherited from sync_mefi_leads.py T-02-11) |
| T-03-04-02 | Tampering | calculation_date parameter (SQL injection vector) | mitigate | `date.fromisoformat(calculation_date)` parses as Python `date` object before query construction — raw strings never reach SQL; invalid input raises ValueError caught by autoretry |
| T-03-04-03 | Information Disclosure | structlog event log | mitigate | Bound context includes tenant_id (UUID), kpi_date (date), task name, records_written count — no salesperson names, lead external_ids, or revenue figures (INFRA-06) |
| T-03-04-04 | Denial of Service | task hang on stuck DB query | mitigate | NullPool engine + `task_engine.dispose()` in finally block — connection always closed even on exception (Pitfall 2) |
| T-03-04-05 | Tampering | cross-loop event loop reuse | mitigate | NullPool prevents asyncpg Future cross-loop attachment when `asyncio.run()` creates a fresh loop per task invocation (Pitfall 2, documented in sync_mefi_leads.py docstring) |
| T-03-04-06 | Repudiation | failed task masking | mitigate | Failed runs write SyncRun(source="metrics", status="failed", error_msg=str(exc)[:500], completed_at=now) — every invocation produces an audit row (PIPE-04) |
| T-03-04-SC | Tampering | npm/pip/cargo installs | accept | No new packages — celery 5.6.3, sqlalchemy 2.0.49, asyncpg 0.31.0, zoneinfo (stdlib) all verified per RESEARCH.md Package Legitimacy Audit |
</threat_model>

<verification>
- `cd backend && pytest tests/unit/test_calculate_daily_kpis.py tests/unit/test_sync_mefi_leads.py -x` exits 0
- `cd backend && python -c "from app.tasks.celery_app import celery_app; assert 'tasks.etl.calculate_daily_kpis' in celery_app.tasks"` exits 0
- `cd backend && python -c "from app.tasks.etl.sync_mefi_leads import daily_pipeline; c = daily_pipeline('00000000-0000-0000-0000-000000000001'); names = [t.name for t in c.tasks]; assert names == ['tasks.etl.sync_mefi_leads', 'tasks.etl.calculate_daily_kpis']"` exits 0
- All 9 Phase 3 unit test files GREEN: business_hours, daily_kpi_service, salesperson_kpi_service, source_kpi_service, metrics_repository, calculate_daily_kpis, sync_mefi_leads (existing + new), migration_004, metrics_models
- Manual verification (post-deploy in Docker): `celery -A app.tasks.celery_app call tasks.etl.calculate_daily_kpis --args='["<sofa-belle-uuid>", null]'` then `psql -c "SELECT count(*) FROM daily_kpi WHERE date = CURRENT_DATE - 1; SELECT count(*) FROM source_daily_kpi WHERE date = CURRENT_DATE - 1; SELECT count(*) FROM salesperson_daily_kpi WHERE date = CURRENT_DATE - 1;"` shows 1, 7, and N (active salespeople count, ≤ 11) rows respectively
- Manual verification (re-run idempotency, METR-01 SC#1): re-running the celery call above produces the SAME row counts (no duplicates)
</verification>

<success_criteria>
- 1 new task file + 2 modified files
- All 9 Phase 3 unit test files GREEN end-to-end
- ROADMAP SC#1 satisfied: `celery call calculate_daily_kpis` populates all 3 tables; re-run is idempotent
- ROADMAP SC#6 satisfied: `AT TIME ZONE 'Europe/Bucharest'` used in all date grouping (verifiable via `grep -r "Europe/Bucharest" backend/app/services/metrics/ backend/app/tasks/etl/calculate_daily_kpis.py`)
- METR-01 fully covered (task writes 3 tables, idempotent UPSERT)
- D-18 chain semantics live: `sync_mefi_leads → calculate_daily_kpis` runs at 04:00 Bucharest via existing beat schedule
- Phase 3 ships
</success_criteria>

<output>
Create `.planning/phases/03-metrics-engine/03-04-SUMMARY.md` when done following the standard summary template.
</output>
