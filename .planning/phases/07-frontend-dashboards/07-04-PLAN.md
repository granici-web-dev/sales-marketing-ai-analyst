---
phase: 07-frontend-dashboards
plan: 04
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - frontend/src/hooks/useMarketingDashboard.ts
  - frontend/src/components/dashboards/marketing/source-volume-chart.tsx
  - frontend/src/components/dashboards/marketing/junk-pct-table.tsx
  - frontend/src/components/dashboards/marketing/ad-spend-placeholder.tsx
  - frontend/src/app/(dashboard)/marketing/page.tsx
autonomous: true
requirements:
  - MARK-01
  - MARK-02
  - MARK-03
  - MARK-04
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-06
  - UI-07
  - UI-08

must_haves:
  truths:
    - "User navigating to /marketing sees a multi-line chart showing lead volume per source over time"
    - "Site conversion rate card is visible (or null/N/A placeholder when backend returns null)"
    - "Junk lead percentage table shows source, junk count, total leads, junk% columns"
    - "Ad spend placeholder section clearly labeled as Coming in next update with TrendingUp icon"
    - "Date range picker present; changing range refetches data"
    - "Loading states show Skeleton components; error state shows inline Romanian error + Retry"
    - "Empty state for line chart when no source volume data (no blank axes)"
    - "No @tremor/react imports in any marketing component"
  artifacts:
    - path: "frontend/src/hooks/useMarketingDashboard.ts"
      provides: "TanStack Query hook for GET /api/v1/dashboards/marketing"
      exports: ["useMarketingDashboard", "MarketingDashboardData"]
    - path: "frontend/src/components/dashboards/marketing/source-volume-chart.tsx"
      provides: "Multi-series LineChart of lead volume per source over time"
      contains: "ChartContainer"
    - path: "frontend/src/components/dashboards/marketing/junk-pct-table.tsx"
      provides: "Junk % by source table with formatPct display"
      contains: "overflow-x-auto"
    - path: "frontend/src/components/dashboards/marketing/ad-spend-placeholder.tsx"
      provides: "Coming soon placeholder section for CPL/CAC/ROAS"
      contains: "adSpendComingSoon"
    - path: "frontend/src/app/(dashboard)/marketing/page.tsx"
      provides: "Full Marketing dashboard page"
      contains: "useMarketingDashboard"
  key_links:
    - from: "frontend/src/app/(dashboard)/marketing/page.tsx"
      to: "frontend/src/hooks/useMarketingDashboard.ts"
      via: "import useMarketingDashboard"
      pattern: "useMarketingDashboard"
    - from: "frontend/src/hooks/useMarketingDashboard.ts"
      to: "/api/v1/dashboards/marketing"
      via: "apiClient.get queryFn"
      pattern: "dashboards/marketing"
---

<objective>
Implement the Marketing Dashboard page (MARK-01..04) with the multi-series lead volume line chart, junk percentage table, site conversion card, and the ad-spend coming-soon placeholder. Runs in Wave 2 parallel with Plans 02 and 03.

Purpose: Deliver the marketing source analytics view — lead volume trends per source, junk lead quality by source, and clearly labeled placeholders for future ad integrations.
Output: A functional /marketing page with line chart, junk table, and ad-spend placeholder with Romanian strings and full data states.
</objective>

<execution_context>
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-frontend-dashboards/07-CONTEXT.md
@.planning/phases/07-frontend-dashboards/07-RESEARCH.md
@.planning/phases/07-frontend-dashboards/07-PATTERNS.md
@.planning/phases/07-frontend-dashboards/07-UI-SPEC.md
@.planning/phases/07-frontend-dashboards/07-01-SUMMARY.md

<interfaces>
<!-- Backend response shapes from verified schema files -->

MarketingDashboardResponse fields (from backend/app/schemas/dashboards/marketing.py):
  period: { from: string; to: string }
  lead_volume_by_source: Array<{
    source: string;
    total_leads: number | null;
    series: Array<{ date: string; leads: number | null }>;
  }>
  site_conversion_rate: string | null   (Decimal serialized, e.g. "0.0420")
  junk_by_source: Array<{
    source: string;
    junk_count: number | null;   (integer — NOT string)
    total_leads: number | null;  (integer — NOT string)
    junk_pct: string | null;     (Decimal serialized, e.g. "0.2500")
  }>
  ad_spend: null   (always null — MARK-03 explicit placeholder)
  cpl: null
  cac: null
  roas: null

MARK-03: ad_spend/cpl/cac/roas are ALWAYS null from backend. Never try to render them as values.
MARK-02: site_conversion_rate is the lead-to-contract approximation (no GA4 in MVP1).

Multi-series line chart: lead_volume_by_source has one entry per source category.
Each entry has a series[] array of {date, leads} time points.
Transform to Recharts format: array of { date: string, [sourceKey]: number }[] flat objects.

Existing utilities from Plan 01:
  import { formatPct, formatDate, getDefaultDateRange } from "@/lib/formatters"
  import { apiClient } from "@/lib/api-client"
  import { DateRangePicker } from "@/components/date-range-picker"

shadcn components (installed in Plan 01):
  import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table"
  import { Skeleton } from "@/components/ui/skeleton"
  import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card"
  import { ChartContainer, ChartTooltip, ChartTooltipContent, ChartLegend, type ChartConfig } from "@/components/ui/chart"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useMarketingDashboard hook + ad-spend placeholder</name>
  <files>
    frontend/src/hooks/useMarketingDashboard.ts,
    frontend/src/components/dashboards/marketing/ad-spend-placeholder.tsx
  </files>
  <read_first>
    - frontend/src/hooks/useSalesDashboard.ts (Pattern to follow exactly — same structure)
    - backend/app/schemas/dashboards/marketing.py (verified field types)
    - .planning/phases/07-frontend-dashboards/07-PATTERNS.md (ad-spend-placeholder section — uses stub page pattern)
    - frontend/src/app/(dashboard)/marketing/page.tsx (current stub — shows the coming-soon pattern to replicate in placeholder component)
  </read_first>
  <action>
    Step 1 — Create frontend/src/hooks/useMarketingDashboard.ts following the same pattern as useSalesDashboard.ts (read that file for the exact structure). Add "use client". Define LeadVolumeBySourcePoint: { date: string; leads: number|null }. Define LeadVolumeBySource: { source: string; total_leads: number|null; series: LeadVolumeBySourcePoint[] }. Define JunkBySource: { source: string; junk_count: number|null; total_leads: number|null; junk_pct: string|null }. Define MarketingDashboardData with period, lead_volume_by_source, site_conversion_rate: string|null, junk_by_source, ad_spend: null, cpl: null, cac: null, roas: null. Export useMarketingDashboard(from, to) with queryKey: ["marketing", from, to], queryFn hitting GET /api/v1/dashboards/marketing?from=${from}&to=${to}.

    Step 2 — Create frontend/src/components/dashboards/marketing/ad-spend-placeholder.tsx. Add "use client". Import TrendingUp from lucide-react; useTranslations from next-intl; Card, CardContent from @/components/ui/card. The component renders a Card with a centered layout inside: TrendingUp icon (size 32, text-muted-foreground), title paragraph from t("marketing.adSpendTitle"), description paragraph from t("marketing.adSpendComingSoon"). Styling: min-h-[200px] rounded-lg border flex flex-col items-center justify-center p-6 text-center. This is MARK-03 — always shown (not conditional on data).
  </action>
  <verify>
    <automated>cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/frontend && pnpm typecheck</automated>
  </verify>
  <done>
    - frontend/src/hooks/useMarketingDashboard.ts exports useMarketingDashboard and MarketingDashboardData
    - MarketingDashboardData has lead_volume_by_source, site_conversion_rate, junk_by_source, ad_spend: null
    - junk_by_source[].junk_count and total_leads typed as number|null (integer, not string)
    - junk_by_source[].junk_pct typed as string|null (Decimal serialized)
    - frontend/src/components/dashboards/marketing/ad-spend-placeholder.tsx exists with TrendingUp icon
    - pnpm typecheck exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Source volume chart + junk table + Marketing page</name>
  <files>
    frontend/src/components/dashboards/marketing/source-volume-chart.tsx,
    frontend/src/components/dashboards/marketing/junk-pct-table.tsx,
    frontend/src/app/(dashboard)/marketing/page.tsx
  </files>
  <read_first>
    - frontend/src/app/(dashboard)/marketing/page.tsx (current stub — read before replacing)
    - frontend/src/components/ui/chart.tsx (ChartContainer, ChartLegend API)
    - frontend/src/app/globals.css (CSS variables for multi-series line colors)
    - .planning/phases/07-frontend-dashboards/07-RESEARCH.md (Pattern 5 for ChartContainer, D-06 multi-series LineChart pattern, D-23 mobile chart rules)
    - .planning/phases/07-frontend-dashboards/07-UI-SPEC.md (Charts General section, Color section for multi-series palette)
    - .planning/phases/07-frontend-dashboards/07-PATTERNS.md (source-volume-chart section — role-match to revenue-trend)
  </read_first>
  <action>
    Step 1 — Create frontend/src/components/dashboards/marketing/source-volume-chart.tsx. Add "use client". Props: leadVolumeBySource (LeadVolumeBySource[]). Import LineChart, Line, XAxis, YAxis, CartesianGrid from "recharts"; ChartContainer, ChartTooltip, ChartTooltipContent, ChartLegend, ChartLegendContent, type ChartConfig from "@/components/ui/chart"; formatDate from @/lib/formatters; TrendingUp from lucide-react; useTranslations from next-intl.

    Multi-series data transformation: pivot the per-source series arrays into a flat array of objects keyed by date. Each unique date becomes a row; each source name becomes a key in that row. Algorithm: collect all unique dates from all sources' series arrays, sort ascending, for each date create an object { date: dateStr, ...sources_map_for_this_date }. Map source names to safe key strings (replace spaces/special chars with underscores for object keys).

    ChartConfig: build dynamically from the list of sources. Assign colors from CSS variables in order: primary "var(--color-accent)", secondary "hsl(221 83% 75%)", tertiary "var(--color-muted-foreground)", then Tailwind zinc colors for additional series. The config maps each source key to its label and color.

    Render ChartContainer with height 200px mobile / 260px desktop. Inside: LineChart with the pivoted data. One Line per source with dataKey = safe source key, stroke from ChartConfig color, dot={false} for cleaner lines. XAxis dataKey="date" with formatter showing short date (just day or DD.MM). YAxis. On mobile <640px: hide CartesianGrid, hide Legend (ChartLegend only on sm:block wrapper), simplify XAxis to show only day number. ChartTooltip with ChartTooltipContent.

    Empty state: when leadVolumeBySource.length === 0 or all series arrays are empty, render TrendingUp icon + t("marketing.sourceVolume.noData") + t("marketing.sourceVolume.noDataDesc").

    Site conversion card: NOT in this component — it will be a simple inline Card on the page.

    Step 2 — Create frontend/src/components/dashboards/marketing/junk-pct-table.tsx. Add "use client". Props: junkBySource (JunkBySource[]). Import Table, TableBody, TableCell, TableHead, TableHeader, TableRow from @/components/ui/table; formatPct from @/lib/formatters; useTranslations from next-intl; PieChart from lucide-react. Empty state: when array is empty, PieChart icon + t("marketing.junk.title") + "—". Non-empty: overflow-x-auto wrapper, Table min-w-[400px], columns: Source | Junk Count | Total Leads | Junk %. junk_pct formatted via formatPct(row.junk_pct). junk_count and total_leads are integers — display directly with ?? "—" for null. All column headers from useTranslations("marketing").

    Step 3 — Replace frontend/src/app/(dashboard)/marketing/page.tsx entirely. Add "use client". Same Suspense + inner content component pattern. MarketingPageContent: useSearchParams(), getDefaultDateRange(), useMarketingDashboard(from, to). Page layout: space-y-6. Header: h1 t("marketing.title") + DateRangePicker. Sections in order: (1) Source volume chart in a Card — isLoading → Skeleton h-[260px]; isError → inline error; else SourceVolumeChart. (2) Site conversion rate — simple Card with one KPI value: formatPct(data.site_conversion_rate), label t("marketing.siteConversion.title"). When site_conversion_rate is null show "N/A". (3) Junk % table in a Card — isLoading → Skeleton; isError → inline error; else JunkPctTable. (4) Ad spend placeholder — always render AdSpendPlaceholder regardless of loading state (it has no dynamic data). Import AdSpendPlaceholder from @/components/dashboards/marketing/ad-spend-placeholder.
  </action>
  <verify>
    <automated>cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/frontend && pnpm typecheck && pnpm lint</automated>
  </verify>
  <done>
    - frontend/src/components/dashboards/marketing/source-volume-chart.tsx exists with multi-series data transformation
    - SourceVolumeChart uses LineChart with one Line per source from lead_volume_by_source
    - ChartConfig built dynamically from source list
    - frontend/src/components/dashboards/marketing/junk-pct-table.tsx exists with overflow-x-auto wrapper
    - junk_pct displayed via formatPct (not raw decimal string)
    - frontend/src/app/(dashboard)/marketing/page.tsx imports all 3 marketing components
    - AdSpendPlaceholder always rendered (not gated on data load)
    - No @tremor/react imports
    - pnpm typecheck exits 0
    - pnpm lint exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params → API call | ?from= and ?to= from useSearchParams fed to marketing API endpoint |
| API response → chart data | Source names from backend used as chart series keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-10 | Tampering | useMarketingDashboard queryFn | mitigate | from/to validated same as other dashboard hooks; enabled: Boolean(from && to) |
| T-7-11 | Tampering | source-volume-chart.tsx data keys | mitigate | Source names used as chart data keys are sanitized (replace non-word chars with _) before use as object properties; rendered as JSX text in legend/tooltip (auto-escaped) |
| T-7-12 | Tampering | junk-pct-table.tsx source names | mitigate | Source names rendered as JSX text nodes; no dangerouslySetInnerHTML |
| T-7-13 | Info Disclosure | MARK-03 ad_spend always null | accept | Backend enforces null — no sensitive data ever exposed; frontend explicitly handles null with placeholder |
</threat_model>

<verification>
1. `cd frontend && pnpm typecheck` exits 0
2. `cd frontend && pnpm lint` exits 0
3. `grep -r "@tremor" frontend/src/components/dashboards/marketing` returns zero matches
4. `grep -c "AdSpendPlaceholder" frontend/src/app/(dashboard)/marketing/page.tsx` >= 1
5. `grep -c "overflow-x-auto" frontend/src/components/dashboards/marketing/junk-pct-table.tsx` >= 1
6. `grep -c "formatPct" frontend/src/components/dashboards/marketing/junk-pct-table.tsx` >= 1
</verification>

<success_criteria>
- useMarketingDashboard types junk_by_source correctly (junk_count as number|null, junk_pct as string|null)
- SourceVolumeChart transforms per-source series arrays into flat Recharts data format
- Multi-series chart uses ChartContainer (not direct Recharts import in page)
- JunkPctTable has horizontal scroll wrapper and formats junk_pct via formatPct
- AdSpendPlaceholder is always rendered (MARK-03 — not conditional on data)
- Marketing page has all loading/empty/error states
- pnpm typecheck and pnpm lint both exit 0
</success_criteria>

<output>
Create `.planning/phases/07-frontend-dashboards/07-04-SUMMARY.md` when done.
</output>
