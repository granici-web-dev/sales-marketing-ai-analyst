---
phase: 07-frontend-dashboards
plan: 05
type: execute
wave: 3
depends_on:
  - "07-01"
  - "07-02"
  - "07-03"
  - "07-04"
files_modified:
  - frontend/src/hooks/useInsights.ts
  - frontend/src/components/dashboards/insights/insight-summary.tsx
  - frontend/src/components/dashboards/insights/problem-card.tsx
  - frontend/src/components/dashboards/insights/generation-failed-banner.tsx
  - frontend/src/components/dashboards/insights/fallback-anomaly-list.tsx
  - frontend/src/app/(dashboard)/insights/page.tsx
autonomous: false
requirements:
  - INSI-01
  - INSI-02
  - INSI-03
  - INSI-04
  - INSI-05
  - INSI-06
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-06
  - UI-07
  - UI-08

must_haves:
  truths:
    - "User navigating to /insights sees summary paragraph (the AI-generated daily overview) at top of page"
    - "Up to 3 problem cards appear below summary, each collapsed by default with severity badge + title + estimated_loss_ron"
    - "Expanding a problem card reveals description + flat bullet list of recommended_actions with owner and deadline"
    - "Reîmprospătează button is visible and disabled with countdown when rate-limited (429 Retry-After)"
    - "A historical date picker allows viewing insights for past dates"
    - "When insight.status='fallback' AND problems.length > 0, a Romanian notice banner appears above ProblemCard components rendered from payload.problems[]"
    - "When insight.status='failed' OR payload is null, a Romanian notice banner appears above a static 'no data' paragraph"
    - "When no insight exists for selected date, an empty state is shown"
    - "Data freshness footer shows InsightEnvelope.date (not today's date) as the insight report date"
    - "INSI-04: each problem card shows the rule_id (problem.id) as a source trace label"
    - "No @tremor/react imports in any insights component"
  artifacts:
    - path: "frontend/src/hooks/useInsights.ts"
      provides: "useInsightsToday + useInsightsByDate + useInsightsRefresh hooks"
      exports: ["useInsightsToday", "useInsightsByDate", "useInsightsRefresh", "InsightEnvelope"]
    - path: "frontend/src/components/dashboards/insights/insight-summary.tsx"
      provides: "AI summary display — uses payload.summary (NOT payload.summary_ro)"
      contains: "payload.summary"
    - path: "frontend/src/components/dashboards/insights/problem-card.tsx"
      provides: "Collapsible problem card with severity badge, estimated_loss_ron, actions list"
      contains: "Collapsible"
    - path: "frontend/src/components/dashboards/insights/generation-failed-banner.tsx"
      provides: "Romanian failure notice banner"
      contains: "generationFailed"
    - path: "frontend/src/components/dashboards/insights/fallback-anomaly-list.tsx"
      provides: "Dual-mode fallback: renders ProblemCard list for status='fallback', static paragraph for status='failed'"
      contains: "generation_failed"
    - path: "frontend/src/app/(dashboard)/insights/page.tsx"
      provides: "Full Insights page with refresh rate-limit countdown and historical date picker"
      contains: "useInsightsToday"
  key_links:
    - from: "frontend/src/app/(dashboard)/insights/page.tsx"
      to: "frontend/src/hooks/useInsights.ts"
      via: "import useInsightsToday, useInsightsByDate"
      pattern: "useInsightsToday"
    - from: "frontend/src/hooks/useInsights.ts"
      to: "/api/v1/insights/today"
      via: "apiClient.get queryFn"
      pattern: "insights/today"
    - from: "frontend/src/components/dashboards/insights/problem-card.tsx"
      to: "@/components/ui/collapsible"
      via: "import Collapsible"
      pattern: "Collapsible"
---

<objective>
Implement the Insights Dashboard page (INSI-01..06) with today's AI insight summary, up to 3 collapsible problem cards with severity/loss/actions, historical date picker, manual refresh button with rate-limit countdown, and generation-failed fallback display. This plan runs in Wave 3 after all Wave 2 dashboard plans complete.

Purpose: Deliver the AI insights view — the daily Romanian summary, problem cards with actions, and graceful fallback when Claude generation fails.
Output: A functional /insights page with Collapsible problem cards, rate-limited refresh button, historical navigation, and full Romanian i18n.
</objective>

<execution_context>
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-frontend-dashboards/07-CONTEXT.md
@.planning/phases/07-frontend-dashboards/07-RESEARCH.md
@.planning/phases/07-frontend-dashboards/07-PATTERNS.md
@.planning/phases/07-frontend-dashboards/07-UI-SPEC.md
@.planning/phases/07-frontend-dashboards/07-01-SUMMARY.md
@.planning/phases/07-frontend-dashboards/07-02-SUMMARY.md

<interfaces>
<!-- InsightEnvelope shape — verified from backend/app/api/v1/insights.py -->

InsightEnvelope (from backend — exact field names):
  date: string          (YYYY-MM-DD — this is yesterday in Bucharest TZ per Pitfall 8; display this, NOT new Date())
  status: string        ("success" | "failed" | "fallback" | "running")
  generation_failed: boolean   (true when status in ("failed", "fallback"))
  generated_at: string | null  (ISO datetime)
  payload: InsightPayload | null

InsightPayload (from DailyInsightResponse Pydantic schema — daily_insight_schema.py):
  summary: string          (B3 CRITICAL FIX: the backend field is "summary" NOT "summary_ro".
                             CONTEXT.md D-15 references 'summary_ro' — this is WRONG.
                             Verified from backend/app/schemas/insights/daily_insight_schema.py.
                             Always use payload.summary. NEVER use payload.summary_ro.)
  problems: Problem[]      (max 3 per Pydantic max_length=3)
  positives: Positive[]    (0-3)
  warnings: Warning[]      (0-N)
  weekly_action_plan: string[]  (5-7 items)
  generated_at: string     (ISO datetime — also in InsightEnvelope.generated_at)

Problem (from DailyInsightResponse):
  id: string               (= rule_id from detected_problems — INSI-04 source traceability)
  severity: "high" | "medium" | "low"
  category: "marketing" | "sales" | "team" | "funnel"
  title: string
  description: string
  estimated_loss_ron: number  (Decimal in Python but NOT field_serialized in DailyInsightResponse — arrives as number, not string; per RESEARCH.md Pitfall 2/Assumption A2)
  actions: ActionItem[]

ActionItem:
  order: number
  description: string
  owner: string
  deadline: "Azi" | "Mâine" | "Săptămâna aceasta" | "Luna aceasta"
  expected_outcome: string

RefreshResponse (from POST /api/v1/insights/refresh):
  pipeline_run_id: string
  enqueued_at: string
  On 429: Retry-After header with seconds. Response body may not be RefreshResponse shape on 429.

INSI-05 Fallback Behavior (B6 fix — CORRECTED from original plan):
  InsightEnvelope.generation_failed is true when status="fallback" OR status="failed".

  CASE A — status="fallback": Backend's InsightService._build_fallback() generates a full
    DailyInsightResponse from detected_problems rows. payload IS populated with problems[].
    The payload.problems[] array contains the algorithmic anomaly data as Problem objects.
    FallbackAnomalyList should: show the generationFailed banner + render payload.problems[]
    using the same ProblemCard component as normal success display.
    Condition check: envelope.status === "fallback" && envelope.payload?.problems?.length > 0

  CASE B — status="failed": Celery task-level failure. payload_json may be null.
    FallbackAnomalyList should: show the generationFailed banner + static paragraph:
    "Nu există date disponibile pentru această perioadă."

  The FallbackAnomalyList component receives { problems: Problem[] | null, status: string }
  and decides which case to render. Do NOT make a separate API call to /detected_problems.

INSI-02 Historical: GET /api/v1/insights?date=YYYY-MM-DD. Returns 404 when no insight for that date.
  useInsightsByDate returns null on 404 (not an error). Show empty state.

INSI-03 Rate limiting: POST /api/v1/insights/refresh returns 429 with Retry-After header (seconds).
  Parse: parseInt(response.headers.get("Retry-After") ?? "0")
  Display countdown in button label: "Reîmprospătează (N:SS)" using setInterval every second.
  Button disabled while rate-limited.

Existing utilities from Plan 01:
  import { formatRON, formatDate, formatTimestamp, getDefaultDateRange } from "@/lib/formatters"
  import { apiClient } from "@/lib/api-client"

shadcn components (installed in Plan 01):
  import { Collapsible, CollapsibleContent, CollapsibleTrigger } from "@/components/ui/collapsible"
  import { Card, CardContent, CardHeader } from "@/components/ui/card"
  import { Badge } from "@/components/ui/badge"
  import { Skeleton } from "@/components/ui/skeleton"
  import { Button } from "@/components/ui/button"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useInsights hooks + InsightSummary + ProblemCard + GenerationFailedBanner + FallbackAnomalyList</name>
  <files>
    frontend/src/hooks/useInsights.ts,
    frontend/src/components/dashboards/insights/insight-summary.tsx,
    frontend/src/components/dashboards/insights/problem-card.tsx,
    frontend/src/components/dashboards/insights/generation-failed-banner.tsx,
    frontend/src/components/dashboards/insights/fallback-anomaly-list.tsx
  </files>
  <read_first>
    - frontend/src/hooks/useSalesDashboard.ts (Pattern base for useInsightsToday and useInsightsByDate)
    - frontend/src/components/ui/collapsible.tsx (Collapsible, CollapsibleContent, CollapsibleTrigger API)
    - frontend/src/components/ui/card.tsx (Card, CardContent, CardHeader API)
    - frontend/src/components/ui/badge.tsx (Badge variants API)
    - .planning/phases/07-frontend-dashboards/07-PATTERNS.md (problem-card.tsx section — collapsed-by-default pattern, generation-failed-banner section)
    - .planning/phases/07-frontend-dashboards/07-RESEARCH.md (InsightEnvelope TypeScript type in Code Examples section, Pitfall 2 on summary not summary_ro, Pitfall 8 on insights date)
    - .planning/phases/07-frontend-dashboards/07-UI-SPEC.md (Insight Problem Cards section, Error States copywriting)
    - backend/app/api/v1/insights.py (InsightEnvelope class definition lines 24-35)
    - backend/app/schemas/insights/daily_insight_schema.py (Verify: DailyInsightResponse has field "summary: str" NOT "summary_ro")
  </read_first>
  <action>
    Step 1 — Create frontend/src/hooks/useInsights.ts. Add "use client". Define all TypeScript interfaces matching the backend schema exactly: ActionItem (order, description, owner, deadline union, expected_outcome), Problem (id, severity union, category union, title, description, estimated_loss_ron: number — NOT string per Assumption A2, actions: ActionItem[]), Positive, Warning, InsightPayload (summary: string — CRITICAL: NOT "summary_ro". B3 fix: the actual DailyInsightResponse field verified from daily_insight_schema.py is "summary". Using "summary_ro" will result in undefined at runtime. Always use "summary".), InsightEnvelope: { date: string; status: string; generation_failed: boolean; generated_at: string|null; payload: InsightPayload|null }.

    Export three hooks/functions: (a) useInsightsToday(): useQuery<InsightEnvelope> with queryKey: ["insights", "today"], queryFn hitting GET /api/v1/insights/today. (b) useInsightsByDate(date: string|null): useQuery<InsightEnvelope|null> with queryKey: ["insights", date], queryFn that returns null on 404, throws on other errors, enabled: Boolean(date). (c) useInsightsRefresh(): W10 fix — use useMutation from @tanstack/react-query instead of a raw async function. Implementation:
      const queryClient = useQueryClient()
      return useMutation({
        mutationFn: async () => {
          const res = await apiClient.post("/api/v1/insights/refresh", {})
          if (res.status === 429) {
            const retryAfter = parseInt(res.headers.get("Retry-After") ?? "0", 10)
            return { ok: false, retryAfterSeconds: retryAfter }
          }
          if (!res.ok) throw new Error(`HTTP ${res.status}`)
          return { ok: true, retryAfterSeconds: null }
        },
        onSuccess: (data) => {
          if (data.ok) {
            queryClient.invalidateQueries({ queryKey: ["insights", "today"] })
          }
        },
      })
    The page calls mutation.mutate(), checks mutation.isPending for loading state, reads the return value { ok, retryAfterSeconds } from onSuccess/mutation.data to set the rate-limit countdown. Export the useMutation return shape; the page extracts mutation.mutate, mutation.isPending, mutation.isError from it.

    Step 2 — Create frontend/src/components/dashboards/insights/insight-summary.tsx. Add "use client". Props: summary: string; insightDate: string. Import useTranslations from next-intl; formatDate from @/lib/formatters. Renders: heading t("insights.summary.heading") as text-sm text-muted-foreground label. Below it, the summary string as a paragraph with text-sm leading-relaxed (line-height 1.6 per UI-SPEC). Below the summary, a metadata line showing t("insights.summary.dataFor") + formatDate(insightDate). This component uses the "summary" prop directly — the caller passes payload.summary (NOT payload.summary_ro). Do NOT display new Date() as the date — display the InsightEnvelope.date field per Pitfall 8. W8 fix: this component renders the payload.summary field; must NOT reference payload.summary_ro anywhere in the file.

    Step 3 — Create frontend/src/components/dashboards/insights/problem-card.tsx. Add "use client". Props: problem: Problem (type from useInsights.ts). Import: useState from react; Collapsible, CollapsibleContent, CollapsibleTrigger from @/components/ui/collapsible; Card, CardContent, CardHeader from @/components/ui/card; Badge from @/components/ui/badge; ChevronDown, ChevronUp from lucide-react; formatRON from @/lib/formatters; useTranslations from next-intl; cn from @/lib/utils. Implement collapsed-by-default (useState false). Render Collapsible open={open} onOpenChange={setOpen}. Inside Card: CardHeader contains CollapsibleTrigger. Trigger is a button-styled div with cursor-pointer, full width, min-h-[44px] touch target. Trigger shows: severity Badge (CSS classes: high = "bg-red-100 text-red-700 border border-red-200", medium = "bg-orange-100 text-orange-700 border border-orange-200", low = "bg-yellow-100 text-yellow-700 border border-yellow-200"), problem title as font-medium text-sm, estimated_loss_ron formatted as formatRON(problem.estimated_loss_ron) (it's already a number — pass directly, no parseFloat needed), and ChevronDown/ChevronUp icon on right. Severity badge label: t("insights.severity.high"/"medium"/"low"). CollapsibleContent inside CardContent pt-0: (a) description paragraph text-sm leading-relaxed mb-3. (b) t("insights.problem.actions") subheading. (c) ul list of actions: each li shows formatted bullet "action.description — owner: action.owner, deadline: action.deadline". Display action.expected_outcome as italic smaller text below. (d) INSI-04 source trace: small text at bottom "Sursă: {problem.id}" using t("insights.problem.source") + problem.id.

    Step 4 — Create frontend/src/components/dashboards/insights/generation-failed-banner.tsx. Add "use client". Import useTranslations from next-intl; AlertTriangle from lucide-react. Renders a red warning banner: border-l-4 border-destructive bg-red-50 px-4 py-3 text-red-700 rounded-r-md role="alert". Shows AlertTriangle icon + t("insights.generationFailed"). This component takes no props — it is always visible when rendered.

    Step 5 — Create frontend/src/components/dashboards/insights/fallback-anomaly-list.tsx (B6 fix — complete rewrite from original plan). Add "use client". Props: problems: Problem[] | null; status: string. Import: useTranslations from next-intl; ProblemCard from ./problem-card; Card, CardContent from @/components/ui/card.

    CRITICAL: This component implements two distinct display modes based on the status and problems data:

    CASE A — status === "fallback" AND problems !== null AND problems.length > 0:
      Render: a heading using t("insights.fallbackTitle") (text-sm font-medium text-muted-foreground mb-2),
      then map problems as: {problems.map(p => <ProblemCard key={p.id} problem={p} />)}.
      This reuses the exact same ProblemCard component as the normal success path.
      The GenerationFailedBanner is rendered by the PARENT page ABOVE this component — not inside it.

    CASE B — status === "failed" OR problems === null OR problems.length === 0:
      Render: a Card with CardContent showing a simple paragraph: "Nu există date disponibile pentru această perioadă."

    Implementation:
      const showProblems = status === "fallback" && problems !== null && problems.length > 0
      if (showProblems) { return <div>...</div> with heading + problem cards }
      return <Card><CardContent>...</CardContent></Card> with static paragraph

    The parent page checks: envelope.generation_failed === true, then renders:
      1. <GenerationFailedBanner /> (always, for both case A and B)
      2. <FallbackAnomalyList problems={envelope.payload?.problems ?? null} status={envelope.status} />
    This means the banner always appears when generation_failed=true; the list component handles
    the rest of the display based on whether anomaly data is available in the payload.
  </action>
  <verify>
    <automated>cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/frontend && pnpm typecheck</automated>
  </verify>
  <done>
    - frontend/src/hooks/useInsights.ts exports useInsightsToday, useInsightsByDate, useInsightsRefresh (useMutation), InsightEnvelope, Problem
    - InsightPayload.summary typed as string (NOT summary_ro — B3 verified)
    - Problem.estimated_loss_ron typed as number (not string)
    - useInsightsByDate returns null on 404 (not throws)
    - useInsightsRefresh uses useMutation (not raw async function — W10 fix)
    - problem-card.tsx imports Collapsible from @/components/ui/collapsible
    - problem-card.tsx has min-h-[44px] on trigger
    - problem-card.tsx shows problem.id as INSI-04 source trace
    - generation-failed-banner.tsx exists with role="alert"
    - fallback-anomaly-list.tsx renders ProblemCard list when status="fallback" and problems.length > 0 (B6 Case A)
    - fallback-anomaly-list.tsx renders static paragraph when status="failed" or problems empty (B6 Case B)
    - insight-summary.tsx does NOT reference summary_ro anywhere (W8 verified)
    - pnpm typecheck exits 0
  </done>
</task>

<task type="checkpoint:human-verify">
  <name>Task 2: Full Insights page with refresh rate-limit countdown + historical picker</name>
  <what-built>
    Implement the complete frontend/src/app/(dashboard)/insights/page.tsx replacing the current stub. This includes the Reîmprospătează button with rate-limit countdown (using useMutation from useInsightsRefresh), historical date picker for INSI-02, all component wiring, and INSI-05 dual-mode fallback rendering.
  </what-built>

  <!-- NOTE: This checkpoint exists because the Insights page combines real-time rate-limit state
       (countdown timer), historical navigation, and a real backend POST call. The executor should
       automate everything, then the human verifies the countdown behavior works correctly. -->

  <files>
    frontend/src/app/(dashboard)/insights/page.tsx
  </files>
  <read_first>
    - frontend/src/app/(dashboard)/insights/page.tsx (current stub — read before replacing)
    - frontend/src/hooks/useInsights.ts (just created — read the exact exports and types; especially the useMutation return shape)
    - frontend/src/components/dashboards/insights/insight-summary.tsx (just created)
    - frontend/src/components/dashboards/insights/problem-card.tsx (just created)
    - frontend/src/components/dashboards/insights/generation-failed-banner.tsx (just created)
    - frontend/src/components/dashboards/insights/fallback-anomaly-list.tsx (just created)
    - .planning/phases/07-frontend-dashboards/07-CONTEXT.md (D-16 generation failure fallback — NOTE: D-15 references summary_ro which is WRONG; actual field is "summary" per B3 fix)
    - .planning/phases/07-frontend-dashboards/07-UI-SPEC.md (copywriting contract — Reîmprospătează button, rate-limit message, Primary CTAs)
  </read_first>
  <action>
    Replace frontend/src/app/(dashboard)/insights/page.tsx entirely. Add "use client". Export default InsightsPage wrapping <Suspense fallback={<Skeleton className="h-96 w-full" />}><InsightsPageContent /></Suspense>.

    InsightsPageContent implements:
    (a) useInsightsToday() for today's default view.
    (b) useState for selectedDate: string|null (null = show today's insight).
    (c) useInsightsByDate(selectedDate) — only active when selectedDate !== null.
    (d) Derived activeData: when selectedDate !== null use insightsByDate data, else use insightsToday data.
    (e) W10 fix — useInsightsRefresh returns a useMutation result. Destructure:
        const refreshMutation = useInsightsRefresh()
        Use refreshMutation.mutate() on button click, refreshMutation.isPending for loading state.
    (f) Rate-limit state: useState for retryAfterSeconds: number|null and countdown display string.
        In the useMutation onSuccess handler (or in the page via a useEffect watching refreshMutation.data):
        when refreshMutation.data?.retryAfterSeconds is non-null, set retryAfterSeconds state.
    (g) useEffect: when retryAfterSeconds is non-null, start setInterval(1000) counting down from
        retryAfterSeconds to 0; on reaching 0, clear interval and set retryAfterSeconds to null.
        Update countdown string each tick: format as "N:SS" (minutes:seconds).
    (h) handleRefresh: calls refreshMutation.mutate(). On the useMutation result's onSuccess
        callback (or via useEffect), extract retryAfterSeconds from returned data and set state.

    Page layout space-y-6:

    (1) Header row: flex items-center justify-between flex-wrap gap-2. Left: h1 text-xl font-semibold t("insights.title"). Right: flex gap-2 items-center. Historical date input (type="date", className min-h-[44px]) — when changed sets selectedDate and clears the countdown. When selectedDate is non-null show a "Astăzi" button (variant="outline") to reset back to today. Reîmprospătează button: variant="default", disabled when retryAfterSeconds !== null OR refreshMutation.isPending. Label: when rate-limited show t("insights.refreshCountdown", { remaining: countdownDisplay }); else t("insights.refreshButton"). Show a RefreshCw icon (lucide-react) when not pending, Loader2 with animate-spin class when refreshMutation.isPending.

    (2) Loading state: if isLoading (for the active query), render Skeleton h-8 w-full mb-4 (summary skeleton) + 3 × Skeleton h-[80px] rounded-lg.

    (3) Error state: if isError (for the active query), render inline Romanian error + Retry button.

    (4) No data state: if activeData === null (404 from history query), render Lightbulb icon + t("insights.noInsight") + t("insights.noInsightDesc").

    (5) When activeData is present AND generation_failed === false AND payload !== null:
        Render in order:
        - InsightSummary (summary=payload.summary, insightDate=activeData.date)
          B3 fix: pass payload.summary NOT payload.summary_ro — "summary_ro" is not a valid field.
        - {payload.problems.map(p => <ProblemCard key={p.id} problem={p} />)}

    (6) When activeData is present AND generation_failed === true:
        B6 fix — render both components:
        - <GenerationFailedBanner /> (always shown when generation_failed=true)
        - <FallbackAnomalyList
            problems={activeData.payload?.problems ?? null}
            status={activeData.status}
          />
        The FallbackAnomalyList handles Case A (fallback + problems) vs Case B (failed or no problems)
        internally. The page does not need to check further — just pass status and payload.problems.

    (7) INSI-06: Footer metadata line showing generated_at timestamp via formatTimestamp(activeData.generated_at). Label: t("insights.summary.dataFor").

    Notes on selectedDate input: use a plain HTML input type="date" with value={selectedDate ?? ""} and onChange. The date picker for insights history is simpler than the DateRangePicker — a single date only. It does NOT use react-day-picker for this use case. Format the date value as YYYY-MM-DD for the API call. When user picks a historical date, call useInsightsByDate with that date. When user clicks "Astăzi" reset selectedDate to null (reverts to useInsightsToday).
  </action>
  <verify>
    <automated>cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/frontend && pnpm typecheck && pnpm lint</automated>
  </verify>
  <how-to-verify>
    1. Start the frontend dev server: cd frontend && pnpm dev
    2. Navigate to http://localhost:3000/insights
    3. Verify the summary paragraph loads (or Skeleton shows while loading)
    4. If backend is live: up to 3 collapsed problem cards appear; click one to expand and see actions list
    5. Click "Reîmprospătează" once — should succeed (202) or show rate-limit countdown
    6. If 429 received: button label changes to "Reîmprospătează (N:SS)" countdown; button is disabled
    7. Change the date input to a past date (e.g. yesterday) — page refetches that date's insight
    8. Set date to far past date that has no insight — empty state shows t("insights.noInsight")
    9. Verify insight date shown is InsightEnvelope.date (yesterday's date), not today
    10. Verify at 360px mobile viewport (DevTools): page renders without overflow, all buttons 44px tap target
    11. (B6) If a fallback insight exists: verify GenerationFailedBanner + ProblemCard list renders when status="fallback"
    12. (B3) Verify the summary text renders (not blank/undefined) — if blank, "summary_ro" may have been used instead of "summary"
  </how-to-verify>
  <resume-signal>Type "approved" when rate-limit countdown, historical date picker, and insight display all work correctly. Describe any issues found.</resume-signal>
  <done>
    - frontend/src/app/(dashboard)/insights/page.tsx imports useInsightsToday, useInsightsByDate, useInsightsRefresh and all 4 insights sub-components
    - Reîmprospătează button is disabled and shows countdown when rate-limited
    - Refresh uses refreshMutation.mutate() (useMutation pattern — W10 fix)
    - Historical date input allows viewing past insights
    - payload.summary (NOT payload.summary_ro) is passed to InsightSummary (B3 fix)
    - Problem cards are collapsed by default; expand on click
    - problem.id is shown as INSI-04 source trace in each problem card
    - generation_failed=true renders GenerationFailedBanner + FallbackAnomalyList with status + problems props (B6 fix)
    - No @tremor/react imports
    - pnpm typecheck exits 0
    - pnpm lint exits 0
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Mobile viewport verification — D-17 NON-NEGOTIABLE (B5 fix)</name>
  <!-- B5 fix: D-17 requires full 360px mobile support. The CEO of Sofa Belle works exclusively
       from a phone. This checkpoint CANNOT be skipped. Phase cannot be marked complete without it. -->
  <what-built>
    All 4 dashboard pages (Sales, Salespeople, Marketing, Insights) have been implemented across
    Plans 02, 03, 04, and 05. This checkpoint verifies the D-17 mobile responsiveness requirement
    across all four pages before the phase is closed.
  </what-built>
  <files>
    <!-- No files modified — verification only -->
  </files>
  <how-to-verify>
    STEP 1: Start dev server: cd frontend && pnpm dev

    STEP 2: Open Chrome DevTools (F12) → click "Toggle device toolbar" (Ctrl+Shift+M / Cmd+Shift+M)

    STEP 3: For each breakpoint below, test ALL FOUR dashboard pages:
      - http://localhost:3000/sales
      - http://localhost:3000/salespeople
      - http://localhost:3000/marketing
      - http://localhost:3000/insights

    BREAKPOINTS TO TEST:
      A) 360px width (Galaxy S8 — primary user device): Set in DevTools responsive mode to 360px
      B) 768px width (iPad): Set in DevTools to 768px
      C) 1280px width (desktop): Set in DevTools to 1280px

    FOR EACH PAGE AT EACH BREAKPOINT, VERIFY:
      1. NO horizontal overflow — page fits within viewport width, no horizontal scrollbar on body
      2. Hamburger menu: at 360px and 768px, the fixed 240px sidebar is HIDDEN (not visible in layout)
         and a hamburger Menu icon is visible in the topbar
      3. Hamburger opens: tap/click the hamburger icon — a slide-over/Sheet overlay opens showing
         all 8 nav items; tap/click the X or outside to close
      4. All dashboard data is VISIBLE without horizontal scroll (exception: Salespeople table
         may have its own internal horizontal scroll with sticky first column — that is expected)
      5. Touch targets: all buttons, nav items, and interactive elements appear to be at least 44px tall
      6. KPI card grid: at 360px shows 1 column; at 768px shows 2 columns; at 1280px shows 4 columns
      7. Charts: render within the viewport width without overflow (Recharts ResponsiveContainer)
      8. DateRangePicker: at 360px tapping the date button opens a Sheet (bottom drawer);
         at 1280px it opens a Popover
      9. Funnel chart: at 360px shows vertical stacked layout; at 1280px shows horizontal bar chart

    PHASE CANNOT BE MARKED COMPLETE if any of the following are true:
      - Body horizontal overflow at 360px on any page
      - Sidebar visible at 360px (should be hidden — mobile only shows hamburger)
      - Hamburger button missing at 360px
      - Any interactive button visually appears smaller than 44px tall
  </how-to-verify>
  <resume-signal>
    Type "approved" after verifying all 4 pages at all 3 breakpoints. List any issues found with
    page name and breakpoint (e.g. "sales at 360px: horizontal overflow on revenue chart").
    If issues found, fix them before approving.
  </resume-signal>
  <done>
    - All 4 dashboard pages render correctly at 360px, 768px, and 1280px
    - No horizontal body overflow at 360px on any page
    - Hamburger sidebar navigation works on mobile (opens/closes)
    - All touch targets visually appear >= 44px
    - KPI grid responsive (1 → 2 → 4 columns)
    - Charts fit within viewport at all breakpoints
    - Human has explicitly typed "approved" to confirm
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| POST /insights/refresh | User-triggered pipeline run — rate-limited by backend Redis (1/hr); credentials: include cookie |
| Historical date param | User-supplied date string used in API call URL |
| API insight payload → JSX | AI-generated text from Claude rendered as JSX text nodes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-14 | Spoofing | POST /api/v1/insights/refresh | mitigate | apiClient.post uses credentials: "include"; SameSite=Lax backend cookie (Phase 1) prevents CSRF; rate-limit enforced server-side with Redis SET NX EX 3600 |
| T-7-15 | Tampering | Historical date input value → API URL | mitigate | Input type="date" constrains format to YYYY-MM-DD by browser; additional: validate with new Date(selectedDate) + isNaN check before passing to useInsightsByDate |
| T-7-16 | Tampering | AI-generated text in insight payload | mitigate | payload.summary, problem.title, problem.description rendered as JSX text nodes (React auto-escapes); no dangerouslySetInnerHTML; no eval(); no innerHTML assignments |
| T-7-17 | Info Disclosure | estimated_loss_ron as number type | accept | Business analytics data shown to authenticated users only; auth enforced by apiClient 401 interceptor |
| T-7-18 | Denial of Service | Rate-limit bypass attempt | accept | Backend enforces rate limit with Redis; frontend countdown is UX-only and does not gate security |
| T-7-19 | Elevation of Privilege | insights/refresh triggering pipeline | accept | Pipeline runs in Celery worker under service account; no privilege escalation from frontend |
</threat_model>

<verification>
1. `cd frontend && pnpm typecheck` exits 0
2. `cd frontend && pnpm lint` exits 0
3. `grep -r "@tremor" frontend/src/components/dashboards/insights` returns zero matches
4. `grep -c "dangerouslySetInnerHTML" frontend/src/app/(dashboard)/insights/page.tsx` returns 0
5. `grep -c "summary_ro" frontend/src/hooks/useInsights.ts` returns 0 (B3: must use "summary" not "summary_ro")
6. `grep -c "summary_ro" frontend/src/components/dashboards/insights/insight-summary.tsx` returns 0 (B3/W8)
7. `grep -c "payload.summary" frontend/src/components/dashboards/insights/insight-summary.tsx` >= 1 (W8: verify correct field)
8. `grep -c "Collapsible" frontend/src/components/dashboards/insights/problem-card.tsx` >= 1
9. `grep -c "problem.id" frontend/src/components/dashboards/insights/problem-card.tsx` >= 1 (INSI-04 trace)
10. `grep -c "Retry-After" frontend/src/hooks/useInsights.ts` >= 1 (rate limit parsing)
11. `grep -c "useMutation" frontend/src/hooks/useInsights.ts` >= 1 (W10: useMutation pattern)
12. `grep -c "problems" frontend/src/components/dashboards/insights/fallback-anomaly-list.tsx` >= 1 (B6: dual-mode)
13. Manual human verification: countdown, historical picker, card expand/collapse, fallback modes
14. Mobile viewport verification checkpoint (Task 3) must be explicitly approved (B5)
</verification>

<success_criteria>
- useInsights.ts exports useInsightsToday, useInsightsByDate (returns null on 404), and useInsightsRefresh (useMutation — W10)
- InsightPayload uses "summary" field (NOT "summary_ro") — B3 fix verified
- Problem.estimated_loss_ron typed as number (not string)
- ProblemCard uses shadcn Collapsible, collapsed by default, shows severity badge with correct colors
- INSI-04: problem.id shown as source trace label in each expanded card
- Reîmprospătează button disabled + countdown when 429 Retry-After received
- Historical date input switches between useInsightsToday and useInsightsByDate
- INSI-05 B6 fix: generation_failed=true renders GenerationFailedBanner + FallbackAnomalyList(problems, status)
  - Case A (status="fallback", problems present): banner + ProblemCard list from payload.problems[]
  - Case B (status="failed" or no problems): banner + static "Nu există date disponibile" paragraph
- insight-summary.tsx renders payload.summary (W8: contains "payload.summary" NOT "payload.summary_ro")
- INSI-06: InsightEnvelope.date shown as data date (not new Date())
- Human verification approved for countdown + historical picker + card expand behavior (Task 2)
- Mobile viewport verification approved at 360px/768px/1280px for all 4 pages (Task 3 — B5 fix)
- pnpm typecheck and pnpm lint both exit 0
</success_criteria>

<output>
Create `.planning/phases/07-frontend-dashboards/07-05-SUMMARY.md` when done.
</output>
