---
phase: 07-frontend-dashboards
plan: 03
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - frontend/src/hooks/useSalespeopleDashboard.ts
  - frontend/src/components/dashboards/salespeople/leaderboard-table.tsx
  - frontend/src/app/(dashboard)/salespeople/page.tsx
autonomous: true
requirements:
  - SALES-01
  - SALES-02
  - SALES-03
  - SALES-04
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-06
  - UI-07
  - UI-08

must_haves:
  truths:
    - "User navigating to /salespeople sees a sortable leaderboard table of 6 Sofa Belle reps"
    - "Table columns: rank, name, leads, visits, offers, contracts, revenue (RON), win-rate (%), avg_time_to_first_touch (duration), data_completeness_pct (%)"
    - "avg_time_to_first_touch cell shows red background (bg-red-50 text-red-700) when value > 240 minutes"
    - "Table is sortable by clicking column headers; default sort is contracts descending"
    - "On mobile the table has horizontal scroll with sticky first column (rep name)"
    - "Date range picker present on page; selecting a range updates URL and refetches data"
    - "Loading state shows table Skeleton; error state shows inline Romanian message + Retry; empty state shows Romanian copy"
    - "Revenue column shows RON-formatted strings; win-rate and data_completeness show percentages"
    - "No @tremor/react imports in any salespeople component"
  artifacts:
    - path: "frontend/src/hooks/useSalespeopleDashboard.ts"
      provides: "TanStack Query hook for GET /api/v1/dashboards/salespeople"
      exports: ["useSalespeopleDashboard", "SalespeopleDashboardData"]
    - path: "frontend/src/components/dashboards/salespeople/leaderboard-table.tsx"
      provides: "Sortable salespeople leaderboard with red TTFT highlight, mobile scroll, sticky column"
      contains: "overflow-x-auto"
    - path: "frontend/src/app/(dashboard)/salespeople/page.tsx"
      provides: "Full Salespeople dashboard page with Suspense + date range"
      contains: "useSalespeopleDashboard"
  key_links:
    - from: "frontend/src/app/(dashboard)/salespeople/page.tsx"
      to: "frontend/src/hooks/useSalespeopleDashboard.ts"
      via: "import useSalespeopleDashboard"
      pattern: "useSalespeopleDashboard"
    - from: "frontend/src/hooks/useSalespeopleDashboard.ts"
      to: "/api/v1/dashboards/salespeople"
      via: "apiClient.get queryFn"
      pattern: "dashboards/salespeople"
    - from: "frontend/src/components/dashboards/salespeople/leaderboard-table.tsx"
      to: "@/components/ui/table"
      via: "import Table, TableBody, etc."
      pattern: "from.*ui/table"
---

<objective>
Implement the Salespeople Dashboard page (SALES-01..04) with the sortable leaderboard table, TanStack Query hook, and full loading/empty/error states. This plan runs in Wave 2 parallel with Plans 02 and 04 — it shares no files with those plans.

Purpose: Deliver the salesperson performance view — leaderboard with per-rep funnel counts, revenue, win-rate, time-to-first-touch with red highlight, and data completeness.
Output: A fully functional /salespeople page with sortable table, responsive mobile layout with horizontal scroll and sticky column, and Romanian strings.
</objective>

<execution_context>
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-frontend-dashboards/07-CONTEXT.md
@.planning/phases/07-frontend-dashboards/07-RESEARCH.md
@.planning/phases/07-frontend-dashboards/07-PATTERNS.md
@.planning/phases/07-frontend-dashboards/07-UI-SPEC.md
@.planning/phases/07-frontend-dashboards/07-01-SUMMARY.md

<interfaces>
<!-- Backend response shapes from verified schema files -->

SalespeopleDashboardResponse fields (from backend/app/schemas/dashboards/salespeople.py):
  period: { from: string; to: string }
  salespeople: Array<SalespersonRow>

SalespersonRow fields:
  external_id: string
  name: string | null
  leads_assigned: number | null       (integer — NOT string)
  visits_conducted: number | null     (integer — NOT string)
  offers_sent: number | null          (integer — NOT string)
  deals_won: number | null            (integer — NOT string)
  revenue: string | null              (Decimal serialized as string per DATA-04)
  avg_deal_size: string | null        (Decimal — string)
  win_rate: string | null             (Decimal — string, e.g. "0.2100")
  avg_time_to_first_touch_minutes: number | null  (integer minutes — NOT string; red when > 240)
  data_completeness_pct: string | null (Decimal — string, e.g. "0.8500")
  conversion_l_to_v: string | null
  conversion_v_to_o: string | null
  conversion_o_to_c: string | null
  conversion_l_to_c: string | null

CRITICAL: avg_time_to_first_touch_minutes is number|null (INTEGER not Decimal), not string.
Revenue, win_rate, data_completeness_pct ARE strings (Decimal serialized).

Existing utilities from Plan 01:
  import { formatRON, formatPct, formatDate, formatDuration, getDefaultDateRange } from "@/lib/formatters"
  import { apiClient } from "@/lib/api-client"
  import { DateRangePicker } from "@/components/date-range-picker"

shadcn components (installed in Plan 01):
  import { Table, TableBody, TableCell, TableHead, TableHeader, TableRow } from "@/components/ui/table"
  import { Skeleton } from "@/components/ui/skeleton"
  import { Button } from "@/components/ui/button"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useSalespeopleDashboard hook</name>
  <files>
    frontend/src/hooks/useSalespeopleDashboard.ts
  </files>
  <read_first>
    - frontend/src/hooks/useSalesDashboard.ts (created in Plan 02 — use as pattern exactly, same structure)
    - frontend/src/lib/api-client.ts (apiClient.get signature)
    - backend/app/schemas/dashboards/salespeople.py (verified field types — especially avg_time_to_first_touch_minutes is number not string)
    - .planning/phases/07-frontend-dashboards/07-PATTERNS.md (useSalespeopleDashboard section — role-match to useSalesDashboard)
  </read_first>
  <action>
    Create frontend/src/hooks/useSalespeopleDashboard.ts following the identical pattern as useSalesDashboard.ts (read that file first for the exact structure). Add "use client". Define SalespersonRow interface with all fields as verified: avg_time_to_first_touch_minutes is number|null (NOT string), revenue/win_rate/data_completeness_pct/conversion_* are string|null. Define SalespeopleDashboardData interface with period and salespeople: SalespersonRow[]. Export useSalespeopleDashboard(from: string, to: string) using useQuery<SalespeopleDashboardData> with queryKey: ["salespeople", from, to], queryFn hitting GET /api/v1/dashboards/salespeople?from=${from}&to=${to}, enabled: Boolean(from && to). Do NOT set staleTime per-query (inherits 5min from layout QueryClient default).
  </action>
  <verify>
    <automated>cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/frontend && pnpm typecheck</automated>
  </verify>
  <done>
    - frontend/src/hooks/useSalespeopleDashboard.ts exports useSalespeopleDashboard and SalespeopleDashboardData
    - SalespersonRow interface has avg_time_to_first_touch_minutes: number|null (integer, not string)
    - SalespersonRow interface has revenue, win_rate, data_completeness_pct as string|null
    - pnpm typecheck exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: LeaderboardTable component + Salespeople page</name>
  <files>
    frontend/src/components/dashboards/salespeople/leaderboard-table.tsx,
    frontend/src/app/(dashboard)/salespeople/page.tsx
  </files>
  <read_first>
    - frontend/src/app/(dashboard)/salespeople/page.tsx (current stub — read before replacing)
    - frontend/src/components/ui/table.tsx (Table, TableBody, TableCell, TableHead, TableHeader, TableRow API)
    - frontend/src/components/ui/skeleton.tsx (Skeleton component API)
    - .planning/phases/07-frontend-dashboards/07-PATTERNS.md (leaderboard-table.tsx section — mobile scroll pattern, red cell pattern, active sort pattern)
    - .planning/phases/07-frontend-dashboards/07-UI-SPEC.md (Salespeople Table section — sortable, sticky column, red cell TTFT)
    - .planning/phases/07-frontend-dashboards/07-RESEARCH.md (Phase 6 API section for SalespersonRow field confirmations)
  </read_first>
  <action>
    Step 1 — Create frontend/src/components/dashboards/salespeople/leaderboard-table.tsx. Add "use client". Props: salespeople (SalespersonRow[]) imported from useSalespeopleDashboard. Imports: Table, TableBody, TableCell, TableHead, TableHeader, TableRow from @/components/ui/table; useState from react; formatRON, formatPct, formatDuration from @/lib/formatters; useTranslations from next-intl; Users from lucide-react; ChevronUp, ChevronDown from lucide-react; cn from @/lib/utils.

    Implement sorting state: type SortField = "leads_assigned" | "visits_conducted" | "offers_sent" | "deals_won" | "revenue" | "win_rate" | "avg_time_to_first_touch_minutes" | "data_completeness_pct"; type SortDir = "asc" | "desc"; useState with default { field: "deals_won", dir: "desc" }. sortedData useMemo: sort salespeople array by sortState — for string fields (revenue, win_rate) parse with parseFloat before comparing; for integer fields compare directly; handle null values by treating null as 0. Rank is the position in the sorted array (1-indexed).

    Column headers render as button elements (for a11y): onClick cycles asc → desc → reset to default. Active column shows ChevronUp/ChevronDown icon. Buttons must have min-h-[44px] for touch target compliance (D-24).

    Empty state: when salespeople.length === 0 render Users icon + t("salespeople.noData") + t("salespeople.noDataDesc") instead of the table.

    Mobile scroll: wrap entire table in div className="overflow-x-auto". Table element className="min-w-[800px]" to force horizontal scroll on mobile. First column (name/rank) must be sticky: TableHead className="sticky left-0 bg-background z-10" and corresponding TableCell className="sticky left-0 bg-background z-10".

    Each row: (a) Rank cell (position number). (b) Name cell sticky. (c) leads_assigned. (d) visits_conducted (nullable — show "—" when null). (e) offers_sent. (f) deals_won. (g) Revenue: formatRON(row.revenue), right-aligned. (h) win_rate: formatPct(row.win_rate), right-aligned. (i) avg_time_to_first_touch_minutes: formatDuration(row.avg_time_to_first_touch_minutes) — CRITICAL: apply bg-red-50 text-red-700 font-medium to THIS CELL ONLY when row.avg_time_to_first_touch_minutes !== null AND > 240 (the 4h threshold per D-05, SALES-02). (j) data_completeness_pct: formatPct(row.data_completeness_pct).

    Step 2 — Replace frontend/src/app/(dashboard)/salespeople/page.tsx entirely. Add "use client". Same Suspense + inner content component pattern as sales/page.tsx. SalespersonContent: useSearchParams(), getDefaultDateRange(), useSalespeopleDashboard(from, to). Page layout: space-y-6. Header with h1 t("salespeople.title") + DateRangePicker. One main section: if isLoading show 5 Skeleton rows (each h-[44px]); if isError show inline Romanian error + Retry button; else if data.salespeople.length > 0 show LeaderboardTable; else show empty state. Import DateRangePicker from @/components/date-range-picker.
  </action>
  <verify>
    <automated>cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/frontend && pnpm typecheck && pnpm lint</automated>
  </verify>
  <done>
    - frontend/src/components/dashboards/salespeople/leaderboard-table.tsx exists
    - Table wrapper has overflow-x-auto and min-w-[800px] Table
    - First column header and cells have sticky left-0 classes
    - avg_time_to_first_touch_minutes cell has conditional bg-red-50 text-red-700 when > 240
    - Sorting state implemented with default deals_won descending
    - Column headers are button elements with chevron icons
    - frontend/src/app/(dashboard)/salespeople/page.tsx imports useSalespeopleDashboard and LeaderboardTable
    - No @tremor/react imports
    - No dangerouslySetInnerHTML
    - pnpm typecheck exits 0
    - pnpm lint exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params → API call | ?from= and ?to= values from useSearchParams fed to apiClient.get URL |
| API response → table render | Salesperson names, revenue values rendered as React text nodes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-06 | Tampering | useSalespeopleDashboard queryFn URL | mitigate | from/to validated: only passed when both present (enabled: Boolean(from && to)); DateRangePicker only emits valid YYYY-MM-DD strings |
| T-7-07 | Tampering | LeaderboardTable sort state | accept | Client-side sort only; no SQL/server impact; sort state is in-memory React state |
| T-7-08 | Tampering | Salesperson name rendered in table | mitigate | Rendered as JSX text node (auto-escaped); no dangerouslySetInnerHTML |
| T-7-09 | Spoofing | POST /api/v1/* not called by this page | accept | Salespeople page is read-only; no mutation endpoints used |
</threat_model>

<verification>
1. `cd frontend && pnpm typecheck` exits 0
2. `cd frontend && pnpm lint` exits 0
3. `grep -r "@tremor" frontend/src/components/dashboards/salespeople` returns zero matches
4. `grep -c "overflow-x-auto" frontend/src/components/dashboards/salespeople/leaderboard-table.tsx` >= 1
5. `grep -c "bg-red-50" frontend/src/components/dashboards/salespeople/leaderboard-table.tsx` >= 1
6. `grep -c "> 240" frontend/src/components/dashboards/salespeople/leaderboard-table.tsx` >= 1
</verification>

<success_criteria>
- useSalespeopleDashboard hook types avg_time_to_first_touch_minutes as number|null (not string)
- LeaderboardTable renders with horizontal scroll on mobile and sticky first column
- Red cell applied to avg_time_to_first_touch_minutes when value > 240 minutes
- Table is sortable with default contracts (deals_won) descending
- Salespeople page has Suspense wrapper and full loading/empty/error states
- All text strings via useTranslations — no hardcoded Romanian
- pnpm typecheck and pnpm lint both exit 0
</success_criteria>

<output>
Create `.planning/phases/07-frontend-dashboards/07-03-SUMMARY.md` when done.
</output>
