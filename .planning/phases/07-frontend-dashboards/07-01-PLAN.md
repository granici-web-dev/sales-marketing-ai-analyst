---
phase: 07-frontend-dashboards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/postcss.config.mjs
  - frontend/src/app/(dashboard)/layout.tsx
  - frontend/src/components/sidebar.tsx
  - frontend/src/components/topbar.tsx
  - frontend/src/lib/formatters.ts
  - frontend/src/lib/__tests__/formatters.test.ts
  - frontend/messages/ro.json
  - frontend/messages/en.json
autonomous: false
requirements:
  - UI-01
  - UI-02
  - UI-03
  - UI-04
  - UI-05
  - UI-06
  - UI-07
  - UI-08

must_haves:
  truths:
    - "pnpm dev serves the app with styled sidebar (gray background, blue active item), styled topbar, styled login page — no unstyled HTML"
    - "formatRON(85000) returns '85.000,00 RON' (Romanian locale dot-thousands, comma-decimal)"
    - "formatDate('2026-05-28') returns '28.05.2026'"
    - "formatDuration(150) returns '2h 30min'"
    - "formatWowDelta('0.12') returns { label: '▲ +12.0%', positive: true }"
    - "Dashboard layout renders with QueryClientProvider and DataFreshnessBanner slot"
    - "On mobile (<768px) sidebar is hidden (hidden md:flex on aside); topbar starts at left-0 not left-240px"
    - "All 6 new i18n namespaces present in both ro.json and en.json"
  artifacts:
    - path: "frontend/postcss.config.mjs"
      provides: "PostCSS config enabling @tailwindcss/postcss pipeline (KI-01 fix)"
      contains: "@tailwindcss/postcss"
    - path: "frontend/src/lib/formatters.ts"
      provides: "formatRON, formatPct, formatDate, formatTimestamp, formatDuration, formatWowDelta, getDefaultDateRange"
      exports: ["formatRON", "formatDate", "formatTimestamp", "formatDuration", "formatPct", "formatWowDelta", "getDefaultDateRange"]
    - path: "frontend/src/lib/__tests__/formatters.test.ts"
      provides: "Unit tests for all 6 formatter functions"
      min_lines: 40
    - path: "frontend/src/app/(dashboard)/layout.tsx"
      provides: "QueryClientProvider with getQueryClient singleton, DataFreshnessBanner, mobile-responsive main margin"
      contains: "QueryClientProvider"
    - path: "frontend/src/components/data-freshness-banner.tsx"
      provides: "Shared freshness/stale data banner driven by useHealthData()"
      contains: "useHealthData"
    - path: "frontend/src/hooks/useHealthData.ts"
      provides: "TanStack Query hook for /api/v1/health/data with 60s staleTime"
      contains: "health"
  key_links:
    - from: "frontend/src/app/(dashboard)/layout.tsx"
      to: "frontend/src/components/data-freshness-banner.tsx"
      via: "import DataFreshnessBanner"
      pattern: "DataFreshnessBanner"
    - from: "frontend/src/components/data-freshness-banner.tsx"
      to: "/api/v1/health/data"
      via: "useHealthData() queryFn"
      pattern: "health/data"
---

<objective>
Fix KI-01 (CSS/Tailwind not loading), install all required packages and shadcn components, wire QueryClientProvider into the dashboard layout, create shared formatter utilities with unit tests, implement the mobile-responsive layout fixes, and add all i18n string namespaces. This is the prerequisite Wave 1 plan — Plans 02-05 cannot start until this plan is complete and CSS renders correctly.

Purpose: Unblock all dashboard content plans by ensuring PostCSS/Tailwind work, Recharts is available, shadcn components are installed, and shared utilities exist.
Output: Working styled frontend dev server, 7 new shadcn components, formatters.ts with tests, responsive layout, 6 new i18n namespaces.
</objective>

<execution_context>
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-frontend-dashboards/07-CONTEXT.md
@.planning/phases/07-frontend-dashboards/07-RESEARCH.md
@.planning/phases/07-frontend-dashboards/07-PATTERNS.md
@.planning/phases/07-frontend-dashboards/07-UI-SPEC.md

<interfaces>
<!-- Key existing files the executor must understand before modifying. -->

From frontend/src/app/(dashboard)/layout.tsx (current state — Server Component, no QueryClientProvider):
The file has NO "use client" directive. Must be fully replaced with the Client Component pattern from RESEARCH.md Pattern 2.

From frontend/src/lib/api-client.ts:
export const apiClient = {
  get: (url: string) => apiFetch(url),   // handles credentials + 401 refresh
  post: (url: string, body: unknown) => apiFetch(url, { method: "POST", ... }),
};
apiClient.get(url) returns Promise<Response>. Always check res.ok before res.json().

From frontend/messages/ro.json (current top-level keys):
{ "auth": {...}, "nav": {...}, "placeholder": {...}, "locale": {...} }
Do NOT restructure existing keys — only append 6 new top-level namespaces.

From frontend/src/app/globals.css — CSS variables used for colors:
--color-accent: hsl(221 83% 53%)
--color-background: hsl(0 0% 100%)
--color-secondary: hsl(240 5% 96%)
--color-foreground: hsl(240 10% 4%)
--color-muted-foreground: hsl(240 4% 46%)
--color-border: hsl(240 6% 90%)
--color-destructive: hsl(0 72% 51%)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PostCSS fix + package installs + shadcn components + formatters.ts with tests</name>
  <files>
    frontend/postcss.config.mjs,
    frontend/src/lib/formatters.ts,
    frontend/src/lib/__tests__/formatters.test.ts,
    frontend/src/hooks/useHealthData.ts,
    frontend/src/components/data-freshness-banner.tsx
  </files>
  <read_first>
    - frontend/next.config.ts (ESM export default pattern to follow)
    - frontend/src/lib/utils.ts (same directory, same named-export pattern for formatters.ts)
    - frontend/src/lib/api-client.ts (apiClient.get usage in queryFn)
    - frontend/src/app/globals.css (CSS variables — use --color-accent, --color-border etc.)
    - frontend/package.json (confirm @tailwindcss/postcss in devDependencies before creating postcss.config.mjs)
    - .planning/phases/07-frontend-dashboards/07-RESEARCH.md (Pattern 3 for useHealthData, formatters code examples section)
  </read_first>
  <behavior>
    - formatRON(85000) === "85.000,00 RON"
    - formatRON(null) === "N/A"
    - formatRON("85000.50") === "85.000,50 RON"
    - formatRON(1234, true) (compact=true) === "1.234 RON" (0 decimal places)
    - formatPct("0.285") === "28,5%"
    - formatPct(null) === "N/A"
    - formatDate("2026-05-28") === "28.05.2026"
    - formatDate(null) === ""
    - formatDuration(150) === "2h 30min"
    - formatDuration(45) === "45min"
    - formatDuration(60) === "1h"
    - formatDuration(null) === "N/A"
    - formatWowDelta("0.12") === { label: "▲ +12.0%", positive: true }
    - formatWowDelta("-0.05") === { label: "▼ -5.0%", positive: false }
    - formatWowDelta(null) === { label: "", positive: null }
    - getDefaultDateRange() returns { from: YYYY-MM-DD (29 days ago), to: YYYY-MM-DD (today) }
  </behavior>
  <action>
    Step 1 — Create frontend/postcss.config.mjs with content: export default { plugins: { "@tailwindcss/postcss": {} } }. Use ESM export default (not module.exports). File extension must be .mjs (not .js) for Next.js 16 + ESM compatibility per D-01.

    Step 2 — Install packages. From the frontend/ directory run: pnpm add recharts react-day-picker. Then run: npx shadcn add card skeleton badge table chart collapsible sheet. Accept all prompts with defaults (components.json already configured). Do NOT run pnpm add @tailwindcss/postcss — it is already in devDependencies.

    Step 3 — Create frontend/src/lib/formatters.ts. This is a pure utility file — NO "use client" directive needed. Implement these 7 named exports using the exact signatures from RESEARCH.md Code Examples section: formatRON(value, compact?), formatPct(value), formatDate(dateStr), formatTimestamp(dateStr), formatDuration(minutes), formatWowDelta(delta), getDefaultDateRange(). All monetary formatting via Intl.NumberFormat('ro-RO', { style: 'currency', currency: 'RON' }). All percentage via Intl.NumberFormat('ro-RO', { style: 'percent', maximumFractionDigits: 1 }). All date via Intl.DateTimeFormat('ro-RO', ...). Never use .toFixed(). formatWowDelta: parse delta string via parseFloat, multiply by 100 for display, use ▲ for positive and ▼ for negative, return { label, positive } shape. getDefaultDateRange: today minus 29 days as from, today as to, both as YYYY-MM-DD strings via toISOString().split("T")[0].

    Step 4 — Create frontend/src/lib/__tests__/formatters.test.ts. Write vitest or jest unit tests covering all behaviors listed in the behavior block above. Import from "@/lib/formatters" (or relative path "../formatters"). Check that the test runner configured in package.json supports the import alias — if not, use relative import.

    Step 5 — Create frontend/src/hooks/useHealthData.ts. Add "use client" directive. Import useQuery from @tanstack/react-query, apiClient from @/lib/api-client. Define HealthDataResponse interface: { last_sync_at: string | null; last_pipeline_status: string | null; stale: boolean }. Export useHealthData(): useQuery<HealthDataResponse> with queryKey: ["health", "data"], queryFn hitting GET /api/v1/health/data, staleTime override of 60 * 1000 (overrides the layout default of 5min — freshness needs more frequent refresh per PATTERNS.md).

    Step 6 — Create frontend/src/components/data-freshness-banner.tsx. Add "use client". Import useHealthData from @/hooks/useHealthData, useTranslations from next-intl, AlertTriangle from lucide-react, formatTimestamp from @/lib/formatters. Component reads data?.stale and data?.last_pipeline_status. Return null when !data?.stale. When stale: amber banner with bg-amber-50, border-l-4 border-amber-500, text-amber-800, role="alert". Include last_sync_at formatted via formatTimestamp if present. Add localStorage dismissal: on mount check localStorage.getItem('freshness_dismissed') and if set do not render. Add an X button that calls localStorage.setItem('freshness_dismissed', '1') and hides the banner. Banner is NOT dismissible across page loads — just within the session; use useState to track dismissed state, set it when X is clicked. Romanian text from useTranslations("errors"): errors.dataStale key.
  </action>
  <verify>
    <automated>cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/frontend && pnpm typecheck</automated>
  </verify>
  <done>
    - frontend/postcss.config.mjs exists and contains "@tailwindcss/postcss"
    - frontend/src/lib/formatters.ts exports formatRON, formatPct, formatDate, formatTimestamp, formatDuration, formatWowDelta, getDefaultDateRange
    - frontend/src/lib/__tests__/formatters.test.ts exists with at least 12 test cases covering all behaviors
    - frontend/src/hooks/useHealthData.ts exports useHealthData with staleTime: 60000
    - frontend/src/components/data-freshness-banner.tsx exists and imports useHealthData
    - All shadcn components installed: frontend/src/components/ui/card.tsx, skeleton.tsx, badge.tsx, table.tsx, chart.tsx, collapsible.tsx, sheet.tsx
    - pnpm typecheck exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Dashboard layout + sidebar + topbar mobile fixes + i18n namespaces</name>
  <files>
    frontend/src/app/(dashboard)/layout.tsx,
    frontend/src/components/sidebar.tsx,
    frontend/src/components/topbar.tsx,
    frontend/messages/ro.json,
    frontend/messages/en.json
  </files>
  <read_first>
    - frontend/src/app/(dashboard)/layout.tsx (current state — Server Component, 18 lines, must be read before replacing)
    - frontend/src/components/sidebar.tsx (full file — current aside className, navItems array, active link pattern)
    - frontend/src/components/topbar.tsx (full file — current header left-[240px], locale toggle, handleLocaleToggle)
    - frontend/messages/ro.json (current 4 top-level keys — must append without restructuring)
    - frontend/messages/en.json (current state — same structure as ro.json, expand in sync)
    - .planning/phases/07-frontend-dashboards/07-PATTERNS.md (layout.tsx, sidebar.tsx, topbar.tsx modification sections with exact code)
    - .planning/phases/07-frontend-dashboards/07-UI-SPEC.md (i18n contract section, copywriting contract section)
  </read_first>
  <action>
    Step 1 — Replace frontend/src/app/(dashboard)/layout.tsx entirely. Add "use client" at top. Import: isServer, QueryClient, QueryClientProvider from @tanstack/react-query; Sidebar from @/components/sidebar; Topbar from @/components/topbar; DataFreshnessBanner from @/components/data-freshness-banner. Implement makeQueryClient() returning new QueryClient({ defaultOptions: { queries: { staleTime: 5*60*1000, refetchOnWindowFocus: false } } }). Implement module-level browserQueryClient: QueryClient | undefined = undefined. Implement getQueryClient(): if isServer return makeQueryClient(); if !browserQueryClient assign makeQueryClient(); return browserQueryClient. Export DashboardLayout that calls getQueryClient() (NOT useState), wraps children in QueryClientProvider, renders Sidebar + Topbar + main. Main className: "ml-0 md:ml-[240px] mt-14 p-4 md:p-8 bg-white min-h-[calc(100vh-56px)]". Inside main render DataFreshnessBanner before {children}. Do NOT use useState for the QueryClient — the getQueryClient singleton pattern is mandatory per D-08.

    Step 2 — Modify frontend/src/components/sidebar.tsx. Read the full file first. Find the aside element opening tag. Change its className by adding "hidden md:flex" to the existing classes — the aside must be hidden on mobile. The desktop sidebar becomes: className="hidden md:flex fixed left-0 top-0 h-screen w-[240px] bg-[hsl(240_5%_96%)] border-r border-[hsl(240_6%_90%)] flex-col". No other changes to sidebar.tsx — the mobile Sheet nav lives in topbar.tsx.

    Step 3 — Modify frontend/src/components/topbar.tsx. Read the full file first. Make the following changes: (a) Change header element left-[240px] to left-0 md:left-[240px]. (b) Add import: useState from react; Sheet, SheetContent from @/components/ui/sheet; Menu, X from lucide-react. (c) Add const [mobileMenuOpen, setMobileMenuOpen] = useState(false) inside the component. (d) Add a hamburger button inside the header, before the page title span — className="md:hidden mr-3 min-h-[44px] min-w-[44px] flex items-center justify-center rounded-md hover:bg-[hsl(240_5%_92%)]", aria-label uses t("common.openMenu"), onClick calls setMobileMenuOpen(true). Renders Menu icon (size 24). (e) After the closing </header> tag, add a Sheet component: Sheet open={mobileMenuOpen} onOpenChange={setMobileMenuOpen}. Inside SheetContent side="left" className="w-[280px] p-0" render the same 8 nav items as the desktop sidebar. Import navItems pattern from sidebar.tsx or duplicate the nav JSX inline — use the same active link className pattern from sidebar.tsx (the blue left-border active state). Add a close button at top of SheetContent with X icon (44px tap target). All nav items in mobile Sheet must be Link components with the same href values as the desktop sidebar.

    Step 4 — Expand frontend/messages/ro.json. Append 6 new top-level namespaces after the existing "locale" key. Do NOT remove or modify "auth", "nav", "placeholder", "locale". Add these namespaces with complete Romanian strings: "sales" namespace covering: title ("Vânzări"), funnel.title ("Palnie de vânzări"), funnel.stages.lead/visit/offer/contract ("Lead"/"Vizita"/"Oferta"/"Contract"), funnel.conversion ("Conversie"), funnel.noData ("Nicio activitate"), funnel.noDataDesc ("Nu au fost înregistrate lead-uri în această perioadă."), funnel.noDataIcon ("BarChart3"), kpi.leads ("Lead-uri"), kpi.visits ("Vizite"), kpi.offers ("Oferte"), kpi.contracts ("Contracte"), kpi.revenue ("Venituri"), kpi.avgDealSize ("Valoare medie"), sources.title ("Surse lead-uri"), sources.leadsColumn ("Lead-uri"), sources.conversionColumn ("Conversie"), sources.noData ("Fără date"), sources.noDataDesc ("Nu există date de sursă pentru perioada selectată."), stuckOffers.title ("Oferte blocate"), stuckOffers.days ("{days} zile"), stuckOffers.noData ("Fără oferte blocate"), stuckOffers.noDataDesc ("Nu există oferte blocate în acest interval. Excelent!"), revenue.title ("Evoluție venituri"), revenue.noData ("Fără venituri înregistrate"), revenue.noDataDesc ("Nu există date de vânzări pentru perioada selectată."); "salespeople" namespace covering: title ("Agenți de vânzări"), table.rank ("#"), table.name ("Agent"), table.leads ("Lead-uri"), table.visits ("Vizite"), table.offers ("Oferte"), table.contracts ("Contracte"), table.revenue ("Venituri"), table.winRate ("Rată câștig"), table.ttft ("Timp prim contact"), table.completeness ("Completitudine"), noData ("Niciun agent activ"), noDataDesc ("Nu există date de performanță pentru perioada selectată."); "marketing" namespace covering: title ("Marketing"), sourceVolume.title ("Volum lead-uri pe sursă"), sourceVolume.noData ("Fără date de marketing"), sourceVolume.noDataDesc ("Nu există date de volum pentru perioada selectată."), junk.title ("Lead-uri junk pe sursă"), junk.source ("Sursă"), junk.junkCount ("Junk"), junk.totalLeads ("Total"), junk.junkPct ("% Junk"), siteConversion.title ("Conversie site"), adSpendTitle ("Date publicitate"), adSpendComingSoon ("CPL, CAC și ROAS vor fi disponibile după conectarea conturilor publicitare."); "insights" namespace covering: title ("Analize AI"), summary.heading ("Rezumat zilnic"), summary.dataFor ("Date pentru:"), problem.estimatedLoss ("Pierdere estimată"), problem.actions ("Acțiuni recomandate"), problem.owner ("Responsabil"), problem.deadline ("Termen"), problem.source ("Sursă"), severity.high ("Critică"), severity.medium ("Ridicată"), severity.low ("Medie"), refreshButton ("Reîmprospătează"), refreshCountdown ("Reîmprospătează ({remaining})"), rateLimitMessage ("Poți reîmprospăta o dată pe oră. Mai ai {minutes} minute."), generationFailed ("Generarea automată a eșuat. Se afișează problemele detectate automat."), noInsight ("Analiză indisponibilă"), noInsightDesc ("Nu există o analiză generată pentru data selectată."), datePicker ("Alege data"), fallbackTitle ("Probleme detectate automat"); "common" namespace covering: loading ("Se încarcă..."), error ("A apărut o eroare. Încearcă din nou."), retry ("Încearcă din nou"), apply ("Aplică perioada"), openMenu ("Deschide meniu"), closeMenu ("Închide meniu"), noData ("Fără date disponibile"), noDataDesc ("Nu există date disponibile pentru perioada selectată."), dataStale ("Datele nu au fost actualizate recent. Ultima sincronizare: {lastSync}."), never ("niciodată"); "errors" namespace covering: networkError ("Eroare de rețea. Verifică conexiunea."), serverError ("Eroare server. Încearcă din nou."), pipelineFailed ("Ultima sincronizare a eșuat. Datele pot fi incomplete.").

    Step 5 — Expand frontend/messages/en.json with the same 6 namespaces as English mirrors. English translations: "sales".title = "Sales", "salespeople".title = "Salespeople", "marketing".title = "Marketing", "insights".title = "AI Insights", "common".loading = "Loading...", "common".error = "An error occurred. Try again.", "common".retry = "Try again", "common".openMenu = "Open menu", "common".closeMenu = "Close menu", "common".apply = "Apply period", "common".noData = "No data available", "common".dataStale = "Data has not been updated recently. Last sync: {lastSync}.", "errors".pipelineFailed = "Last sync failed. Data may be incomplete." — all other English strings should be reasonable English translations of the Romanian counterparts. Keep currency format as RON (not £/$/€) in both locales per D-25.
  </action>
  <verify>
    <automated>cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/frontend && pnpm typecheck && pnpm lint</automated>
    <human-check>Run pnpm dev, open http://localhost:3000/login and verify: sidebar shows gray background with blue active nav item, topbar is full-width at top, login form has correct shadcn styling. This is the D-03 visual audit — CSS must render correctly before Wave 2 plans execute.</human-check>
  </verify>
  <done>
    - frontend/src/app/(dashboard)/layout.tsx contains "use client", QueryClientProvider, getQueryClient(), DataFreshnessBanner, and main className includes "ml-0 md:ml-[240px]"
    - frontend/src/components/sidebar.tsx aside element has "hidden md:flex" in className
    - frontend/src/components/topbar.tsx header element has "left-0 md:left-[240px]" and contains hamburger Menu button with md:hidden class
    - frontend/messages/ro.json contains 10 top-level keys (4 existing + 6 new): auth, nav, placeholder, locale, sales, salespeople, marketing, insights, common, errors
    - frontend/messages/en.json mirrors same 10 top-level keys
    - pnpm typecheck exits 0
    - pnpm lint exits 0
    - Visual: sidebar renders with shadcn styles (colors, hover states) when CSS is fixed
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL params → query key | ?from= and ?to= read via useSearchParams() and passed to TanStack Query key; must not allow injection |
| API response → JSX render | All API data rendered as JSX text nodes (never dangerouslySetInnerHTML) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-01 | Tampering | useHealthData queryFn | mitigate | apiClient.get uses credentials: "include"; relative URL /api/v1/health/data — no absolute URL in bundle |
| T-7-02 | Tampering | data-freshness-banner.tsx | accept | Read-only display component; no user input; no dangerouslySetInnerHTML |
| T-7-03 | Info Disclosure | formatters.ts | accept | Pure compute utility; no secrets, no I/O |
| T-7-04 | Spoofing | QueryClientProvider layout | accept | Auth handled by apiClient 401 interceptor (Phase 1); no new auth surface |
| T-7-SC | Tampering | pnpm add recharts react-day-picker | mitigate | Both packages verified in RESEARCH.md Package Legitimacy Audit as VERIFIED npm packages with 10+ year history |
</threat_model>

<verification>
After both tasks complete:
1. `cd frontend && pnpm typecheck` exits 0
2. `cd frontend && pnpm lint` exits 0
3. `grep -r "@tremor" frontend/src` returns zero matches
4. `grep -c "@tailwindcss/postcss" frontend/postcss.config.mjs` returns 1
5. `ls frontend/src/components/ui/chart.tsx` exists
6. `ls frontend/src/components/ui/card.tsx` exists
7. `ls frontend/src/components/ui/sheet.tsx` exists
8. `ls frontend/src/lib/formatters.ts` exists
9. D-03 visual audit: pnpm dev + sidebar/topbar/login page renders with correct shadcn styles
</verification>

<success_criteria>
- postcss.config.mjs created with @tailwindcss/postcss plugin (KI-01 resolved)
- recharts + react-day-picker installed; shadcn card/skeleton/badge/table/chart/collapsible/sheet installed
- formatters.ts exports 7 functions; formatRON("85000") returns "85.000,00 RON" using ro-RO locale
- Dashboard layout is a Client Component with QueryClientProvider getQueryClient singleton (NOT useState)
- Main content margin is ml-0 md:ml-[240px] (mobile responsive)
- Sidebar aside has hidden md:flex (hidden on mobile)
- Topbar header has left-0 md:left-[240px] and contains hamburger button with md:hidden
- Mobile Sheet overlay renders all 8 nav items when hamburger tapped
- Both ro.json and en.json have 6 new namespaces: sales, salespeople, marketing, insights, common, errors
- pnpm typecheck && pnpm lint both exit 0
</success_criteria>

<output>
Create `.planning/phases/07-frontend-dashboards/07-01-SUMMARY.md` when done.
</output>
