---
phase: 05-ai-insights
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - backend/alembic/versions/008_daily_insights.py
  - backend/app/models/insights/__init__.py
  - backend/app/models/insights/daily_insight.py
  - backend/app/models/__init__.py
autonomous: true
requirements:
  - AI-01
  - AI-08

must_haves:
  truths:
    - "Alembic migration 008 creates daily_insights table with all D-16 columns and UNIQUE(tenant_id, date)"
    - "DailyInsight ORM model maps to the table and is discoverable by Alembic autogenerate"
    - "UPSERT conflict target columns match between migration DDL and ORM model exactly"
  artifacts:
    - path: "backend/alembic/versions/008_daily_insights.py"
      provides: "DDL for daily_insights table"
      contains: "UNIQUE(tenant_id, date)"
    - path: "backend/app/models/insights/daily_insight.py"
      provides: "DailyInsight SQLAlchemy ORM model"
      exports: ["DailyInsight"]
    - path: "backend/app/models/insights/__init__.py"
      provides: "Package init for Alembic model discovery"
    - path: "backend/app/models/__init__.py"
      provides: "Updated to import models.insights for autogenerate"
  key_links:
    - from: "backend/alembic/versions/008_daily_insights.py"
      to: "backend/alembic/versions/007_detected_problems.py"
      via: "down_revision"
      pattern: "down_revision = \"007\""
    - from: "backend/app/models/insights/daily_insight.py"
      to: "backend/app/db/base.py"
      via: "TenantScopedMixin"
      pattern: "class DailyInsight(Base, TenantScopedMixin)"
---

<objective>
Create Alembic migration 008 and the DailyInsight SQLAlchemy ORM model.

Purpose: The database table and ORM model are the foundation for InsightRepository (Wave 2). All D-16 columns must be present in both migration DDL and ORM model with exact type matching.
Output: migration 008_daily_insights.py + DailyInsight model + package init files.
</objective>

<execution_context>
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ai-insights/05-CONTEXT.md

# Migration pattern to replicate:
@backend/alembic/versions/007_detected_problems.py

# ORM model pattern to replicate:
@backend/app/models/anomaly/detected_problem.py

# Base classes to use:
@backend/app/db/base.py

# Package init pattern:
@backend/app/models/anomaly/__init__.py
</context>

<interfaces>
<!-- D-16 full column spec — migration and ORM must match exactly -->

daily_insights table columns (exact order for migration DDL):
  id: UUID PRIMARY KEY DEFAULT gen_random_uuid() — from TenantScopedMixin
  tenant_id: UUID NOT NULL REFERENCES tenants(id) — from TenantScopedMixin
  created_at: TIMESTAMPTZ NOT NULL DEFAULT now() — from TenantScopedMixin
  updated_at: TIMESTAMPTZ NOT NULL DEFAULT now() — from TenantScopedMixin
  date: DATE NOT NULL — business date for the insight
  status: TEXT NOT NULL CHECK (status IN ('running', 'success', 'failed', 'fallback')) — D-14 state machine
  payload_json: JSONB nullable — serialized DailyInsightResponse
  generated_at: TIMESTAMPTZ nullable — when Claude finished generating
  input_tokens: INTEGER nullable — AI-08
  output_tokens: INTEGER nullable — AI-08
  cost_usd: NUMERIC(10, 6) nullable — AI-08, D-16
  raw_response: TEXT nullable — last Claude response string (preserved on failure)

UNIQUE constraint: (tenant_id, date) — one insight row per tenant per day
Performance index: ix_daily_insights_tenant_date on (tenant_id, date)

ORM mapped columns (Mapped[...] syntax, SQLAlchemy 2.x):
  date: Mapped[date_type] = mapped_column(Date, nullable=False)
  status: Mapped[str] = mapped_column(Text, nullable=False)
  payload_json: Mapped[dict | None] = mapped_column(JSONB, nullable=True)
  generated_at: Mapped[datetime | None] = mapped_column(TIMESTAMPTZ, nullable=True)
  input_tokens: Mapped[int | None] = mapped_column(Integer, nullable=True)
  output_tokens: Mapped[int | None] = mapped_column(Integer, nullable=True)
  cost_usd: Mapped[Decimal | None] = mapped_column(Numeric(10, 6), nullable=True)
  raw_response: Mapped[str | None] = mapped_column(Text, nullable=True)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Alembic migration 008_daily_insights.py</name>
  <files>backend/alembic/versions/008_daily_insights.py</files>
  <read_first>
    backend/alembic/versions/007_detected_problems.py
    .planning/phases/05-ai-insights/05-CONTEXT.md
  </read_first>
  <action>
Create backend/alembic/versions/008_daily_insights.py following the exact structure of 007_detected_problems.py.

Module docstring: "Create daily_insights table for Phase 5 AI Insights generation. Revision: 008, Revises: 007, Create Date: 2026-05-28. One row per tenant per day — upsert on (tenant_id, date). See 05-CONTEXT.md D-16 for full column spec."

revision = "008", down_revision = "007", branch_labels = None, depends_on = None.

upgrade() function creates daily_insights table with op.create_table() including ALL D-16 columns in this exact order:
1. id: UUID PK server_default gen_random_uuid()
2. tenant_id: UUID NOT NULL
3. created_at: TIMESTAMPTZ NOT NULL DEFAULT now()
4. updated_at: TIMESTAMPTZ NOT NULL DEFAULT now()
5. date: sa.Date NOT NULL — business date
6. status: sa.Text NOT NULL — no CHECK constraint in DDL (status validation in application layer)
7. payload_json: JSONB nullable
8. generated_at: TIMESTAMP(timezone=True) nullable
9. input_tokens: sa.Integer nullable
10. output_tokens: sa.Integer nullable
11. cost_usd: sa.Numeric(10, 6) nullable
12. raw_response: sa.Text nullable

Add ForeignKeyConstraint for tenant_id → tenants.id named "fk_daily_insights_tenant_id".
Add UniqueConstraint on ("tenant_id", "date") named "uq_daily_insights_tenant_date".

After create_table, add one performance index: op.create_index("ix_daily_insights_tenant_date", "daily_insights", ["tenant_id", "date"]).

downgrade() drops index then table (reverse order).

Imports needed: from __future__ import annotations; import sqlalchemy as sa; from alembic import op; from sqlalchemy.dialects.postgresql import JSONB, UUID
  </action>
  <verify>
    <automated>cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend && python -c "import alembic.versions.versions_008_daily_insights" 2>&1 || python -c "import importlib.util; spec=importlib.util.spec_from_file_location('m','alembic/versions/008_daily_insights.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); print(m.revision, m.down_revision)"</automated>
  </verify>
  <done>Migration file parses without error; revision="008" and down_revision="007"; upgrade() and downgrade() functions defined; UniqueConstraint on (tenant_id, date) present in the op.create_table() call</done>
</task>

<task type="auto">
  <name>Task 2: DailyInsight ORM model + package init files</name>
  <files>
    backend/app/models/insights/__init__.py
    backend/app/models/insights/daily_insight.py
    backend/app/models/__init__.py
  </files>
  <read_first>
    backend/app/models/anomaly/detected_problem.py
    backend/app/models/anomaly/__init__.py
    backend/app/models/__init__.py
    backend/app/db/base.py
    .planning/phases/05-ai-insights/05-CONTEXT.md
  </read_first>
  <action>
Create three files:

1. backend/app/models/insights/__init__.py — empty module init with from __future__ import annotations and a docstring: "Insights SQLAlchemy models package (Phase 5)."

2. backend/app/models/insights/daily_insight.py — SQLAlchemy ORM model following detected_problem.py structure exactly.
   - Module docstring explaining D-16 column spec, UPSERT conflict target, status state machine (D-14).
   - from __future__ import annotations
   - Imports: from datetime import date as date_type, datetime; from decimal import Decimal; from sqlalchemy import Date, Integer, Numeric, Text, UniqueConstraint; from sqlalchemy.dialects.postgresql import JSONB; from sqlalchemy.orm import Mapped, mapped_column; from app.db.base import Base, TenantScopedMixin, TIMESTAMPTZ
   - Class: class DailyInsight(Base, TenantScopedMixin):
   - __tablename__ = "daily_insights"
   - __table_args__ includes UniqueConstraint("tenant_id", "date", name="uq_daily_insights_tenant_date")
   - All 8 domain columns as Mapped[...] with correct types (see interfaces block above)
   - Class docstring: "One row per tenant per day — upsert on (tenant_id, date). D-16 column spec. D-14 status state machine: running | success | failed | fallback."

3. backend/app/models/__init__.py — read existing file first, then add the insights package import. Add "from app.models.insights import daily_insight as _di  # noqa: F401 — Alembic autogenerate discovery" after the existing anomaly import line. Follow the exact same comment style and import pattern already present for anomaly models.
  </action>
  <verify>
    <automated>cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend && python -c "from app.models.insights.daily_insight import DailyInsight; print(DailyInsight.__tablename__)"</automated>
  </verify>
  <done>DailyInsight.__tablename__ == "daily_insights"; model imports cleanly; UniqueConstraint present in __table_args__; all 8 domain columns (date, status, payload_json, generated_at, input_tokens, output_tokens, cost_usd, raw_response) are Mapped attributes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration DDL → PostgreSQL | Column definitions must match ORM model to prevent runtime type coercion errors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Information Disclosure | daily_insights.raw_response column | mitigate | raw_response stores Claude's JSON output only — never includes customer PII; prompt builder filters context_json before API call (guardrail in Wave 2) |
| T-05-02-02 | Tampering | Alembic migration chain | mitigate | down_revision="007" hardcoded; alembic upgrade head validates chain before applying DDL |
| T-05-02-03 | Elevation of Privilege | tenant_id NOT NULL constraint | mitigate | FK to tenants.id prevents phantom tenant_id insertion; TenantScopedMixin requires tenant_id in every row |
</threat_model>

<verification>
After both tasks:
1. `cd backend && python -c "from app.models.insights.daily_insight import DailyInsight; cols = [c.name for c in DailyInsight.__table__.columns]; print(cols)"` — prints all column names including cost_usd, raw_response
2. `cd backend && python -c "import importlib.util; spec=importlib.util.spec_from_file_location('m','alembic/versions/008_daily_insights.py'); m=importlib.util.module_from_spec(spec); spec.loader.exec_module(m); print(m.revision, m.down_revision)"` — prints "008 007"
3. `cd backend && grep -n "uq_daily_insights_tenant_date" alembic/versions/008_daily_insights.py` — returns a match
4. Wave 0 tests for model/migration: `cd backend && python -m pytest tests/unit/test_daily_insight_schema.py --collect-only` — still RED (schema module not yet created in Wave 2)
</verification>

<success_criteria>
- backend/alembic/versions/008_daily_insights.py exists with revision="008", down_revision="007"
- DDL has all 12 columns including cost_usd NUMERIC(10,6) and raw_response TEXT
- UNIQUE(tenant_id, date) defined in both migration and ORM model
- DailyInsight ORM model imports cleanly without database connection
- backend/app/models/__init__.py updated so Alembic autogenerate discovers DailyInsight
</success_criteria>

<output>
Create `.planning/phases/05-ai-insights/05-02-SUMMARY.md` when done
</output>
