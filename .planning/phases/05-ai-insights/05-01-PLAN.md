---
phase: 05-ai-insights
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/tests/factories/insight_factory.py
  - backend/tests/unit/test_daily_insight_schema.py
  - backend/tests/unit/test_prompt_builder.py
  - backend/tests/unit/test_insight_service.py
  - backend/tests/unit/test_number_validator.py
  - backend/tests/unit/test_insight_repository.py
  - backend/tests/integration/test_generate_daily_insights_task.py
autonomous: true
requirements:
  - AI-01
  - AI-02
  - AI-03
  - AI-04
  - AI-05
  - AI-06
  - AI-07
  - AI-08
  - AI-09

must_haves:
  truths:
    - "All RED-state test files exist and fail with ImportError/ModuleNotFoundError (not syntax errors)"
    - "insight_factory.py builds DailyInsightResponse, Problem, ActionItem, Positive, Warning dicts and ORM-compatible row dicts without a DB"
    - "Test contracts cover schema validation, number cross-check, fallback, Romanian language checks, token tracking, and the AI-09 grep gate"
  artifacts:
    - path: "backend/tests/factories/insight_factory.py"
      provides: "Factory-boy factories for insight test data"
    - path: "backend/tests/unit/test_daily_insight_schema.py"
      provides: "RED contracts for DailyInsightResponse Pydantic schema (D-01..D-05, D-19)"
    - path: "backend/tests/unit/test_prompt_builder.py"
      provides: "RED contracts for build_system_prompt() and build_user_message()"
    - path: "backend/tests/unit/test_insight_service.py"
      provides: "RED contracts for InsightService.run() success + fallback paths (AI-06, AI-07)"
    - path: "backend/tests/unit/test_number_validator.py"
      provides: "RED contracts for number extraction and cross-check logic (AI-06)"
    - path: "backend/tests/unit/test_insight_repository.py"
      provides: "RED contracts for InsightRepository.upsert_daily_insight() UPSERT"
    - path: "backend/tests/integration/test_generate_daily_insights_task.py"
      provides: "RED integration contracts for Celery task (AI-01, AI-08, AI-09)"
  key_links:
    - from: "backend/tests/unit/test_daily_insight_schema.py"
      to: "backend/app/schemas/insights/daily_insight_schema.py"
      via: "DailyInsightResponse import"
      pattern: "from app.schemas.insights.daily_insight_schema import"
    - from: "backend/tests/unit/test_insight_service.py"
      to: "backend/app/services/insights/insight_service.py"
      via: "InsightService import"
      pattern: "from app.services.insights.insight_service import"
---

<objective>
Create all Wave 0 RED-state test stubs and factory-boy fixtures for Phase 5 AI Insights.

Purpose: Establish the full test contract before any production code exists. All test files must FAIL with ImportError/ModuleNotFoundError (target modules do not exist yet), proving the RED state is valid.
Output: 7 files — 1 factory + 6 test files covering schema, prompt builder, insight service, number validator, repository, and Celery task.
</objective>

<execution_context>
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-ai-insights/05-CONTEXT.md
@.planning/phases/05-ai-insights/05-AI-SPEC.md

# Phase 4 factory pattern to replicate:
@backend/tests/factories/anomaly_factory.py

# Phase 4 RED test pattern to replicate:
@backend/tests/unit/test_anomaly_service.py
@backend/tests/unit/test_anomaly_repository.py
@backend/tests/integration/test_detect_anomalies_task.py
</context>

<interfaces>
<!-- Key types the executor needs to write test stubs against. These are the contracts to be implemented in Wave 2. -->

<!-- DailyInsightResponse schema (D-05) — target module: backend/app/schemas/insights/daily_insight_schema.py -->
ActionItem fields: order: int, description: str, owner: str, deadline: str, expected_outcome: str
Problem fields: id: str, severity: Literal["high","medium","low"], category: Literal["marketing","sales","team","funnel"], title: str, description: str, estimated_loss_ron: Decimal, actions: list[ActionItem]
Positive fields: title: str, description: str, recommendation: str
Warning fields: title: str, description: str
DailyInsightResponse fields: summary: str, problems: list[Problem] with max_length=3, positives: list[Positive], warnings: list[Warning], weekly_action_plan: list[str], generated_at: datetime

<!-- DailyInsight ORM model (D-16) — target module: backend/app/models/insights/daily_insight.py -->
DailyInsight columns: id UUID PK, tenant_id UUID, date DATE, status TEXT CHECK IN ('running','success','failed','fallback'), payload_json JSONB, generated_at TIMESTAMPTZ, input_tokens INT, output_tokens INT, cost_usd NUMERIC(10,6), raw_response TEXT, created_at TIMESTAMPTZ, updated_at TIMESTAMPTZ
UNIQUE constraint: (tenant_id, date)

<!-- InsightService (D-18, AI-SPEC §4) — target module: backend/app/services/insights/insight_service.py -->
InsightService.__init__(session: AsyncSession, tenant_id: UUID) -> None
InsightService.run(kpi_date: date) -> tuple[dict, str]  # dict = payload+token fields, str = 'success'|'fallback'|'failed'
InsightService._build_fallback(problems_rows: list) -> DailyInsightResponse

<!-- InsightRepository — target module: backend/app/services/repositories/insight_repository.py -->
InsightRepository.__init__(session: AsyncSession, tenant_id: UUID) -> None
InsightRepository.upsert_daily_insight(row: dict) -> None  # UPSERT ON CONFLICT (tenant_id, date)

<!-- prompt_builder — target module: backend/app/services/insights/prompt_builder.py -->
build_system_prompt() -> list[dict]  # returns list of system blocks, last block has cache_control
build_user_message(kpi_row: dict | None, problems_rows: list[dict]) -> str  # JSON string, max 3 problems by estimated_loss_ron

<!-- number_validator — target module: backend/app/services/insights/number_validator.py -->
extract_numbers_from_text(text: str) -> list[float]
cross_check(parsed: DailyInsightResponse, kpi_snapshot: dict, problems_input: list[dict]) -> bool
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: insight_factory.py — factory-boy fixtures for test data</name>
  <files>backend/tests/factories/insight_factory.py</files>
  <read_first>
    backend/tests/factories/anomaly_factory.py
    backend/tests/factories/metrics_factory.py
    .planning/phases/05-ai-insights/05-CONTEXT.md
  </read_first>
  <action>
Create backend/tests/factories/insight_factory.py following the exact pattern of anomaly_factory.py. All factories use Meta.model=dict (plain dicts, not ORM instances). TENANT_ID = UUID("00000000-0000-0000-0000-000000000001"). TODAY = date(2026, 5, 28).

Define the following factory classes and builder helpers:

ActionItemFactory: order=1, description="Contactați lead-urile noi în primele 2 ore", owner="Roibu Valeria", deadline="Azi", expected_outcome="Rata de răspuns scade de la 3h la sub 2h"

ProblemFactory: id="slow_first_touch", severity="high", category="sales", title="Timp de răspuns lent", description="3 lead-uri nu au fost contactate în primele 5 ore de business. Estimăm o pierdere de ~5.000 RON.", estimated_loss_ron=Decimal("5000.00"), actions=factory.LazyFunction(lambda: [ActionItemFactory.build()])

PositiveFactory: title="Raileanu Leon — performanță excelentă", description="8.3% conversie L→C", recommendation="Distribuiți mai multe lead-uri showroom"

WarningFactory: title="Oferte în așteptare", description="2 oferte la limita de 14 zile fără activitate"

DailyInsightResponseDictFactory: summary="Zi cu rezultate mixte. Există 3 probleme de prioritat înaltă.", problems=factory.LazyFunction(lambda: [ProblemFactory.build()]), positives=factory.LazyFunction(lambda: [PositiveFactory.build()]), warnings=factory.LazyFunction(lambda: [WarningFactory.build()]), weekly_action_plan=factory.LazyFunction(lambda: ["Acțiune 1", "Acțiune 2", "Acțiune 3", "Acțiune 4", "Acțiune 5"]), generated_at=factory.LazyFunction(lambda: datetime(2026, 5, 28, 6, 0, 0, tzinfo=timezone.utc))

DailyInsightRowFactory (for DB rows, Meta.model=dict): tenant_id=TENANT_ID, date=factory.LazyFunction(lambda: TODAY), status="success", payload_json=factory.LazyFunction(lambda: DailyInsightResponseDictFactory.build()), generated_at=factory.LazyFunction(lambda: datetime(2026, 5, 28, 6, 5, 0, tzinfo=timezone.utc)), input_tokens=3000, output_tokens=2000, cost_usd=Decimal("0.039000"), raw_response='{"summary": "test"}'

KpiSnapshotFactory (for daily_kpi row dicts): tenant_id=TENANT_ID, date=TODAY, leads_total=12, visits_count=4, offers_count=5, contracts_closed=1, conversion_l_to_v=Decimal("0.3333"), conversion_v_to_o=Decimal("0.8000"), conversion_o_to_c=Decimal("0.2000"), conversion_l_to_c=Decimal("0.0833"), avg_deal_size=Decimal("20000.00"), wow_delta_pct=Decimal("-0.0500"), mom_delta_pct=Decimal("0.1200")

DetectedProblemInputFactory (for detected_problems rows passed to Claude): rule_id="slow_first_touch", severity="high", estimated_loss_ron=Decimal("5000.00"), current_value=Decimal("360.00"), expected_value=Decimal("300.00"), context_json={"count": 2, "worst_hours_elapsed": 6.0}

Add module-level builder helpers: make_insight_row(**overrides), make_kpi_snapshot(**overrides), make_detected_problem_input(**overrides), make_three_anomaly_day() (returns list of 3 DetectedProblemInputFactory dicts with different rule_ids).

Imports needed: from __future__ import annotations, from datetime import date, datetime, timezone, from decimal import Decimal, from uuid import UUID, import factory
  </action>
  <verify>
    <automated>cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend && python -c "from tests.factories.insight_factory import DailyInsightRowFactory, make_kpi_snapshot; r = DailyInsightRowFactory.build(); print(r['status']); s = make_kpi_snapshot(); print(s['leads_total'])"</automated>
  </verify>
  <done>insight_factory.py imports cleanly and builds dicts with correct field values; DailyInsightRowFactory.build()['status'] == 'success'; make_kpi_snapshot()['leads_total'] == 12</done>
</task>

<task type="auto">
  <name>Task 2: Six RED-state test files for Phase 5 contracts</name>
  <files>
    backend/tests/unit/test_daily_insight_schema.py
    backend/tests/unit/test_prompt_builder.py
    backend/tests/unit/test_insight_service.py
    backend/tests/unit/test_number_validator.py
    backend/tests/unit/test_insight_repository.py
    backend/tests/integration/test_generate_daily_insights_task.py
  </files>
  <read_first>
    backend/tests/unit/test_anomaly_service.py
    backend/tests/unit/test_anomaly_repository.py
    backend/tests/integration/test_detect_anomalies_task.py
    .planning/phases/05-ai-insights/05-AI-SPEC.md
    .planning/phases/05-ai-insights/05-CONTEXT.md
  </read_first>
  <action>
Create all six test files with RED-state contracts. Each file imports from not-yet-existing modules, so tests will fail with ImportError/ModuleNotFoundError on collection. Every test has an assert statement that would pass if the module existed correctly.

--- test_daily_insight_schema.py ---
Docstring references: AI-02, D-01 through D-05, D-10, D-19.
Imports: from app.schemas.insights.daily_insight_schema import ActionItem, Problem, Positive, Warning, DailyInsightResponse; from tests.factories.insight_factory import ProblemFactory, ActionItemFactory
Tests:
- test_schema_validates_valid_payload: DailyInsightResponse.model_validate({...}) succeeds with all required fields present; assert isinstance(result.problems[0].estimated_loss_ron, Decimal)
- test_schema_rejects_four_problems: pytest.raises(ValidationError) when problems list has 4 items (D-02 max_length=3)
- test_estimated_loss_ron_is_decimal: assert isinstance(problem.estimated_loss_ron, Decimal) not float (D-19)
- test_problem_id_is_string: assert isinstance(problem.id, str) and len > 0 (D-04)
- test_weekly_action_plan_non_empty: assert len(result.weekly_action_plan) >= 1 (D-03)
- test_allowed_deadline_labels: ALLOWED_DEADLINES = {"Azi", "Mâine", "Săptămâna aceasta", "Luna aceasta"}; assert action.deadline in ALLOWED_DEADLINES (D-10)
- test_action_owner_non_empty: assert action.owner.strip() != "" for each action
- test_generated_at_is_datetime: assert isinstance(result.generated_at, datetime) (set server-side)

--- test_prompt_builder.py ---
Docstring references: D-07, D-08, D-09, D-10, D-11, AI-04.
Imports: from app.services.insights.prompt_builder import build_system_prompt, build_user_message; from tests.factories.insight_factory import make_kpi_snapshot, make_three_anomaly_day
Tests:
- test_build_system_prompt_returns_list: result = build_system_prompt(); assert isinstance(result, list) and len(result) >= 2
- test_last_block_has_cache_control: last = build_system_prompt()[-1]; assert "cache_control" in last and last["cache_control"] == {"type": "ephemeral"} (D-07)
- test_system_prompt_contains_salesperson_roster: text = " ".join(b["text"] for b in build_system_prompt()); assert "Dragoi Mihaela" in text and "Raileanu Leon" in text (D-09)
- test_system_prompt_contains_romanian_deadline_labels: assert "Azi" in text and "Mâine" in text (D-10)
- test_system_prompt_max_3_problems_instruction: assert "Maximum 3 probleme" in text or "maximum 3" in text.lower() (D-02, D-08)
- test_build_user_message_returns_string: result = build_user_message(make_kpi_snapshot(), make_three_anomaly_day()); assert isinstance(result, str)
- test_user_message_is_valid_json: import json; data = json.loads(build_user_message(make_kpi_snapshot(), make_three_anomaly_day())); assert "kpi_snapshot" in data and "detected_problems" in data (D-06)
- test_user_message_caps_problems_at_three: problems = make_three_anomaly_day() + [{"rule_id": "extra", "estimated_loss_ron": Decimal("1")}]; data = json.loads(build_user_message(make_kpi_snapshot(), problems)); assert len(data["detected_problems"]) == 3 (D-02 top-3 selection)
- test_user_message_no_pii_in_context_json: data = json.loads(build_user_message(make_kpi_snapshot(), make_three_anomaly_day())); for p in data["detected_problems"]: assert "email" not in str(p) and "phone" not in str(p)
- test_zero_anomaly_user_message: data = json.loads(build_user_message(make_kpi_snapshot(), [])); assert data["detected_problems"] == [] (D-13)

--- test_insight_service.py ---
Docstring references: AI-06, AI-07, D-14, D-15.
Imports: from unittest.mock import AsyncMock, MagicMock, patch; from app.services.insights.insight_service import InsightService; from tests.factories.insight_factory import make_kpi_snapshot, make_detected_problem_input; pytest.mark.asyncio required
Tests use AsyncMock for the DB session; patch("app.services.insights.insight_service.AsyncAnthropic") for the Anthropic client.
- test_run_returns_success_status_on_valid_response: mock Claude returns valid DailyInsightResponse tool_block; result_dict, status = await svc.run(date.today()); assert status == "success"
- test_run_returns_fallback_on_three_validation_failures: mock number validator always returns False; result_dict, status = await svc.run(date.today()); assert status == "fallback"
- test_fallback_summary_is_romanian_error_message: fallback_dict, _ = ...; assert fallback_dict["payload"]["summary"] == "Generare AI eșuată — raport bazat pe anomalii detectate automat" (D-15)
- test_fallback_problems_from_detected_problems_rows: fallback has len(problems) == len(detected_problems_input[:3]); each problem.actions == []
- test_compute_cost_formula: from app.services.insights.insight_service import InsightService; mock_usage with input_tokens=3000, output_tokens=2000; cost = InsightService._compute_cost(mock_usage); assert cost == Decimal("0.039000") (formula: 3/1M*3.0 + 2/1M*15.0)
- test_raw_response_preserved_on_failure: on fallback path, result_dict["raw_response"] is a string (not None or empty) (D-16 raw_response)
- test_zero_anomaly_day_still_runs: pass empty list as problems; run() still calls Claude; no error raised (D-13)

--- test_number_validator.py ---
Docstring references: AI-06.
Imports: from app.services.insights.number_validator import extract_numbers_from_text, cross_check
Tests:
- test_extract_romanian_formatted_number: assert 23400.0 in extract_numbers_from_text("23.400 RON") (Romanian thousands separator)
- test_extract_percentage: assert 8.3 in extract_numbers_from_text("8,3% conversie") (Romanian decimal comma)
- test_extract_plain_integer: assert 351.0 in extract_numbers_from_text("351 lead-uri")
- test_cross_check_passes_within_tolerance: parsed mock with summary="12 leads"; kpi_snapshot={"leads_total": 12}; assert cross_check(parsed, kpi, problems) is True
- test_cross_check_fails_on_fabricated_ron: summary="ai pierdut 47320 RON"; estimated_loss_ron=Decimal("5000"); assert cross_check(parsed, kpi, problems) is False (47320 not within ±2% of 5000)
- test_cross_check_passes_with_two_percent_tolerance: value in text = 5050; input value = 5000; 50/5000 = 1% — should pass; assert cross_check returns True
- test_cross_check_zero_anomaly_day: empty problems_input list; assert cross_check(no_number_summary, {}, []) is True

--- test_insight_repository.py ---
Docstring references: D-16, Pitfall 6 (tenant isolation on Core INSERT).
Imports: from unittest.mock import AsyncMock, MagicMock; from app.services.repositories.insight_repository import InsightRepository; from tests.factories.insight_factory import make_insight_row
Tests (no real DB — mock session):
- test_upsert_calls_session_execute: mock_session = AsyncMock(); repo.upsert_daily_insight(row); mock_session.execute.assert_called_once()
- test_upsert_raises_on_missing_tenant_id: row without tenant_id; pytest.raises(ValueError)
- test_upsert_raises_on_cross_tenant_write: repo has tenant_id=A; row has tenant_id=B; pytest.raises(ValueError)
- test_upsert_does_not_commit: mock_session.commit.assert_not_called() (caller commits — WR-04)

--- test_generate_daily_insights_task.py ---
Docstring references: AI-01, AI-08, AI-09, D-17, D-18, D-20.
This is an integration test file. Top section: pytestmark = pytest.mark.skipif(not os.getenv("TEST_DATABASE_URL"), reason="integration test — requires TEST_DATABASE_URL").
Imports: from app.tasks.etl.generate_daily_insights import generate_daily_insights, _generate_async
Tests:
- test_task_writes_success_row (skipped without TEST_DATABASE_URL): pytest.mark.asyncio; mocked Anthropic client; calls await _generate_async(tenant_id, date.today()); asserts DailyInsight row in DB with status='success', input_tokens > 0, cost_usd > 0
- test_token_usage_logged (skipped): asserts input_tokens, output_tokens, cost_usd all not None (AI-08)
- test_upsert_is_idempotent (skipped): call _generate_async twice for same date; table has exactly 1 row
- test_no_claude_calls_in_http_handlers (NOT skipped — pure grep, no DB): subprocess.run(["grep", "-r", "AsyncAnthropic", "--include=*.py", "backend/app/api/"]); assert result.stdout == "" (AI-09 violation check)
- test_fallback_on_all_claude_failures (skipped): mock Anthropic to always raise exception; _generate_async completes; DB row has status='fallback'
  </action>
  <verify>
    <automated>cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend && python -m pytest tests/unit/test_daily_insight_schema.py tests/unit/test_prompt_builder.py tests/unit/test_insight_service.py tests/unit/test_number_validator.py tests/unit/test_insight_repository.py tests/integration/test_generate_daily_insights_task.py --collect-only 2>&1 | grep -E "ERROR|error|ImportError|ModuleNotFoundError" | head -20</automated>
  </verify>
  <done>All 6 test files collect cleanly (no syntax errors) and fail with ImportError or ModuleNotFoundError confirming RED state. The pytest --collect-only output shows test names discovered but errors on import.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code → factory code | Factory dicts must not contain PII; only synthetic values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Information Disclosure | insight_factory.py | accept | Factory uses synthetic TENANT_ID and synthetic lead data only — no real customer data in test fixtures |
| T-05-01-SC | Tampering | no new packages in this plan | accept | No new pip/npm dependencies installed in Wave 0 — test stubs use only existing test infrastructure |
</threat_model>

<verification>
After both tasks complete:
1. `cd backend && python -m pytest tests/unit/test_daily_insight_schema.py --collect-only` — shows tests but fails import
2. `cd backend && python -c "from tests.factories.insight_factory import DailyInsightRowFactory; print(DailyInsightRowFactory.build()['status'])"` — prints "success"
3. `cd backend && python -c "from tests.factories.insight_factory import make_three_anomaly_day; print(len(make_three_anomaly_day()))"` — prints "3"
4. `cd backend && python -m pytest tests/integration/test_generate_daily_insights_task.py -k "test_no_claude_calls_in_http_handlers"` — this one test (grep gate) must PASS because app/api/ does not contain AsyncAnthropic yet
</verification>

<success_criteria>
- backend/tests/factories/insight_factory.py exists and imports cleanly
- 6 test files exist across tests/unit/ and tests/integration/
- All test files have RED state: collection succeeds (no syntax errors) but imports fail (ModuleNotFoundError for app.schemas.insights.daily_insight_schema etc.)
- test_no_claude_calls_in_http_handlers passes immediately (AI-09 grep gate)
- Factory builds dict with correct field types (Decimal for monetary, datetime for generated_at)
</success_criteria>

<output>
Create `.planning/phases/05-ai-insights/05-01-SUMMARY.md` when done
</output>
