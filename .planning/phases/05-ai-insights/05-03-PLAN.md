---
phase: 05-ai-insights
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - backend/app/schemas/insights/__init__.py
  - backend/app/schemas/insights/daily_insight_schema.py
  - backend/app/services/insights/__init__.py
  - backend/app/services/insights/prompt_builder.py
  - backend/app/services/insights/number_validator.py
  - backend/app/services/insights/insight_service.py
  - backend/app/services/repositories/insight_repository.py
autonomous: true
requirements:
  - AI-02
  - AI-03
  - AI-04
  - AI-05
  - AI-06
  - AI-07
  - AI-08
  - AI-09

must_haves:
  truths:
    - "DailyInsightResponse validates correctly and rejects >3 problems (D-02)"
    - "build_system_prompt() returns list with cache_control on last block; contains salesperson roster (D-09)"
    - "build_user_message() selects top 3 by estimated_loss_ron; filters PII from context_json"
    - "InsightService.run() calls Claude with tool-use forced JSON; retries up to 2 times on number mismatch; returns fallback on exhaustion"
    - "InsightRepository.upsert_daily_insight() uses ON CONFLICT (tenant_id, date) DO UPDATE"
    - "All unit tests for schema, prompt_builder, insight_service, number_validator, insight_repository turn GREEN"
  artifacts:
    - path: "backend/app/schemas/insights/daily_insight_schema.py"
      provides: "Full DailyInsightResponse Pydantic schema (D-05)"
      exports: ["ActionItem", "Problem", "Positive", "Warning", "DailyInsightResponse"]
    - path: "backend/app/services/insights/prompt_builder.py"
      provides: "build_system_prompt() + build_user_message()"
      exports: ["build_system_prompt", "build_user_message"]
    - path: "backend/app/services/insights/number_validator.py"
      provides: "extract_numbers_from_text() + cross_check()"
      exports: ["extract_numbers_from_text", "cross_check"]
    - path: "backend/app/services/insights/insight_service.py"
      provides: "InsightService with run(), _build_fallback(), _compute_cost()"
      exports: ["InsightService"]
    - path: "backend/app/services/repositories/insight_repository.py"
      provides: "InsightRepository with upsert_daily_insight()"
      exports: ["InsightRepository"]
  key_links:
    - from: "backend/app/services/insights/insight_service.py"
      to: "backend/app/schemas/insights/daily_insight_schema.py"
      via: "deferred import DailyInsightResponse"
      pattern: "from app.schemas.insights.daily_insight_schema import"
    - from: "backend/app/services/insights/insight_service.py"
      to: "backend/app/services/insights/number_validator.py"
      via: "cross_check call"
      pattern: "from app.services.insights.number_validator import cross_check"
    - from: "backend/app/services/repositories/insight_repository.py"
      to: "backend/app/models/insights/daily_insight.py"
      via: "deferred import DailyInsight"
      pattern: "from app.models.insights.daily_insight import DailyInsight"
---

<objective>
Implement the full service layer for Phase 5: Pydantic schema, prompt builder, number validator, InsightService (Claude API orchestration), and InsightRepository (UPSERT writer).

Purpose: This is the core logic of Phase 5. Claude is called here with tool-use forced JSON, number cross-check validation runs, and fallback behavior triggers on exhaustion.
Output: 7 files — 2 package inits + 5 implementation modules. Wave 0 unit tests must turn GREEN after this plan.
</objective>

<execution_context>
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/workflows/execute-plan.md
@/Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ai-insights/05-CONTEXT.md
@.planning/phases/05-ai-insights/05-AI-SPEC.md

# Pattern files to mirror:
@backend/app/services/anomaly/anomaly_service.py
@backend/app/services/repositories/anomaly_repository.py
@backend/app/schemas/mefi.py

# DB base classes:
@backend/app/db/base.py

# Settings for anthropic_api_key:
@backend/app/core/config.py

# Existing model to import:
@backend/app/models/insights/daily_insight.py
</context>

<interfaces>
<!-- All imports the executor should use — extracted from existing codebase -->

From backend/app/db/base.py:
  Base, TenantScopedMixin, TIMESTAMPTZ

From backend/app/models/insights/daily_insight.py (Wave 2 output):
  class DailyInsight(Base, TenantScopedMixin): __tablename__="daily_insights"; fields: date, status, payload_json, generated_at, input_tokens, output_tokens, cost_usd, raw_response

From backend/app/services/repositories/anomaly_repository.py (UPSERT pattern):
  pg_insert from sqlalchemy.dialects.postgresql; on_conflict_do_update(index_elements=[...], set_={...})
  Tenant validation guard before INSERT: if row["tenant_id"] != self._tenant_id: raise ValueError

From backend/app/core/config.py:
  settings.anthropic_api_key (field to be added in Wave 3 plan 04, but used here via deferred import)

From AI-SPEC.md §3 (Framework Quick Reference):
  AsyncAnthropic instantiation: inside async function body (INFRA-05)
  Tool-use pattern: tools=[{"name": "generate_daily_insight", "description": "...", "input_schema": DailyInsightResponse.model_json_schema()}]
  tool_choice: {"type": "tool", "name": "generate_daily_insight"}
  response.content: next(b for b in response.content if b.type == "tool_use")
  usage fields: response.usage.input_tokens, response.usage.output_tokens, response.usage.cache_read_input_tokens or 0, response.usage.cache_creation_input_tokens or 0

Model string (LOCKED — do NOT change): "claude-sonnet-4-5"
MAX_TOKENS = 4096
TEMPERATURE = 0.2
_MAX_CLAUDE_RETRIES = 2  # attempts 0, 1, 2 = 3 total
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Pydantic schema + prompt builder + number validator</name>
  <files>
    backend/app/schemas/insights/__init__.py
    backend/app/schemas/insights/daily_insight_schema.py
    backend/app/services/insights/__init__.py
    backend/app/services/insights/prompt_builder.py
    backend/app/services/insights/number_validator.py
  </files>
  <read_first>
    .planning/phases/05-ai-insights/05-CONTEXT.md
    .planning/phases/05-ai-insights/05-AI-SPEC.md
    SPEC.md (section 10 — system prompt draft lines 1143-1157, input structure, output schema)
    docs/SOFABELLE.md
    docs/api-references/mefi/enums.md
    backend/app/schemas/mefi.py
  </read_first>
  <action>
Create 5 files:

1. backend/app/schemas/insights/__init__.py — empty init with from __future__ import annotations and docstring "AI Insights Pydantic schemas (Phase 5)."

2. backend/app/schemas/insights/daily_insight_schema.py — Full DailyInsightResponse Pydantic v2 schema per D-05.
   All classes have from __future__ import annotations. Imports: from datetime import datetime; from decimal import Decimal; from typing import Literal; from pydantic import BaseModel, Field.
   Classes in order: ActionItem, Problem, Positive, Warning, DailyInsightResponse (per D-05 exact field definitions from CONTEXT.md).
   ActionItem: order: int, description: str, owner: str, deadline: str, expected_outcome: str
   Problem: id: str, severity: Literal["high","medium","low"], category: Literal["marketing","sales","team","funnel"], title: str, description: str, estimated_loss_ron: Decimal (not float — D-19), actions: list[ActionItem]
   Positive: title: str, description: str, recommendation: str
   Warning: title: str, description: str
   DailyInsightResponse: summary: str, problems: list[Problem] = Field(max_length=3), positives: list[Positive], warnings: list[Warning], weekly_action_plan: list[str], generated_at: datetime
   Module docstring references D-05, D-02 (max_length=3), D-19 (Decimal not float).

3. backend/app/services/insights/__init__.py — empty init with from __future__ import annotations and docstring "AI Insights service package (Phase 5)."

4. backend/app/services/insights/prompt_builder.py — Two exported functions.
   Module docstring references D-07, D-08, D-09, D-11, AI-04, GDPR PII guard.
   
   SYSTEM_PROMPT_TEXT module constant (multi-line string, NOT in function body) — use SPEC.md §10 Romanian draft as the starting text, then extend with:
   - Sofa Belle company facts: "Companie: Sofa Belle — mobilă premium, 3 showroom-uri (Brașov, București, Cluj-Napoca), ciclu de vânzare 2 săptămâni–2 luni, valoare medie contract >20.000 RON"
   - Domain warning: "IMPORTANT: estimated_value este NULL pentru toate lead-urile Sofa Belle — nu prezenta valori de vânzări ca precise. Folosește 'estimat' sau evită valorile monetare absolute."
   - Showroom note: "Showroom este canalul principal de conversie (29.5% din lead-uri, 55% din contracte, 10.8% conversie L→C). Nu confunda sursa Showroom cu etapa funnel Vizita."
   - Funnel glossary: "Funnel Sofa Belle: Lead → Vizita (= walk-in showroom, sursa source_id=5) → Ofertă → Contract."
   - Salesperson roster (D-09): "Echipa de vânzări (6 persoane):\n- Roibu Valeria\n- Raileanu Leon (best performer: 8.3% conversie L→C)\n- Godja Adina Maria\n- Dragoi Mihaela (atenție: 351 lead-uri, 2.8% conversie — cu mult sub media echipei de 5.8%)\n- Zagrian Emilia\n- Moaca Andreea\nPentru reguli individuale (underperforming_salesperson, slow_first_touch): atribuie acțiunile persoanei specifice.\nPentru reguli sistemice (showroom_traffic_drop, junk_lead_quality): atribuie Manager sau Marketing Sofa."
   - Action format rules (D-10, D-11, D-12): "Termene relative (nu date ISO): 'Azi', 'Mâine', 'Săptămâna aceasta', 'Luna aceasta'. Fiecare problemă are 2-3 acțiuni concrete, nu mai mult (D-11). expected_outcome trebuie să numească o metrică și o valoare țintă concretă (ex: 'Rata de răspuns scade de la 3h la sub 2h')."
   - weekly_action_plan instructions (D-03): "weekly_action_plan: 5-7 acțiuni prioritizate, concise, gata de copiat în WhatsApp sau standup."
   - Schema format instruction (last line of constant): "Format de răspuns: JSON conform schemei specificate în tool."
   
   def build_system_prompt() -> list[dict]:
     Returns two-element list of text blocks. First block has all SYSTEM_PROMPT_TEXT. Second block (last) has the cache_control key: {"type": "text", "text": "Format de răspuns: JSON conform schemei specificate în tool.", "cache_control": {"type": "ephemeral"}}. NOTE: to maximize cache efficiency, the FIRST block contains all stable content, the SECOND block (with cache_control) is the cache sentinel. Return the list as: [{"type": "text", "text": SYSTEM_PROMPT_TEXT}, {"type": "text", "text": "Format de răspuns: JSON conform schemei specificate în tool.", "cache_control": {"type": "ephemeral"}}]
   
   def build_user_message(kpi_row: dict | None, problems_rows: list[dict]) -> str:
     Top-3 selection: sort problems_rows by estimated_loss_ron descending (convert to Decimal for comparison), take first 3 (D-02).
     PII filter: for each problem row, include only fields: rule_id, severity, estimated_loss_ron (as str for JSON), current_value (as str), expected_value (as str). Strip context_json entirely OR include only non-PII fields: for context_json extract only numeric/count fields, never lead_ids or salesperson_ids (GDPR guardrail from AI-SPEC §6).
     KPI snapshot: include from kpi_row (if not None): date, leads_total, visits_count, offers_count, contracts_closed, conversion_l_to_v, conversion_v_to_o, conversion_o_to_c, conversion_l_to_c, avg_deal_size, wow_delta_pct, mom_delta_pct — all serialized as strings (Decimal is not JSON-serializable natively).
     Return json.dumps({"date": kpi_row["date"].isoformat() if kpi_row else None, "kpi_snapshot": {...}, "detected_problems": [...]}, ensure_ascii=False).
     Handle zero anomalies gracefully — return valid JSON with detected_problems=[].

5. backend/app/services/insights/number_validator.py — Two exported functions.
   Module docstring references AI-06.
   NUMBER_PATTERN module constant: re.compile(r'\b(\d{1,3}(?:[.,]\d{3})*(?:[.,]\d+)?|\d+(?:[.,]\d+)?)\s*%?') — matches Romanian-formatted numbers (. as thousands, , as decimal).
   
   def extract_numbers_from_text(text: str) -> list[float]:
     Find all NUMBER_PATTERN matches. For each match, normalize: replace "." with "" (strip thousands separator), replace "," with "." (decimal separator). Convert to float. Return list. Filter out numbers that look like years (1900-2100 range can still be included — no filtering needed; the cross-check handles tolerance).
   
   def cross_check(parsed: object, kpi_snapshot: dict, problems_input: list[dict]) -> bool:
     Build reference set of input values: all numeric values from kpi_snapshot (converted to float) + all estimated_loss_ron from problems_input (converted to float). If reference set is empty, return True (no numbers to check against — safe).
     Extract all numbers from parsed.summary and from each problem.description in parsed.problems using extract_numbers_from_text.
     For each extracted number > 10 (ignore small integers like "3 leads" — those are counts not monetary): check if it is within ±2% of ANY reference value. If a number is NOT within ±2% of any reference value: return False.
     Return True if all narrative numbers match at least one reference value.
     ±2% formula: abs(actual - expected) / max(abs(expected), 1e-9) <= 0.02
  </action>
  <verify>
    <automated>cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend && python -c "from app.schemas.insights.daily_insight_schema import DailyInsightResponse; from app.services.insights.prompt_builder import build_system_prompt, build_user_message; from app.services.insights.number_validator import cross_check, extract_numbers_from_text; blocks = build_system_prompt(); print(len(blocks), 'cache_control' in blocks[-1])"</automated>
  </verify>
  <done>
    - DailyInsightResponse imports cleanly; model_validate rejects 4-problem payload (Pydantic ValidationError)
    - build_system_prompt() returns list of 2 dicts; last dict has cache_control={"type": "ephemeral"}
    - "Dragoi Mihaela" present in system prompt text
    - build_user_message(None, []) returns valid JSON with empty detected_problems
    - extract_numbers_from_text("23.400 RON") includes 23400.0
    - Wave 0 unit tests for schema and prompt_builder turn GREEN: pytest tests/unit/test_daily_insight_schema.py tests/unit/test_prompt_builder.py tests/unit/test_number_validator.py
  </done>
</task>

<task type="auto">
  <name>Task 2: InsightService + InsightRepository</name>
  <files>
    backend/app/services/insights/insight_service.py
    backend/app/services/repositories/insight_repository.py
  </files>
  <read_first>
    .planning/phases/05-ai-insights/05-AI-SPEC.md
    .planning/phases/05-ai-insights/05-CONTEXT.md
    backend/app/services/anomaly/anomaly_service.py
    backend/app/services/repositories/anomaly_repository.py
  </read_first>
  <action>
Create two files:

1. backend/app/services/insights/insight_service.py

Module docstring references D-14, D-15, AI-06, AI-07, AI-08, D-model.

Module-level constants (NOT inside class body):
  _MAX_CLAUDE_RETRIES = 2  # 3 total attempts: 0, 1, 2 (AI-06)
  MODEL = "claude-sonnet-4-5"  # LOCKED — do NOT change (D-model, CLAUDE.md, STATE.md)
  MAX_TOKENS = 4096
  TEMPERATURE = 0.2

class InsightService:
  Docstring: "Transforms detected_problems + daily_kpi into DailyInsightResponse via Claude Sonnet 4.5. Constructor takes AsyncSession and tenant_id — same pattern as AnomalyService. All Claude calls are async. Instantiated per-invocation inside _generate_async(). D-model: model='claude-sonnet-4-5' — do NOT change."

  __init__(self, session: AsyncSession, tenant_id: UUID) -> None: store as self._session and self._tenant_id; bind structlog logger with tenant_id and service="insight_service".

  async def run(self, kpi_date: date) -> tuple[dict, str]:
    Docstring: "Generate DailyInsightResponse for kpi_date. Returns (result_dict, status) where status in ('success', 'fallback', 'failed')."
    ALL imports inside method body (INFRA-05): from anthropic import AsyncAnthropic; from app.core.config import settings; from app.schemas.insights.daily_insight_schema import DailyInsightResponse; from app.services.insights.prompt_builder import build_system_prompt, build_user_message; from app.services.insights.number_validator import cross_check.
    Fetch detected_problems for kpi_date (SELECT ... WHERE tenant_id=self._tenant_id AND date=kpi_date ORDER BY estimated_loss_ron DESC LIMIT 3) using SQLAlchemy Core text() or ORM; use deferred import of DetectedProblem.
    Fetch kpi_row from daily_kpi for (tenant_id, kpi_date) — select only the needed columns (D-06). If not found, pass None to build_user_message.
    Build system_blocks = build_system_prompt().
    Build user_content = build_user_message(kpi_row_dict, problems_rows_dicts).
    Build tool_schema = {"name": "generate_daily_insight", "description": "Generează raportul zilnic de business în română pentru Sofa Belle.", "input_schema": DailyInsightResponse.model_json_schema()}.
    Instantiate client = AsyncAnthropic(api_key=settings.anthropic_api_key) INSIDE the method body.
    Retry loop: for attempt in range(_MAX_CLAUDE_RETRIES + 1):  # 0, 1, 2
      response = await client.messages.create(model=MODEL, max_tokens=MAX_TOKENS, temperature=TEMPERATURE, system=system_blocks, messages=[{"role": "user", "content": user_content}], tools=[tool_schema], tool_choice={"type": "tool", "name": "generate_daily_insight"})
      last_usage = response.usage; last_raw = ""
      tool_block = next((b for b in response.content if b.type == "tool_use"), None)
      If tool_block is None: log warning "insight.no_tool_block"; continue
      last_raw = json.dumps(tool_block.input)
      Try: parsed = DailyInsightResponse.model_validate(tool_block.input)
      Except ValidationError: log warning "insight.pydantic_validation_failed" with attempt and error field paths only (not full text — may contain business data); continue
      If not cross_check(parsed, kpi_row_dict or {}, problems_dicts): log warning "insight.number_mismatch" with attempt; continue
      SUCCESS PATH: parsed.generated_at = datetime.now(UTC); cost_usd = self._compute_cost(last_usage)
      Return ({"payload": parsed.model_dump(mode="json"), "input_tokens": last_usage.input_tokens, "output_tokens": last_usage.output_tokens, "cost_usd": cost_usd, "raw_response": last_raw}, "success")
    FALLBACK (all retries exhausted): fallback = self._build_fallback(problems_rows_orm_objects); return ({"payload": fallback.model_dump(mode="json"), "input_tokens": last_usage.input_tokens if last_usage else 0, "output_tokens": last_usage.output_tokens if last_usage else 0, "cost_usd": self._compute_cost(last_usage) if last_usage else Decimal("0"), "raw_response": last_raw}, "fallback")

  @staticmethod
  def _compute_cost(usage: object | None) -> Decimal:
    Docstring: "Claude Sonnet 4.5 pricing: $3/MTok input, $15/MTok output (SPEC.md Section 10)."
    If usage is None: return Decimal("0")
    input_tok = usage.input_tokens or 0; output_tok = usage.output_tokens or 0
    return Decimal(str((input_tok / 1_000_000) * 3.0 + (output_tok / 1_000_000) * 15.0))

  def _build_fallback(self, problems_rows: list) -> object:
    Docstring: "D-14/D-15: construct DailyInsightResponse from detected_problems rows without Claude. summary = 'Generare AI eșuată — raport bazat pe anomalii detectate automat'."
    Deferred imports inside method: from app.schemas.insights.daily_insight_schema import DailyInsightResponse, Problem
    Build Problem objects from rows: id=row.rule_id, severity=row.severity, category="sales", title=row.rule_id.replace("_", " ").title(), description=f"Anomalie detectată: {row.rule_id}", estimated_loss_ron=row.estimated_loss_ron or Decimal("0"), actions=[]
    Return DailyInsightResponse(summary="Generare AI eșuată — raport bazat pe anomalii detectate automat", problems=problems[:3], positives=[], warnings=[], weekly_action_plan=[], generated_at=datetime.now(UTC))

2. backend/app/services/repositories/insight_repository.py

Follow AnomalyRepository pattern exactly (same file structure, same validation guards).
Module docstring references D-16 UPSERT on (tenant_id, date), Pitfall 6 (Core INSERT bypasses with_loader_criteria).
class InsightRepository:
  __init__(self, session: AsyncSession, tenant_id: UUID) -> None
  async def upsert_daily_insight(self, row: dict) -> None:
    Docstring: "UPSERT daily_insights row. Conflict target: (tenant_id, date) — one row per tenant per day. Does NOT commit — caller commits atomically (WR-04)."
    Validate tenant_id present and not None: raise ValueError with field info if missing.
    Validate row["tenant_id"] == self._tenant_id: raise ValueError on cross-tenant write.
    Deferred import: from app.models.insights.daily_insight import DailyInsight
    2-col conflict target: exclude id + conflict-target cols (id, tenant_id, date) from update set.
    update_cols = [c for c in row if c not in {"id", "tenant_id", "date"}]
    stmt = pg_insert(DailyInsight).values([row])
    stmt = stmt.on_conflict_do_update(index_elements=["tenant_id", "date"], set_={col: stmt.excluded[col] for col in update_cols})
    await self._session.execute(stmt)
  </action>
  <verify>
    <automated>cd /Users/sergheigranici/Desktop/sofabelle/sales-marketing-ai-analyst/backend && python -c "from app.services.insights.insight_service import InsightService; from app.services.repositories.insight_repository import InsightRepository; print('OK')"</automated>
  </verify>
  <done>
    - InsightService and InsightRepository import cleanly
    - InsightService._compute_cost(mock_usage_with_3000_input_2000_output) == Decimal("0.039000")
    - InsightRepository raises ValueError when row missing tenant_id
    - Wave 0 unit tests turn GREEN: pytest tests/unit/test_insight_service.py tests/unit/test_insight_repository.py
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Python code → Anthropic API | User message must not include customer PII; context_json filtered before sending |
| Claude API response → DB | Schema validation (Pydantic) + number cross-check gate before write |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | Information Disclosure | build_user_message() PII filter | mitigate | context_json stripped to {rule_id, current_value, expected_value} only — no lead_ids, salesperson_ids, customer names (GDPR, CLAUDE.md Principle 6) |
| T-05-03-02 | Information Disclosure | InsightService logging | mitigate | structlog logs only counts, dates, rule names, attempt numbers — never log prompt text, raw_response content, or context_json fields |
| T-05-03-03 | Spoofing | AsyncAnthropic instantiated at module level | mitigate | AsyncAnthropic is ONLY instantiated inside InsightService.run() method body (INFRA-05 + AI-SPEC Common Pitfall 1) |
| T-05-03-04 | Tampering | Model string "claude-sonnet-4-5" | mitigate | MODEL constant in insight_service.py; comment says LOCKED — do NOT change; any change visible in code review |
| T-05-03-05 | Information Disclosure | raw_response column | accept | raw_response stores Claude's JSON output — contains business metrics but no customer PII; the prompt builder prevents PII from entering the prompt |
</threat_model>

<verification>
After both tasks:
1. `cd backend && python -m pytest tests/unit/test_daily_insight_schema.py tests/unit/test_prompt_builder.py tests/unit/test_number_validator.py tests/unit/test_insight_service.py tests/unit/test_insight_repository.py -v` — all GREEN
2. `cd backend && python -c "from app.services.insights.prompt_builder import build_system_prompt; blocks = build_system_prompt(); last = blocks[-1]; print('cache' in last, 'Dragoi' in blocks[0]['text'])"` — prints "True True"
3. `cd backend && python -c "from app.services.insights.number_validator import extract_numbers_from_text; print(23400.0 in extract_numbers_from_text('23.400 RON'))"` — prints "True"
4. `cd backend && grep -n "claude-sonnet-4-5" app/services/insights/insight_service.py` — returns the MODEL constant line
5. `cd backend && grep -rn "AsyncAnthropic" app/api/ 2>/dev/null | wc -l` — prints 0 (AI-09 verified)
</verification>

<success_criteria>
- All 5 unit test files from Wave 0 pass after this plan: test_daily_insight_schema, test_prompt_builder, test_number_validator, test_insight_service, test_insight_repository
- build_system_prompt() last block has cache_control with ephemeral type
- build_user_message caps detected_problems at 3 even if more are passed
- InsightService._compute_cost(usage_3000_input_2000_output) == Decimal("0.039000")
- DailyInsightResponse rejects payload with 4 problems (Pydantic ValidationError)
- InsightRepository.upsert_daily_insight raises ValueError on missing tenant_id
</success_criteria>

<output>
Create `.planning/phases/05-ai-insights/05-03-SUMMARY.md` when done
</output>
