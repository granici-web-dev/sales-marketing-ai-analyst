# nosemgrep: package_managers.npm.npm-missing-minimum-release-age.npm-missing-minimum-release-age
#
# То же, что в pnpm-workspace.yaml: выдержка перед установкой свежих версий
# появилась в поздних npm, проект ставит зависимости pnpm 9. Причина целиком
# записана там.
# pnpm configuration
# onlyBuiltDependencies is only needed for pnpm v10+
# This project uses pnpm@9 in Docker where it is not needed
